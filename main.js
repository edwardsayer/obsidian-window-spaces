/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/main.ts
var main_exports = {};
__export(main_exports, {
  default: () => WindowSpacesPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian8 = require("obsidian");

// src/types.ts
var DEFAULT_COLOR_PRESETS = [
  "#3b82f6",
  "#8b5cf6",
  "#10b981",
  "#f59e0b",
  "#f43f5e",
  "#06b6d4",
  "#6366f1"
];

// src/manager.ts
var import_obsidian3 = require("obsidian");

// src/i18n/en.ts
var en = {
  common: {
    save: "Save",
    restore: "Restore",
    cancel: "Cancel",
    delete: "Delete",
    rename: "Rename",
    confirm: "Confirm",
    success: "Success",
    error: "Error",
    warning: "Warning",
    loading: "Loading",
    close: "Close",
    ok: "OK",
    layoutLabel: "Window Space",
    noLayout: "No space applied",
    newWindow: "New Window",
    edit: "Edit",
    windowLayouts: "Window Spaces",
    openAsPanel: "Panel location"
  },
  commands: {
    saveLayout: "Save current Space",
    openLayouts: "Open switcher",
    openLayoutsRibbon: "Open Window Spaces switcher",
    openLayoutsPanel: "Open as tab panel",
    openLayoutsPanelLeft: "Open in left sidebar",
    openLayoutsPanelRight: "Open in right sidebar",
    toggleLeftActivityBar: "Toggle left Activity Bar",
    toggleRightActivityBar: "Toggle right Activity Bar",
    toggleCurrentPaneGroup: "Toggle current pane group"
  },
  saveModal: {
    title: "Space Settings",
    nameLabel: "Space Name",
    namePlaceholder: "Enter space name...",
    descriptionLabel: "Description (optional)",
    descriptionPlaceholder: "Enter space description...",
    infoSection: "Space Details",
    windowSize: "Window Size",
    includePosition: "Include Window Position",
    includePositionDesc: "Save the window position coordinates on screen",
    includeWindowSize: "Include window size",
    includeWindowSizeDesc: "Save the window width and height",
    includeGeometry: "Include window position & size",
    includeGeometryDesc: "Save the window coordinates and dimensions on screen",
    andOthers: "and others",
    overwriteNotice: "Will overwrite existing space",
    saveButton: "Save Space",
    cancelButton: "Cancel",
    emptyNameError: "Space name cannot be empty",
    duplicateNameError: "A space with this name already exists",
    iconLabel: "Space Logo",
    iconPlaceholder: "e.g. \u{1F680} or star (icon or emoji)",
    iconDesc: "Supports both icons and emoji",
    colorLabel: "Window Frame Color",
    colorPresetLabel: "Color Swatches",
    clearColor: "Clear Color",
    saveSuccess: "Space saved successfully",
    autoSaveToggle: "Enable auto-save for this space",
    borderInset: "Frame inset thickness",
    borderInsetDesc: "Set a value to draw a custom frame; leave unchanged to keep Obsidian's native accent border",
    foldedCorner: "Show folded corner",
    foldedCornerDesc: "Leave unchanged to follow the global default",
    activityBarsSection: "Activity Bars",
    showActivityBar: "Show",
    showActivityBarTitle: "Show Activity Bar",
    showActivityBarDesc: "When enabled, this space will display this side Activity Bar in popout windows",
    showLeftActivityBar: "Show Left Activity Bar",
    showRightActivityBar: "Show Right Activity Bar",
    activityBarViewButtons: "View buttons",
    spaceSection: "Space",
    windowAppearanceSection: "Window Appearance",
    behaviorSection: "Space Behavior",
    sectionsSection: "Sections"
  },
  restoreModal: {
    title: "Restore Space",
    selectLayout: "Select a space to restore:",
    noLayoutsMessage: "No saved spaces found.",
    restoreButton: "Restore Space",
    restoreHint: "Restore space (click or Enter for a new window; long-press the item or Restore button, Shift+click, or Shift+Enter for the current window)",
    cancelButton: "Cancel",
    restoreSuccess: "Space restored successfully",
    restoreError: "Failed to restore space",
    includedFiles: "Included files",
    restoringLayout: "Restoring space"
  },
  manageModal: {
    title: "Manage Window Spaces",
    noLayoutsMessage: "No saved spaces found.",
    searchPlaceholder: "Find or create a space...",
    enterToCreate: "Enter to create",
    clearSearch: "Clear search",
    saveCurrentButton: "Save",
    layoutName: "Space Name",
    createdDate: "Created",
    updatedDate: "Updated",
    fileCount: "Files",
    tabCount: "Tabs",
    actions: "Actions",
    renameButton: "Rename",
    deleteButton: "Delete",
    confirmDeleteTitle: "Delete Space",
    confirmDeleteMessage: "Are you sure you want to delete this space? This action cannot be undone.",
    deleteSuccess: "Space deleted successfully",
    renameSuccess: "Space renamed successfully",
    sortDateDesc: "Updated (Newest)",
    sortDateAsc: "Updated (Oldest)",
    sortUpdatedDesc: "Updated (Newest)",
    sortUpdatedAsc: "Updated (Oldest)",
    sortCreatedDesc: "Created (Newest)",
    sortCreatedAsc: "Created (Oldest)",
    sortNameAsc: "Name (A to Z)",
    sortNameDesc: "Name (Z to A)",
    autoSaveEnabled: "Auto-save enabled",
    autoSaveDisabled: "Auto-save disabled",
    windowOpenBadge: "Active",
    viewOptions: "View Options",
    groupBySection: "Group by Section",
    flatView: "Flat List",
    showArchived: "Show Archived",
    hideArchived: "Hide Archived",
    uncategorized: "Uncategorized",
    archivedGroup: "Archived",
    archiveSpace: "Archive Space",
    unarchiveSpace: "Unarchive Space",
    archiveSuccess: "Space archived",
    unarchiveSuccess: "Space unarchived",
    renameSection: "Rename Section",
    sectionsLabel: "Sections",
    sectionsPlaceholder: "Type section name and press Enter...",
    sectionHeaderHint: "Click to collapse/expand \xB7 Double-click to rename \xB7 Drag to reorder",
    sectionHeaderHintStatic: "Click to collapse/expand"
  },
  settings: {
    title: "Window Spaces Settings",
    description: "Manage your Window Spaces and configure auto-save options.",
    generalSection: "Window Spaces General Settings",
    autoSaveDescription: "Automatically save Window Spaces at regular intervals.",
    autoSaveEnabled: "Enable auto-save",
    showNotifications: "Show notifications",
    showNotificationsDesc: "Display notice toasts on saving or restoring spaces",
    popoutDefaultsSection: "New Popout Defaults",
    showLayoutStatusBar: "Show space status bar",
    showLayoutStatusBarDesc: "Display the current space name and a Save button in a status-style bar at the bottom-left of every Popout window",
    showWindowLayoutsRibbonIcon: "Show 'Window Spaces' ribbon icon",
    showWindowLayoutsRibbonIconDesc: "Display one quick-access icon for restoring and managing Window Spaces",
    autoSaveInterval: "Auto-save interval",
    minutes: "minutes",
    resetSettings: "Reset Settings",
    resetSettingsDescription: "Reset all settings to default values.",
    resetButton: "Reset Settings",
    resetConfirmTitle: "Confirm Reset",
    resetConfirmMessage: "Are you sure you want to reset all settings? This will not delete your saved spaces.",
    resetSuccess: "Settings reset successfully",
    popoutSidebarSection: "Popout Window Views",
    enableInterceptor: "Route views to their popout windows",
    enableInterceptorDesc: "When opening a view from a popout window\u2014whether in a sidebar or editor tab area\u2014keep it in that popout instead of routing it to the main window.",
    leftBar: "Left Activity Bar",
    rightBar: "Right Activity Bar",
    addView: "Add view",
    removeView: "Remove",
    viewTypePlaceholder: "View type (e.g. file-explorer)",
    pickIcon: "Choose logo",
    restoreDefaultButtons: "Restore default buttons",
    restoreDefaultIcon: "Restore default icon",
    accentSection: "Window Accent & Icons",
    defaultIcon: "Default Space Logo",
    defaultIconDesc: "Default icon or emoji used for space logo",
    defaultBorderInset: "Default frame inset thickness",
    defaultBorderInsetDesc: "Set to 0 to hide the Popout frame border",
    defaultFoldedCorner: "Show default folded corner",
    defaultFoldedCornerDesc: "Show the folded-corner decoration in the top-left of Popout windows",
    defaultActivityBarVisibility: "Show this Activity Bar by default for new Spaces",
    defaultActivityBarVisibilityDesc: "Controls whether the entire Activity Bar is shown",
    dragToReorder: "Drag to reorder",
    keepOneActivityBarView: "Keep at least one Activity Bar view button when another view is available",
    popoutAccentsTitle: "Popout Space Theme Accents",
    popoutAccentsDesc: "Inject Space theme colors into Popout windows. Can be disabled if it conflicts with third-party themes.",
    popoutAccentsEnable: "Enable Space theme visual accents",
    popoutAccentsEnableDesc: "Apply tab header tint & border, splitter highlight, pane border, and Activity Bar atmosphere tint",
    popoutAccentsSplitter: "Splitters & resize handle highlights",
    popoutAccentsSplitterDesc: "Tint splitters with space color and highlight on hover or drag",
    popoutAccentsActivityBar: "Activity Bar atmosphere tint",
    popoutAccentsActivityBarDesc: "Inject a gentle space theme background tint into left and right Activity Bars",
    manageSpaces: "Manage spaces...",
    showInSystemExplorer: "Show in system explorer",
    copyPath: "Copy path",
    pathCopied: "Path copied to clipboard"
  },
  notifications: {
    layoutSaved: "Space saved successfully",
    layoutOverwritten: "Space overwritten successfully",
    layoutRestored: "Space restored successfully",
    layoutDeleted: "Space deleted successfully",
    layoutRenamed: "Space renamed successfully",
    settingsReset: "Settings reset successfully",
    errorOccurred: "An error occurred",
    invalidLayout: "Invalid space data",
    cannotRestore: "Cannot restore space",
    missingFilesNotice: "missing files",
    switchedToOpenWindow: 'Switched to active window for space "{{name}}"'
  },
  errors: {
    failedToSave: "Failed to save space",
    failedToRestore: "Failed to restore space",
    failedToDelete: "Failed to delete space",
    failedToRename: "Failed to rename space",
    layoutNotFound: "Space not found",
    invalidData: "Invalid data format",
    permissionDenied: "Permission denied",
    notInPopoutWindow: "Please execute this command inside a popout window.",
    unknownError: "An unknown error occurred"
  },
  instructions: {
    navigate: "to navigate",
    use: "to use",
    useNewWindow: "in new window",
    dismiss: "to dismiss"
  },
  activityBar: {
    toggleColumn: "Toggle sidebar",
    toggleGroup: "Toggle current pane group",
    cannotHideLastPane: "Cannot hide the last visible pane",
    onlyInPopout: "This command only works inside a Popout window",
    openSettings: "Open Window Spaces settings",
    leftActivityBar: "Left activity bar",
    leftSidebar: "Left sidebar",
    rightActivityBar: "Right activity bar",
    rightSidebar: "Right sidebar"
  }
};

// src/i18n/zh-TW.ts
var zhTW = {
  common: {
    save: "\u5132\u5B58",
    restore: "\u6062\u5FA9",
    cancel: "\u53D6\u6D88",
    delete: "\u522A\u9664",
    rename: "\u91CD\u65B0\u547D\u540D",
    confirm: "\u78BA\u8A8D",
    success: "\u6210\u529F",
    error: "\u932F\u8AA4",
    warning: "\u8B66\u544A",
    loading: "\u8F09\u5165\u4E2D",
    close: "\u95DC\u9589",
    ok: "\u78BA\u5B9A",
    layoutLabel: "\u7A7A\u9593",
    noLayout: "\u5C1A\u672A\u5957\u7528\u7A7A\u9593",
    newWindow: "\u65B0\u8996\u7A97",
    edit: "\u7DE8\u8F2F",
    windowLayouts: "Window Spaces",
    openAsPanel: "\u9762\u677F\u4F4D\u7F6E"
  },
  commands: {
    saveLayout: "\u5132\u5B58\u76EE\u524D\u7A7A\u9593",
    saveLayoutDirect: "\u76F4\u63A5\u5132\u5B58\u76EE\u524D\u7A7A\u9593",
    openLayouts: "\u958B\u555F\u5207\u63DB\u5668",
    openLayoutsRibbon: "\u958B\u555F Window Spaces \u5207\u63DB\u5668",
    openLayoutsPanel: "\u958B\u555F\u70BA\u6A19\u7C64\u9762\u677F",
    openLayoutsPanelLeft: "\u958B\u555F\u5728\u5DE6\u5074\u908A\u6B04",
    openLayoutsPanelRight: "\u958B\u555F\u5728\u53F3\u5074\u908A\u6B04",
    toggleLeftActivityBar: "\u5207\u63DB\u5DE6\u5074 Activity Bar",
    toggleRightActivityBar: "\u5207\u63DB\u53F3\u5074 Activity Bar",
    toggleCurrentPaneGroup: "\u5207\u63DB\u76EE\u524D\u5206\u9801\u7FA4\u7D44"
  },
  saveModal: {
    title: "\u7A7A\u9593\u8A2D\u5B9A",
    nameLabel: "\u7A7A\u9593\u540D\u7A31",
    namePlaceholder: "\u8F38\u5165\u7A7A\u9593\u540D\u7A31...",
    descriptionLabel: "\u63CF\u8FF0\uFF08\u9078\u586B\uFF09",
    descriptionPlaceholder: "\u8F38\u5165\u7A7A\u9593\u63CF\u8FF0...",
    infoSection: "\u7A7A\u9593\u8CC7\u8A0A",
    windowSize: "\u8996\u7A97\u5927\u5C0F",
    includePosition: "\u5305\u542B\u8996\u7A97\u4F4D\u7F6E",
    includePositionDesc: "\u5132\u5B58\u8996\u7A97\u5728\u87A2\u5E55\u4E0A\u7684\u4F4D\u7F6E",
    includeWindowSize: "\u5305\u542B\u8996\u7A97\u5927\u5C0F",
    includeWindowSizeDesc: "\u5132\u5B58\u8996\u7A97\u7684\u5BEC\u5EA6\u548C\u9AD8\u5EA6",
    includeGeometry: "\u5305\u542B\u8996\u7A97\u4F4D\u7F6E\u8207\u5927\u5C0F",
    includeGeometryDesc: "\u4FDD\u5B58\u4E26\u9084\u539F\u8996\u7A97\u5728\u87A2\u5E55\u4E0A\u7684\u5EA7\u6A19\u4F4D\u7F6E\u8207\u5BEC\u9AD8\u5C3A\u5BF8",
    andOthers: "\u53CA\u5176\u4ED6",
    overwriteNotice: "\u5C07\u8986\u84CB\u66F4\u65B0\u65E2\u6709\u7A7A\u9593",
    saveButton: "\u5132\u5B58\u7A7A\u9593",
    cancelButton: "\u53D6\u6D88",
    emptyNameError: "\u7A7A\u9593\u540D\u7A31\u4E0D\u80FD\u70BA\u7A7A",
    duplicateNameError: "\u6B64\u540D\u7A31\u7684\u7A7A\u9593\u5DF2\u7D93\u5B58\u5728",
    iconLabel: "\u7A7A\u9593 Logo",
    iconPlaceholder: "\u4F8B\u5982\uFF1A\u{1F680} \u6216 star\uFF08icon \u6216 emoji\uFF09",
    iconDesc: "\u652F\u63F4\u4F7F\u7528 icon \u6216 emoji",
    colorLabel: "\u8996\u7A97\u908A\u6846\u984F\u8272",
    colorPresetLabel: "\u5FEB\u6377\u8ABF\u8272\u76E4",
    clearColor: "\u6E05\u9664\u984F\u8272",
    saveSuccess: "\u7A7A\u9593\u5132\u5B58\u6210\u529F",
    autoSaveToggle: "\u555F\u7528\u6B64\u7A7A\u9593\u7684\u81EA\u52D5\u4FDD\u5B58",
    borderInset: "\u908A\u6846 inset \u539A\u5EA6",
    borderInsetDesc: "\u8A2D\u5B9A\u6578\u503C\u6642\u7E6A\u88FD\u81EA\u8A02\u908A\u6846\uFF1B\u672A\u4FEE\u6539\u6642\u4FDD\u7559 Obsidian \u539F\u751F accent \u908A\u6846",
    foldedCorner: "\u986F\u793A\u5DE6\u4E0A\u89D2\u6298\u9801\u88DD\u98FE",
    foldedCornerDesc: "\u672A\u4FEE\u6539\u6642\u8DDF\u96A8\u5168\u57DF\u9810\u8A2D\u503C",
    activityBarsSection: "Activity Bars",
    showActivityBar: "\u986F\u793A",
    showActivityBarTitle: "\u986F\u793A\u6B64\u5074 Activity Bar",
    showActivityBarDesc: "\u958B\u555F\u6642\uFF0C\u6B64\u7A7A\u9593\u6703\u5728 Popout \u8996\u7A97\u986F\u793A\u8A72\u5074 Activity Bar",
    showLeftActivityBar: "\u986F\u793A\u5DE6\u5074 Activity Bar",
    showRightActivityBar: "\u986F\u793A\u53F3\u5074 Activity Bar",
    activityBarViewButtons: "View \u6309\u9215",
    spaceSection: "\u7A7A\u9593",
    windowAppearanceSection: "\u8996\u7A97\u5916\u89C0",
    behaviorSection: "\u7A7A\u9593\u884C\u70BA",
    sectionsSection: "Section \u5206\u7D44"
  },
  restoreModal: {
    title: "\u5FA9\u539F\u7A7A\u9593",
    selectLayout: "\u9078\u64C7\u8981\u5FA9\u539F\u7684\u7A7A\u9593\uFF1A",
    noLayoutsMessage: "\u6C92\u6709\u627E\u5230\u5DF2\u5132\u5B58\u7684\u7A7A\u9593\u3002",
    restoreButton: "\u5FA9\u539F\u7A7A\u9593",
    restoreHint: "\u5FA9\u539F\u7A7A\u9593\uFF08\u9EDE\u64CA\u6216\u6309 Enter \u5728\u65B0\u8996\u7A97\u958B\u555F\uFF1B\u9577\u6309\u7A7A\u9593\u9805\u76EE\u6216 Restore \u6309\u9215\u3001\u6309\u4F4F Shift \u9EDE\u64CA\u6216\u6309 Shift+Enter \u5957\u7528\u81F3\u76EE\u524D\u8996\u7A97\uFF09",
    cancelButton: "\u53D6\u6D88",
    restoreSuccess: "\u7A7A\u9593\u5FA9\u539F\u6210\u529F",
    restoreError: "\u5FA9\u539F\u7A7A\u9593\u5931\u6557",
    includedFiles: "\u6536\u9304\u6A94\u6848",
    restoringLayout: "\u6B63\u5728\u5FA9\u539F\u7A7A\u9593"
  },
  manageModal: {
    title: "\u7BA1\u7406 Window Spaces",
    noLayoutsMessage: "\u6C92\u6709\u627E\u5230\u5DF2\u5132\u5B58\u7684\u7A7A\u9593\u3002",
    searchPlaceholder: "\u5C0B\u627E\u6216\u5EFA\u7ACB\u7A7A\u9593...",
    enterToCreate: "Enter \u9375\u4EE5\u5EFA\u7ACB",
    clearSearch: "\u6E05\u9664\u641C\u5C0B",
    saveCurrentButton: "Save",
    layoutName: "\u7A7A\u9593\u540D\u7A31",
    createdDate: "\u5EFA\u7ACB\u6642\u9593",
    updatedDate: "\u66F4\u65B0\u6642\u9593",
    fileCount: "\u6A94\u6848\u6578",
    tabCount: "\u5206\u9801\u6578",
    actions: "\u64CD\u4F5C",
    renameButton: "\u91CD\u547D\u540D",
    deleteButton: "\u522A\u9664",
    confirmDeleteTitle: "\u522A\u9664\u7A7A\u9593",
    confirmDeleteMessage: "\u60A8\u78BA\u5B9A\u8981\u522A\u9664\u9019\u500B\u7A7A\u9593\u55CE\uFF1F\u6B64\u64CD\u4F5C\u7121\u6CD5\u5FA9\u539F\u3002",
    deleteSuccess: "\u7A7A\u9593\u522A\u9664\u6210\u529F",
    renameSuccess: "\u7A7A\u9593\u91CD\u65B0\u547D\u540D\u6210\u529F",
    sortDateDesc: "\u66F4\u65B0\u6642\u9593 (\u6700\u65B0)",
    sortDateAsc: "\u66F4\u65B0\u6642\u9593 (\u8F03\u820A)",
    sortUpdatedDesc: "\u66F4\u65B0\u6642\u9593 (\u6700\u65B0)",
    sortUpdatedAsc: "\u66F4\u65B0\u6642\u9593 (\u8F03\u820A)",
    sortCreatedDesc: "\u5EFA\u7ACB\u6642\u9593 (\u6700\u65B0)",
    sortCreatedAsc: "\u5EFA\u7ACB\u6642\u9593 (\u8F03\u820A)",
    sortNameAsc: "\u540D\u7A31 (A-Z)",
    sortNameDesc: "\u540D\u7A31 (Z-A)",
    autoSaveEnabled: "\u81EA\u52D5\u4FDD\u5B58\u5DF2\u555F\u7528",
    autoSaveDisabled: "\u81EA\u52D5\u4FDD\u5B58\u5DF2\u505C\u7528",
    windowOpenBadge: "\u8996\u7A97\u958B\u555F\u4E2D",
    viewOptions: "\u986F\u793A\u9078\u9805",
    groupBySection: "\u4F9D Section \u5206\u7D44",
    flatView: "\u4E0D\u5206\u7D44\u6E05\u55AE",
    showArchived: "\u986F\u793A\u5C01\u5B58\u7A7A\u9593",
    hideArchived: "\u96B1\u85CF\u5C01\u5B58\u7A7A\u9593",
    uncategorized: "\u672A\u5206\u985E",
    archivedGroup: "\u{1F4E6} \u5C01\u5B58\u7A7A\u9593",
    archiveSpace: "\u5C01\u5B58\u7A7A\u9593",
    unarchiveSpace: "\u53D6\u6D88\u5C01\u5B58",
    archiveSuccess: "\u5DF2\u5C01\u5B58\u7A7A\u9593",
    unarchiveSuccess: "\u5DF2\u53D6\u6D88\u5C01\u5B58\u7A7A\u9593",
    renameSection: "\u91CD\u547D\u540D Section",
    sectionsLabel: "Section \u5206\u7D44\u6A19\u7C64",
    sectionsPlaceholder: "\u8F38\u5165 Section \u540D\u7A31\u5F8C\u6309 Enter...",
    sectionHeaderHint: "\u9EDE\u64CA\u647A\u758A/\u5C55\u958B \xB7 \u96D9\u64CA\u91CD\u65B0\u547D\u540D \xB7 \u62D6\u66F3\u6392\u5E8F",
    sectionHeaderHintStatic: "\u9EDE\u64CA\u647A\u758A/\u5C55\u958B"
  },
  settings: {
    title: "Window Spaces \u8A2D\u5B9A",
    description: "\u7BA1\u7406\u60A8\u7684 Window Spaces \u4E26\u8A2D\u5B9A\u81EA\u52D5\u5132\u5B58\u9078\u9805\u3002",
    generalSection: "Window Spaces \u4E00\u822C\u8A2D\u5B9A",
    autoSaveDescription: "\u5B9A\u671F\u81EA\u52D5\u5132\u5B58 Window Spaces\u3002",
    autoSaveEnabled: "\u555F\u7528\u81EA\u52D5\u5132\u5B58",
    showNotifications: "\u986F\u793A\u901A\u77E5",
    showNotificationsDesc: "\u5728\u5132\u5B58\u6216\u6062\u5FA9\u7A7A\u9593\u6642\u986F\u793A\u901A\u77E5\u5FEB\u8A0A",
    popoutDefaultsSection: "\u65B0 Popout \u9810\u8A2D\u8A2D\u5B9A",
    showLayoutStatusBar: "\u986F\u793A\u7A7A\u9593\u72C0\u614B\u5217",
    showLayoutStatusBarDesc: "\u5728\u6BCF\u500B Popout \u8996\u7A97\u5DE6\u4E0B\u65B9\u4EE5\u72C0\u614B\u5217\u6A23\u5F0F\u986F\u793A\u76EE\u524D\u7A7A\u9593\u540D\u7A31\u8207\u5132\u5B58\u6309\u9215",
    showWindowLayoutsRibbonIcon: "\u986F\u793A\u300CWindow Spaces\u300D\u5074\u908A\u6B04\u5716\u793A",
    showWindowLayoutsRibbonIconDesc: "\u5728\u4E3B\u8996\u7A97\u5DE6\u5074\u908A\u6B04\u986F\u793A\u6062\u5FA9\u8207\u7BA1\u7406 Window Spaces \u7684\u55AE\u4E00\u5165\u53E3",
    autoSaveInterval: "\u81EA\u52D5\u5132\u5B58\u9593\u9694",
    minutes: "\u5206\u9418",
    resetSettings: "\u91CD\u8A2D\u8A2D\u5B9A",
    resetSettingsDescription: "\u5C07\u6240\u6709\u8A2D\u5B9A\u91CD\u8A2D\u70BA\u9810\u8A2D\u503C\u3002",
    resetButton: "\u91CD\u8A2D\u8A2D\u5B9A",
    resetConfirmTitle: "\u78BA\u8A8D\u91CD\u8A2D",
    resetConfirmMessage: "\u60A8\u78BA\u5B9A\u8981\u91CD\u8A2D\u6240\u6709\u8A2D\u5B9A\u55CE\uFF1F\u9019\u4E0D\u6703\u522A\u9664\u60A8\u5DF2\u5132\u5B58\u7684\u7A7A\u9593\u3002",
    resetSuccess: "\u8A2D\u5B9A\u91CD\u8A2D\u6210\u529F",
    popoutSidebarSection: "Popout \u8996\u7A97 View \u8A2D\u5B9A",
    enableInterceptor: "\u5C07 View \u8DEF\u7531\u81F3\u6240\u5C6C\u7684 Popout \u8996\u7A97",
    enableInterceptorDesc: "\u7576\u5F9E Popout \u8996\u7A97\u958B\u555F View \u6642\uFF0C\u4E0D\u8AD6\u76EE\u6A19\u662F\u5074\u6B04\u6216 editor tab area\uFF0C\u90FD\u5C07 View \u4FDD\u7559\u5728\u8A72 Popout\uFF0C\u800C\u4E0D\u662F\u8DEF\u7531\u56DE\u4E3B\u8996\u7A97\u3002",
    leftBar: "\u5DE6\u5074 Activity Bar",
    rightBar: "\u53F3\u5074 Activity Bar",
    addView: "\u65B0\u589E View",
    removeView: "\u79FB\u9664",
    viewTypePlaceholder: "View type\uFF08\u4F8B\u5982 file-explorer\uFF09",
    pickIcon: "\u9078\u64C7 Logo",
    restoreDefaultButtons: "\u9084\u539F\u9810\u8A2D\u6309\u9215",
    restoreDefaultIcon: "\u9084\u539F\u9810\u8A2D\u5716\u793A",
    accentSection: "\u8996\u7A97\u5916\u89C0\u8207\u5716\u793A",
    defaultIcon: "\u9810\u8A2D\u7A7A\u9593 Logo",
    defaultIconDesc: "\u7A7A\u9593 Logo \u4F7F\u7528\u7684\u9810\u8A2D icon \u6216 emoji",
    defaultBorderInset: "\u9810\u8A2D\u908A\u6846 inset \u539A\u5EA6",
    defaultBorderInsetDesc: "\u8A2D\u5B9A\u70BA 0 \u6642\u4E0D\u986F\u793A Popout \u8996\u7A97\u908A\u6846",
    defaultFoldedCorner: "\u9810\u8A2D\u986F\u793A\u5DE6\u4E0A\u89D2\u6298\u9801\u88DD\u98FE",
    defaultFoldedCornerDesc: "\u5728 Popout \u8996\u7A97\u5DE6\u4E0A\u89D2\u986F\u793A\u6298\u9801\u88DD\u98FE",
    defaultActivityBarVisibility: "\u65B0\u5EFA Window Space \u6642\u9810\u8A2D\u986F\u793A\u6B64 Activity Bar",
    defaultActivityBarVisibilityDesc: "\u63A7\u5236\u662F\u5426\u986F\u793A\u6574\u500B Activity Bar",
    dragToReorder: "\u62D6\u66F3\u6392\u5E8F",
    keepOneActivityBarView: "\u4ECD\u6709\u5176\u4ED6\u53EF\u7528 View \u6642\uFF0CActivity Bar \u81F3\u5C11\u8981\u4FDD\u7559\u4E00\u500B\u6309\u9215",
    popoutAccentsTitle: "Popout \u7A7A\u9593\u4E3B\u984C\u88DD\u98FE\u589E\u5F37",
    popoutAccentsDesc: "\u70BA Popout \u8996\u7A97\u6CE8\u5165\u7A7A\u9593\u4E3B\u984C\u8272\u3002\u82E5\u8207\u7B2C\u4E09\u65B9\u4E3B\u984C\u6A23\u5F0F\u885D\u7A81\uFF0C\u53EF\u96A8\u6642\u95DC\u9589\u3002",
    popoutAccentsEnable: "\u555F\u7528\u7A7A\u9593\u4E3B\u984C\u88DD\u98FE\u589E\u5F37",
    popoutAccentsEnableDesc: "\u5728 Popout \u8996\u7A97\u4E2D\u5957\u7528\u5206\u9801\u6A19\u7C64\u5E95\u8272\u8207\u908A\u6846\u3001\u7A97\u683C\u5206\u9694\u7DDA\u9AD8\u4EAE\u3001\u7A97\u683C\u908A\u6846\u8207\u6D3B\u52D5\u5217\u6C1B\u570D\u8272",
    popoutAccentsSplitter: "\u7A97\u683C\u8ABF\u6574\u5206\u9694\u7DDA\u8207\u9AD8\u4EAE",
    popoutAccentsSplitterDesc: "\u7A97\u683C\u5206\u5272\u689D\u5E36\u6709\u4E3B\u984C\u8272\u5FAE\u5149\uFF0C\u61F8\u6D6E\u8207\u62D6\u66F3\u6642\u986F\u793A\u9AD8\u4EAE\u8207\u5149\u6688",
    popoutAccentsActivityBar: "Activity Bar \u6C1B\u570D\u5E95\u8272",
    popoutAccentsActivityBarDesc: "\u5728\u5DE6\u53F3\u5169\u5074 Activity Bar \u6CE8\u5165\u67D4\u548C\u7684\u7A7A\u9593\u4E3B\u984C\u5E95\u8272",
    manageSpaces: "\u7BA1\u7406\u7A7A\u9593...",
    showInSystemExplorer: "\u5728\u7CFB\u7D71\u8CC7\u6599\u593E\u4E2D\u986F\u793A",
    copyPath: "\u8907\u88FD\u8DEF\u5F91",
    pathCopied: "\u8DEF\u5F91\u5DF2\u8907\u88FD\u5230\u526A\u8CBC\u7C3F"
  },
  notifications: {
    layoutSaved: "\u7A7A\u9593\u5132\u5B58\u6210\u529F",
    layoutOverwritten: "\u5DF2\u8986\u84CB\u66F4\u65B0\u7A7A\u9593",
    layoutRestored: "\u7A7A\u9593\u5FA9\u539F\u6210\u529F",
    layoutDeleted: "\u7A7A\u9593\u522A\u9664\u6210\u529F",
    layoutRenamed: "\u7A7A\u9593\u91CD\u65B0\u547D\u540D\u6210\u529F",
    settingsReset: "\u8A2D\u5B9A\u91CD\u8A2D\u6210\u529F",
    errorOccurred: "\u767C\u751F\u932F\u8AA4",
    invalidLayout: "\u7121\u6548\u7684\u7A7A\u9593\u8CC7\u6599",
    cannotRestore: "\u7121\u6CD5\u5FA9\u539F\u7A7A\u9593",
    missingFilesNotice: "\u5305\u542B\u4E0D\u5B58\u5728\u7684\u6A94\u6848",
    switchedToOpenWindow: "\u5DF2\u5207\u63DB\u81F3\u300C{{name}}\u300D\u4E4B\u5DF2\u958B\u555F\u7A7A\u9593"
  },
  errors: {
    failedToSave: "\u5132\u5B58\u7A7A\u9593\u5931\u6557",
    failedToRestore: "\u6062\u5FA9\u7A7A\u9593\u5931\u6557",
    failedToDelete: "\u522A\u9664\u7A7A\u9593\u5931\u6557",
    failedToRename: "\u91CD\u65B0\u547D\u540D\u7A7A\u9593\u5931\u6557",
    layoutNotFound: "\u627E\u4E0D\u5230\u7A7A\u9593",
    invalidData: "\u7121\u6548\u7684\u8CC7\u6599\u683C\u5F0F",
    permissionDenied: "\u6B0A\u9650\u88AB\u62D2\u7D55",
    notInPopoutWindow: "\u8ACB\u5728\u5F48\u51FA\u8996\u7A97\uFF08Popout Window\uFF09\u4E2D\u57F7\u884C\u6B64\u547D\u4EE4\u3002",
    unknownError: "\u767C\u751F\u672A\u77E5\u932F\u8AA4"
  },
  instructions: {
    navigate: "\u9078\u64C7\u9805\u76EE",
    use: "\u5957\u7528",
    useNewWindow: "\u5728\u65B0\u8996\u7A97\u958B\u555F",
    dismiss: "\u95DC\u9589"
  },
  activityBar: {
    toggleColumn: "\u5207\u63DB\u5074\u6B04",
    toggleGroup: "\u5207\u63DB\u76EE\u524D\u5206\u9801\u7FA4\u7D44",
    cannotHideLastPane: "\u7121\u6CD5\u96B1\u85CF\u6700\u5F8C\u4E00\u500B\u53EF\u898B\u7684\u5206\u9801",
    onlyInPopout: "\u6B64\u547D\u4EE4\u50C5\u53EF\u5728 Popout \u8996\u7A97\u5167\u4F7F\u7528",
    openSettings: "\u958B\u555F Window Spaces \u8A2D\u5B9A",
    leftActivityBar: "Left activity bar",
    leftSidebar: "Left sidebar",
    rightActivityBar: "Right activity bar",
    rightSidebar: "Right sidebar"
  }
};

// src/i18n/zh-CN.ts
var zhCN = {
  common: {
    save: "\u4FDD\u5B58",
    restore: "\u6062\u590D",
    cancel: "\u53D6\u6D88",
    delete: "\u5220\u9664",
    rename: "\u91CD\u547D\u540D",
    confirm: "\u786E\u8BA4",
    success: "\u6210\u529F",
    error: "\u9519\u8BEF",
    warning: "\u8B66\u544A",
    loading: "\u52A0\u8F7D\u4E2D",
    close: "\u5173\u95ED",
    ok: "\u786E\u5B9A",
    layoutLabel: "\u7A7A\u95F4",
    noLayout: "\u5C1A\u672A\u5E94\u7528\u7A7A\u95F4",
    newWindow: "\u65B0\u7A97\u53E3",
    edit: "\u7F16\u8F91",
    windowLayouts: "Window Spaces",
    openAsPanel: "\u9762\u677F\u4F4D\u7F6E"
  },
  commands: {
    saveLayout: "\u4FDD\u5B58\u5F53\u524D\u7A7A\u95F4",
    saveLayoutDirect: "\u76F4\u63A5\u4FDD\u5B58\u5F53\u524D\u7A7A\u95F4",
    openLayouts: "\u6253\u5F00\u5207\u6362\u5668",
    openLayoutsRibbon: "\u6253\u5F00 Window Spaces \u5207\u6362\u5668",
    openLayoutsPanel: "\u4F5C\u4E3A\u6807\u7B7E\u9875\u9762\u677F\u6253\u5F00",
    openLayoutsPanelLeft: "\u5728\u5DE6\u4FA7\u8FB9\u680F\u6253\u5F00",
    openLayoutsPanelRight: "\u5728\u53F3\u4FA7\u8FB9\u680F\u6253\u5F00",
    toggleLeftActivityBar: "\u5207\u6362\u5DE6\u4FA7 Activity Bar",
    toggleRightActivityBar: "\u5207\u6362\u53F3\u4FA7 Activity Bar",
    toggleCurrentPaneGroup: "\u5207\u6362\u5F53\u524D\u6807\u7B7E\u7EC4"
  },
  saveModal: {
    title: "\u7A7A\u95F4\u8BBE\u7F6E",
    nameLabel: "\u7A7A\u95F4\u540D\u79F0",
    namePlaceholder: "\u8F93\u5165\u7A7A\u95F4\u540D\u79F0...",
    descriptionLabel: "\u63CF\u8FF0\uFF08\u53EF\u9009\uFF09",
    descriptionPlaceholder: "\u8F93\u5165\u7A7A\u95F4\u63CF\u8FF0...",
    infoSection: "\u7A7A\u95F4\u4FE1\u606F",
    windowSize: "\u7A97\u53E3\u5927\u5C0F",
    includePosition: "\u5305\u542B\u7A97\u53E3\u4F4D\u7F6E",
    includePositionDesc: "\u4FDD\u5B58\u7A97\u53E3\u5728\u5C4F\u5E55\u4E0A\u7684\u4F4D\u7F6E\u5750\u6807",
    includeWindowSize: "\u5305\u542B\u7A97\u53E3\u5927\u5C0F",
    includeWindowSizeDesc: "\u4FDD\u5B58\u7A97\u53E3\u7684\u5BBD\u5EA6\u4E0E\u9AD8\u5EA6",
    includeGeometry: "\u5305\u542B\u7A97\u53E3\u4F4D\u7F6E\u4E0E\u5927\u5C0F",
    includeGeometryDesc: "\u4FDD\u5B58\u5E76\u6062\u590D\u7A97\u53E3\u5728\u5C4F\u5E55\u4E0A\u7684\u5750\u6807\u4F4D\u7F6E\u4E0E\u5BBD\u9AD8\u5C3A\u5BF8",
    andOthers: "\u53CA\u5176\u4ED6",
    overwriteNotice: "\u5C06\u8986\u76D6\u66F4\u65B0\u73B0\u6709\u7A7A\u95F4",
    saveButton: "\u4FDD\u5B58\u7A7A\u95F4",
    cancelButton: "\u53D6\u6D88",
    emptyNameError: "\u7A7A\u95F4\u540D\u79F0\u4E0D\u80FD\u4E3A\u7A7A",
    duplicateNameError: "\u6B64\u540D\u79F0\u7684\u7A7A\u95F4\u5DF2\u5B58\u5728",
    iconLabel: "\u7A7A\u95F4 Logo",
    iconPlaceholder: "\u4F8B\u5982\uFF1A\u{1F680} \u6216 star\uFF08icon \u6216 emoji\uFF09",
    iconDesc: "\u652F\u6301\u4F7F\u7528 icon \u6216 emoji",
    colorLabel: "\u7A97\u53E3\u8FB9\u6846\u989C\u8272",
    colorPresetLabel: "\u5FEB\u6377\u8C03\u8272\u76D8",
    clearColor: "\u6E05\u9664\u989C\u8272",
    saveSuccess: "\u7A7A\u95F4\u4FDD\u5B58\u6210\u529F",
    autoSaveToggle: "\u542F\u7528\u6B64\u7A7A\u95F4\u7684\u81EA\u52A8\u4FDD\u5B58",
    borderInset: "\u8FB9\u6846 inset \u539A\u5EA6",
    borderInsetDesc: "\u8BBE\u7F6E\u6570\u503C\u65F6\u7ED8\u5236\u81EA\u5B9A\u4E49\u8FB9\u6846\uFF1B\u672A\u4FEE\u6539\u65F6\u4FDD\u7559 Obsidian \u539F\u751F accent \u8FB9\u6846",
    foldedCorner: "\u663E\u793A\u5DE6\u4E0A\u89D2\u6298\u9875\u88C5\u9970",
    foldedCornerDesc: "\u672A\u4FEE\u6539\u65F6\u8DDF\u968F\u5168\u5C40\u9ED8\u8BA4\u503C",
    activityBarsSection: "Activity Bars",
    showActivityBar: "\u663E\u793A",
    showActivityBarTitle: "\u663E\u793A\u6B64\u4FA7 Activity Bar",
    showActivityBarDesc: "\u5F00\u542F\u65F6\uFF0C\u6B64\u7A7A\u95F4\u4F1A\u5728 Popout \u7A97\u53E3\u663E\u793A\u8BE5\u4FA7 Activity Bar",
    showLeftActivityBar: "\u663E\u793A\u5DE6\u4FA7 Activity Bar",
    showRightActivityBar: "\u663E\u793A\u53F3\u4FA7 Activity Bar",
    activityBarViewButtons: "View \u6309\u94AE",
    spaceSection: "\u7A7A\u95F4",
    windowAppearanceSection: "\u7A97\u53E3\u5916\u89C2",
    behaviorSection: "\u7A7A\u95F4\u884C\u4E3A",
    sectionsSection: "Section \u5206\u7EC4"
  },
  restoreModal: {
    title: "\u6062\u590D\u7A7A\u95F4",
    selectLayout: "\u9009\u62E9\u8981\u6062\u590D\u7684\u7A7A\u95F4\uFF1A",
    noLayoutsMessage: "\u6CA1\u6709\u627E\u5230\u5DF2\u4FDD\u5B58\u7684\u7A7A\u95F4\u3002",
    restoreButton: "\u6062\u590D\u7A7A\u95F4",
    restoreHint: "\u6062\u590D\u7A7A\u95F4\uFF08\u70B9\u51FB\u6216\u6309 Enter \u5728\u65B0\u7A97\u53E3\u6253\u5F00\uFF1B\u957F\u6309\u7A7A\u95F4\u9879\u6216\u6062\u590D\u6309\u94AE\u3001\u6309\u4F4F Shift \u70B9\u51FB\u6216\u6309 Shift+Enter \u5E94\u7528\u81F3\u5F53\u524D\u7A97\u53E3\uFF09",
    cancelButton: "\u53D6\u6D88",
    restoreSuccess: "\u7A7A\u95F4\u6062\u590D\u6210\u529F",
    restoreError: "\u6062\u590D\u7A7A\u95F4\u5931\u8D25",
    includedFiles: "\u5305\u542B\u6587\u4EF6",
    restoringLayout: "\u6B63\u5728\u6062\u590D\u7A7A\u95F4"
  },
  manageModal: {
    title: "\u7BA1\u7406 Window Spaces",
    noLayoutsMessage: "\u6CA1\u6709\u627E\u5230\u5DF2\u4FDD\u5B58\u7684\u7A7A\u95F4\u3002",
    searchPlaceholder: "\u67E5\u627E\u6216\u521B\u5EFA\u7A7A\u95F4...",
    enterToCreate: "Enter \u952E\u4EE5\u521B\u5EFA",
    clearSearch: "\u6E05\u9664\u641C\u7D22",
    saveCurrentButton: "Save",
    layoutName: "\u7A7A\u95F4\u540D\u79F0",
    createdDate: "\u521B\u5EFA\u65F6\u95F4",
    updatedDate: "\u66F4\u65B0\u65F6\u95F4",
    fileCount: "\u6587\u4EF6\u6570",
    tabCount: "\u6807\u7B7E\u9875\u6570",
    actions: "\u64CD\u4F5C",
    renameButton: "\u91CD\u547D\u540D",
    deleteButton: "\u5220\u9664",
    confirmDeleteTitle: "\u5220\u9664\u7A7A\u95F4",
    confirmDeleteMessage: "\u60A8\u786E\u5B9A\u8981\u5220\u9664\u8FD9\u4E2A\u7A7A\u95F4\u5417\uFF1F\u6B64\u64CD\u4F5C\u65E0\u6CD5\u64A4\u9500\u3002",
    deleteSuccess: "\u7A7A\u95F4\u5220\u9664\u6210\u529F",
    renameSuccess: "\u7A7A\u95F4\u91CD\u547D\u540D\u6210\u529F",
    sortDateDesc: "\u66F4\u65B0\u65F6\u95F4 (\u6700\u65B0)",
    sortDateAsc: "\u66F4\u65B0\u65F6\u95F4 (\u8F83\u65E7)",
    sortUpdatedDesc: "\u66F4\u65B0\u65F6\u95F4 (\u6700\u65B0)",
    sortUpdatedAsc: "\u66F4\u65B0\u65F6\u95F4 (\u8F83\u65E7)",
    sortCreatedDesc: "\u521B\u5EFA\u65F6\u95F4 (\u6700\u65B0)",
    sortCreatedAsc: "\u521B\u5EFA\u65F6\u95F4 (\u8F83\u65E7)",
    sortNameAsc: "\u540D\u79F0 (A-Z)",
    sortNameDesc: "\u540D\u79F0 (Z-A)",
    autoSaveEnabled: "\u81EA\u52A8\u4FDD\u5B58\u5DF2\u542F\u7528",
    autoSaveDisabled: "\u81EA\u52A8\u4FDD\u5B58\u5DF2\u7981\u7528",
    windowOpenBadge: "\u7A97\u53E3\u5DF2\u6253\u5F00",
    viewOptions: "\u663E\u793A\u9009\u9879",
    groupBySection: "\u6309 Section \u5206\u7EC4",
    flatView: "\u4E0D\u5206\u7EC4\u5217\u8868",
    showArchived: "\u663E\u793A\u5F52\u6863\u7A7A\u95F4",
    hideArchived: "\u9690\u85CF\u5F52\u6863\u7A7A\u95F4",
    uncategorized: "\u672A\u5206\u7C7B",
    archivedGroup: "\u{1F4E6} \u5F52\u6863\u7A7A\u95F4",
    archiveSpace: "\u5F52\u6863\u7A7A\u95F4",
    unarchiveSpace: "\u53D6\u6D88\u5F52\u6863",
    archiveSuccess: "\u5DF2\u5F52\u6863\u7A7A\u95F4",
    unarchiveSuccess: "\u5DF2\u53D6\u6D88\u5F52\u6863\u7A7A\u95F4",
    renameSection: "\u91CD\u547D\u540D Section",
    sectionsLabel: "Section \u5206\u7EC4\u6807\u7B7E",
    sectionsPlaceholder: "\u8F93\u5165 Section \u540D\u79F0\u540E\u6309 Enter...",
    sectionHeaderHint: "\u70B9\u51FB\u6298\u53E0/\u5C55\u5F00 \xB7 \u53CC\u51FB\u91CD\u547D\u540D \xB7 \u62D6\u62FD\u6392\u5E8F",
    sectionHeaderHintStatic: "\u70B9\u51FB\u6298\u53E0/\u5C55\u5F00"
  },
  settings: {
    title: "Window Spaces \u8BBE\u7F6E",
    description: "\u7BA1\u7406\u60A8\u7684 Window Spaces \u5E76\u914D\u7F6E\u81EA\u52A8\u4FDD\u5B58\u9009\u9879\u3002",
    generalSection: "Window Spaces \u5E38\u89C4\u8BBE\u7F6E",
    autoSaveDescription: "\u5B9A\u671F\u81EA\u52A8\u4FDD\u5B58 Window Spaces\u3002",
    autoSaveEnabled: "\u542F\u7528\u81EA\u52A8\u4FDD\u5B58",
    showNotifications: "\u663E\u793A\u901A\u77E5",
    showNotificationsDesc: "\u5728\u4FDD\u5B58\u6216\u6062\u590D\u7A7A\u95F4\u65F6\u663E\u793A\u901A\u77E5\u6D88\u606F",
    popoutDefaultsSection: "\u65B0 Popout \u9ED8\u8BA4\u8BBE\u7F6E",
    showLayoutStatusBar: "\u663E\u793A\u7A7A\u95F4\u72B6\u6001\u680F",
    showLayoutStatusBarDesc: "\u5728\u6BCF\u4E2A Popout \u7A97\u53E3\u5DE6\u4E0B\u65B9\u4EE5\u72B6\u6001\u680F\u6837\u5F0F\u663E\u793A\u5F53\u524D\u7A7A\u95F4\u540D\u79F0\u4E0E\u4FDD\u5B58\u6309\u94AE",
    showWindowLayoutsRibbonIcon: "\u663E\u793A\u201CWindow Spaces\u201D\u4FA7\u8FB9\u680F\u56FE\u6807",
    showWindowLayoutsRibbonIconDesc: "\u5728\u4E3B\u7A97\u53E3\u5DE6\u4FA7\u8FB9\u680F\u663E\u793A\u6062\u590D\u4E0E\u7BA1\u7406 Window Spaces \u7684\u5355\u4E00\u5165\u53E3",
    autoSaveInterval: "\u81EA\u52A8\u4FDD\u5B58\u95F4\u9694",
    minutes: "\u5206\u949F",
    resetSettings: "\u91CD\u7F6E\u8BBE\u7F6E",
    resetSettingsDescription: "\u5C06\u6240\u6709\u8BBE\u7F6E\u91CD\u7F6E\u4E3A\u9ED8\u8BA4\u503C\u3002",
    resetButton: "\u91CD\u7F6E\u8BBE\u7F6E",
    resetConfirmTitle: "\u786E\u8BA4\u91CD\u7F6E",
    resetConfirmMessage: "\u60A8\u786E\u5B9A\u8981\u91CD\u7F6E\u6240\u6709\u8BBE\u7F6E\u5417\uFF1F\u8FD9\u4E0D\u4F1A\u5220\u9664\u60A8\u5DF2\u4FDD\u5B58\u7684\u7A7A\u95F4\u3002",
    resetSuccess: "\u8BBE\u7F6E\u91CD\u7F6E\u6210\u529F",
    popoutSidebarSection: "Popout \u7A97\u53E3 View \u8BBE\u7F6E",
    enableInterceptor: "\u5C06 View \u8DEF\u7531\u81F3\u6240\u5C5E\u7684 Popout \u7A97\u53E3",
    enableInterceptorDesc: "\u4ECE Popout \u7A97\u53E3\u6253\u5F00 View \u65F6\uFF0C\u65E0\u8BBA\u76EE\u6807\u662F\u4FA7\u680F\u8FD8\u662F editor tab area\uFF0C\u90FD\u4F1A\u4FDD\u7559\u5728\u8BE5 Popout\uFF0C\u800C\u4E0D\u662F\u8DEF\u7531\u56DE\u4E3B\u7A97\u53E3\u3002",
    leftBar: "\u5DE6\u4FA7 Activity Bar",
    rightBar: "\u53F3\u4FA7 Activity Bar",
    addView: "\u6DFB\u52A0 View",
    removeView: "\u79FB\u9664",
    viewTypePlaceholder: "View type\uFF08\u4F8B\u5982 file-explorer\uFF09",
    pickIcon: "\u9009\u62E9 Logo",
    restoreDefaultButtons: "\u8FD8\u539F\u9ED8\u8BA4\u6309\u94AE",
    restoreDefaultIcon: "\u8FD8\u539F\u9ED8\u8BA4\u56FE\u6807",
    accentSection: "\u7A97\u53E3\u5916\u89C2\u4E0E\u56FE\u6807",
    defaultIcon: "\u9ED8\u8BA4\u7A7A\u95F4 Logo",
    defaultIconDesc: "\u7A7A\u95F4 Logo \u4F7F\u7528\u7684\u9ED8\u8BA4 icon \u6216 emoji",
    defaultBorderInset: "\u9ED8\u8BA4\u8FB9\u6846 inset \u539A\u5EA6",
    defaultBorderInsetDesc: "\u8BBE\u7F6E\u4E3A 0 \u65F6\u4E0D\u663E\u793A Popout \u7A97\u53E3\u8FB9\u6846",
    defaultFoldedCorner: "\u9ED8\u8BA4\u663E\u793A\u5DE6\u4E0A\u89D2\u6298\u9875\u88C5\u9970",
    defaultFoldedCornerDesc: "\u5728 Popout \u7A97\u53E3\u5DE6\u4E0A\u89D2\u663E\u793A\u6298\u9875\u88C5\u9970",
    defaultActivityBarVisibility: "\u65B0\u5EFA Window Space \u65F6\u9ED8\u8BA4\u663E\u793A\u6B64 Activity Bar",
    defaultActivityBarVisibilityDesc: "\u63A7\u5236\u662F\u5426\u663E\u793A\u6574\u4E2A Activity Bar",
    dragToReorder: "\u62D6\u653E\u6392\u5E8F",
    keepOneActivityBarView: "\u4ECD\u6709\u5176\u4ED6\u53EF\u7528 View \u65F6\uFF0CActivity Bar \u81F3\u5C11\u8981\u4FDD\u7559\u4E00\u4E2A\u6309\u94AE",
    popoutAccentsTitle: "Popout \u7A7A\u95F4\u4E3B\u9898\u88C5\u9970\u589E\u5F3A",
    popoutAccentsDesc: "\u4E3A Popout \u7A97\u53E3\u6CE8\u5165\u7A7A\u95F4\u4E3B\u9898\u8272\u3002\u82E5\u4E0E\u7B2C\u4E09\u65B9\u4E3B\u9898\u6837\u5F0F\u51B2\u7A81\uFF0C\u53EF\u968F\u65F6\u5173\u95ED\u3002",
    popoutAccentsEnable: "\u542F\u7528\u7A7A\u95F4\u4E3B\u9898\u88C5\u9970\u589E\u5F3A",
    popoutAccentsEnableDesc: "\u5728 Popout \u7A97\u53E3\u4E2D\u5E94\u7528\u6807\u7B7E\u9875\u5E95\u8272\u4E0E\u8FB9\u6846\u3001\u7A97\u683C\u5206\u9694\u7EBF\u9AD8\u4EAE\u3001\u7A97\u683C\u8FB9\u6846\u4E0E\u6D3B\u52A8\u680F\u6C1B\u56F4\u8272",
    popoutAccentsSplitter: "\u7A97\u683C\u8C03\u6574\u5206\u9694\u7EBF\u4E0E\u9AD8\u4EAE",
    popoutAccentsSplitterDesc: "\u7A97\u683C\u5206\u5272\u6761\u5E26\u6709\u4E3B\u9898\u8272\u5FAE\u5149\uFF0C\u60AC\u505C\u4E0E\u62D6\u62FD\u65F6\u663E\u793A\u9AD8\u4EAE\u4E0E\u5149\u6655",
    popoutAccentsActivityBar: "Activity Bar \u6C1B\u56F4\u5E95\u8272",
    popoutAccentsActivityBarDesc: "\u5728\u5DE6\u53F3\u4E24\u4FA7 Activity Bar \u6CE8\u5165\u67D4\u548C\u7684\u7A7A\u95F4\u4E3B\u9898\u5E95\u8272",
    manageSpaces: "\u7BA1\u7406\u7A7A\u95F4...",
    showInSystemExplorer: "\u5728\u7CFB\u7EDF\u6587\u4EF6\u5939\u4E2D\u663E\u793A",
    copyPath: "\u590D\u5236\u8DEF\u5F84",
    pathCopied: "\u8DEF\u5F84\u5DF2\u590D\u5236\u5230\u526A\u8D34\u677F"
  },
  notifications: {
    layoutSaved: "\u7A7A\u95F4\u4FDD\u5B58\u6210\u529F",
    layoutOverwritten: "\u5DF2\u8986\u76D6\u66F4\u65B0\u7A7A\u95F4",
    layoutRestored: "\u7A7A\u95F4\u6062\u590D\u6210\u529F",
    layoutDeleted: "\u7A7A\u95F4\u5220\u9664\u6210\u529F",
    layoutRenamed: "\u7A7A\u95F4\u91CD\u547D\u540D\u6210\u529F",
    settingsReset: "\u8BBE\u7F6E\u91CD\u7F6E\u6210\u529F",
    errorOccurred: "\u53D1\u751F\u9519\u8BEF",
    invalidLayout: "\u65E0\u6548\u7684\u7A7A\u95F4\u6570\u636E",
    cannotRestore: "\u65E0\u6CD5\u6062\u590D\u7A7A\u95F4",
    missingFilesNotice: "\u5305\u542B\u4E0D\u5B58\u5728\u7684\u6587\u4EF6",
    switchedToOpenWindow: "\u5DF2\u5207\u6362\u81F3\u300C{{name}}\u300D\u7684\u5DF2\u6253\u5F00\u7A7A\u95F4"
  },
  errors: {
    failedToSave: "\u4FDD\u5B58\u7A7A\u95F4\u5931\u8D25",
    failedToRestore: "\u6062\u590D\u7A7A\u95F4\u5931\u8D25",
    failedToDelete: "\u5220\u9664\u7A7A\u95F4\u5931\u8D25",
    failedToRename: "\u91CD\u547D\u540D\u7A7A\u95F4\u5931\u8D25",
    layoutNotFound: "\u627E\u4E0D\u5230\u7A7A\u95F4",
    invalidData: "\u65E0\u6548\u7684\u6570\u636E\u683C\u5F0F",
    permissionDenied: "\u6743\u9650\u88AB\u62D2\u7EDD",
    notInPopoutWindow: "\u8BF7\u5728\u5F39\u51FA\u7A97\u53E3\uFF08Popout Window\uFF09\u4E2D\u6267\u884C\u6B64\u547D\u4EE4\u3002",
    unknownError: "\u53D1\u751F\u672A\u77E5\u9519\u8BEF"
  },
  instructions: {
    navigate: "\u9009\u62E9",
    use: "\u5E94\u7528",
    useNewWindow: "\u5728\u65B0\u7A97\u53E3\u6253\u5F00",
    dismiss: "\u5173\u95ED"
  },
  activityBar: {
    toggleColumn: "\u5207\u6362\u4FA7\u680F",
    toggleGroup: "\u5207\u6362\u5F53\u524D\u6807\u7B7E\u7EC4",
    cannotHideLastPane: "\u65E0\u6CD5\u9690\u85CF\u6700\u540E\u4E00\u4E2A\u53EF\u89C1\u7684\u6807\u7B7E",
    onlyInPopout: "\u6B64\u547D\u4EE4\u53EA\u80FD\u5728 Popout \u7A97\u53E3\u5185\u4F7F\u7528",
    openSettings: "\u6253\u5F00 Window Spaces \u8BBE\u7F6E",
    leftActivityBar: "Left activity bar",
    leftSidebar: "Left sidebar",
    rightActivityBar: "Right activity bar",
    rightSidebar: "Right sidebar"
  }
};

// src/i18n/index.ts
var I18nManager = class {
  app;
  currentLocale = "en";
  translations = {
    en,
    "zh-TW": zhTW,
    "zh-CN": zhCN
  };
  constructor(app) {
    this.app = app;
    this.currentLocale = this.detectLocale();
  }
  /**
   * 檢測當前語言環境 (多重源實時動態求值)
   */
  detectLocale() {
    try {
      const langStorage = typeof window !== "undefined" ? window.localStorage.getItem("language") : null;
      const momentLocale = typeof window !== "undefined" && window.moment?.locale ? window.moment.locale() : null;
      const vaultConfig = this.app?.vault?.config;
      const vaultLocale = vaultConfig?.locale || vaultConfig?.userLanguage;
      const rawLocale = String(langStorage || momentLocale || vaultLocale || "en").toLowerCase();
      if (rawLocale.startsWith("zh")) {
        if (rawLocale.includes("tw") || rawLocale.includes("hk") || rawLocale.includes("mo") || rawLocale.includes("hant") || rawLocale === "zh-cht") {
          return "zh-TW";
        }
        return "zh-CN";
      }
    } catch (e) {
      console.warn("[Window Spaces] Failed to detect locale, fallback to en:", e);
    }
    return "en";
  }
  /**
   * 獲取當前語言環境
   */
  getCurrentLocale() {
    return this.detectLocale();
  }
  /**
   * 設置語言環境
   */
  setLocale(locale) {
    this.currentLocale = locale;
  }
  /**
   * 獲取翻譯字符串 (實時動態取得當前最新語系)
   */
  t(key) {
    const activeLocale = this.detectLocale();
    const keys = key.split(".");
    let value = this.translations[activeLocale];
    for (const k of keys) {
      if (value && typeof value === "object" && k in value) {
        value = value[k];
      } else {
        value = this.translations["en"];
        for (const fallbackKey of keys) {
          if (value && typeof value === "object" && fallbackKey in value) {
            value = value[fallbackKey];
          } else {
            return key;
          }
        }
        break;
      }
    }
    return typeof value === "string" ? value : key;
  }
  /**
   * 獲取帶參數的翻譯字符串
   */
  tWithParams(key, params) {
    let translation = this.t(key);
    for (const [param, value] of Object.entries(params)) {
      translation = translation.replace(
        new RegExp(`{{${param}}}`, "g"),
        String(value)
      );
    }
    return translation;
  }
  /**
   * 獲取所有支持的語言
   */
  getSupportedLocales() {
    return Object.keys(this.translations);
  }
  /**
   * 獲取語言的顯示名稱
   */
  getLocaleDisplayName(locale) {
    const displayNames = {
      en: "English",
      "zh-TW": "\u7E41\u9AD4\u4E2D\u6587",
      "zh-CN": "\u7B80\u4F53\u4E2D\u6587"
    };
    return displayNames[locale] || locale;
  }
  /**
   * 檢查是否為從右到左的語言
   */
  isRTL() {
    return false;
  }
  /**
   * 格式化日期
   */
  formatDate(date) {
    const localeMap = {
      en: "en-US",
      "zh-TW": "zh-TW",
      "zh-CN": "zh-CN"
    };
    try {
      return date.toLocaleDateString(localeMap[this.detectLocale()], {
        year: "numeric",
        month: "short",
        day: "numeric",
        hour: "2-digit",
        minute: "2-digit"
      });
    } catch (error) {
      return date.toISOString();
    }
  }
  /**
   * 格式化數字
   */
  formatNumber(num) {
    const localeMap = {
      en: "en-US",
      "zh-TW": "zh-TW",
      "zh-CN": "zh-CN"
    };
    try {
      return num.toLocaleString(localeMap[this.detectLocale()]);
    } catch (error) {
      return String(num);
    }
  }
};
var i18nInstance = null;
function initI18n(app) {
  i18nInstance = new I18nManager(app);
  return i18nInstance;
}
function getI18n() {
  if (!i18nInstance) {
    throw new Error("I18n not initialized. Call initI18n() first.");
  }
  return i18nInstance;
}
function t(key) {
  return getI18n().t(key);
}
function tWithParams(key, params) {
  return getI18n().tWithParams(key, params);
}

// src/spaceVisuals.ts
var DEFAULT_SPACE_ICON = "square";
function resolveSpaceIcon(icon, configuredDefaultIcon) {
  return icon?.trim() || configuredDefaultIcon?.trim() || DEFAULT_SPACE_ICON;
}
function isSpaceEmoji(value) {
  return /\p{Extended_Pictographic}/u.test(value) || !/^[a-zA-Z0-9-]+$/.test(value);
}

// src/modals/restoreModal.ts
var import_obsidian2 = require("obsidian");

// src/popout/viewRegistry.ts
var import_obsidian = require("obsidian");
var BUILTIN_SIDEBAR_VIEWS = [
  { viewType: "file-explorer", label: "File explorer", icon: "folder", side: "left" },
  { viewType: "search", label: "Search", icon: "search", side: "left" },
  { viewType: "outline", label: "Outline", icon: "list-tree", side: "left" },
  { viewType: "window-spaces-layouts", label: "Window Spaces", icon: "layout", side: "left" },
  { viewType: "bookmarks", label: "Bookmarks", icon: "bookmark", side: "right" },
  { viewType: "backlink", label: "Backlinks", icon: "link", side: "right" },
  { viewType: "tag", label: "Tags", icon: "tag", side: "right" },
  { viewType: "all-properties", label: "Properties", icon: "list", side: "right" },
  { viewType: "canvas", label: "Canvas", icon: "frame", side: "right" }
];
var EXCLUDED_VIEW_TYPES = /* @__PURE__ */ new Set([
  "empty",
  "markdown",
  "pdf",
  "image",
  "audio",
  "video",
  "release-notes",
  "sync"
]);
function getViewRegistry(app) {
  return app?.viewRegistry ?? {};
}
function getRegistryViewTypes(app) {
  try {
    const registry = getViewRegistry(app);
    const viewByType = registry.viewByType;
    if (!viewByType || typeof viewByType !== "object") return [];
    return Object.keys(viewByType).filter((type) => !EXCLUDED_VIEW_TYPES.has(type));
  } catch {
    return [];
  }
}
function setIconWithCheck(el, name) {
  try {
    el.empty();
    (0, import_obsidian.setIcon)(el, name);
    const svg = el.querySelector("svg");
    return !!svg && svg.children.length > 0;
  } catch {
    return false;
  }
}
var FILE_VIEW_ICONS = {
  markdown: "file-text",
  pdf: "file-text",
  image: "image",
  audio: "audio",
  video: "video",
  canvas: "frame"
};
function resolveViewIcon(_app, viewType) {
  const fixedFileIcon = FILE_VIEW_ICONS[viewType];
  if (fixedFileIcon) return fixedFileIcon;
  const cached = iconCache.get(viewType);
  if (cached?.icon) return cached.icon;
  const builtin = BUILTIN_SIDEBAR_VIEWS.find((item) => item.viewType === viewType);
  const icon = builtin?.icon ?? null;
  iconCache.set(viewType, { icon, dynamicAttempted: cached?.dynamicAttempted ?? false });
  return icon ?? "layout";
}
function applyViewIcon(btn, app, viewType, opts) {
  const fixedFileIcon = FILE_VIEW_ICONS[viewType];
  if (fixedFileIcon && setIconWithCheck(btn, fixedFileIcon)) return;
  const cached = iconCache.get(viewType);
  const icon = cached?.icon ?? BUILTIN_SIDEBAR_VIEWS.find((item) => item.viewType === viewType)?.icon ?? null;
  if (icon) {
    setIconWithCheck(btn, icon);
  } else {
    (0, import_obsidian.setIcon)(btn, "layout");
  }
  if (!icon && opts?.allowDynamicIcon && !cached?.dynamicAttempted) {
    iconCache.set(viewType, { icon: null, dynamicAttempted: true });
    void detectViewIcon(app, viewType).then((dynamicIcon) => {
      if (!dynamicIcon) return;
      iconCache.set(viewType, { icon: dynamicIcon, dynamicAttempted: true });
      if (btn.isConnected) setIconWithCheck(btn, dynamicIcon);
    });
  }
}
function applyItemIcon(btn, app, item) {
  if (item.icon && setIconWithCheck(btn, item.icon)) return;
  applyViewIcon(btn, app, item.viewType, { allowDynamicIcon: true });
}
var iconCache = /* @__PURE__ */ new Map();
function findIconFromOpenLeaves(app, viewType) {
  let found = null;
  const ws = app.workspace;
  if (typeof ws.iterateAllLeaves !== "function") return null;
  ws.iterateAllLeaves((leaf) => {
    if (found) return;
    const view = leaf?.view;
    if (!view) return;
    try {
      const vt = typeof view.getViewType === "function" ? view.getViewType() : "";
      if (vt !== viewType) return;
      const icon = typeof view.getIcon === "function" ? view.getIcon() : "";
      if (icon && typeof icon === "string") found = icon;
    } catch {
    }
  });
  return found;
}
function getViewCreatorForType(app, viewType) {
  const registry = getViewRegistry(app);
  let creator = null;
  if (typeof registry.getViewCreator === "function") {
    const c = registry.getViewCreator(viewType);
    if (typeof c === "function") creator = c;
  }
  if (!creator) {
    const entry = (registry.viewByType ?? {})[viewType];
    if (typeof entry === "function") {
      creator = entry;
    } else if (entry && typeof entry === "object") {
      for (const key of ["creator", "view", "viewCreator"]) {
        const candidate = entry[key];
        if (typeof candidate === "function") {
          creator = candidate;
          break;
        }
      }
    }
  }
  return creator;
}
function getIconFromRegistryEntry(app, viewType) {
  const entry = getViewRegistry(app).viewByType?.[viewType];
  if (entry && typeof entry.getIcon === "function") {
    try {
      const icon = entry.getIcon();
      if (icon && typeof icon === "string") return icon;
    } catch {
    }
  }
  return null;
}
async function getIconFromEphemeralView(app, viewType) {
  const creator = getViewCreatorForType(app, viewType);
  if (!creator) return null;
  const host = document.createElement("div");
  host.style.display = "none";
  try {
    document.body.appendChild(host);
    const leaf = {
      app,
      containerEl: host,
      view: null,
      viewState: { type: viewType, state: {}, eState: {} },
      // View 基底建構子建立導覽按鈕時會讀 leaf.history.backHistory / forwardHistory
      history: { backHistory: [], forwardHistory: [] },
      parent: null,
      getViewState: () => ({ type: viewType, state: {}, eState: {} }),
      setViewState: () => Promise.resolve(),
      getRoot: () => null,
      detach: () => void 0
    };
    const view = creator(leaf);
    leaf.view = view;
    const icon = typeof view.getIcon === "function" ? view.getIcon() : "";
    return icon && typeof icon === "string" ? icon : null;
  } catch (error) {
    console.debug(`[Window Spaces] Ephemeral view creation failed for "${viewType}"`, error);
    return null;
  } finally {
    host.remove();
  }
}
async function getIconFromRealLeaf(app, viewType) {
  const workspace = app.workspace;
  if (typeof workspace.getLeaf !== "function") return null;
  let leaf = null;
  try {
    leaf = workspace.getLeaf("tab");
    const container = leaf.containerEl;
    if (container instanceof HTMLElement) {
      container.style.display = "none";
    }
    await leaf.setViewState({ type: viewType, active: false, state: {} });
    const view = leaf.view;
    const icon = view && typeof view.getIcon === "function" ? view.getIcon() : "";
    return icon && typeof icon === "string" ? icon : null;
  } catch {
    return null;
  } finally {
    if (leaf && typeof leaf.detach === "function") {
      try {
        leaf.detach();
      } catch {
      }
    }
  }
}
async function detectViewIcon(app, viewType) {
  const openIcon = findIconFromOpenLeaves(app, viewType);
  if (openIcon) return openIcon;
  const entryIcon = getIconFromRegistryEntry(app, viewType);
  if (entryIcon) return entryIcon;
  const ephemeralIcon = await getIconFromEphemeralView(app, viewType);
  if (ephemeralIcon) return ephemeralIcon;
  return getIconFromRealLeaf(app, viewType);
}
async function ensureViewIcon(app, viewType) {
  const cached = iconCache.get(viewType);
  if (cached?.icon) return cached.icon;
  const icon = await detectViewIcon(app, viewType);
  iconCache.set(viewType, { icon, dynamicAttempted: true });
  return icon;
}
var ICON_CHOICES = [
  "folder",
  "search",
  "list-tree",
  "bookmark",
  "link",
  "tag",
  "list",
  "layout",
  "frame",
  "canvas",
  "history",
  "star",
  "hash",
  "file-text",
  "image",
  "audio",
  "video",
  "calendar",
  "mail",
  "message-square",
  "command",
  "terminal",
  "code",
  "pen",
  "pencil",
  "note",
  "copy",
  "settings",
  "sliders-horizontal",
  "filter",
  "globe",
  "eye",
  "eye-off",
  "check",
  "x",
  "plus",
  "minus",
  "arrow-right",
  "arrow-left",
  "arrow-up",
  "arrow-down",
  "chevron-up",
  "chevron-down",
  "chevron-left",
  "chevron-right",
  "panel-left",
  "panel-right",
  "panel-top",
  "panel-bottom",
  "columns",
  "rows",
  "layout-grid",
  "layout-list",
  "grid",
  "inbox",
  "archive",
  "trash",
  "refresh-cw",
  "more-horizontal",
  "more-vertical"
];
function formatViewTypeId(viewType) {
  if (!viewType) return "";
  return viewType.split(/[-_]+/).filter(Boolean).map((word) => word.charAt(0).toUpperCase() + word.slice(1)).join(" ");
}
function resolveViewLabel(app, viewType) {
  try {
    const registry = getViewRegistry(app);
    const label = typeof registry.getDisplayText === "function" ? registry.getDisplayText(viewType) : "";
    if (label) return label;
  } catch {
  }
  const builtin = BUILTIN_SIDEBAR_VIEWS.find((item) => item.viewType === viewType);
  if (builtin?.label) return builtin.label;
  return formatViewTypeId(viewType);
}
function sortViewTypesByLabel(app, viewTypes) {
  return [...viewTypes].sort((a, b) => {
    const labelOrder = resolveViewLabel(app, a).localeCompare(
      resolveViewLabel(app, b),
      void 0,
      { sensitivity: "base" }
    );
    return labelOrder || a.localeCompare(b);
  });
}
function enumerateAvailableViews(app) {
  const seen = /* @__PURE__ */ new Set();
  const left = [];
  const right = [];
  const push = (item) => {
    if (seen.has(item.viewType)) return;
    seen.add(item.viewType);
    if (item.side === "right") right.push(item);
    else left.push(item);
  };
  BUILTIN_SIDEBAR_VIEWS.forEach(push);
  const registryTypes = getRegistryViewTypes(app);
  for (const type of registryTypes) {
    if (!seen.has(type)) {
      push({
        viewType: type,
        side: "left"
      });
    }
  }
  return { left, right };
}

// src/modals/restoreModal.ts
var WindowLayoutsModal = class _WindowLayoutsModal extends import_obsidian2.Modal {
  plugin;
  targetWindow;
  searchInput;
  listEl;
  filteredLayouts = [];
  selectedIndex = 0;
  keydownListener;
  keydownTarget;
  isPanelActive;
  panelRootEl;
  panelMode = false;
  externalHostClose;
  initialFocusTimer;
  initialSearchQuery;
  clearSearchBtn;
  static activeInstances = /* @__PURE__ */ new Set();
  static renderAllInstances() {
    for (const instance of _WindowLayoutsModal.activeInstances) {
      instance.renderLayouts();
    }
  }
  constructor(app, plugin, targetWindow) {
    super(app);
    this.plugin = plugin;
    this.targetWindow = targetWindow;
  }
  onOpen() {
    try {
      _WindowLayoutsModal.activeInstances.add(this);
      this.modalEl.addClass("window-layouts-modal");
      const modalWindow = this.modalEl.ownerDocument?.defaultView;
      const modalBody = modalWindow?.document?.body;
      if (modalWindow && modalBody && (modalBody.classList.contains("is-popout-window") || modalBody.classList.contains("mod-popout"))) {
        this.targetWindow = modalWindow;
      }
      this.setTitle(t("common.windowLayouts"));
      const titleHeader = this.containerEl.querySelector(".modal-title");
      if (titleHeader) {
        this.createHeaderActions(titleHeader);
      }
      this.renderContent();
    } catch (err) {
      console.error("[WindowSpaces] Error during WindowLayoutsModal onOpen:", err);
      _WindowLayoutsModal.activeInstances.delete(this);
      this.removeKeydownListener();
      const message = err instanceof Error ? err.message : String(err);
      this.contentEl.empty();
      this.contentEl.createEl("p", {
        text: `Error loading Window Spaces: ${message}`
      });
    }
  }
  /**
   * Render this modal's content inside an ItemView (or another host).
   * The modal instance is intentionally kept as the controller so the panel
   * and modal always expose the same layout actions and keyboard behavior.
   */
  mountInContainer(rootEl, isSidebar, isPanelActive) {
    void isSidebar;
    _WindowLayoutsModal.activeInstances.add(this);
    this.panelRootEl = rootEl;
    this.isPanelActive = isPanelActive;
    this.panelMode = true;
    this.renderContent();
  }
  /**
   * Mount the shared picker inside a plain native Obsidian Modal.
   *
   * This deliberately avoids calling WindowLayoutsModal.open(): Obsidian
   * 1.13 can fail while opening a subclass with a complex onOpen lifecycle,
   * while a native Modal plus mounted content remains reliable.
   */
  mountInModalContainer(rootEl, closeHost) {
    _WindowLayoutsModal.activeInstances.add(this);
    this.panelRootEl = rootEl;
    this.isPanelActive = void 0;
    this.panelMode = false;
    this.externalHostClose = closeHost;
    this.renderContent();
  }
  unmountFromContainer() {
    _WindowLayoutsModal.activeInstances.delete(this);
    this.removeKeydownListener();
    this.panelRootEl?.empty();
    this.panelRootEl = void 0;
    this.isPanelActive = void 0;
    this.panelMode = false;
    this.externalHostClose = void 0;
  }
  getRootEl() {
    return this.panelRootEl || this.contentEl;
  }
  /**
   * Find the .modal-container that hosts this instance, if any. Panels hosted
   * in sidebars or editor tabs return null; popup pickers mounted inside a
   * native Modal return that modal's container so stacked modals (rename
   * dialog, Command Palette, ...) can be told apart from the picker itself.
   */
  getOwnModalContainer() {
    let el = this.getRootEl();
    const doc = el?.ownerDocument;
    while (el && doc && el !== doc.documentElement) {
      if (typeof el.classList?.contains === "function" && el.classList.contains("modal-container")) {
        return el;
      }
      el = el.parentElement;
    }
    return null;
  }
  closeHost() {
    if (this.externalHostClose) {
      this.externalHostClose();
    } else if (!this.panelMode) {
      this.close();
    }
  }
  renderContent() {
    const contentEl = this.getRootEl();
    contentEl.empty();
    contentEl.addClass("window-layouts-modal");
    contentEl.addClass("window-spaces-modal");
    if (this.panelMode) contentEl.addClass("window-layouts-panel");
    if (this.panelMode) {
      const panelHeader = contentEl.createDiv("nav-header window-layouts-panel-header");
      this.createHeaderActions(panelHeader);
    }
    const toolbar = contentEl.createDiv("window-layouts-toolbar");
    const searchContainer = toolbar.createDiv("window-layouts-search-container search-input-container");
    this.searchInput = searchContainer.createEl("input");
    this.searchInput.type = "search";
    this.searchInput.placeholder = t("manageModal.searchPlaceholder");
    this.searchInput.setAttribute("aria-label", t("manageModal.searchPlaceholder"));
    if (this.initialSearchQuery !== void 0) {
      this.searchInput.value = this.initialSearchQuery;
      this.initialSearchQuery = void 0;
    }
    this.clearSearchBtn = searchContainer.createDiv("window-layouts-search-clear");
    (0, import_obsidian2.setTooltip)(this.clearSearchBtn, t("manageModal.clearSearch") || "Clear search");
    this.clearSearchBtn.onclick = (e) => {
      e.stopPropagation();
      if (this.searchInput) {
        this.searchInput.value = "";
        this.searchInput.focus();
      }
      this.selectedIndex = 0;
      this.renderLayouts();
    };
    this.searchInput.addEventListener("input", () => {
      this.selectedIndex = 0;
      this.renderLayouts();
    });
    this.listEl = contentEl.createDiv("window-layouts-list");
    this.listEl.setAttribute("role", "listbox");
    this.renderLayouts();
    const instructionsEl = contentEl.createDiv("prompt-instructions window-layouts-instructions");
    const navInst = instructionsEl.createDiv("prompt-instruction");
    navInst.createSpan({ text: "\u2191 \u2193", cls: "prompt-instruction-command" });
    navInst.createSpan({ text: t("instructions.navigate") });
    const useInst = instructionsEl.createDiv("prompt-instruction");
    useInst.createSpan({ text: "Shift \u21B5", cls: "prompt-instruction-command" });
    useInst.createSpan({ text: t("instructions.use") });
    const newWinInst = instructionsEl.createDiv("prompt-instruction");
    newWinInst.createSpan({ text: "\u21B5", cls: "prompt-instruction-command" });
    newWinInst.createSpan({ text: t("instructions.useNewWindow") });
    const dismissInst = instructionsEl.createDiv("prompt-instruction");
    dismissInst.createSpan({ text: "esc", cls: "prompt-instruction-command" });
    dismissInst.createSpan({ text: t("instructions.dismiss") });
    const targetDoc = contentEl.ownerDocument || document;
    const targetWindow = targetDoc.defaultView || window;
    this.removeKeydownListener();
    this.keydownListener = (event) => {
      if (event.key !== "ArrowDown" && event.key !== "ArrowUp" && event.key !== "Enter") {
        return;
      }
      const activeEl = targetDoc.activeElement;
      const ownRootEl = this.getRootEl();
      if (activeEl && !ownRootEl.contains(activeEl)) {
        const tagName = activeEl.tagName?.toUpperCase();
        if (tagName === "INPUT" || tagName === "TEXTAREA" || tagName === "SELECT" || activeEl.isContentEditable || activeEl.classList?.contains("cm-content") || Boolean(activeEl.closest(".modal-container, .modal, .prompt, .prompt-container, .menu"))) {
          return;
        }
      }
      let focusedInstance = null;
      for (const instance of _WindowLayoutsModal.activeInstances) {
        const root = instance.getRootEl();
        if (activeEl && root && root.ownerDocument === targetDoc && root.contains(activeEl)) {
          focusedInstance = instance;
          break;
        }
      }
      const eventView = event.view;
      const eventFromThisWindow = eventView == null || eventView === targetWindow;
      const eventTargetDoc = event.target?.ownerDocument ?? null;
      const eventTargetsThisDocument = eventTargetDoc == null || eventTargetDoc === targetDoc;
      const thisDocumentFocused = targetDoc.hasFocus();
      const ownModalContainer = this.getOwnModalContainer();
      const overlays = Array.from(
        targetDoc.querySelectorAll(
          ".modal-container, .modal, .prompt, .prompt-container, .menu"
        )
      );
      const otherModalOpen = overlays.some((el) => {
        if (ownModalContainer && (el === ownModalContainer || ownModalContainer.contains(el))) {
          return false;
        }
        if (ownRootEl.contains(el)) {
          return false;
        }
        const style = targetDoc.defaultView?.getComputedStyle(el);
        return !el.classList.contains("is-hidden") && style?.display !== "none" && style?.visibility !== "hidden";
      });
      const menuOpen = Boolean(
        targetDoc.querySelector(".menu:not(.is-hidden)")
      );
      const anyPopupOpen = Array.from(_WindowLayoutsModal.activeInstances).some(
        (instance) => !instance.panelMode
      );
      const shouldHandle = eventFromThisWindow && eventTargetsThisDocument && thisDocumentFocused && (this.panelMode ? !otherModalOpen && !menuOpen && (focusedInstance === this || !anyPopupOpen && this.isPanelActive?.() === true) : !otherModalOpen && !menuOpen);
      if (!shouldHandle) return;
      if (event.key === "ArrowDown" || event.key === "ArrowUp") {
        event.preventDefault();
        event.stopImmediatePropagation();
        this.handleArrowKey(event.key === "ArrowDown" ? 1 : -1);
        return;
      }
      if (activeEl && activeEl.tagName === "BUTTON") return;
      event.preventDefault();
      event.stopImmediatePropagation();
      const rawQuery = this.searchInput?.value.trim() || "";
      const entry = this.renderedLayoutEntries[this.selectedIndex >= 0 ? this.selectedIndex : 0];
      const selectedLayout = entry ? entry.layout : this.filteredLayouts[0];
      if (selectedLayout) {
        void this.restoreLayout(selectedLayout, !event.shiftKey);
      } else if (rawQuery) {
        void this.createAndSaveLayout(rawQuery, !event.shiftKey);
      }
    };
    this.keydownTarget = targetWindow;
    this.keydownTarget.addEventListener("keydown", this.keydownListener, true);
    if (this.initialFocusTimer !== void 0) {
      const timerWindow = this.modalEl?.ownerDocument?.defaultView || window;
      timerWindow.clearTimeout(this.initialFocusTimer);
    }
    const focusWindow = this.modalEl?.ownerDocument?.defaultView || window;
    this.initialFocusTimer = focusWindow.setTimeout(() => {
      this.initialFocusTimer = void 0;
      if (this.searchInput && this.searchInput.isConnected !== false) {
        this.searchInput.focus();
      }
    }, 50);
  }
  createPanelButton(parentEl) {
    const panelButton = parentEl.createEl("button", {
      cls: "clickable-icon nav-action-button window-layouts-panel-btn",
      attr: { "aria-label": t("common.openAsPanel") }
    });
    (0, import_obsidian2.setIcon)(panelButton, "layout");
    (0, import_obsidian2.setTooltip)(panelButton, t("common.openAsPanel"));
    panelButton.onclick = (e) => {
      e.preventDefault();
      e.stopPropagation();
      this.showPanelMenu(e);
    };
  }
  createHeaderActions(parentEl) {
    const isPanelHeader = parentEl.classList.contains("window-layouts-panel-header");
    const actionsEl = parentEl.createDiv(
      isPanelHeader ? "nav-buttons-container window-layouts-header-actions" : "window-layouts-header-actions"
    );
    const viewOptionsButton = actionsEl.createEl("button", {
      cls: "clickable-icon nav-action-button window-layouts-view-options-btn",
      attr: { "aria-label": t("manageModal.viewOptions") || "View Options" }
    });
    (0, import_obsidian2.setIcon)(viewOptionsButton, "eye");
    (0, import_obsidian2.setTooltip)(viewOptionsButton, t("manageModal.viewOptions") || "View Options");
    viewOptionsButton.onclick = (e) => {
      e.preventDefault();
      e.stopPropagation();
      this.showViewOptionsMenu(e);
    };
    const sortButton = actionsEl.createEl("button", {
      cls: "clickable-icon nav-action-button window-layouts-sort-btn",
      attr: { "aria-label": t("manageModal.sortDateDesc") }
    });
    (0, import_obsidian2.setIcon)(sortButton, "sort-asc");
    (0, import_obsidian2.setTooltip)(sortButton, t("manageModal.sortDateDesc"));
    sortButton.onclick = (e) => {
      e.preventDefault();
      e.stopPropagation();
      this.showSortMenu(e);
    };
    this.createPanelButton(actionsEl);
  }
  /**
   * Mount the shared header action buttons (view options, sort, panel menu)
   * into a native modal's title bar so popup windows expose the same three
   * toolbar actions as the persistent panels.
   */
  mountHeaderActions(titleEl) {
    titleEl.classList.add("has-header-actions");
    if (titleEl.querySelector(".window-layouts-header-actions")) return;
    this.createHeaderActions(titleEl);
  }
  showViewOptionsMenu(event) {
    const menu = new import_obsidian2.Menu();
    const settings = this.plugin?.settings || {};
    const isGrouped = settings.groupBySection !== false;
    const isShowArchived = settings.showArchived === true;
    menu.addItem((item) => {
      item.setTitle(isGrouped ? t("manageModal.groupBySection") || "Group by Section" : t("manageModal.flatView") || "Flat List").setIcon(isGrouped ? "check" : "grid").onClick(async () => {
        settings.groupBySection = !isGrouped;
        await this.plugin?.saveSettings();
        _WindowLayoutsModal.renderAllInstances();
      });
    });
    menu.addSeparator();
    menu.addItem((item) => {
      item.setTitle(isShowArchived ? t("manageModal.hideArchived") || "Hide Archived" : t("manageModal.showArchived") || "Show Archived").setIcon(isShowArchived ? "check" : "box").onClick(async () => {
        settings.showArchived = !isShowArchived;
        await this.plugin?.saveSettings();
        _WindowLayoutsModal.renderAllInstances();
      });
    });
    const target = event.currentTarget;
    const rect = target.getBoundingClientRect();
    menu.showAtPosition({ x: rect.left, y: rect.bottom + 4 });
  }
  handleArrowKey(direction) {
    if (this.renderedLayoutEntries.length === 0) return;
    if (this.selectedIndex < 0) {
      this.selectedIndex = direction > 0 ? 0 : this.renderedLayoutEntries.length - 1;
    } else {
      this.selectedIndex = (this.selectedIndex + direction + this.renderedLayoutEntries.length) % this.renderedLayoutEntries.length;
    }
    this.updateSelectedItemHighlight();
    this.scrollSelectedIntoView();
  }
  updateSelectedItemHighlight() {
    this.renderedLayoutEntries.forEach((entry, idx) => {
      const isSelected = idx === this.selectedIndex;
      entry.element.setAttribute("aria-selected", String(isSelected));
      entry.element.classList.toggle("is-selected", isSelected);
    });
  }
  updateSearchUI() {
    const rawQuery = this.searchInput?.value.trim() || "";
    if (this.clearSearchBtn) {
      this.clearSearchBtn.style.display = rawQuery ? "flex" : "none";
    }
  }
  async createAndSaveLayout(name, forceNewWindow = true) {
    const cleanName = name.trim();
    if (!cleanName) return;
    try {
      const isPopout = (win) => {
        if (!win || !win.document?.body) return false;
        const cl = win.document.body.classList;
        return cl.contains("is-popout-window") || cl.contains("mod-popout");
      };
      const activeWin = typeof this.plugin.manager?.getActiveWindow === "function" ? this.plugin.manager.getActiveWindow() : this.targetWindow || (typeof activeWindow !== "undefined" ? activeWindow : window);
      const popoutWin = isPopout(activeWin) ? activeWin : isPopout(this.targetWindow) ? this.targetWindow : null;
      if (popoutWin) {
        const originalName = typeof this.plugin.manager?.getLayoutNameForWindow === "function" ? this.plugin.manager.getLayoutNameForWindow(popoutWin) : null;
        const layout = await this.plugin.manager.captureCurrentLayout({ name: cleanName }, popoutWin);
        layout.name = cleanName;
        if (forceNewWindow && this.plugin.manager?.layoutWindows) {
          this.plugin.manager.layoutWindows.delete(layout);
        }
        await this.plugin.manager.saveLayout(layout);
        if (forceNewWindow) {
          if (originalName && typeof this.plugin.manager?.setLayoutLabelForWindow === "function") {
            this.plugin.manager.setLayoutLabelForWindow(popoutWin, originalName);
          }
          this.closeHost();
          await this.plugin.manager.restoreLayout(layout, { forceNewWindow: true, targetWindow: popoutWin });
          const newTargetWin = typeof this.plugin.manager?.getWindowForLayout === "function" ? this.plugin.manager.getWindowForLayout(layout) : null;
          if (newTargetWin && newTargetWin !== popoutWin && typeof newTargetWin.focus === "function") {
            try {
              newTargetWin.focus();
            } catch {
            }
            newTargetWin.setTimeout(() => {
              try {
                newTargetWin.focus();
              } catch {
              }
            }, 100);
            newTargetWin.setTimeout(() => {
              try {
                newTargetWin.focus();
              } catch {
              }
            }, 300);
          }
        } else {
          if (this.plugin.manager?.layoutWindows) {
            this.plugin.manager.layoutWindows.set(layout, popoutWin);
          }
          if (typeof this.plugin.manager?.setLayoutLabelForWindow === "function") {
            this.plugin.manager.setLayoutLabelForWindow(popoutWin, cleanName);
          }
          this.closeHost();
        }
      } else {
        const newWin = await this.plugin.manager.openNewPopoutWindow({ initializeDefaults: true });
        if (!newWin) {
          throw new Error(t("errors.cannotRestore"));
        }
        const emptyLayout = {
          id: typeof this.plugin.manager?.generateId === "function" ? this.plugin.manager.generateId() : `layout_${Date.now()}`,
          name: cleanName,
          timestamp: Date.now(),
          windowState: {
            size: { width: 800, height: 600 },
            position: void 0
          },
          workspace: {
            layout: {
              type: "leaf",
              id: `leaf_${Date.now()}`,
              state: { type: "empty", state: {} }
            },
            activeFile: void 0,
            leaves: []
          },
          activityBars: this.plugin.activityBars?.getDefaultSettingsForNewSpace(),
          metadata: {
            fileCount: 0,
            tabCount: 0,
            splitCount: 0,
            createdAt: (/* @__PURE__ */ new Date()).toISOString(),
            obsidianVersion: this.app.version || "unknown",
            pluginVersion: this.plugin?.manifest?.version || "1.0.0"
          }
        };
        await this.plugin.manager.saveLayout(emptyLayout);
        if (newWin) {
          if (this.plugin.manager?.layoutWindows) {
            this.plugin.manager.layoutWindows.set(emptyLayout, newWin);
          }
          if (typeof this.plugin.manager?.setLayoutLabelForWindow === "function") {
            this.plugin.manager.setLayoutLabelForWindow(newWin, cleanName);
            newWin.setTimeout(() => {
              this.plugin.manager.setLayoutLabelForWindow(newWin, cleanName);
            }, 50);
            newWin.setTimeout(() => {
              this.plugin.manager.setLayoutLabelForWindow(newWin, cleanName);
            }, 300);
          }
          this.closeHost();
          if (typeof newWin.focus === "function") {
            try {
              newWin.focus();
            } catch {
            }
            newWin.setTimeout(() => {
              try {
                newWin.focus();
              } catch {
              }
            }, 100);
          }
        } else {
          this.closeHost();
        }
      }
      if (this.plugin.settings.showNotifications !== false) {
        new import_obsidian2.Notice(`${t("saveModal.saveSuccess")}: ${cleanName}`);
      }
      if (this.searchInput) {
        this.searchInput.value = "";
      }
      this.selectedIndex = 0;
      _WindowLayoutsModal.renderAllInstances();
    } catch (err) {
      this.closeHost();
      const message = err instanceof Error ? err.message : String(err);
      new import_obsidian2.Notice(message);
    }
  }
  static collapsedSections = /* @__PURE__ */ new Set();
  renderedLayoutEntries = [];
  renderLayouts() {
    if (!this.listEl) return;
    this.listEl.empty();
    this.renderedLayoutEntries = [];
    const rawQuery = this.searchInput?.value.trim() || "";
    const query = rawQuery.toLowerCase();
    const allSpaces = this.plugin?.manager?.getSavedLayouts() || [];
    const settings = this.plugin?.settings || {};
    const showArchived = settings.showArchived === true;
    const groupBySection = settings.groupBySection !== false;
    const searchFiltered = allSpaces.filter((layout) => {
      if (!showArchived && layout.archived === true) return false;
      if (!query) return true;
      const matchName = layout.name.toLowerCase().includes(query);
      const matchSec = (layout.sections || []).some((s) => s.toLowerCase().includes(query));
      return matchName || matchSec;
    });
    this.filteredLayouts = searchFiltered;
    if (this.filteredLayouts.length > 0) {
      if (this.selectedIndex < 0 || this.selectedIndex >= this.filteredLayouts.length) {
        this.selectedIndex = 0;
      }
    } else {
      this.selectedIndex = -1;
    }
    this.updateSearchUI();
    if (this.filteredLayouts.length === 0) {
      if (rawQuery) {
        const createItem = this.listEl.createDiv("suggestion-item window-layout-item is-selected");
        const content = createItem.createDiv("suggestion-content qsp-content");
        const title = content.createDiv("suggestion-title qsp-title");
        title.createSpan({ text: rawQuery });
        const aux = createItem.createDiv("suggestion-aux qsp-aux");
        aux.createSpan({
          text: t("manageModal.enterToCreate") || "Enter to create",
          cls: "suggestion-flair"
        });
        createItem.onclick = (e) => {
          void this.createAndSaveLayout(rawQuery, !e.shiftKey);
        };
      } else {
        this.listEl.createEl("p", {
          text: t("manageModal.noLayoutsMessage"),
          cls: "setting-item-description"
        });
      }
      return;
    }
    if (!groupBySection || rawQuery) {
      const activeSpaces = this.filteredLayouts.filter((l) => !l.archived);
      const archivedSpaces = this.filteredLayouts.filter((l) => l.archived === true);
      activeSpaces.forEach((layout) => {
        this.renderLayoutItem(this.listEl, layout);
      });
      if (showArchived) {
        archivedSpaces.forEach((layout) => {
          this.renderLayoutItem(this.listEl, layout);
        });
      }
      this.updateSelectedItemHighlight();
      return;
    }
    const knownSectionsOrder = Array.from(settings.sectionsOrder || []);
    const presentSectionsSet = /* @__PURE__ */ new Set();
    this.filteredLayouts.forEach((space) => {
      (space.sections || []).forEach((sec) => presentSectionsSet.add(sec));
    });
    presentSectionsSet.forEach((sec) => {
      if (!knownSectionsOrder.includes(sec)) {
        knownSectionsOrder.push(sec);
      }
    });
    knownSectionsOrder.forEach((secName) => {
      const matchingSpaces = this.filteredLayouts.filter((s) => (s.sections || []).includes(secName));
      const activeInSec = matchingSpaces.filter((s) => !s.archived);
      const archivedInSec = showArchived ? matchingSpaces.filter((s) => s.archived === true) : [];
      const totalCount = activeInSec.length + archivedInSec.length;
      if (totalCount === 0) return;
      this.renderSectionHeader(this.listEl, secName, totalCount, knownSectionsOrder, true);
      const isCollapsed = _WindowLayoutsModal.collapsedSections.has(secName);
      if (!isCollapsed) {
        const secContainer = this.listEl.createDiv("space-section-container");
        activeInSec.forEach((layout) => {
          this.renderLayoutItem(secContainer, layout);
        });
        archivedInSec.forEach((layout) => {
          this.renderLayoutItem(secContainer, layout);
        });
      }
    });
    const uncategorizedSpaces = this.filteredLayouts.filter((s) => !s.sections || s.sections.length === 0);
    const activeUncat = uncategorizedSpaces.filter((s) => !s.archived);
    const archivedUncat = showArchived ? uncategorizedSpaces.filter((s) => s.archived === true) : [];
    const totalUncat = activeUncat.length + archivedUncat.length;
    if (totalUncat > 0) {
      const uncatTitle = t("manageModal.uncategorized") || "Uncategorized";
      this.renderSectionHeader(this.listEl, uncatTitle, totalUncat, null, false);
      const isCollapsed = _WindowLayoutsModal.collapsedSections.has(uncatTitle);
      if (!isCollapsed) {
        const secContainer = this.listEl.createDiv("space-section-container");
        activeUncat.forEach((layout) => {
          this.renderLayoutItem(secContainer, layout);
        });
        archivedUncat.forEach((layout) => {
          this.renderLayoutItem(secContainer, layout);
        });
      }
    }
    this.updateSelectedItemHighlight();
  }
  renderSectionHeader(parentEl, secName, count, allSectionsOrder, isReorderable = true) {
    const headerEl = parentEl.createDiv(
      isReorderable ? "space-section-header" : "space-section-header is-static"
    );
    if (isReorderable && allSectionsOrder) {
      headerEl.setAttribute("draggable", "true");
      headerEl.ondragstart = (e) => {
        e.dataTransfer?.setData("text/plain", secName);
        headerEl.addClass("is-dragging");
      };
      headerEl.ondragend = () => {
        headerEl.removeClass("is-dragging");
      };
      headerEl.ondragover = (e) => {
        e.preventDefault();
      };
      headerEl.ondrop = async (e) => {
        e.preventDefault();
        const draggedSec = e.dataTransfer?.getData("text/plain");
        if (draggedSec && draggedSec !== secName && allSectionsOrder.includes(draggedSec)) {
          const fromIdx = allSectionsOrder.indexOf(draggedSec);
          const toIdx = allSectionsOrder.indexOf(secName);
          if (fromIdx !== -1 && toIdx !== -1) {
            const newOrder = [...allSectionsOrder];
            newOrder.splice(fromIdx, 1);
            newOrder.splice(toIdx, 0, draggedSec);
            await this.plugin.manager.reorderSections(newOrder);
          }
        }
      };
    }
    const isCollapsed = _WindowLayoutsModal.collapsedSections.has(secName);
    const headerHint = isReorderable ? t("manageModal.sectionHeaderHint") : t("manageModal.sectionHeaderHintStatic");
    (0, import_obsidian2.setTooltip)(headerEl, headerHint);
    const leftEl = headerEl.createDiv("space-section-header-left");
    const dragIndicator = leftEl.createSpan({
      cls: isReorderable ? "space-section-drag-indicator" : "space-section-drag-indicator is-static"
    });
    (0, import_obsidian2.setIcon)(dragIndicator, isReorderable ? "grip-vertical" : "lock");
    const titleSpan = leftEl.createSpan({
      text: secName,
      cls: "space-section-title"
    });
    leftEl.createSpan({ text: `(${count})`, cls: "space-section-count" });
    const triggerInlineRename = () => {
      const input = headerEl.createEl("input", {
        type: "text",
        value: secName,
        cls: "space-section-rename-input"
      });
      titleSpan.replaceWith(input);
      input.focus();
      const commitRename = async () => {
        const newName = input.value.trim();
        if (newName && newName !== secName) {
          await this.plugin.manager.renameSection(secName, newName);
        } else {
          _WindowLayoutsModal.renderAllInstances();
        }
      };
      input.onblur = () => {
        void commitRename();
      };
      input.onkeydown = (ke) => {
        if (ke.key === "Enter") {
          ke.preventDefault();
          input.blur();
        } else if (ke.key === "Escape") {
          _WindowLayoutsModal.renderAllInstances();
        }
      };
    };
    if (isReorderable) {
      headerEl.ondblclick = (e) => {
        e.stopPropagation();
        triggerInlineRename();
      };
      headerEl.oncontextmenu = (e) => {
        e.preventDefault();
        e.stopPropagation();
        const menu = new import_obsidian2.Menu();
        menu.addItem((item) => {
          item.setTitle(t("manageModal.renameSection") || "Rename Section").setIcon("pencil").onClick(() => triggerInlineRename());
        });
        menu.showAtMouseEvent(e);
      };
    }
    const rightEl = headerEl.createDiv("space-section-header-right");
    const arrowIcon = rightEl.createSpan({ cls: "clickable-icon space-section-arrow" });
    (0, import_obsidian2.setIcon)(arrowIcon, isCollapsed ? "chevron-right" : "chevron-down");
    headerEl.onclick = (e) => {
      if (e.detail > 1) return;
      const target = e.target;
      if (target.tagName === "INPUT") return;
      if (_WindowLayoutsModal.collapsedSections.has(secName)) {
        _WindowLayoutsModal.collapsedSections.delete(secName);
      } else {
        _WindowLayoutsModal.collapsedSections.add(secName);
      }
      this.renderLayouts();
    };
  }
  renderLayoutItem(containerEl, layout) {
    const layoutEl = containerEl.createDiv("suggestion-item window-layout-item");
    const itemIndex = this.renderedLayoutEntries.length;
    this.renderedLayoutEntries.push({ layout, element: layoutEl });
    const isSelected = itemIndex === this.selectedIndex;
    layoutEl.setAttribute("role", "option");
    layoutEl.setAttribute("aria-selected", String(isSelected));
    if (isSelected) layoutEl.addClass("is-selected");
    if (layout.archived === true) {
      layoutEl.addClass("is-archived");
    }
    if (layout.color?.trim()) {
      layoutEl.style.setProperty("--window-space-color", layout.color.trim());
      layoutEl.addClass("has-window-space-accent");
      const showFold = layout.showFoldedCorner ?? this.plugin.settings.defaultShowFoldedCorner !== false;
      if (showFold) {
        layoutEl.addClass("has-window-space-accent-fold");
      }
    }
    this.setFilesTooltipForLayout(layoutEl, layout);
    let holdTimer = null;
    let isLongPress = false;
    const isActionButtonTarget = (target) => Boolean(target?.closest?.("button"));
    const cancelHold = () => {
      if (holdTimer !== null) {
        window.clearTimeout(holdTimer);
        holdTimer = null;
      }
    };
    layoutEl.addEventListener("click", (e) => {
      if (isActionButtonTarget(e.target)) return;
      this.selectedIndex = itemIndex;
      this.updateSelectedItemHighlight();
    });
    const itemContentEl = layoutEl.createDiv("suggestion-content qsp-content");
    const titleEl = itemContentEl.createDiv({
      cls: "suggestion-title qsp-title"
    });
    const visualEl = titleEl.createSpan({ cls: "window-space-title-visual" });
    const iconSpan = visualEl.createSpan({ cls: "window-space-item-icon" });
    const val = resolveSpaceIcon(layout.icon, this.plugin.settings?.defaultIcon);
    if (isSpaceEmoji(val)) {
      iconSpan.setText(val);
    } else {
      const iconDiv = iconSpan.createDiv();
      if (!setIconWithCheck(iconDiv, val)) {
        (0, import_obsidian2.setIcon)(iconDiv, "square");
      }
    }
    titleEl.createSpan({ text: layout.name });
    if (layout.archived === true) {
      const archivedBadge = titleEl.createSpan({
        cls: "layout-archived-badge"
      });
      archivedBadge.setText("\u{1F4E6}");
      (0, import_obsidian2.setTooltip)(archivedBadge, t("manageModal.archivedGroup") || "Archived");
    }
    if (layout.autoSave) {
      const autoSaveBadge = titleEl.createSpan({
        cls: "layout-auto-save-badge"
      });
      (0, import_obsidian2.setIcon)(autoSaveBadge, "refresh-cw");
      (0, import_obsidian2.setTooltip)(autoSaveBadge, t("manageModal.autoSaveEnabled"));
    }
    const noteEl = itemContentEl.createDiv("suggestion-note qsp-note");
    const pathEl = noteEl.createDiv("qsp-path");
    const totalTabs = layout.metadata?.tabCount || layout.workspace?.leaves?.length || 0;
    pathEl.createSpan({
      text: `${t("manageModal.tabCount")}: ${totalTabs}`,
      cls: "layout-files"
    });
    const openWin = typeof this.plugin.manager?.getOpenWindowForLayout === "function" ? this.plugin.manager.getOpenWindowForLayout(layout) : null;
    if (openWin) {
      pathEl.createSpan({
        text: `\u{1F7E2} ${t("manageModal.windowOpenBadge") || "\u8996\u7A97\u958B\u555F\u4E2D"}`,
        cls: "layout-open-status"
      });
    }
    const actionsEl = layoutEl.createDiv("suggestion-aux qsp-aux layout-actions");
    const restoreButton = actionsEl.createEl("button", {
      text: t("common.restore"),
      cls: "layout-restore-btn mod-cta"
    });
    (0, import_obsidian2.setTooltip)(restoreButton, t("restoreModal.restoreHint"));
    restoreButton.addEventListener("mousedown", (e) => {
      if (e.button !== 0) return;
      isLongPress = false;
      holdTimer = window.setTimeout(() => {
        isLongPress = true;
        void this.restoreLayout(layout, false);
      }, 450);
    });
    restoreButton.addEventListener("mouseup", cancelHold);
    restoreButton.addEventListener("mouseleave", cancelHold);
    restoreButton.onclick = (e) => {
      e.stopPropagation();
      if (isLongPress) return;
      const forceNewWindow = !e.shiftKey;
      void this.restoreLayout(layout, forceNewWindow);
    };
    const moreButton = actionsEl.createEl("button", {
      cls: "layout-more-btn mod-cta"
    });
    (0, import_obsidian2.setIcon)(moreButton, "chevron-down");
    (0, import_obsidian2.setTooltip)(moreButton, t("manageModal.actions"));
    moreButton.onclick = (e) => {
      e.stopPropagation();
      this.showLayoutItemMenu(e, layout);
    };
  }
  async restoreLayout(layout, forceNewWindow = false) {
    try {
      this.closeHost();
      await this.plugin.manager.restoreLayout(layout, {
        // forceNewWindow 只控制 restore 的目標是否新建；仍需傳入來源視窗，
        // 讓 manager 能保留該 popout 原本的 layout 名稱與狀態列。
        // focusExistingWindow：若該 space 已在某個 Popout 開啟，直接聚焦
        // 既有視窗，避免重複 restore 產生重複視窗 (clone 流程不傳此旗標)。
        targetWindow: this.targetWindow,
        forceNewWindow,
        forceReload: !forceNewWindow,
        focusExistingWindow: true
      });
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      new import_obsidian2.Notice(`${t("errors.failedToRestore")}: ${message}`);
    }
  }
  scrollSelectedIntoView() {
    const entry = this.renderedLayoutEntries[this.selectedIndex];
    if (entry && entry.element && typeof entry.element.scrollIntoView === "function") {
      entry.element.scrollIntoView({ block: "nearest" });
    } else {
      const selected = this.listEl?.querySelector(".window-layout-item.is-selected");
      if (selected && typeof selected.scrollIntoView === "function") {
        selected.scrollIntoView({ block: "nearest" });
      }
    }
  }
  showRenameDialog(layout) {
    const modal = new import_obsidian2.Modal(this.app);
    modal.setTitle(t("manageModal.renameButton"));
    modal.onOpen = () => {
      let input;
      const setting = new import_obsidian2.Setting(modal.contentEl).setName(t("saveModal.nameLabel")).addText((text) => {
        input = text.inputEl;
        input.value = layout.name;
        input.focus();
        input.select();
        input.addEventListener("keydown", (e) => {
          if (e.key === "Enter") {
            e.preventDefault();
            void submit();
          }
        });
      });
      setting.settingEl.addClass("window-spaces-setting-full-width");
      const buttonContainer = modal.contentEl.createDiv("ws-dialog-actions");
      const cancelButton = buttonContainer.createEl("button", {
        text: t("common.cancel")
      });
      cancelButton.onclick = () => modal.close();
      const saveButton = buttonContainer.createEl("button", {
        text: t("common.save"),
        cls: "mod-cta"
      });
      const submit = async () => {
        const newName = input.value.trim();
        if (!newName) {
          new import_obsidian2.Notice(t("saveModal.emptyNameError"));
          input.focus();
          return;
        }
        const duplicate = this.plugin.settings.spaces.some(
          (item) => item.id !== layout.id && item.name === newName
        );
        if (duplicate) {
          new import_obsidian2.Notice(t("saveModal.duplicateNameError"));
          input.focus();
          return;
        }
        layout.name = newName;
        await this.plugin.saveSettings();
        modal.close();
        _WindowLayoutsModal.renderAllInstances();
        if (this.plugin.settings.showNotifications !== false) {
          new import_obsidian2.Notice(t("notifications.layoutRenamed"));
        }
      };
      saveButton.onclick = () => void submit();
    };
    modal.onClose = () => modal.contentEl.empty();
    modal.open();
  }
  showDeleteDialog(layout) {
    void this.showConfirmDialog(
      `${t("manageModal.confirmDeleteMessage")}

${layout.name}`,
      t("manageModal.confirmDeleteTitle")
    ).then(async (confirmed) => {
      if (!confirmed) return;
      try {
        await this.plugin.manager.deleteLayout(layout.id);
        _WindowLayoutsModal.renderAllInstances();
      } catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        new import_obsidian2.Notice(`${t("errors.failedToDelete")}: ${message}`);
      }
    });
  }
  showConfirmDialog(message, title = t("common.confirm")) {
    return new Promise((resolve) => {
      const modal = new import_obsidian2.Modal(this.app);
      modal.setTitle(title);
      modal.onOpen = () => {
        modal.contentEl.createEl("p", { text: message });
        const buttonContainer = modal.contentEl.createDiv("ws-dialog-actions");
        const cancelButton = buttonContainer.createEl("button", {
          text: t("common.cancel")
        });
        cancelButton.onclick = () => {
          resolve(false);
          modal.close();
        };
        const confirmButton = buttonContainer.createEl("button", {
          text: t("common.confirm"),
          cls: "mod-warning"
        });
        confirmButton.onclick = () => {
          resolve(true);
          modal.close();
        };
      };
      modal.onClose = () => modal.contentEl.empty();
      modal.open();
    });
  }
  setFilesTooltipForLayout(element, layout) {
    const leaves = this.plugin?.manager ? this.plugin.manager.getSavedViewStates(layout) : [];
    const files = [];
    leaves.forEach((leaf) => {
      const filePath = this.plugin?.manager ? this.plugin.manager.getFilePathFromLeafState(leaf) : null;
      if (filePath) {
        const fileName = filePath.split("/").pop() || filePath;
        if (!files.includes(fileName)) {
          files.push(fileName);
        }
      }
    });
    if (files.length > 0) {
      const displayFiles = files.slice(0, 15);
      let tooltipText = `${t("restoreModal.includedFiles")} (${files.length}):
` + displayFiles.map((f) => `\u2022 ${f}`).join("\n");
      if (files.length > 15) {
        tooltipText += `
... (+${files.length - 15})`;
      }
      (0, import_obsidian2.setTooltip)(element, tooltipText);
    }
  }
  showSortMenu(event) {
    const menu = new import_obsidian2.Menu();
    const currentSort = this.plugin?.settings?.sortBy || "updated-desc";
    const addSortItem = (id, label, icon) => {
      menu.addItem((item) => {
        item.setTitle(label).setIcon(icon).setChecked(currentSort === id).onClick(async () => {
          this.plugin.settings.sortBy = id;
          await this.plugin.saveSettings();
          _WindowLayoutsModal.renderAllInstances();
        });
      });
    };
    addSortItem("updated-desc", t("manageModal.sortUpdatedDesc"), "history");
    addSortItem("updated-asc", t("manageModal.sortUpdatedAsc"), "history");
    addSortItem("created-desc", t("manageModal.sortCreatedDesc"), "calendar-days");
    addSortItem("created-asc", t("manageModal.sortCreatedAsc"), "calendar");
    addSortItem("name-asc", t("manageModal.sortNameAsc"), "sort-asc");
    addSortItem("name-desc", t("manageModal.sortNameDesc"), "sort-desc");
    menu.showAtMouseEvent(event);
  }
  showPanelMenu(event) {
    const menu = new import_obsidian2.Menu();
    const openPanel = (location) => {
      this.closeHost();
      const targetWin = this.targetWindow || (typeof this.plugin.manager?.getActiveWindow === "function" ? this.plugin.manager.getActiveWindow() : void 0);
      void this.plugin.openWindowLayoutsPanel(location, targetWin);
    };
    menu.addItem((item) => {
      item.setTitle(t("commands.openLayoutsPanel")).setIcon("layout").onClick(() => openPanel("tab"));
    });
    menu.addItem((item) => {
      item.setTitle(t("commands.openLayouts")).setIcon("layout").onClick(() => {
        this.closeHost();
        const targetWin = this.targetWindow || (typeof this.plugin.manager?.getActiveWindow === "function" ? this.plugin.manager.getActiveWindow() : void 0);
        this.plugin.openWindowLayoutsModal(targetWin);
      });
    });
    menu.addItem((item) => {
      item.setTitle(t("commands.openLayoutsPanelLeft")).setIcon("panel-left").onClick(() => openPanel("left"));
    });
    menu.addItem((item) => {
      item.setTitle(t("commands.openLayoutsPanelRight")).setIcon("panel-right").onClick(() => openPanel("right"));
    });
    menu.showAtMouseEvent(event);
  }
  showLayoutItemMenu(event, layout) {
    const menu = new import_obsidian2.Menu();
    menu.addItem((item) => {
      item.setTitle(
        layout.autoSave ? t("manageModal.autoSaveEnabled") : t("manageModal.autoSaveDisabled")
      ).setIcon("refresh-cw").setChecked(!!layout.autoSave).onClick(async () => {
        layout.autoSave = !layout.autoSave;
        await this.plugin.saveSettings();
        if (this.plugin.settings.showNotifications !== false) {
          new import_obsidian2.Notice(
            layout.autoSave ? `${layout.name}: ${t("manageModal.autoSaveEnabled")}` : `${layout.name}: ${t("manageModal.autoSaveDisabled")}`
          );
        }
        _WindowLayoutsModal.renderAllInstances();
      });
    });
    menu.addSeparator();
    menu.addItem((item) => {
      item.setTitle(t("common.rename")).setIcon("pencil").onClick(() => {
        this.showRenameDialog(layout);
      });
    });
    menu.addItem((item) => {
      item.setTitle(t("common.edit")).setIcon("edit-3").onClick(() => {
        this.closeHost();
        this.plugin.openSaveLayoutModal(layout);
      });
    });
    menu.addItem((item) => {
      const isArchived = layout.archived === true;
      item.setTitle(isArchived ? t("manageModal.unarchiveSpace") || "Unarchive Space" : t("manageModal.archiveSpace") || "Archive Space").setIcon("box").onClick(() => {
        void this.plugin.manager.toggleArchiveSpace(layout.id);
      });
    });
    menu.addSeparator();
    menu.addItem((item) => {
      item.setTitle(t("common.delete")).setIcon("trash-2").setWarning(true).onClick(() => {
        this.showDeleteDialog(layout);
      });
    });
    menu.showAtMouseEvent(event);
  }
  onClose() {
    _WindowLayoutsModal.activeInstances.delete(this);
    this.removeKeydownListener();
    if (this.initialFocusTimer !== void 0) {
      const timerWindow = this.modalEl?.ownerDocument?.defaultView || window;
      timerWindow.clearTimeout(this.initialFocusTimer);
      this.initialFocusTimer = void 0;
    }
    this.contentEl.empty();
  }
  removeKeydownListener() {
    if (this.keydownListener) {
      const target = this.keydownTarget || this.panelRootEl?.ownerDocument || this.modalEl?.ownerDocument || document;
      target.removeEventListener("keydown", this.keydownListener, true);
      target.removeEventListener("keydown", this.keydownListener, false);
      this.keydownListener = void 0;
      this.keydownTarget = void 0;
    }
  }
};

// src/manager.ts
var WINDOW_GEOMETRY_TOLERANCE = 5;
var WindowLayoutManager = class {
  plugin;
  app;
  layoutWindows = /* @__PURE__ */ new WeakMap();
  popoutWindows = /* @__PURE__ */ new Set();
  layoutNames = /* @__PURE__ */ new Map();
  layoutLabels = /* @__PURE__ */ new Map();
  activeRestorePromise = null;
  isMatchingUnlabeled = false;
  isRestoringLayout = false;
  renderAttemptedLeaves = /* @__PURE__ */ new WeakSet();
  deferredViewLoads = /* @__PURE__ */ new WeakMap();
  constructor(plugin) {
    this.plugin = plugin;
    this.app = plugin.app;
  }
  /** 記錄 Obsidian 建立的 Popout，供 label 與 title 生命週期管理使用。 */
  registerPopoutWindow(targetWin) {
    if (!targetWin) return;
    this.popoutWindows.add(targetWin);
    this.matchUnlabeledPopoutWindows();
    this.refreshLayoutStatusBar(targetWin);
    this.hookPopoutWindowTitle(targetWin);
    this.schedulePopoutViewRendering(targetWin);
    targetWin.setTimeout(() => {
      this.matchUnlabeledPopoutWindows();
      this.refreshLayoutStatusBar(targetWin);
      this.hookPopoutWindowTitle(targetWin);
    }, 0);
    targetWin.setTimeout(() => {
      this.matchUnlabeledPopoutWindows();
      this.refreshLayoutStatusBar(targetWin);
      this.hookPopoutWindowTitle(targetWin);
    }, 100);
    targetWin.setTimeout(() => {
      this.matchUnlabeledPopoutWindows();
      this.refreshLayoutStatusBar(targetWin);
      this.hookPopoutWindowTitle(targetWin);
    }, 300);
    targetWin.setTimeout(() => {
      this.matchUnlabeledPopoutWindows();
      this.refreshLayoutStatusBar(targetWin);
      this.hookPopoutWindowTitle(targetWin);
    }, 800);
  }
  /**
   * 在 Popout 建立/重新註冊後，以延遲生命週期工作重建尚未渲染的非檔案 view。
   * 不可掛在 active-leaf-change：File Explorer 的 mousedown/mouseup 期間重建
   * view 會阻止 Chromium 產生 click，造成使用者必須點兩次。
   */
  schedulePopoutViewRendering(targetWin) {
    const render = () => {
      if (targetWin.closed) return;
      this.getLeavesForWindow(targetWin).forEach((leaf) => {
        this.ensureViewRendered(leaf);
      });
    };
    targetWin.setTimeout(render, 150);
    targetWin.setTimeout(render, 400);
    targetWin.setTimeout(render, 900);
  }
  /** 插件重新載入時，補註冊已經存在的 Popout。 */
  registerExistingPopoutWindows() {
    this.app.workspace.iterateAllLeaves((leaf) => {
      const extLeaf = leaf;
      const targetWin = extLeaf.containerEl?.ownerDocument?.defaultView;
      if (targetWin && this.isPopoutDocument(targetWin.document)) {
        this.registerPopoutWindow(targetWin);
      }
    });
    this.matchUnlabeledPopoutWindows();
  }
  /**
   * 啟動時對已由 Obsidian native workspace 還原的 Popout 做 target-only
   * reconcile：只重建「結構與 snapshot 不一致」的目標視窗本身，絕不觸發
   * 全域 changeLayout（clearLayout 會把其他 Popout 與主視窗全部 unload、
   * detach、重建）。辨識成功的 space 依序處理；個別失敗僅 warn，不影響
   * 其他視窗，失敗的視窗維持 native 還原結構（可由使用者手動 restore）。
   */
  async reconcileOpenSpacesOnStartup() {
    this.matchUnlabeledPopoutWindows();
    const identified = /* @__PURE__ */ new Map();
    this.getLivePopoutWindows().forEach((targetWin) => {
      const layoutName = this.getLayoutNameForWindow(targetWin);
      if (layoutName) identified.set(layoutName, targetWin);
    });
    if (identified.size === 0) return;
    this.isRestoringLayout = true;
    try {
      for (const [spaceName, targetWin] of identified) {
        const layout = (this.plugin.settings.spaces || []).find(
          (space) => space.name === spaceName
        );
        if (!layout || targetWin.closed) continue;
        try {
          await this.restoreOpenSpaceInPlace(layout, targetWin, {
            showNotifications: false,
            // 啟動時不搬動視窗：Obsidian 已在關閉時的位置開啟它，
            // 搬動只會造成重繪與位移；幾何由手動 restore 套用。
            skipGeometry: true
          });
          this.plugin.activityBars?.resetSidebarHints(targetWin);
        } catch (error) {
          console.warn(`[Window Spaces] Failed to reconcile startup space "${spaceName}":`, error);
        }
      }
    } finally {
      this.isRestoringLayout = false;
    }
  }
  /**
   * 對已由 Obsidian native workspace 還原的 Popout 做 target-only reconcile。
   * 這條路徑刻意不呼叫 restoreLayout/changeLayout，避免 clearLayout 把其他
   * Popout 與主視窗全部 unload、detach、重建。
   */
  async restoreOpenSpaceInPlace(layout, targetWin, options = {}) {
    if (!this.validateLayout(layout)) {
      throw new Error(t("errors.invalidData"));
    }
    const rawRoot = this.extractLayoutRootNode(layout.workspace?.layout);
    const normalizedRoot = this.normalizeWindowLayout(rawRoot);
    const effLayout = normalizedRoot && rawRoot !== normalizedRoot ? { ...layout, workspace: { ...layout.workspace, layout: normalizedRoot } } : layout;
    const savedLeaves = this.getSavedViewStates(effLayout);
    const rootNode = this.extractLayoutRootNode(effLayout.workspace?.layout);
    let builtLeaves = null;
    const shapeMatches = this.isWindowLayoutAtSavedSnapshot(targetWin, effLayout);
    if (!shapeMatches) {
      builtLeaves = await this.rebuildTargetWindowStructure(targetWin, rootNode);
      if (!builtLeaves) {
        console.warn(
          `[Window Spaces] Space "${layout.name}" could not rebuild its target shape; preserving the native Popout structure to avoid a global layout rebuild.`
        );
      }
    }
    const missingFiles = options.validateFiles === false ? [] : await this.restoreFileStatesForWindow(
      targetWin,
      savedLeaves,
      layout.workspace?.activeFile,
      true
    );
    if (!builtLeaves) {
      await this.restoreSavedTabSelections(targetWin, rootNode);
    }
    if (builtLeaves && builtLeaves.length > 0) {
      this.applyPinnedStateToBuiltLeaves(builtLeaves, savedLeaves);
      if (layout.hidden?.hiddenLeafIds?.length) {
        const hiddenIds = new Set(layout.hidden.hiddenLeafIds);
        layout.hidden.hiddenLeafIds = builtLeaves.filter((_, i) => hiddenIds.has(savedLeaves[i]?.id)).map((leaf) => leaf.id || "");
      }
      this.applySavedSplitDimensions(targetWin, rootNode);
    }
    this.setLayoutLabelForWindow(targetWin, layout.name);
    this.syncWindowLeafMarker(targetWin, layout);
    this.refreshLayoutLabels();
    if (layout.hidden) {
      this.applyHiddenStateAfterRestore(targetWin, layout.hidden);
    }
    this.layoutWindows.set(layout, targetWin);
    this.plugin.activityBars?.renderWindow(targetWin);
    if (options.skipGeometry !== true) {
      this.restoreWindowGeometry(targetWin, layout.windowState, layout.includeGeometry, false);
    }
    this.ensureDeferredViewsLoaded(targetWin);
    WindowLayoutsModal.renderAllInstances();
    if (missingFiles.length > 0) {
      console.warn(
        `[Window Spaces] Space "${layout.name}" has missing files: ${missingFiles.join(", ")}`
      );
    }
    if (options.showNotifications !== false && this.plugin.settings.showNotifications !== false) {
      if (missingFiles.length > 0) {
        const missingList = missingFiles.slice(0, 3).join(", ") + (missingFiles.length > 3 ? "..." : "");
        new import_obsidian3.Notice(`\u26A0\uFE0F ${t("notifications.layoutRestored")}: ${layout.name} (${t("notifications.missingFilesNotice")}: ${missingList})`, 8e3);
      } else {
        new import_obsidian3.Notice(`${t("notifications.layoutRestored")}: ${layout.name}`);
      }
    }
  }
  /**
   * 將 target Popout 收斂為單一可重用 leaf，再交給既有 leaf-level builder
   * 建立 saved split/tabs。leaf.detach() 只會觸碰該 leaf 所在的 parent tree。
   */
  async resetPopoutToSingleLeaf(targetWin) {
    const leaves = this.getLeavesForWindow(targetWin);
    const anchor = leaves[0] || null;
    if (!anchor) return null;
    for (const leaf of leaves.slice(1)) {
      try {
        leaf.detach();
      } catch (error) {
        console.warn("[Window Spaces] Failed to detach stale startup leaf:", error);
      }
    }
    const extAnchor = anchor;
    if (extAnchor.getViewState?.().pinned === true && extAnchor.togglePinned) {
      try {
        extAnchor.togglePinned();
      } catch {
      }
    }
    try {
      await anchor.setViewState({ type: "empty", active: false, state: {} });
    } catch (error) {
      console.warn("[Window Spaces] Failed to clear startup anchor leaf:", error);
    }
    return anchor;
  }
  /**
   * 只恢復 target Popout 各 tabs group 的 currentTab。這裡不使用
   * changeLayout；revealLeaf/setActiveLeaf 會讓 Obsidian 更新對應 tabs
   * group 的 active leaf，而不會重建其他 WorkspaceWindow。
   */
  async restoreSavedTabSelections(targetWin, rootNode) {
    if (!rootNode) return;
    const liveLeaves = this.getLeavesForWindow(targetWin);
    let leafIndex = 0;
    const activeLeaves = [];
    const collectLeaves = (node) => {
      if (!node || typeof node !== "object") return [];
      if (node.type === "leaf") {
        const leaf = liveLeaves[leafIndex++];
        return leaf ? [leaf] : [];
      }
      if (!Array.isArray(node.children)) return [];
      const children = node.children.flatMap((child) => collectLeaves(child));
      if (node.type === "tabs" && children.length > 0) {
        const currentTab = typeof node.currentTab === "number" ? node.currentTab : 0;
        const activeLeaf = children[Math.max(0, Math.min(currentTab, children.length - 1))];
        if (activeLeaf) activeLeaves.push(activeLeaf);
      }
      return children;
    };
    collectLeaves(rootNode);
    for (const leaf of activeLeaves) {
      try {
        await this.app.workspace.revealLeaf(leaf);
        this.app.workspace.setActiveLeaf(leaf, { focus: false });
      } catch {
      }
    }
  }
  /**
   * 比對 native startup layout 與 Window Spaces snapshot 的實際內容。
   * Obsidian 每次重建 WorkspaceWindow 都可能重新產生 wrapper id，因此只
   * 比對 view type/state、split direction/dimension 與 tabs 的 active index，
   * 忽略 window/item/leaf id 與顯示用 icon/title。
   * INTERNAL API: Workspace.getLayout()（d.ts @public 但官方文件未記載；
   * 序列化格式依內部實作，read-only，低風險）。
   */
  isWindowLayoutAtSavedSnapshot(targetWin, layout) {
    try {
      const savedRoot = layout.workspace?.layout;
      if (!savedRoot) return false;
      const floatingWindows = this.getFloatingWindows(
        this.app.workspace.getLayout()
      );
      const targetIndex = this.findFloatingWindowIndexForWindow(targetWin, floatingWindows);
      if (targetIndex < 0 || !floatingWindows[targetIndex]) return false;
      return JSON.stringify(this.normalizeStartupLayoutNode(savedRoot)) === JSON.stringify(this.normalizeStartupLayoutNode(floatingWindows[targetIndex]));
    } catch {
      return false;
    }
  }
  normalizeStartupLayoutNode(node) {
    if (!node || typeof node !== "object") return null;
    const normalized = {
      type: typeof node.type === "string" ? node.type : ""
    };
    if (node.type === "leaf") {
      const state = node.state && typeof node.state === "object" ? node.state : {};
      normalized.viewType = typeof state.type === "string" ? state.type : null;
      return normalized;
    }
    if (typeof node.direction === "string") normalized.direction = node.direction;
    if (Array.isArray(node.children)) {
      normalized.children = node.children.map((child) => this.normalizeStartupLayoutNode(child));
    }
    return normalized;
  }
  /** Popout 關閉時移除對應的 layout label 與追蹤狀態。 */
  unregisterPopoutWindow(targetWin) {
    if (!targetWin) return;
    this.unhookPopoutWindowTitle(targetWin);
    this.removeLayoutLabel(targetWin);
    this.popoutWindows.delete(targetWin);
    this.layoutNames.delete(targetWin);
  }
  /** Plugin 卸載時清除所有由本 plugin 建立的 Popout label 與 Title hook。 */
  clearLayoutLabels() {
    for (const [targetWin, labels] of this.layoutLabels) {
      this.unhookPopoutWindowTitle(targetWin);
      labels.statusBar?.remove();
      this.popoutWindows.delete(targetWin);
    }
    this.layoutNames.clear();
    this.layoutLabels.clear();
    this.popoutWindows.clear();
  }
  /**
   * 針對 Popout 視窗進行標題 Hook（雙重防護：DOM document.title setter 劫持 + WorkspaceWindow.setTitle）
   */
  hookPopoutWindowTitle(targetWin) {
    if (!targetWin || !targetWin.document) return;
    const targetDoc = targetWin.document;
    const docRecord = targetDoc;
    if (!docRecord._hasWindowSpacesTitlePatch) {
      const originalDescriptor = Object.getOwnPropertyDescriptor(Document.prototype, "title") || Object.getOwnPropertyDescriptor(targetDoc, "title");
      if (originalDescriptor && originalDescriptor.set) {
        const originalSet = originalDescriptor.set;
        const originalGet = originalDescriptor.get;
        const self = this;
        docRecord._hasWindowSpacesTitlePatch = true;
        docRecord._originalTitleSet = originalSet;
        Object.defineProperty(targetDoc, "title", {
          configurable: true,
          enumerable: true,
          get() {
            try {
              return originalGet ? originalGet.call(targetDoc) : "";
            } catch {
              return "";
            }
          },
          set(newTitle) {
            const spaceName = self.getLayoutNameForWindow(targetWin);
            if (spaceName) {
              let formattedTitle = newTitle;
              if (newTitle && !newTitle.startsWith(spaceName)) {
                formattedTitle = `${spaceName} - ${newTitle}`;
              } else if (!newTitle) {
                formattedTitle = spaceName;
              }
              originalSet.call(targetDoc, formattedTitle);
            } else {
              originalSet.call(targetDoc, newTitle);
            }
          }
        });
      }
    }
    const extApp = this.app;
    const floatingSplit = extApp.workspace?.floatingSplit;
    const workspaceWindow = floatingSplit?.children?.find(
      (child) => child.win === targetWin
    );
    if (workspaceWindow && typeof workspaceWindow.setTitle === "function") {
      if (!workspaceWindow._originalSetTitle) {
        workspaceWindow._originalSetTitle = workspaceWindow.setTitle;
        const self = this;
        workspaceWindow.setTitle = function(originalTitle) {
          const spaceName = self.getLayoutNameForWindow(targetWin);
          const origFn = this._originalSetTitle;
          if (spaceName && typeof origFn === "function") {
            let formattedTitle = originalTitle;
            if (originalTitle && !originalTitle.startsWith(spaceName)) {
              formattedTitle = `${spaceName} - ${originalTitle}`;
            } else if (!originalTitle) {
              formattedTitle = spaceName;
            }
            return origFn.call(this, formattedTitle);
          }
          if (typeof origFn === "function") {
            return origFn.call(this, originalTitle);
          }
        };
      }
    }
    try {
      const spaceName = this.getLayoutNameForWindow(targetWin);
      if (spaceName && targetDoc.title) {
        const currentTitle = targetDoc.title;
        targetDoc.title = currentTitle;
      }
    } catch {
    }
  }
  unhookPopoutWindowTitle(targetWin) {
    if (!targetWin || !targetWin.document) return;
    const targetDoc = targetWin.document;
    const docRecord = targetDoc;
    if (docRecord._hasWindowSpacesTitlePatch && typeof docRecord._originalTitleSet === "function") {
      const originalSet = docRecord._originalTitleSet;
      delete docRecord._hasWindowSpacesTitlePatch;
      delete docRecord._originalTitleSet;
      Object.defineProperty(targetDoc, "title", {
        configurable: true,
        enumerable: true,
        get() {
          return Object.getOwnPropertyDescriptor(Document.prototype, "title")?.get?.call(targetDoc) ?? "";
        },
        set(newTitle) {
          originalSet.call(targetDoc, newTitle);
        }
      });
    }
    const extApp = this.app;
    const floatingSplit = extApp.workspace?.floatingSplit;
    const workspaceWindow = floatingSplit?.children?.find(
      (child) => child.win === targetWin
    );
    if (workspaceWindow && workspaceWindow._originalSetTitle) {
      workspaceWindow.setTitle = workspaceWindow._originalSetTitle;
      delete workspaceWindow._originalSetTitle;
    }
  }
  /**
   * 在指定 Popout 的內容區顯示目前套用的 layout 名稱，
   * 並呼叫 hookPopoutWindowTitle 覆寫視窗標題。
   */
  setLayoutLabelForWindow(targetWin, layoutName) {
    if (!targetWin || !layoutName?.trim()) return;
    this.registerPopoutWindow(targetWin);
    this.layoutNames.set(targetWin, layoutName);
    const targetDocument = targetWin.document;
    const body = targetDocument?.body;
    if (body) {
      body.setAttribute("data-layout-name", layoutName);
      body.querySelectorAll(".window-spaces-layout-label").forEach((element) => element.remove());
    }
    this.refreshLayoutStatusBar(targetWin);
    this.hookPopoutWindowTitle(targetWin);
    this.plugin.activityBars?.renderWindow(targetWin);
    targetWin.setTimeout(() => this.hookPopoutWindowTitle(targetWin), 50);
    targetWin.setTimeout(() => this.hookPopoutWindowTitle(targetWin), 200);
    targetWin.setTimeout(() => this.hookPopoutWindowTitle(targetWin), 500);
  }
  getLayoutNameForWindow(targetWin) {
    if (!targetWin) return null;
    const nameFromMap = this.layoutNames.get(targetWin);
    if (nameFromMap) return nameFromMap;
    const nameFromDOM = typeof targetWin.document?.body?.getAttribute === "function" ? targetWin.document.body.getAttribute("data-layout-name") : null;
    if (nameFromDOM) {
      this.layoutNames.set(targetWin, nameFromDOM);
      return nameFromDOM;
    }
    if (!this.isMatchingUnlabeled && this.isPopoutDocument(targetWin.document)) {
      this.matchUnlabeledPopoutWindows();
      return this.layoutNames.get(targetWin) || null;
    }
    return null;
  }
  /**
   * 當 Obsidian 啟動或多個 Popout 視窗重開時，若 Popout 視窗尚未標籤 space name，
   * 自動比對該視窗現有的 Leaves / 檔案與已儲存的 Layout (spaces)，
   * 為無標籤 Popout 視窗一對一辨識還原其 space name 及狀態列 / 側欄樣式。
   */
  /**
   * 把視窗目前的 live leaf id 集合回寫為該 layout 的識別記號（leafIdMarker）。
   * Obsidian 重啟後 floating 視窗的 leaf id 穩定保留，因此下次重啟可直接
   * 核對此記號識別 space，不需內容比對。靜默保存（不發通知、不拋錯）。
   */
  syncWindowLeafMarker(targetWin, layout) {
    if (!layout || !targetWin || targetWin.closed) return;
    try {
      const ids = this.getLeavesForWindow(targetWin).map((leaf) => leaf.id).filter((id) => !!id);
      if (ids.length === 0) return;
      const current = layout.leafIdMarker;
      const same = Array.isArray(current) && current.length === ids.length && current.every((id, i) => id === ids[i]);
      if (same) return;
      layout.leafIdMarker = ids;
      void this.plugin.saveSettings().catch(() => {
      });
    } catch {
    }
  }
  matchUnlabeledPopoutWindows() {
    if (this.isMatchingUnlabeled) return;
    this.isMatchingUnlabeled = true;
    try {
      const workspace = this.app.workspace;
      if (!workspace || typeof workspace.iterateAllLeaves !== "function") return;
      const allPopouts = /* @__PURE__ */ new Set();
      const claimedLayoutNames = /* @__PURE__ */ new Set();
      workspace.iterateAllLeaves((leaf) => {
        const win = this.getWindowForLeaf(leaf);
        if (win && !win.closed && this.isPopoutDocument(win.document)) {
          allPopouts.add(win);
          const name = this.layoutNames.get(win) || (typeof win.document?.body?.getAttribute === "function" ? win.document.body.getAttribute("data-layout-name") : null);
          if (name) {
            claimedLayoutNames.add(name);
          }
        }
      });
      const unlabeledWindows = Array.from(allPopouts).filter((win) => {
        const name = this.layoutNames.get(win) || (typeof win.document?.body?.getAttribute === "function" ? win.document.body.getAttribute("data-layout-name") : null);
        return !name;
      });
      if (unlabeledWindows.length === 0) return;
      const availableSpaces = (this.plugin.settings.spaces || []).filter(
        (space) => space && space.name && !claimedLayoutNames.has(space.name)
      );
      if (availableSpaces.length === 0) return;
      for (const win of unlabeledWindows) {
        const existingName = this.layoutNames.get(win) || (typeof win.document?.body?.getAttribute === "function" ? win.document.body.getAttribute("data-layout-name") : null);
        if (existingName) continue;
        const winLeaves = this.getLeavesForWindow(win);
        if (winLeaves.length === 0) continue;
        const winLeafIds = new Set(
          winLeaves.map((leaf) => leaf.id).filter(Boolean)
        );
        const winFiles = new Set(
          winLeaves.map((leaf) => {
            const state = typeof leaf.getViewState === "function" ? leaf.getViewState()?.state : null;
            return this.getFilePathFromLeafState({ state: state || {} });
          }).filter((f) => !!f)
        );
        const winPanelIds = new Set(
          winLeaves.map((leaf) => {
            const state = typeof leaf.getViewState === "function" ? leaf.getViewState()?.state : null;
            const panelId = state?.panelId;
            return typeof panelId === "string" && panelId ? panelId : null;
          }).filter((pid) => !!pid)
        );
        let bestSpace = null;
        let bestScore = 0;
        let bestStrongHit = false;
        let bestScoreAmbiguous = false;
        for (const space of availableSpaces) {
          if (claimedLayoutNames.has(space.name)) continue;
          const savedLeaves = this.getSavedViewStates(space);
          if (savedLeaves.length === 0) continue;
          const savedLeafIds = new Set(savedLeaves.map((l) => l.id).filter(Boolean));
          const savedFiles = new Set(
            savedLeaves.map((l) => this.getFilePathFromLeafState(l)).filter((f) => !!f)
          );
          const savedPanelIds = new Set(
            savedLeaves.map((l) => {
              const panelId = l.state?.panelId;
              return typeof panelId === "string" && panelId ? panelId : null;
            }).filter((pid) => !!pid)
          );
          const matchingPanelIds = Array.from(winPanelIds).filter((pid) => savedPanelIds.has(pid)).length;
          if (winPanelIds.size > 0 && savedPanelIds.size > 0 && matchingPanelIds === 0) {
            continue;
          }
          let score = 0;
          let strongHit = false;
          if (Array.isArray(space.leafIdMarker) && space.leafIdMarker.length > 0) {
            let markerHit = 0;
            for (const id of winLeafIds) {
              if (space.leafIdMarker.includes(id)) markerHit++;
            }
            score += markerHit * 200;
            if (markerHit > 0) strongHit = true;
          }
          for (const id of winLeafIds) {
            if (savedLeafIds.has(id)) {
              score += 100;
              strongHit = true;
            }
          }
          let matchingFilesCount = 0;
          for (const file of winFiles) {
            if (savedFiles.has(file)) matchingFilesCount++;
          }
          score += matchingFilesCount * 10;
          for (const pid of winPanelIds) {
            if (savedPanelIds.has(pid)) {
              score += 300;
              strongHit = true;
            }
          }
          if (winFiles.size >= 2 && winFiles.size === savedFiles.size && matchingFilesCount === winFiles.size) {
            score += 50;
          }
          const savedWindow = space.windowState;
          if (score > 0 && savedWindow && savedWindow.size) {
            const widthDiff = Math.abs(win.outerWidth - savedWindow.size.width);
            const heightDiff = Math.abs(win.outerHeight - savedWindow.size.height);
            if (widthDiff < 50 && heightDiff < 50) {
              score += 5;
            }
          }
          if (score > bestScore || score === bestScore && strongHit && !bestStrongHit) {
            bestScore = score;
            bestSpace = space;
            bestStrongHit = strongHit;
            bestScoreAmbiguous = false;
          } else if (score > 0 && score === bestScore && strongHit === bestStrongHit) {
            bestScoreAmbiguous = true;
          }
        }
        if (bestSpace && !bestScoreAmbiguous && bestScore >= (bestStrongHit ? 10 : 30)) {
          claimedLayoutNames.add(bestSpace.name);
          this.setLayoutLabelForWindow(win, bestSpace.name);
          this.layoutWindows.set(bestSpace, win);
          this.syncWindowLeafMarker(win, bestSpace);
        }
      }
    } finally {
      this.isMatchingUnlabeled = false;
    }
  }
  refreshLayoutLabels() {
    for (const targetWin of this.popoutWindows) {
      this.refreshLayoutStatusBar(targetWin);
    }
  }
  refreshLayoutStatusBar(targetWin) {
    const targetDocument = targetWin.document;
    const body = targetDocument?.body;
    if (!body || !this.isPopoutDocument(targetDocument)) return;
    const labels = this.layoutLabels.get(targetWin) || { statusBar: null };
    this.layoutLabels.set(targetWin, labels);
    const layoutName = this.getLayoutNameForWindow(targetWin);
    if (this.plugin.settings.showLayoutStatusBar === true) {
      labels.statusBar = this.ensureLayoutLabelElement(
        targetDocument,
        body,
        labels.statusBar,
        "window-spaces-layout-status"
      );
      this.updateLayoutLabelElement(
        labels.statusBar,
        layoutName || t("common.noLayout"),
        targetWin
      );
    } else {
      labels.statusBar?.remove();
      labels.statusBar = null;
    }
  }
  ensureLayoutLabelElement(targetDocument, body, current, className) {
    if (current && current.ownerDocument === targetDocument) {
      if (!current.isConnected) body.appendChild(current);
      return current;
    }
    const existing = body.querySelector(`.${className}`);
    if (existing) return existing;
    const element = body.createDiv({ cls: className });
    return element;
  }
  updateLayoutLabelElement(element, layoutName, targetWin) {
    const iconElement = element.querySelector(".window-spaces-layout-icon");
    if (iconElement) {
      iconElement.remove();
    }
    let nameWrap = element.querySelector(".window-spaces-layout-name-wrap");
    if (!nameWrap) {
      nameWrap = element.createDiv({ cls: "window-spaces-layout-name-wrap" });
    }
    let nameIcon = nameWrap.querySelector(".window-spaces-layout-name-icon");
    if (!nameIcon) {
      nameIcon = nameWrap.createDiv({ cls: "window-spaces-layout-name-icon" });
      (0, import_obsidian3.setIcon)(nameIcon, "chevrons-up-down");
    }
    let nameElement = nameWrap.querySelector(".window-spaces-layout-name");
    if (!nameElement) {
      nameElement = nameWrap.createSpan({ cls: "window-spaces-layout-name" });
    }
    let actionsElement = element.querySelector(".window-spaces-layout-actions");
    if (!actionsElement) {
      actionsElement = element.createDiv({ cls: "window-spaces-layout-actions" });
    }
    if (nameWrap.nextElementSibling !== actionsElement) {
      element.insertBefore(nameWrap, actionsElement);
    }
    const ensureActionButton = (className, icon, label, onClick, onContextMenu) => {
      let button = actionsElement ? actionsElement.querySelector(`.${className}`) : null;
      if (!button && actionsElement) {
        button = actionsElement.createEl("button", {
          cls: `window-spaces-layout-action ${className} clickable-icon`,
          attr: { type: "button" }
        });
        (0, import_obsidian3.setIcon)(button, icon);
      }
      button.onclick = (event) => {
        event.preventDefault();
        event.stopPropagation();
        onClick(event);
      };
      if (onContextMenu) {
        button.oncontextmenu = (event) => {
          event.preventDefault();
          event.stopPropagation();
          onContextMenu(event);
        };
      } else {
        button.oncontextmenu = null;
      }
      button.setAttribute("aria-label", label);
      button.title = label;
      return button;
    };
    nameElement.textContent = layoutName;
    nameWrap.onclick = (e) => {
      e.preventDefault();
      e.stopPropagation();
      this.showSpaceMenu(targetWin, nameWrap, e);
    };
    nameWrap.oncontextmenu = (e) => {
      e.preventDefault();
      e.stopPropagation();
      this.showSpaceContextMenu(targetWin, e);
    };
    nameWrap.classList.toggle(
      "window-spaces-layout-switcher",
      layoutName !== t("common.noLayout")
    );
    const currentLayout = this.plugin.settings.spaces.find((l) => l.name === layoutName);
    const isAutoSave = !!currentLayout?.autoSave;
    const oldSaveBtn = actionsElement?.querySelector(".window-spaces-layout-save");
    if (oldSaveBtn) oldSaveBtn.remove();
    const oldOpenBtn = actionsElement?.querySelector(".window-spaces-layout-open");
    if (oldOpenBtn) oldOpenBtn.remove();
    ensureActionButton(
      "window-spaces-layout-direct-save",
      "save",
      t("commands.saveLayoutDirect") || t("commands.saveLayout"),
      () => void this.saveLayoutDirectFromWindow(targetWin)
    );
    ensureActionButton(
      "window-spaces-layout-edit-settings",
      "pencil",
      t("saveModal.title"),
      () => void this.saveLayoutFromWindow(targetWin),
      (e) => {
        if (this.plugin.activityBars && typeof this.plugin.activityBars.showVisibilityMenu === "function") {
          this.plugin.activityBars.showVisibilityMenu(targetWin, e);
        }
      }
    );
    const autoSaveBtn = ensureActionButton(
      "window-spaces-layout-auto-save",
      "refresh-cw",
      isAutoSave ? t("manageModal.autoSaveEnabled") : t("manageModal.autoSaveDisabled"),
      () => {
        void (async () => {
          const targetLayout = this.plugin.settings.spaces.find((l) => l.name === layoutName);
          if (targetLayout) {
            targetLayout.autoSave = !targetLayout.autoSave;
            await this.plugin.saveSettings();
            new import_obsidian3.Notice(
              targetLayout.autoSave ? `${layoutName}: ${t("manageModal.autoSaveEnabled")}` : `${layoutName}: ${t("manageModal.autoSaveDisabled")}`
            );
            this.updateLayoutLabelElement(element, layoutName, targetWin);
          } else {
            void this.saveLayoutFromWindow(targetWin);
          }
        })();
      }
    );
    if (isAutoSave) {
      autoSaveBtn.classList.add("is-active");
    } else {
      autoSaveBtn.classList.remove("is-active");
    }
    ensureActionButton(
      "window-spaces-layout-settings",
      "settings",
      t("activityBar.openSettings"),
      () => this.openPluginSettings()
    );
    element.setAttribute("aria-label", `${t("common.layoutLabel")}: ${layoutName}`);
    element.setAttribute("title", layoutName);
    element.dataset.layoutName = layoutName;
  }
  /** 仿 vault switcher 左鍵選單：列出所有非 Archived spaces，底部附 Manage spaces...。 */
  showSpaceMenu(targetWin, anchor, _e) {
    const menu = new import_obsidian3.Menu();
    const spaces = this.plugin.settings.spaces.filter((s) => s.archived !== true);
    const activeSpaceNames = /* @__PURE__ */ new Set();
    this.getLivePopoutWindows().forEach((win) => {
      const layoutName = this.getLayoutNameForWindow(win);
      if (layoutName) activeSpaceNames.add(layoutName);
    });
    spaces.forEach((space) => {
      menu.addItem((item) => {
        const iconVal = resolveSpaceIcon(space.icon, this.plugin.settings?.defaultIcon);
        let title = space.name;
        if (isSpaceEmoji(iconVal)) {
          const itemAny = item;
          if (itemAny.iconEl) {
            itemAny.iconEl.empty();
            itemAny.iconEl.setText(iconVal);
          } else {
            title = `${iconVal} ${space.name}`;
          }
        } else {
          item.setIcon(iconVal);
        }
        const itemDom = item.dom;
        if (itemDom) {
          if (activeSpaceNames.has(space.name)) {
            itemDom.classList.add("window-spaces-menu-active");
          }
          const color = space.color?.trim();
          if (color) {
            itemDom.style.setProperty("--menu-item-accent", color);
            itemDom.classList.add("window-spaces-space-accent");
          }
          const showFold = !!color && (space.showFoldedCorner ?? this.plugin.settings.defaultShowFoldedCorner !== false);
          if (showFold) {
            itemDom.classList.add("has-space-menu-fold");
            if (color) itemDom.style.setProperty("--fold-color", color);
          }
        }
        item.setTitle(title);
        item.onClick((evt) => {
          const forceNewWindow = !evt.shiftKey;
          void this.plugin.manager.restoreLayout(space, {
            targetWindow: targetWin,
            forceNewWindow,
            forceReload: !forceNewWindow,
            focusExistingWindow: true
          });
        });
      });
    });
    if (spaces.length > 0) menu.addSeparator();
    menu.addItem((item) => {
      item.setTitle(t("settings.manageSpaces"));
      item.setIcon("layout-dashboard");
      item.onClick(() => this.plugin.openWindowLayoutsModal(targetWin));
    });
    const rect = anchor.getBoundingClientRect();
    menu.showAtPosition({ x: rect.left, y: rect.bottom }, targetWin.document);
    this.markSpaceMenu(menu, targetWin);
  }
  /** 仿 vault switcher 右鍵選單：Show in system explorer / Copy path。 */
  showSpaceContextMenu(targetWin, e) {
    const menu = new import_obsidian3.Menu();
    menu.addItem((item) => {
      item.setTitle(t("settings.showInSystemExplorer"));
      item.setIcon("arrow-up-right");
      item.onClick(() => {
        try {
          this.app.showInFolder?.("");
        } catch (err) {
          console.warn("Failed to reveal vault in system explorer:", err);
        }
      });
    });
    menu.addItem((item) => {
      item.setTitle(t("settings.copyPath"));
      item.setIcon("clipboard");
      item.onClick(() => void this.copyVaultPath(targetWin));
    });
    menu.showAtMouseEvent(e);
    this.markSpaceMenu(menu, targetWin);
  }
  /** 標記選單，讓 CSS 統一 menu-item-icon 容器寬度（emoji 與 lucide icon 的 title 對齊）。 */
  markSpaceMenu(menu, targetWin) {
    try {
      const menuEl = menu.dom;
      if (menuEl) {
        menuEl.classList.add("window-spaces-space-menu");
        return;
      }
      const menus = targetWin.document.querySelectorAll(".menu");
      menus[menus.length - 1]?.classList.add("window-spaces-space-menu");
    } catch {
    }
  }
  /**
   * Rebuild only one Popout. Simple layouts use the public leaf APIs; nested
   * layouts use Obsidian's own deserializeLayout() to build WorkspaceItems and
   * attach them to the target WorkspaceWindow. Both paths leave other Popouts
   * untouched and never call Workspace.changeLayout().
   */
  /**
   * 以「全新生成的含 sidebar layout 樹」重建目標 popout（舊 space 首次開啟
   * activity bar，或在既有結構下新增一側 sidebar 時使用）。
   *
   * 概念：不走 createLeafBySplit 逐欄補欄（對「欄位為 workspace-split」的
   * 空間會在錯誤層級建欄、頂層欄位數不增），而是讀取目標視窗目前的 live
   * layout 樹，在其欄位容器 children 之前後**直接插入標準 workspace-tabs
   * sidebar 欄位**（全新生成、怎麼塞都行），再交由既有 target-only leaf
   * rebuild 套用（不觸發全域 changeLayout）。原 content 欄位與 leaf view
   * state 原封保留，僅在最左/最右新增 sidebar。
   */
  async rebuildPopoutLayoutWithSidebars(targetWin, layout, leftView, rightView) {
    if (!targetWin || targetWin.closed || !layout || !layout.workspace) return;
    const liveRoot = this.getLiveWindowLayoutTree(targetWin) || this.extractLayoutRootNode(layout.workspace.layout);
    if (!liveRoot) return;
    const newRoot = this.buildLayoutTreeWithSidebars(liveRoot, leftView, rightView);
    if (!newRoot) return;
    const override = {
      ...layout,
      workspace: {
        ...layout.workspace,
        layout: newRoot,
        leaves: []
      }
    };
    try {
      await this.restoreOpenSpaceInPlace(override, targetWin, {
        showNotifications: false,
        validateFiles: false,
        skipGeometry: true
      });
    } catch (e) {
      console.warn("[Window Spaces] Failed to rebuild layout with sidebars:", e);
    }
  }
  /** 讀取目標 popout 目前的 live layout 樹（含目前開啟的 view state）。 */
  getLiveWindowLayoutTree(win) {
    try {
      const globalLayout = this.app.workspace.getLayout?.();
      if (!globalLayout) return null;
      const floatingWindows = this.getFloatingWindows(globalLayout);
      const idx = this.findFloatingWindowIndexForWindow(win, floatingWindows);
      return idx >= 0 && floatingWindows[idx] ? floatingWindows[idx] : null;
    } catch {
      return null;
    }
  }
  /**
   * 在 layout 樹的欄位容器 children 前後插入標準 workspace-tabs sidebar 欄位。
   * 欄位容器 = window/floating 的第一個 direct child 為 split 者（其 children 為欄位）；
   * 若 rootNode 本身即 split，則視為欄位容器。
   */
  buildLayoutTreeWithSidebars(rootNode, leftView, rightView) {
    if (!rootNode) return null;
    const clone = JSON.parse(JSON.stringify(rootNode));
    const isContainer = clone?.type === "window" || clone?.type === "floating";
    let container;
    if (isContainer) {
      container = (clone.children || []).find((c) => c && c.type === "split");
    } else {
      container = clone;
    }
    if (!container || container.type !== "split") return null;
    const mkTabs = (viewType) => ({
      type: "tabs",
      children: viewType && viewType !== "empty" ? [{ type: "leaf", state: { type: viewType, state: {} } }] : []
    });
    const prior = Array.isArray(container.children) ? container.children.map((c) => this.normalizeColumnNode(c)) : [];
    container.children = [
      ...leftView ? [mkTabs(leftView)] : [],
      ...prior,
      ...rightView ? [mkTabs(rightView)] : []
    ];
    return clone;
  }
  /**
   * 遞迴 normalizer：從「欄位」層級往下，把任何層級的「水平 multi-column
   * （split:vertical，flex-direction: row）」合併為單一 workspace-tabs；
   * 垂直方向的 split（direction: horizontal / 多 row）保留並遞迴其子節點。
   * 這確保側欄/欄位內不會殘留「同一側水平多 column」的異常結構。
   */
  /**
   * 對整棵 layout 樹（window/floating）的「欄位容器 children（各欄位）」套用
   * 遞迴 normalizeColumnNode：收斂水平 multi-column 欄位，回傳收斂後的新樹；
   * 若無需收斂則回傳原 rootNode（不建立克隆）。
   */
  normalizeWindowLayout(rootNode) {
    if (!rootNode) return rootNode;
    const isContainer = rootNode?.type === "window" || rootNode?.type === "floating";
    const containerSource = isContainer ? (rootNode.children || []).find((c) => c && c.type === "split") : rootNode;
    if (!containerSource || containerSource.type !== "split") return rootNode;
    const normalized = (containerSource.children || []).map(
      (c) => this.normalizeColumnNode(c)
    );
    if (normalized.every((c, i) => c === (containerSource.children || [])[i])) {
      return rootNode;
    }
    const clone = JSON.parse(JSON.stringify(rootNode));
    const container = isContainer ? (clone.children || []).find((c) => c && c.type === "split") : clone;
    if (container) container.children = normalized;
    return clone;
  }
  normalizeColumnNode(col) {
    if (!col) return col;
    if (col.type === "split") {
      if (col.direction === "vertical") {
        return this.collapseSplitColumnIntoColumnTabs(col);
      }
      const children = (Array.isArray(col.children) ? col.children : []).map(
        (c) => this.normalizeColumnNode(c)
      );
      return { ...col, children };
    }
    return col;
  }
  /**
   * 將「欄位＝split:vertical（水平並排多 tabs）的異常結構」收斂為單一
   * workspace-tabs：遞迴收集所有 leaf（保留順序）、過濾 empty，若剩餘非空
   * 則建立單一 tabs（多 tab）；否則建立含一個 empty leaf 的 tabs。
   * 垂直方向的 split（direction: horizontal / flex-direction: column，即
   * Obsidian 側欄允許的多 row）與一般 tabs 欄位保持不動。
   */
  collapseSplitColumnIntoColumnTabs(col) {
    if (!col || col.type !== "split" || col.direction !== "vertical") return col;
    const leaves = [];
    const walk = (n) => {
      if (!n) return;
      if (n.type === "leaf") {
        leaves.push(n);
        return;
      }
      if (Array.isArray(n.children)) n.children.forEach(walk);
    };
    walk(col);
    const real = leaves.filter((l) => l?.state?.type && l.state.type !== "empty");
    return {
      type: "tabs",
      ...typeof col.id === "string" ? { id: col.id } : {},
      children: real.length > 0 ? real : [{ type: "leaf", state: { type: "empty", state: {} } }],
      ...typeof col.currentTab === "number" ? { currentTab: 0 } : {}
    };
  }
  async rebuildTargetWindowStructure(targetWin, rootNode) {
    if (!targetWin || !rootNode) return null;
    if (this.isSimpleLayoutStructure(rootNode)) {
      const anchor = await this.resetPopoutToSingleLeaf(targetWin);
      return anchor ? this.buildSimpleWindowStructure(targetWin, rootNode) : null;
    }
    return this.buildWindowStructureWithDeserializer(targetWin, rootNode);
  }
  /**
   * Mirrors Obsidian setLayout's deserializeLayout step, but attaches the
   * resulting top-level items to one existing WorkspaceWindow only. This is
   * intentionally an internal API use: it is the only supported way in this
   * plugin to reproduce nested split rendering without clearing all windows.
   * INTERNAL API: Workspace.deserializeLayout / Workspace.floatingSplit /
   * WorkspaceItem.insertChild / WorkspaceItem.removeChild（d.ts 未宣告）。
   * 全部以 typeof 偵測，失敗回傳 null → 保留 native 結構並 warn。
   */
  async buildWindowStructureWithDeserializer(targetWin, rootNode) {
    const workspace = this.app.workspace;
    const floatingChildren = workspace.floatingSplit?.children || [];
    const targetWorkspaceWindow = floatingChildren.find((child) => child.win === targetWin);
    if (!targetWorkspaceWindow || typeof workspace.deserializeLayout !== "function") return null;
    const topNodes = rootNode.type === "window" || rootNode.type === "floating" ? rootNode.children : [rootNode];
    if (!Array.isArray(topNodes) || topNodes.length === 0 || !Array.isArray(targetWorkspaceWindow.children)) {
      return null;
    }
    const oldLeaves = this.getLeavesForWindow(targetWin);
    const oldChildren = [...targetWorkspaceWindow.children];
    const builtItems = [];
    for (const node of topNodes) {
      const item = await workspace.deserializeLayout(
        JSON.parse(JSON.stringify(node)),
        null
      );
      if (!item) return null;
      builtItems.push(item);
    }
    if (typeof targetWorkspaceWindow.insertChild !== "function") return null;
    for (const item of builtItems) {
      targetWorkspaceWindow.insertChild(targetWorkspaceWindow.children.length, item);
    }
    for (const leaf of oldLeaves) {
      try {
        leaf.detach();
      } catch {
      }
    }
    for (const child of oldChildren) {
      if (targetWorkspaceWindow.children.includes(child)) {
        try {
          const removeChild = targetWorkspaceWindow.removeChild;
          removeChild?.call(targetWorkspaceWindow, child);
        } catch {
        }
      }
    }
    return this.getLeavesForWindow(targetWin);
  }
  /** 複製 vault 根目錄路徑到剪貼簿。 */
  async copyVaultPath(targetWin) {
    try {
      const adapter = this.app.vault.adapter;
      const path = adapter?.getFullPath?.("") ?? this.app.vault.getName();
      await targetWin.navigator.clipboard.writeText(path);
      new import_obsidian3.Notice(`${this.app.vault.getName()}: ${t("settings.pathCopied")}`);
    } catch (err) {
      console.warn("Failed to copy vault path:", err);
    }
  }
  /** 開啟 Window Spaces 設定頁面。 */
  openPluginSettings() {
    void (async () => {
      try {
        const setting = this.app.setting;
        if (!setting) return;
        if (typeof setting.open === "function") {
          await setting.open();
        }
        if (typeof setting.openTabById === "function") {
          setting.openTabById("window-spaces");
        }
      } catch (e) {
        console.warn("Failed to open Window Spaces settings:", e);
      }
    })();
  }
  /** 開啟全新的 Popout 視窗（等待 leaf 與 DOM 都完成掛載後再回傳視窗物件） */
  async openNewPopoutWindow(options = {}) {
    try {
      const result = await this.plugin.popoutLayout.openNewPopoutWindow();
      if (!result) return null;
      if (options.initializeDefaults !== false) {
        try {
          const centerLeaf = this.plugin.popoutLayout.getCenterLeafSync(result.win);
          if (centerLeaf) {
            this.app.workspace.setActiveLeaf(centerLeaf, { focus: true });
          }
        } catch (e) {
          console.warn("Failed to focus center leaf after New Window init:", e);
        }
      }
      return result.win;
    } catch (e) {
      console.warn("Failed to open new popout window:", e);
    }
    return null;
  }
  async saveLayoutFromWindow(targetWin) {
    try {
      const layoutName = this.layoutNames.get(targetWin) || "";
      const existing = this.plugin.settings.spaces.find((l) => l.name === layoutName);
      const layout = await this.captureCurrentLayout(
        { name: layoutName },
        targetWin
      );
      if (existing) {
        layout.autoSave = existing.autoSave;
        layout.icon = existing.icon;
        layout.color = existing.color;
        layout.borderInset = existing.borderInset;
        layout.showFoldedCorner = existing.showFoldedCorner;
        layout.activityBars = existing.activityBars;
      }
      this.plugin.openSaveLayoutModal(layout, targetWin);
    } catch (error) {
      console.error("Failed to capture layout from Popout:", error);
      const message = error instanceof Error ? error.message : String(error);
      new import_obsidian3.Notice(`${t("errors.failedToSave")}: ${message}`);
    }
  }
  async saveLayoutDirectFromWindow(targetWin) {
    try {
      const layoutName = this.layoutNames.get(targetWin) || "";
      const existing = this.plugin.settings.spaces.find((l) => l.name === layoutName);
      const layout = await this.captureCurrentLayout(
        { name: layoutName },
        targetWin
      );
      if (existing) {
        layout.id = existing.id;
        layout.autoSave = existing.autoSave;
        layout.icon = existing.icon;
        layout.color = existing.color;
        layout.borderInset = existing.borderInset;
        layout.showFoldedCorner = existing.showFoldedCorner;
        layout.activityBars = existing.activityBars;
        layout.sections = existing.sections;
        layout.archived = existing.archived;
      }
      await this.saveLayout(layout);
      if (this.plugin.settings.showNotifications !== false) {
        new import_obsidian3.Notice(`${t("notifications.layoutSaved")}: ${layout.name}`);
      }
    } catch (error) {
      console.error("Failed to direct save layout from Popout:", error);
      const message = error instanceof Error ? error.message : String(error);
      new import_obsidian3.Notice(`${t("errors.failedToSave")}: ${message}`);
    }
  }
  autoSaveTimers = /* @__PURE__ */ new Map();
  lastValidSnapshots = /* @__PURE__ */ new Map();
  /**
   * 檢查並發動所有已開啟自動保存的 Popout 視窗的 5 秒 Debounced 自動儲存
   */
  checkAndDebouncedAutoSaveAll() {
    this.layoutNames.forEach((layoutName, targetWin) => {
      const existing = this.plugin.settings.spaces.find((l) => l.name === layoutName);
      if (existing && existing.autoSave === true) {
        void this.captureCurrentLayout({ name: layoutName }, targetWin).then((snapshot) => {
          if (snapshot.metadata?.fileCount > 0 || snapshot.workspace?.leaves && snapshot.workspace.leaves.length > 0) {
            snapshot.autoSave = true;
            snapshot.id = existing.id;
            this.lastValidSnapshots.set(targetWin, snapshot);
          }
        }).catch(() => {
        });
        const existingTimer = this.autoSaveTimers.get(targetWin);
        if (existingTimer !== void 0) {
          window.clearTimeout(existingTimer);
        }
        const timer = window.setTimeout(() => {
          this.autoSaveTimers.delete(targetWin);
          void this.autoSaveWindowLayout(targetWin);
        }, 5e3);
        this.autoSaveTimers.set(targetWin, timer);
      }
    });
  }
  /**
   * 實時自動保存指定的 Popout 視窗佈局 (包含 0 檔案清空防呆保護)
   */
  async autoSaveWindowLayout(targetWin) {
    const layoutName = this.layoutNames.get(targetWin);
    if (!layoutName) return;
    const existing = this.plugin.settings.spaces.find((l) => l.name === layoutName);
    if (!existing || !existing.autoSave) return;
    try {
      let captured = null;
      try {
        const liveCaptured = await this.captureCurrentLayout({ name: layoutName }, targetWin);
        if (liveCaptured.metadata?.fileCount > 0 || liveCaptured.workspace?.leaves && liveCaptured.workspace.leaves.length > 0) {
          captured = liveCaptured;
          captured.autoSave = true;
          captured.id = existing.id;
          captured.includeGeometry = existing.includeGeometry;
          captured.icon = existing.icon;
          captured.color = existing.color;
          captured.borderInset = existing.borderInset;
          captured.showFoldedCorner = existing.showFoldedCorner;
          captured.activityBars = existing.activityBars;
          this.lastValidSnapshots.set(targetWin, captured);
        }
      } catch {
      }
      if (!captured) {
        captured = this.lastValidSnapshots.get(targetWin) || null;
      }
      if (!captured || captured.metadata?.fileCount === 0 && existing.metadata?.fileCount > 0) {
        console.warn(`[Window Spaces] Suppressed auto-save for layout "${layoutName}" to prevent empty file list overwrite.`);
        return;
      }
      const now = Date.now();
      captured.createdAt = existing.createdAt || existing.timestamp || now;
      captured.updatedAt = now;
      captured.timestamp = now;
      const index = this.plugin.settings.spaces.findIndex((l) => l.id === existing.id);
      if (index !== -1) {
        this.plugin.settings.spaces[index] = captured;
      } else {
        this.plugin.settings.spaces.push(captured);
      }
      await this.plugin.saveSettings();
      WindowLayoutsModal.renderAllInstances();
    } catch (e) {
      console.warn(`[Window Spaces] Auto-save on close/change failed for "${layoutName}":`, e);
    }
  }
  removeLayoutLabel(targetWin) {
    const timer = this.autoSaveTimers.get(targetWin);
    if (timer !== void 0) {
      window.clearTimeout(timer);
      this.autoSaveTimers.delete(targetWin);
    }
    void this.autoSaveWindowLayout(targetWin);
    this.lastValidSnapshots.delete(targetWin);
    const labels = this.layoutLabels.get(targetWin);
    labels?.statusBar?.remove();
    this.layoutNames.delete(targetWin);
    this.layoutLabels.delete(targetWin);
  }
  isPopoutDocument(targetDocument) {
    const body = targetDocument?.body;
    return !!body && (body.classList.contains("is-popout-window") || body.classList.contains("mod-popout"));
  }
  /**
   * 檢查指定 Layout 是否目前已在某個存活的 Popout 視窗中開啟。
   */
  getOpenWindowForLayout(layout) {
    if (!layout) return null;
    const mappedWin = this.layoutWindows.get(layout);
    if (mappedWin && !mappedWin.closed && this.isPopoutDocument(mappedWin.document)) {
      if (this.getLeavesForWindow(mappedWin).length > 0) {
        return mappedWin;
      }
    }
    const liveWindows = /* @__PURE__ */ new Set();
    const workspace = this.app.workspace;
    if (typeof workspace.iterateAllLeaves === "function") {
      workspace.iterateAllLeaves((leaf) => {
        const win = this.getWindowForLeaf(leaf);
        if (win && !win.closed && this.isPopoutDocument(win.document)) {
          liveWindows.add(win);
        }
      });
    }
    for (const win of Array.from(liveWindows)) {
      const label = this.getLayoutNameForWindow(win);
      if (label && label === layout.name) {
        return win;
      }
    }
    const savedLeaves = this.getSavedViewStates(layout);
    if (savedLeaves.length > 0) {
      const matchedWin = this.findWindowForSavedLeaves(savedLeaves, void 0, null, /* @__PURE__ */ new Set(), true);
      if (matchedWin && !matchedWin.closed && this.isPopoutDocument(matchedWin.document)) {
        return matchedWin;
      }
    }
    return null;
  }
  getWindowForLayout(layout) {
    return this.getOpenWindowForLayout(layout) || this.layoutWindows.get(layout) || this.findWindowForSavedLeaves(this.getSavedViewStates(layout));
  }
  /** 確認 Popout 仍持有原生焦點，避免延遲重試搶回主視窗焦點。 */
  isWindowFocused(targetWin) {
    try {
      const hasFocus = targetWin.document?.hasFocus;
      return typeof hasFocus === "function" ? hasFocus.call(targetWin.document) : true;
    } catch {
      return true;
    }
  }
  /** 確認主視窗 (plugin realm) 是否持有原生焦點。 */
  isMainWindowFocused() {
    try {
      const hasFocus = window.document?.hasFocus;
      return typeof hasFocus === "function" ? hasFocus.call(window.document) : true;
    } catch {
      return true;
    }
  }
  /**
   * 聚焦 (Focus) 指定 Popout 視窗並啟用合適的 Leaf (顯式呼叫 revealLeaf 確保分頁真實切換為可見)
   */
  focusTargetWindow(targetWin, preferredLeaf) {
    if (!targetWin || targetWin.closed) return;
    const doFocusAndReveal = async (focusWindow) => {
      try {
        if (focusWindow && typeof targetWin.focus === "function") {
          targetWin.focus();
        }
        if (!focusWindow && !this.isWindowFocused(targetWin)) return;
        const freshLeaves = this.getLeavesForWindow(targetWin);
        if (freshLeaves.length === 0) return;
        let targetLeaf = null;
        if (preferredLeaf && freshLeaves.includes(preferredLeaf)) {
          targetLeaf = preferredLeaf;
        } else {
          const activeLeaf = typeof this.app.workspace.getMostRecentLeaf === "function" ? this.app.workspace.getMostRecentLeaf() : this.app.workspace.activeLeaf;
          if (activeLeaf && freshLeaves.includes(activeLeaf)) {
            targetLeaf = activeLeaf;
          } else {
            targetLeaf = freshLeaves[0];
          }
        }
        if (targetLeaf) {
          await this.app.workspace.revealLeaf(targetLeaf);
          if (!this.isWindowFocused(targetWin)) return;
          this.app.workspace.setActiveLeaf(targetLeaf, { focus: focusWindow });
        }
      } catch {
      }
    };
    void doFocusAndReveal(true);
    targetWin.setTimeout(() => {
      void doFocusAndReveal(false);
    }, 50);
    targetWin.setTimeout(() => {
      void doFocusAndReveal(false);
    }, 200);
  }
  /**
   * 取得指定視窗內「目前 active 的 tab」leaf（依 tab header 的 is-active class）。
   * 全域 activeLeaf 可能指向其他視窗（例如使用者已切回主視窗），此時不該
   * fallback 到視窗內第一個 leaf，以免把第一個 tab 搶成 active。
   */
  getActiveLeafInWindow(targetWin) {
    const workspace = this.app.workspace;
    if (typeof workspace.iterateAllLeaves !== "function") return null;
    let found = null;
    workspace.iterateAllLeaves((leaf) => {
      if (found) return;
      const extLeaf = leaf;
      if (extLeaf.containerEl?.ownerDocument?.defaultView !== targetWin) return;
      const tabEl = extLeaf.tabHeaderEl;
      if (tabEl && tabEl.classList.contains("is-active")) {
        found = leaf;
      }
    });
    return found;
  }
  /**
   * 獲取目前活動視窗 (activeWindow) 中真正的 activeLeaf
   */
  getActiveLeafForCurrentWindow(targetWindow) {
    const currentWin = targetWindow || (typeof activeWindow !== "undefined" ? activeWindow : window);
    const globalActiveLeaf = this.app.workspace.getMostRecentLeaf();
    if (globalActiveLeaf && globalActiveLeaf.containerEl?.ownerDocument?.defaultView === currentWin) {
      return globalActiveLeaf;
    }
    let windowLeaf = null;
    this.app.workspace.iterateAllLeaves((leaf) => {
      const extLeaf = leaf;
      if (!windowLeaf && extLeaf.containerEl?.ownerDocument?.defaultView === currentWin) {
        windowLeaf = leaf;
      }
    });
    return windowLeaf || globalActiveLeaf;
  }
  /**
   * 獲取指定 DOM Window 中所有的 WorkspaceLeaves
   */
  getLeavesForWindow(targetWin) {
    const leaves = [];
    this.app.workspace.iterateAllLeaves((leaf) => {
      const extLeaf = leaf;
      if (extLeaf.containerEl?.ownerDocument?.defaultView === targetWin) {
        leaves.push(leaf);
      }
    });
    return leaves;
  }
  /** 保存指定 Window 的 live leaf 狀態，供 changeLayout 後重新辨識視窗。 */
  getViewStatesForWindow(targetWin) {
    return this.getLeavesForWindow(targetWin).map((leaf) => {
      const extLeaf = leaf;
      const viewState = typeof extLeaf.getViewState === "function" ? extLeaf.getViewState() : { id: "", type: leaf.view?.getViewType() || "unknown", state: {} };
      return {
        id: extLeaf.id || this.generateId(),
        type: viewState.type || leaf.view?.getViewType() || "unknown",
        state: viewState.state || {}
      };
    });
  }
  /** 快照目前所有已套用 layout 的 live popout，供新視窗 restore 後復原標籤。 */
  capturePreservedWindowLayouts() {
    const liveWindows = /* @__PURE__ */ new Set();
    this.app.workspace.iterateAllLeaves((leaf) => {
      const targetWin = this.getWindowForLeaf(leaf);
      if (targetWin && this.isPopoutDocument(targetWin.document)) {
        liveWindows.add(targetWin);
      }
    });
    const snapshots = [];
    liveWindows.forEach((targetWin) => {
      const layoutName = this.getLayoutNameForWindow(targetWin);
      if (!layoutName) return;
      snapshots.push({
        window: targetWin,
        layoutName,
        leaves: this.getViewStatesForWindow(targetWin),
        windowState: this.getWindowState(targetWin)
      });
    });
    return snapshots;
  }
  /**
   * 捕獲當前活動視窗的佈局
   */
  async captureCurrentLayout(options = {}, targetWindow) {
    try {
      const workspace = this.app.workspace;
      const fullLayout = workspace.getLayout();
      const activeLeaf = this.getActiveLeafForCurrentWindow(targetWindow);
      const currentWin = targetWindow || (typeof activeWindow !== "undefined" ? activeWindow : window);
      const windowLeaves = this.getLeavesForWindow(currentWin);
      let floatingLayout = this.extractCurrentFloatingLayout(fullLayout, activeLeaf);
      if (!floatingLayout) {
        const extActiveLeaf = activeLeaf;
        const rootInfo = extActiveLeaf && typeof extActiveLeaf.getRoot === "function" ? extActiveLeaf.getRoot()?.constructor?.name : "no-leaf";
        const isPopout = this.isCurrentlyInPopoutWindow(activeLeaf);
        console.warn("[WindowSpaces Debug]", { rootInfo, isPopout, fullLayout });
        throw new Error(`${t("errors.notInPopoutWindow")} (root: ${rootInfo}, isPopout: ${isPopout})`);
      }
      if (windowLeaves.length > 1 && floatingLayout.type === "leaf") {
        floatingLayout = {
          type: "split",
          id: this.generateId(),
          direction: "vertical",
          children: windowLeaves.map((leaf) => {
            const extLeaf = leaf;
            return {
              id: this.generateId(),
              type: "tabs",
              children: [{
                id: extLeaf.id || this.generateId(),
                type: "leaf",
                state: typeof extLeaf.getViewState === "function" ? extLeaf.getViewState() : { type: leaf.view?.getViewType() || "markdown", state: {} }
              }]
            };
          })
        };
      }
      const windowInfo = this.getCurrentWindowState();
      const layoutLeaves = this.extractLeavesFromLayout(floatingLayout);
      const liveLeavesById = /* @__PURE__ */ new Map();
      windowLeaves.forEach((leaf) => {
        const id = leaf.id;
        if (id) liveLeavesById.set(id, leaf);
      });
      const leaves = layoutLeaves.map((layoutLeaf) => {
        const liveLeaf = liveLeavesById.get(layoutLeaf.id);
        if (!liveLeaf) return layoutLeaf;
        const extLiveLeaf = liveLeaf;
        const viewState = typeof extLiveLeaf.getViewState === "function" ? extLiveLeaf.getViewState() : { id: "", type: liveLeaf.view?.getViewType() || layoutLeaf.type, state: {} };
        return {
          id: layoutLeaf.id,
          type: viewState.type || liveLeaf.view?.getViewType() || layoutLeaf.type,
          state: viewState.state || layoutLeaf.state || {}
        };
      });
      const capturedIds = new Set(leaves.map((leaf) => leaf.id));
      windowLeaves.forEach((leaf) => {
        const extLeaf = leaf;
        const id = extLeaf.id || this.generateId();
        if (capturedIds.has(id)) return;
        const viewState = typeof extLeaf.getViewState === "function" ? extLeaf.getViewState() : { id: "", type: leaf.view?.getViewType() || "unknown", state: {} };
        leaves.push({
          id,
          type: viewState.type || leaf.view?.getViewType() || "unknown",
          state: viewState.state || {}
        });
      });
      const capturedLayout = {
        id: this.generateId(),
        name: options.name ?? this.layoutNames.get(currentWin) ?? "",
        timestamp: Date.now(),
        windowState: {
          size: options.includeWindowSize !== false ? windowInfo.size : { width: 0, height: 0 },
          position: options.includePosition !== false ? windowInfo.position : void 0
        },
        workspace: {
          layout: floatingLayout,
          activeFile: activeLeaf?.view?.file?.path,
          leaves
        },
        metadata: {
          fileCount: this.countOpenFiles(leaves),
          tabCount: leaves.length,
          splitCount: 0,
          createdAt: (/* @__PURE__ */ new Date()).toISOString(),
          obsidianVersion: this.app.version || "unknown",
          pluginVersion: this.plugin?.manifest?.version || "1.0.0"
        },
        windowInfo: {
          firstLeafId: leaves.length > 0 ? leaves[0].id : void 0
        }
      };
      const existingLayout = this.plugin.settings?.spaces?.find(
        (l) => l.name === capturedLayout.name
      );
      if (existingLayout) {
        if (existingLayout.includeGeometry !== void 0) {
          capturedLayout.includeGeometry = existingLayout.includeGeometry;
        }
        if (existingLayout.icon !== void 0) {
          capturedLayout.icon = existingLayout.icon;
        }
        if (existingLayout.color !== void 0) {
          capturedLayout.color = existingLayout.color;
        }
        if (existingLayout.borderInset !== void 0) {
          capturedLayout.borderInset = existingLayout.borderInset;
        }
        if (existingLayout.showFoldedCorner !== void 0) {
          capturedLayout.showFoldedCorner = existingLayout.showFoldedCorner;
        }
        if (existingLayout.activityBars !== void 0) {
          capturedLayout.activityBars = existingLayout.activityBars;
        }
      }
      try {
        capturedLayout.hidden = this.plugin.popoutLayout.captureHiddenState(currentWin);
      } catch {
        capturedLayout.hidden = void 0;
      }
      this.layoutWindows.set(capturedLayout, currentWin);
      return capturedLayout;
    } catch (error) {
      console.error("Failed to capture layout:", error);
      throw error;
    }
  }
  /**
   * 恢復指定的佈局 (忠實還原 Tabs, Horizontal/Vertical Splits 與檔案狀態)
   */
  async restoreLayout(layout, options = {}) {
    if (this.activeRestorePromise) return this.activeRestorePromise;
    const restorePromise = this.restoreLayoutInternal(layout, options);
    this.activeRestorePromise = restorePromise;
    try {
      await restorePromise;
    } finally {
      if (this.activeRestorePromise === restorePromise) {
        this.activeRestorePromise = null;
      }
    }
  }
  async restoreLayoutInternal(layout, options = {}) {
    this.isRestoringLayout = true;
    try {
      if (!this.validateLayout(layout)) {
        throw new Error(t("errors.invalidData"));
      }
      if (layout.workspace) {
        const normalizedRoot = this.normalizeWindowLayout(layout.workspace.layout);
        if (normalizedRoot && normalizedRoot !== layout.workspace.layout) {
          layout.workspace.layout = normalizedRoot;
        }
      }
      if (!options.forceReload && (!options.forceNewWindow || options.focusExistingWindow === true)) {
        const existingWin = this.getOpenWindowForLayout(layout);
        if (existingWin && !existingWin.closed && this.isPopoutDocument(existingWin.document)) {
          this.focusTargetWindow(existingWin, this.getActiveLeafInWindow(existingWin));
          this.setLayoutLabelForWindow(existingWin, layout.name);
          this.refreshLayoutLabels();
          if (options.showNotifications !== false && this.plugin.settings.showNotifications !== false) {
            new import_obsidian3.Notice(tWithParams("notifications.switchedToOpenWindow", { name: layout.name }));
          }
          return;
        }
      }
      if (!options.forceNewWindow && (options.forceReload || options.targetWindow)) {
        const existingTarget = options.targetWindow && this.isPopoutDocument(options.targetWindow.document) ? options.targetWindow : this.getOpenWindowForLayout(layout);
        if (existingTarget && !existingTarget.closed) {
          await this.restoreOpenSpaceInPlace(layout, existingTarget, options);
          return;
        }
      }
      const preservedWindowLayouts = this.capturePreservedWindowLayouts().filter(
        (snapshot) => options.forceNewWindow || snapshot.window !== options.targetWindow
      );
      const savedLeaves = this.getSavedViewStates(layout);
      const savedLeafId = layout.windowInfo?.firstLeafId || savedLeaves[0]?.id;
      const workspace = this.app.workspace;
      let currentLayout = workspace.getLayout();
      let floatingWindows = this.getFloatingWindows(currentLayout);
      let targetIndex = -1;
      let targetWin = null;
      let isNewlyCreatedWindow = false;
      if (options.forceNewWindow) {
        targetIndex = -1;
      } else if (options.targetWindow && this.isPopoutDocument(options.targetWindow.document)) {
        targetWin = options.targetWindow;
        targetIndex = this.findFloatingWindowIndexForWindow(
          targetWin,
          floatingWindows
        );
      } else if (savedLeafId && floatingWindows.length > 0) {
        for (let i = 0; i < floatingWindows.length; i++) {
          if (this.floatingWindowContainsLeaf(floatingWindows[i], savedLeafId)) {
            targetIndex = i;
            break;
          }
        }
        const existingTargetLeaf = this.findLeafById(savedLeafId);
        targetWin = this.getWindowForLeaf(existingTargetLeaf);
      }
      if (targetIndex < 0) {
        if (!options.forceNewWindow && options.targetWindow && this.isPopoutDocument(options.targetWindow.document)) {
          targetWin = options.targetWindow;
          targetIndex = this.findPopoutOrdinal(targetWin, floatingWindows.length);
          if (targetIndex < 0) {
            throw new Error(t("errors.cannotRestore"));
          }
        } else {
          const popoutWinsBefore = new Set(this.getLivePopoutWindows());
          isNewlyCreatedWindow = true;
          const extWs = this.app.workspace;
          const popoutInitData = layout.includeGeometry !== false && layout.windowState ? {
            x: layout.windowState.position?.x,
            y: layout.windowState.position?.y,
            size: layout.windowState.size ? {
              width: layout.windowState.size.width,
              height: layout.windowState.size.height
            } : void 0
          } : void 0;
          const popoutLeaf = extWs.openPopoutLeaf?.(popoutInitData);
          let newlyCreatedWin = null;
          for (let attempt = 0; attempt < 40; attempt++) {
            await new Promise((resolve) => window.setTimeout(resolve, 50));
            const currentPopoutWins = this.getLivePopoutWindows();
            newlyCreatedWin = currentPopoutWins.find((w) => !popoutWinsBefore.has(w)) || null;
            if (newlyCreatedWin) break;
          }
          const extPopoutLeaf = popoutLeaf;
          targetWin = newlyCreatedWin || extPopoutLeaf?.containerEl?.ownerDocument?.defaultView || null;
          if (targetWin) {
            this.restoreWindowGeometry(targetWin, layout.windowState, layout.includeGeometry, true);
          }
          currentLayout = workspace.getLayout();
          floatingWindows = this.getFloatingWindows(currentLayout);
          if (targetWin) {
            targetIndex = this.findFloatingWindowIndexForWindow(targetWin, floatingWindows);
          }
          if (targetIndex < 0) {
            targetIndex = floatingWindows.length - 1;
          }
        }
      }
      let builtLeaves = null;
      let targetOnlyBuildAttempted = false;
      if (targetIndex >= 0 && layout.workspace?.layout) {
        const rootNode = this.extractLayoutRootNode(layout.workspace.layout);
        if (isNewlyCreatedWindow && targetWin) {
          targetOnlyBuildAttempted = true;
          builtLeaves = await this.rebuildTargetWindowStructure(targetWin, rootNode);
          if (layout.hidden && targetWin) {
            try {
              if (layout.hidden.leftSidebar) {
                this.plugin.popoutLayout.hideColumn(targetWin, "left");
              }
              if (layout.hidden.rightSidebar) {
                this.plugin.popoutLayout.hideColumn(targetWin, "right");
              }
            } catch {
            }
          }
        }
        if (!targetOnlyBuildAttempted) {
          const currentFloatingWindow = floatingWindows[targetIndex];
          const restoredWindow = this.prepareFloatingWindowForRestore(
            layout.workspace.layout,
            currentFloatingWindow,
            layout.includeGeometry,
            layout.windowState
          );
          const livePopoutsBeforeLayout = this.getLivePopoutWindows();
          const floatingObj = currentLayout.floating;
          if (typeof floatingObj === "object" && floatingObj !== null && "type" in floatingObj && floatingObj.type === "floating" && Array.isArray(floatingObj.children)) {
            floatingObj.children = floatingObj.children.map(
              (child, idx) => {
                if (idx === targetIndex) return restoredWindow;
                const liveWin = livePopoutsBeforeLayout.find(
                  (w) => this.findFloatingWindowIndexForWindow(w, floatingWindows) === idx
                ) || livePopoutsBeforeLayout[idx];
                if (liveWin && !liveWin.closed && this.isPopoutDocument(liveWin.document)) {
                  return this.syncLiveWindowBoundsToFloatingChild(child, liveWin);
                }
                return child;
              }
            );
          } else if (Array.isArray(floatingObj)) {
            floatingObj[targetIndex] = restoredWindow;
          }
          await workspace.changeLayout(currentLayout);
          this.restorePreservedWindowLabels(preservedWindowLayouts, targetWin);
        }
      }
      const livePopouts = this.getLivePopoutWindows();
      let liveTargetWin = null;
      if (options.forceNewWindow) {
        if (options.targetWindow) {
          const nonSourceWins = livePopouts.filter((w) => w !== options.targetWindow);
          if (nonSourceWins.length > 0) {
            liveTargetWin = nonSourceWins[nonSourceWins.length - 1];
          }
        } else {
          liveTargetWin = livePopouts[livePopouts.length - 1] || targetWin;
        }
      } else if (targetIndex >= 0 && targetIndex < livePopouts.length) {
        liveTargetWin = livePopouts[targetIndex];
      }
      targetWin = liveTargetWin || this.findWindowForSavedLeaves(
        savedLeaves,
        options.targetWindow,
        targetWin
      ) || targetWin;
      if (targetWin) {
        this.restoreWindowGeometry(targetWin, layout.windowState, layout.includeGeometry, true);
      }
      await new Promise((resolve) => window.setTimeout(resolve, 150));
      let missingFiles = [];
      if (options.validateFiles !== false && savedLeaves.length > 0) {
        missingFiles = await this.restoreFileStatesForWindow(
          targetWin,
          savedLeaves,
          layout.workspace?.activeFile,
          builtLeaves !== null
        );
      }
      if (builtLeaves && builtLeaves.length > 0) {
        this.applyPinnedStateToBuiltLeaves(builtLeaves, savedLeaves);
        if (layout.hidden?.hiddenLeafIds?.length && targetWin) {
          const hiddenIds = new Set(layout.hidden.hiddenLeafIds);
          layout.hidden.hiddenLeafIds = builtLeaves.filter((_, i) => hiddenIds.has(savedLeaves[i]?.id)).map((leaf) => leaf.id || "");
        }
      }
      this.setLayoutLabelForWindow(targetWin, layout.name);
      this.syncWindowLeafMarker(targetWin, layout);
      this.refreshLayoutLabels();
      if (targetWin && layout.hidden) {
        this.applyHiddenStateAfterRestore(targetWin, layout.hidden);
      }
      if (targetWin) {
        this.layoutWindows.set(layout, targetWin);
        this.plugin.activityBars?.renderWindow(targetWin);
        this.restoreWindowGeometry(targetWin, layout.windowState, layout.includeGeometry, false);
        const canActivatePopout = this.isWindowFocused(targetWin) || !this.isMainWindowFocused();
        const winLeaves = this.getLeavesForWindow(targetWin);
        if (winLeaves.length > 0 && canActivatePopout) {
          const activeLeaf = typeof this.app.workspace.getMostRecentLeaf === "function" ? this.app.workspace.getMostRecentLeaf() : this.app.workspace.activeLeaf;
          if (!activeLeaf || !winLeaves.includes(activeLeaf)) {
            try {
              this.app.workspace.setActiveLeaf(winLeaves[0], { focus: true });
            } catch {
            }
          }
        }
        if (typeof targetWin.focus === "function" && canActivatePopout) {
          try {
            targetWin.focus();
          } catch (e) {
            console.warn("Failed to focus target window:", e);
          }
        }
      }
      if (targetWin) {
        this.ensureDeferredViewsLoaded(targetWin);
      }
      WindowLayoutsModal.renderAllInstances();
      if (options.showNotifications !== false && this.plugin.settings.showNotifications !== false) {
        if (missingFiles.length > 0) {
          const missingList = missingFiles.slice(0, 3).join(", ") + (missingFiles.length > 3 ? "..." : "");
          new import_obsidian3.Notice(`\u26A0\uFE0F ${t("notifications.layoutRestored")}: ${layout.name} (${t("notifications.missingFilesNotice")}: ${missingList})`, 8e3);
        } else {
          new import_obsidian3.Notice(`${t("notifications.layoutRestored")}: ${layout.name}`);
        }
      }
    } catch (error) {
      console.error("Failed to restore layout:", error);
      const message = error instanceof Error ? error.message : String(error);
      throw new Error(`${t("errors.failedToRestore")}: ${message}`);
    } finally {
      this.isRestoringLayout = false;
    }
  }
  /**
   * 獲取所有保存的佈局 (按建立時間降序排列，最新儲存的在最上面)
   */
  getSavedLayouts() {
    const settings = this.plugin.settings || {};
    const spaces = settings.spaces || [];
    return [...spaces].sort((a, b) => {
      const mode = settings.sortBy || "updated-desc";
      const getTimestamp = (l, field) => l[field] ?? l.timestamp ?? 0;
      switch (mode) {
        case "updated-desc":
          return getTimestamp(b, "updatedAt") - getTimestamp(a, "updatedAt");
        case "updated-asc":
          return getTimestamp(a, "updatedAt") - getTimestamp(b, "updatedAt");
        case "created-desc":
          return getTimestamp(b, "createdAt") - getTimestamp(a, "createdAt");
        case "created-asc":
          return getTimestamp(a, "createdAt") - getTimestamp(b, "createdAt");
        case "name-asc":
          return (a.name || "").localeCompare(b.name || "", void 0, { sensitivity: "base" });
        case "name-desc":
          return (b.name || "").localeCompare(a.name || "", void 0, { sensitivity: "base" });
        default:
          return getTimestamp(b, "updatedAt") - getTimestamp(a, "updatedAt");
      }
    });
  }
  /**
   * 保存新佈局或覆蓋既有佈局
   */
  async saveLayout(layout) {
    try {
      const settings = this.plugin.settings;
      if (!settings.spaces) {
        settings.spaces = [];
      }
      const now = Date.now();
      const existingIndex = settings.spaces.findIndex(
        (l) => l.name === layout.name
      );
      const isOverwrite = existingIndex >= 0;
      if (isOverwrite) {
        const existing = settings.spaces[existingIndex];
        layout.id = existing.id;
        layout.createdAt = existing.createdAt || existing.timestamp || now;
        layout.updatedAt = now;
        layout.timestamp = now;
        if (layout.includeGeometry === void 0 && existing.includeGeometry !== void 0) {
          layout.includeGeometry = existing.includeGeometry;
        }
        if (layout.borderInset === void 0 && existing.borderInset !== void 0) {
          layout.borderInset = existing.borderInset;
        }
        if (layout.showFoldedCorner === void 0 && existing.showFoldedCorner !== void 0) {
          layout.showFoldedCorner = existing.showFoldedCorner;
        }
        if (layout.activityBars === void 0 && existing.activityBars !== void 0) {
          layout.activityBars = existing.activityBars;
        }
        settings.spaces[existingIndex] = layout;
      } else {
        layout.createdAt = now;
        layout.updatedAt = now;
        layout.timestamp = now;
        settings.spaces.push(layout);
      }
      await this.plugin.saveSettings();
      WindowLayoutsModal.renderAllInstances();
      const sourceWindow = this.getWindowForLayout(layout);
      this.setLayoutLabelForWindow(sourceWindow, layout.name);
      if (sourceWindow) {
        this.plugin.activityBars?.renderWindow(sourceWindow);
      } else {
        this.plugin.activityBars?.refreshAll();
      }
      if (this.plugin.settings.showNotifications !== false) {
        const noticeMsg = isOverwrite ? `${t("notifications.layoutOverwritten")}: ${layout.name}` : `${t("notifications.layoutSaved")}: ${layout.name}`;
        new import_obsidian3.Notice(noticeMsg);
      }
    } catch (error) {
      console.error("Failed to save layout:", error);
      throw new Error(t("errors.failedToSave"));
    }
  }
  /**
   * 刪除指定佈局
   */
  async deleteLayout(layoutId) {
    try {
      const settings = this.plugin.settings;
      const index = settings.spaces.findIndex((l) => l.id === layoutId);
      if (index >= 0) {
        const deletedLayout = settings.spaces[index];
        settings.spaces.splice(index, 1);
        await this.plugin.saveSettings();
        WindowLayoutsModal.renderAllInstances();
        if (this.plugin.settings.showNotifications !== false) {
          new import_obsidian3.Notice(
            `${t("notifications.layoutDeleted")}: ${deletedLayout.name}`
          );
        }
      }
    } catch (error) {
      console.error("Failed to delete layout:", error);
      throw new Error(t("errors.failedToDelete"));
    }
  }
  /**
   * 重命名 Section 名稱並同步更新所有帶有該標籤的 Space 與 sectionsOrder
   */
  async renameSection(oldName, newName) {
    const cleanOld = oldName.trim();
    const cleanNew = newName.trim();
    if (!cleanOld || !cleanNew || cleanOld === cleanNew) return;
    const settings = this.plugin.settings;
    if (!settings.sectionsOrder) settings.sectionsOrder = [];
    const orderIndex = settings.sectionsOrder.indexOf(cleanOld);
    if (orderIndex !== -1) {
      settings.sectionsOrder[orderIndex] = cleanNew;
    }
    (settings.spaces || []).forEach((space) => {
      if (space.sections && Array.isArray(space.sections)) {
        const secIndex = space.sections.indexOf(cleanOld);
        if (secIndex !== -1) {
          space.sections[secIndex] = cleanNew;
          space.sections = Array.from(new Set(space.sections));
        }
      }
    });
    await this.plugin.saveSettings();
    WindowLayoutsModal.renderAllInstances();
  }
  /**
   * 切換指定 Space 的封存狀態
   */
  async toggleArchiveSpace(spaceId, archiveStatus) {
    const settings = this.plugin.settings;
    const space = (settings.spaces || []).find((s) => s.id === spaceId);
    if (!space) return;
    const newStatus = archiveStatus !== void 0 ? archiveStatus : !space.archived;
    space.archived = newStatus;
    await this.plugin.saveSettings();
    WindowLayoutsModal.renderAllInstances();
    if (this.plugin.settings.showNotifications !== false) {
      let noticeText = newStatus ? "Space archived" : "Space unarchived";
      try {
        noticeText = newStatus ? t("manageModal.archiveSuccess") : t("manageModal.unarchiveSuccess");
      } catch {
      }
      new import_obsidian3.Notice(`${noticeText}: ${space.name}`);
    }
  }
  /**
   * 更新 Section 排序順序
   */
  async reorderSections(newOrder) {
    this.plugin.settings.sectionsOrder = newOrder;
    await this.plugin.saveSettings();
    WindowLayoutsModal.renderAllInstances();
  }
  /**
   * 獲取所有目前開啟中的活體 Popout DOM Window (按 Workspace 順序)
   */
  getLivePopoutWindows() {
    const wins = [];
    this.app.workspace.iterateAllLeaves((leaf) => {
      const extLeaf = leaf;
      const win = extLeaf.containerEl?.ownerDocument?.defaultView;
      if (win && this.isPopoutDocument(win.document) && !wins.includes(win)) {
        wins.push(win);
      }
    });
    return wins;
  }
  /**
   * 獲取當前活發對應的 DOM Window (包含當前命令發起所在 Popout 視窗)
   */
  getActiveWindow() {
    const activeLeaf = this.getActiveLeafForCurrentWindow();
    if (activeLeaf) {
      const extLeaf = activeLeaf;
      const ownerDocument = extLeaf.containerEl?.ownerDocument;
      if (ownerDocument?.defaultView) {
        return ownerDocument.defaultView;
      }
    }
    return typeof activeWindow !== "undefined" ? activeWindow : window;
  }
  /**
   * 獲取當前視窗狀態
   */
  getCurrentWindowState() {
    const activeLeaf = this.getActiveLeafForCurrentWindow();
    let currentWindow = typeof activeWindow !== "undefined" ? activeWindow : window;
    if (activeLeaf) {
      const extLeaf = activeLeaf;
      const ownerDocument = extLeaf.containerEl?.ownerDocument;
      if (ownerDocument?.defaultView) {
        currentWindow = ownerDocument.defaultView;
      }
    }
    return this.getWindowState(currentWindow);
  }
  /**
   * 使用 outer dimensions，與 Window.resizeTo() 的參數語意一致。
   * innerWidth/innerHeight 是內容區大小，不能直接拿來恢復整個視窗。
   */
  getWindowState(targetWin) {
    return {
      size: {
        width: targetWin.outerWidth || targetWin.innerWidth,
        height: targetWin.outerHeight || targetWin.innerHeight
      },
      position: targetWin.screenX !== void 0 ? {
        x: targetWin.screenX,
        y: targetWin.screenY
      } : void 0
    };
  }
  /**
   * 檢查當前活動上下文是否位於 Popout 視窗
   */
  isCurrentlyInPopoutWindow(activeLeaf) {
    if (typeof activeWindow !== "undefined" && activeWindow !== window) {
      return true;
    }
    if (activeLeaf && activeLeaf.containerEl) {
      const doc = activeLeaf.containerEl.ownerDocument;
      if (doc && doc.defaultView && doc.defaultView !== window) {
        return true;
      }
      if (doc && this.isPopoutDocument(doc)) {
        return true;
      }
    }
    if (typeof document !== "undefined" && this.isPopoutDocument(document)) {
      return true;
    }
    return false;
  }
  /**
   * 檢查 floating 視窗佈局中是否包含開啟指定的檔案
   */
  floatingLayoutContainsFile(layout, filePath) {
    if (!layout) return false;
    if (layout.type === "leaf") {
      return layout.state?.state?.file === filePath;
    } else if (Array.isArray(layout.children)) {
      for (let child of layout.children) {
        if (this.floatingLayoutContainsFile(child, filePath)) {
          return true;
        }
      }
    }
    return false;
  }
  /**
   * 只提取當前活動視窗的浮動佈局資訊 (完美支援新開 Popout 視窗)
   */
  extractCurrentFloatingLayout(fullLayout, activeLeaf) {
    const isPopout = this.isCurrentlyInPopoutWindow(activeLeaf);
    const floatingWindows = this.getFloatingWindows(fullLayout);
    if (floatingWindows.length > 0) {
      const activeLeafId = activeLeaf?.id || null;
      if (activeLeafId) {
        for (let floatingItem of floatingWindows) {
          if (this.floatingWindowContainsLeaf(floatingItem, activeLeafId)) {
            return floatingItem;
          }
        }
      }
      const activeFilePath = activeLeaf?.view?.file?.path;
      if (activeFilePath) {
        for (let floatingItem of floatingWindows) {
          if (this.floatingLayoutContainsFile(floatingItem, activeFilePath)) {
            return floatingItem;
          }
        }
      }
      if (isPopout || floatingWindows.length === 1) {
        return floatingWindows[0];
      }
    }
    if (activeLeaf && typeof activeLeaf.getRoot === "function") {
      const root = activeLeaf.getRoot();
      if (root && typeof root.getLayout === "function") {
        const ws = this.app.workspace;
        if (root !== ws.rootSplit && root !== ws.leftSplit && root !== ws.rightSplit) {
          const rootLayout = root.getLayout();
          if (rootLayout) return rootLayout;
        }
      }
    }
    if (isPopout && activeLeaf) {
      const viewState = typeof activeLeaf.getViewState === "function" ? activeLeaf.getViewState() : { type: activeLeaf.view?.getViewType() || "empty", state: {} };
      return {
        id: activeLeaf.id || this.generateId(),
        type: "leaf",
        state: viewState
      };
    }
    return null;
  }
  /** 取得 Obsidian floating container 內的實際 WorkspaceWindow 陣列。 */
  getFloatingWindows(layout) {
    const floating = layout?.floating;
    if (Array.isArray(floating)) return floating;
    if (floating?.type === "floating" && Array.isArray(floating.children)) {
      return floating.children;
    }
    return [];
  }
  /**
   * 以 live leaf 的 WorkspaceWindow root ID 精確定位 floating index。
   * Leaf ID 可能在多個 restored layout 中重複，因此只接受唯一 leaf 匹配。
   */
  findFloatingWindowIndexForWindow(targetWin, floatingWindows) {
    const windowLeaves = this.getLeavesForWindow(targetWin);
    for (const leaf of windowLeaves) {
      const extLeaf = leaf;
      const root = typeof extLeaf.getRoot === "function" ? extLeaf.getRoot() : null;
      const rootLayout = root && typeof root.getLayout === "function" ? root.getLayout() : null;
      const rootIds = new Set(
        [root?.id, rootLayout?.id].filter((id) => !!id)
      );
      if (rootIds.size === 0) continue;
      const index = floatingWindows.findIndex(
        (floatingWindow) => rootIds.has(floatingWindow?.id)
      );
      if (index >= 0) return index;
    }
    const matchingIndices = /* @__PURE__ */ new Set();
    windowLeaves.forEach((leaf) => {
      const leafId = leaf.id;
      if (!leafId) return;
      floatingWindows.forEach((floatingWindow, index) => {
        if (this.floatingWindowContainsLeaf(floatingWindow, leafId)) {
          matchingIndices.add(index);
        }
      });
    });
    return matchingIndices.size === 1 ? Array.from(matchingIndices)[0] : this.findPopoutOrdinal(targetWin, floatingWindows.length);
  }
  /**
   * 以 live WorkspaceWindow container 的順序對應 serialized floating children。
   * Obsidian 公開 API 不保證 WorkspaceWindow 帶有 serialized window ID。
   */
  findPopoutOrdinal(targetWin, floatingCount) {
    const workspace = this.app.workspace;
    const floatingChildren = workspace.floatingSplit?.children || workspace.floating?.children || [];
    if (Array.isArray(floatingChildren)) {
      const directIndex = floatingChildren.findIndex(
        (container) => container?.win === targetWin || container?.doc?.defaultView === targetWin
      );
      if (directIndex >= 0 && directIndex < floatingCount) {
        return directIndex;
      }
    }
    const livePopoutWindows = [];
    this.app.workspace.iterateAllLeaves((leaf) => {
      const container = typeof leaf.getContainer === "function" ? leaf.getContainer() : null;
      const leafWindow = container?.win || this.getWindowForLeaf(leaf);
      if (leafWindow && this.isPopoutDocument(leafWindow.document) && !livePopoutWindows.includes(leafWindow)) {
        livePopoutWindows.push(leafWindow);
      }
    });
    const ordinal = livePopoutWindows.indexOf(targetWin);
    return ordinal >= 0 && ordinal < floatingCount ? ordinal : -1;
  }
  /**
   * 以目前 WorkspaceWindow 的 id/容器為基礎，只替換其 children。
   * Obsidian 1.12 的 layout schema 是 floating -> window -> split/tabs/leaf。
   */
  /**
   * 以 windowState（screenX/screenY、outerWidth/outerHeight）覆寫 floating
   * window 節點的序列化幾何，統一 changeLayout 建立視窗時使用的座標來源，
   * 避免 Obsidian 序列化幾何與 screen 座標不一致造成 restore 後的位移。
   */
  applyWindowStateGeometry(node, windowState) {
    if (!node || !windowState) return;
    const size = windowState.size;
    if (size && size.width > 0 && size.height > 0) {
      node.width = size.width;
      node.height = size.height;
    }
    if (windowState.position) {
      node.x = windowState.position.x;
      node.y = windowState.position.y;
    }
  }
  /**
   * 取得實際的根結構節點。保留 window/floating 包裝：多 child 的 window
   * （如 Professional：tabs + tabs + split）也可由 leaf 層級重建，是否可行
   * 交由 isSimpleLayoutStructure 遞迴判定。
   */
  extractLayoutRootNode(layout) {
    return layout ?? null;
  }
  /**
   * 判斷 layout 結構是否可用 leaf 層級 API 重建：
   * - leaf / tabs：可。
   * - split：children 皆為 tabs/leaf（無巢狀 split）。
   * - window / floating：children 皆為 tabs/leaf/split，且其中的 split
   *   本身無巢狀。
   * 其餘（巢狀 split、未知節點）一律回傳 false，交由全域 changeLayout fallback。
   */
  isSimpleLayoutStructure(node, parentDirection) {
    if (!node) return false;
    if (node.type === "leaf" || node.type === "tabs") return true;
    if (node.type === "split") {
      if (!Array.isArray(node.children) || node.children.length === 0) return false;
      if (parentDirection && node.direction === parentDirection) return false;
      for (const child of node.children) {
        if (!child) return false;
        if (child.type !== "tabs" && child.type !== "leaf") return false;
      }
      return true;
    }
    if (node.type === "window" || node.type === "floating") {
      if (!Array.isArray(node.children) || node.children.length === 0) return false;
      const dir = node.direction || "vertical";
      for (const child of node.children) {
        if (!child) return false;
        if (child.type === "split") {
          if (!this.isSimpleLayoutStructure(child, dir)) return false;
        } else if (child.type !== "tabs" && child.type !== "leaf") {
          return false;
        }
      }
      return true;
    }
    return false;
  }
  /**
   * 在目標視窗內以 leaf 層級 API 重建結構（不觸發全域 changeLayout）。
   *
   * 使用「兩階段」演算法以正確支援單層巢狀 split（如 FET-Migration 的
   * window > [hSplit, tabs, tabs]）：
   * 1. 階段一：先為頂層每個單位建立「佔位 leaf」（同一層級，依序
   *    createLeafBySplit），確保外層分割結構完整。
   * 2. 階段二：逐個單位填充——tabs/leaf 直接填 leaf；split 則以其佔位
   *    leaf 為錨點遞迴展開（此時錨點位於外層正確層級，createLeafBySplit
   *    建立的新 split 會正確替換佔位位置）。
   *
   * 回傳依建立順序（= saved 樹先序）的 leaf 清單，供後續 openFile 配對。
   */
  async buildSimpleWindowStructure(targetWin, rootNode) {
    const workspace = this.app.workspace;
    const initialLeaves = this.getLeavesForWindow(targetWin);
    const built = [];
    const fillTabs = async (leaf, node) => {
      const leafNodes = node.type === "leaf" ? [node] : Array.isArray(node.children) ? node.children.filter((c) => c?.type === "leaf") : [];
      const groupLeaves = [];
      let last = leaf;
      for (let i = 0; i < leafNodes.length; i++) {
        if (i > 0) {
          const parent = last.parent;
          if (!parent) break;
          last = workspace.createLeafInParent(
            parent,
            -1
          );
        }
        await this.applyBuiltLeafState(last, leafNodes[i]);
        groupLeaves.push(last);
        built.push(last);
      }
      if (node.type === "tabs" && groupLeaves.length > 0) {
        const currentTab = typeof node.currentTab === "number" ? node.currentTab : 0;
        const activeIndex = Math.max(0, Math.min(currentTab, groupLeaves.length - 1));
        const groupActive = groupLeaves[activeIndex];
        if (groupActive) {
          try {
            await workspace.revealLeaf(groupActive);
            workspace.setActiveLeaf(groupActive, { focus: false });
          } catch {
          }
        }
      }
      return last;
    };
    const expandSplit = async (node, direction, placeholderLeaf) => {
      let anchor2 = placeholderLeaf;
      for (let i = 0; i < node.children.length; i++) {
        const child = node.children[i];
        if (i > 0) {
          anchor2 = workspace.createLeafBySplit(
            anchor2,
            direction === "horizontal" ? "horizontal" : "vertical"
          );
        }
        anchor2 = await fillTabs(anchor2, child);
      }
      return anchor2;
    };
    const isContainer = rootNode?.type === "window" || rootNode?.type === "floating";
    const topDirection = isContainer ? rootNode.direction || "vertical" : "vertical";
    const topUnits = isContainer ? rootNode.children || [] : [rootNode];
    const placeholders = [];
    let anchor = initialLeaves[0] || null;
    for (let i = 0; i < topUnits.length; i++) {
      if (i > 0) {
        if (!anchor) break;
        try {
          anchor = workspace.createLeafBySplit(anchor, topDirection);
        } catch (e) {
          console.warn("Failed to split leaf during restore:", e);
          break;
        }
      }
      if (!anchor) break;
      placeholders.push(anchor);
    }
    for (let i = 0; i < topUnits.length && i < placeholders.length; i++) {
      const unit = topUnits[i];
      const placeholder = placeholders[i];
      if (unit?.type === "split") {
        await expandSplit(unit, unit.direction, placeholder);
      } else {
        await fillTabs(placeholder, unit);
      }
    }
    this.applySavedSplitDimensions(targetWin, rootNode);
    return built;
  }
  /**
   * 依 saved layout 的 dimension（Obsidian flex-grow 權重，0~100）遞迴
   * 套用 split 比例。與 Obsidian 原生 `setDimension` 語意一致：
   * - flex-grow 權重在容器縮放時自動重新分配；
   * - display:none 隱藏側欄時，剩餘欄位自動填滿（不需 rebalance）。
   */
  applySavedSplitDimensions(win, rootNode) {
    if (!win || win.closed || !rootNode) return;
    const rootEl = win.document?.querySelector(".workspace-split.mod-root");
    if (!rootEl) return;
    const elToItem = /* @__PURE__ */ new Map();
    try {
      this.app.workspace.iterateAllLeaves((leaf) => {
        if (!leaf || this.getWindowForLeaf(leaf) !== win) return;
        let item = leaf.parent;
        let guard = 0;
        while (item && guard++ < 20) {
          const itemEl = item.containerEl;
          if (itemEl instanceof HTMLElement && !elToItem.has(itemEl)) {
            elToItem.set(itemEl, item);
          }
          item = item.parent;
        }
      });
    } catch {
    }
    const getSplitChildren = (el) => Array.from(el.children).filter(
      (child) => child instanceof HTMLElement && (child.classList.contains("workspace-tabs") || child.classList.contains("workspace-split"))
    );
    const setFlexGrow = (domEl, dimension) => {
      const item = elToItem.get(domEl);
      if (item && typeof item.setDimension === "function") {
        if (dimension > 0 && dimension < 100) {
          item.setDimension(dimension);
          return;
        }
      }
      const customEl = domEl;
      const flexGrow = String(dimension);
      if (typeof customEl.setCssProps === "function") {
        customEl.setCssProps({ "flex-grow": flexGrow });
      } else {
        domEl.style.setProperty("flex-grow", flexGrow);
      }
    };
    const applyNode = (node, domEl) => {
      if (!node || !domEl) return;
      const dimension = Number(node.dimension);
      if (Number.isFinite(dimension) && dimension > 0 && dimension <= 100) {
        setFlexGrow(domEl, dimension);
      }
      if (node.type === "split" && Array.isArray(node.children)) {
        const domChildren = getSplitChildren(domEl);
        node.children.forEach((child, index) => {
          applyNode(child, domChildren[index]);
        });
      }
    };
    if (rootNode.type === "window" || rootNode.type === "floating") {
      const domChildren = getSplitChildren(rootEl);
      (Array.isArray(rootNode.children) ? rootNode.children : []).forEach(
        (child, index) => {
          applyNode(child, domChildren[index]);
        }
      );
    } else {
      applyNode(rootNode, rootEl);
    }
  }
  /**
   * 為 leaf 層級建立的 leaf 設定 view state。
   * 檔案 leaf 交由 restoreFileStatesForWindow 的 openFile 處理（此處跳過）；
   * 非檔案 leaf 在此先建立 view，讓後續的 ensureViewRenderedWithRetries 能渲染。
   */
  async applyBuiltLeafState(leaf, node) {
    if (!leaf || !node) return;
    const filePath = this.getFilePathFromLeafState({
      type: node.type,
      state: node.state?.state || {}
    });
    if (filePath) return;
    const nodeState = node.state || {};
    await leaf.setViewState({
      type: nodeState.type || node.type || "empty",
      active: false,
      state: nodeState.state || {}
    });
    const extLeaf = leaf;
    if (extLeaf.isDeferred && typeof extLeaf.loadIfDeferred === "function") {
      try {
        await extLeaf.loadIfDeferred();
      } catch (e) {
        console.warn("Failed to load deferred view during restore:", e);
      }
    }
  }
  /**
   * leaf 層級建立後，依 saved 順序套用 pinned 狀態
   * （檔案 leaf 需於 openFile 之後才 toggle，避免 openFile 重置）。
   */
  applyPinnedStateToBuiltLeaves(built, saved) {
    built.forEach((leaf, i) => {
      const savedLeaf = saved[i];
      if (!savedLeaf || savedLeaf.pinned !== true) return;
      const extLeaf = leaf;
      const isPinned = extLeaf.getViewState?.()?.pinned === true;
      if (!isPinned && typeof extLeaf.togglePinned === "function") {
        try {
          extLeaf.togglePinned();
        } catch {
        }
      }
    });
  }
  prepareFloatingWindowForRestore(savedLayout, currentWindow, includeGeometry = true, windowState) {
    const saved = JSON.parse(JSON.stringify(savedLayout));
    if (currentWindow?.type === "window") {
      if (saved.type === "window") {
        const merged = {
          ...currentWindow,
          ...saved,
          id: currentWindow.id,
          children: Array.isArray(saved.children) ? saved.children.map((child) => this.normalizeFloatingLayout(child)) : []
        };
        if (includeGeometry === false) {
          delete merged.x;
          delete merged.y;
          delete merged.width;
          delete merged.height;
          delete merged.dimension;
          delete merged.zoom;
          delete merged.isMaximized;
          delete merged.isFullScreen;
        } else {
          this.applyWindowStateGeometry(merged, windowState);
        }
        return merged;
      }
      const res = {
        ...currentWindow,
        children: [this.normalizeFloatingLayout(saved)]
      };
      if (includeGeometry === false) {
        delete res.x;
        delete res.y;
        delete res.width;
        delete res.height;
        delete res.dimension;
        delete res.zoom;
        delete res.isMaximized;
        delete res.isFullScreen;
      } else {
        this.applyWindowStateGeometry(res, windowState);
      }
      return res;
    }
    if (includeGeometry === false && saved?.type === "window") {
      delete saved.x;
      delete saved.y;
      delete saved.width;
      delete saved.height;
      delete saved.dimension;
      delete saved.zoom;
      delete saved.isMaximized;
      delete saved.isFullScreen;
    } else if (saved?.type === "window") {
      this.applyWindowStateGeometry(saved, windowState);
    }
    return saved.type === "window" ? saved : this.normalizeFloatingLayout(saved);
  }
  normalizeFloatingLayout(layout) {
    if (!layout) return layout;
    if (layout.type === "leaf") {
      return {
        type: "split",
        id: layout.id || this.generateId(),
        direction: "vertical",
        children: [{
          type: "tabs",
          id: this.generateId(),
          children: [JSON.parse(JSON.stringify(layout))]
        }]
      };
    }
    if (layout.type === "tabs") {
      return {
        ...layout,
        children: Array.isArray(layout.children) ? layout.children.map((child) => JSON.parse(JSON.stringify(child))) : []
      };
    }
    if (layout.type === "window" || layout.type === "floating") {
      return {
        ...layout,
        children: Array.isArray(layout.children) ? layout.children.map((child) => this.normalizeFloatingLayout(child)) : []
      };
    }
    if (layout.type === "split") {
      return {
        ...layout,
        children: Array.isArray(layout.children) ? layout.children.map((child) => {
          if (child?.type === "leaf") {
            return {
              type: "tabs",
              id: this.generateId(),
              children: [JSON.parse(JSON.stringify(child))]
            };
          }
          return this.normalizeFloatingLayout(child);
        }) : []
      };
    }
    return JSON.parse(JSON.stringify(layout));
  }
  /**
   * 檢查 floating 視窗是否包含指定的 leaf
   */
  floatingWindowContainsLeaf(layout, leafId) {
    if (!layout) return false;
    if (layout.type === "leaf") {
      return layout.id === leafId;
    } else if (Array.isArray(layout.children)) {
      for (let child of layout.children) {
        if (this.floatingWindowContainsLeaf(child, leafId)) {
          return true;
        }
      }
    }
    return false;
  }
  /**
   * 從佈局數據中提取 leaf 信息
   */
  extractLeavesFromLayout(layout, leaves = []) {
    if (!layout) return leaves;
    if (layout.type === "leaf") {
      leaves.push({
        id: layout.id || this.generateId(),
        type: layout.state?.type || "unknown",
        state: layout.state?.state || {},
        pinned: layout.pinned === true || layout.state?.pinned === true
      });
    } else if (Array.isArray(layout.children)) {
      layout.children.forEach((child) => {
        this.extractLeavesFromLayout(child, leaves);
      });
    }
    return leaves;
  }
  /**
   * 嘗試從 FolderSpaces View 提取資料夾名稱 (最高優先級)
   */
  getFolderSpaceNameFromLeaf(leaf) {
    if (!leaf) return null;
    const typeStr = leaf.type ? String(leaf.type).toLowerCase() : "";
    const isFolderSpaceView = typeStr === "folder-space-explorer" || typeStr === "folder-space" || typeStr === "folder-spaces" || typeStr.includes("folder-space") || typeStr.includes("folderspace");
    const stateObj = leaf.state;
    if (!stateObj && !isFolderSpaceView) return null;
    let rawFolderPath = null;
    if (typeof stateObj?.folder === "string") {
      rawFolderPath = stateObj.folder;
    } else if (typeof stateObj?.folderPath === "string") {
      rawFolderPath = stateObj.folderPath;
    } else if (typeof stateObj?.path === "string" && (isFolderSpaceView || !stateObj.file)) {
      rawFolderPath = stateObj.path;
    } else if (stateObj?.state && typeof stateObj.state === "object") {
      const innerState = stateObj.state;
      if (typeof innerState.folder === "string") {
        rawFolderPath = innerState.folder;
      } else if (typeof innerState.folderPath === "string") {
        rawFolderPath = innerState.folderPath;
      } else if (typeof innerState.path === "string" && (isFolderSpaceView || !innerState.file)) {
        rawFolderPath = innerState.path;
      }
    }
    if (!rawFolderPath && !isFolderSpaceView) return null;
    if (!rawFolderPath || rawFolderPath === "/" || rawFolderPath === ".") {
      return this.app.vault.getName();
    }
    const normalized = rawFolderPath.replace(/\\/g, "/").replace(/\/+$/, "");
    const lastFolder = normalized.split("/").pop() || normalized;
    return lastFolder.trim() || this.app.vault.getName();
  }
  /**
   * 根據 Folder Space Explorer、Pinned 檔案、Active 檔案與數量自動產生智慧佈局名稱 (UX-006)
   */
  generateSmartLayoutName(layout) {
    const leaves = this.getSavedViewStates(layout);
    for (const leaf of leaves) {
      const folderSpaceName = this.getFolderSpaceNameFromLeaf(leaf);
      if (folderSpaceName) {
        return folderSpaceName;
      }
    }
    const activeFile = layout.workspace?.activeFile;
    const pinnedFileNames = [];
    const activeFileName = [];
    const otherFileNames = [];
    const getBaseName = (path) => {
      const name = path.split("/").pop() || path;
      return name.endsWith(".md") ? name.slice(0, -3) : name;
    };
    leaves.forEach((leaf) => {
      const filePath = this.getFilePathFromLeafState(leaf);
      if (!filePath) return;
      const baseName = getBaseName(filePath);
      if (leaf.pinned) {
        if (!pinnedFileNames.includes(baseName)) {
          pinnedFileNames.push(baseName);
        }
      } else if (activeFile && filePath === activeFile) {
        if (!activeFileName.includes(baseName)) {
          activeFileName.push(baseName);
        }
      } else {
        if (!otherFileNames.includes(baseName)) {
          otherFileNames.push(baseName);
        }
      }
    });
    const orderedNames = [];
    pinnedFileNames.forEach((n) => {
      if (!orderedNames.includes(n)) orderedNames.push(n);
    });
    activeFileName.forEach((n) => {
      if (!orderedNames.includes(n)) orderedNames.push(n);
    });
    otherFileNames.forEach((n) => {
      if (!orderedNames.includes(n)) orderedNames.push(n);
    });
    if (orderedNames.length === 0) {
      const now = new Date(layout.timestamp || Date.now());
      const i18n = getI18n();
      return `${t("saveModal.title")} ${i18n.formatDate(now)}`;
    }
    let generatedName = "";
    if (orderedNames.length === 1) {
      generatedName = orderedNames[0];
    } else if (orderedNames.length === 2) {
      generatedName = `${orderedNames[0]} & ${orderedNames[1]}`;
    } else {
      generatedName = `${orderedNames[0]}, ${orderedNames[1]} ${t("saveModal.andOthers")}`;
    }
    if (generatedName.length > 55) {
      generatedName = generatedName.slice(0, 52) + "...";
    }
    return generatedName;
  }
  /**
   * 取得保存的所有 leaf 狀態。以 layout tree 為主，並補上舊版本資料
   * 中可能存在但未被寫入 layout tree 的 workspace.leaves。
   */
  getSavedViewStates(layout) {
    const fromLayout = this.extractLeavesFromLayout(layout.workspace?.layout);
    const explicitLeaves = Array.isArray(layout.workspace?.leaves) ? layout.workspace.leaves : [];
    const explicitById = new Map(explicitLeaves.map((leaf) => [leaf.id, leaf]));
    const result = fromLayout.map((leaf) => {
      const explicit = explicitById.get(leaf.id);
      if (!explicit) return leaf;
      return {
        ...leaf,
        type: explicit.type || leaf.type,
        state: { ...leaf.state, ...explicit.state }
      };
    });
    const resultIds = new Set(result.map((leaf) => leaf.id));
    explicitLeaves.forEach((leaf) => {
      if (!resultIds.has(leaf.id)) result.push(leaf);
    });
    return result;
  }
  /**
   * 計算開啟的文件數量
   */
  countOpenFiles(leaves) {
    return leaves.filter((leaf) => leaf.state.file).length;
  }
  getFilePathFromLeafState(leafState) {
    if (!leafState) return null;
    if (typeof leafState === "string") return leafState;
    const stateObj = leafState;
    if (typeof stateObj.state?.file === "string") return stateObj.state.file;
    if (typeof stateObj.state?.state?.file === "string") return stateObj.state.state.file;
    if (typeof stateObj.file === "string") return stateObj.file;
    return null;
  }
  /**
   * 恢復檔案狀態 (在對應現有分頁中安全開立檔案，尊重原生 Layout)
   */
  async restoreFileStatesForWindow(targetWin, leaves, activeFilePath, skipUnchanged = false) {
    const currentWin = targetWin || (typeof activeWindow !== "undefined" ? activeWindow : window);
    const windowLeaves = await this.waitForWindowLeaves(currentWin, leaves.length);
    const leavesById = /* @__PURE__ */ new Map();
    windowLeaves.forEach((leaf) => {
      const id = leaf.id;
      if (id) leavesById.set(id, leaf);
    });
    const missingFiles = [];
    let targetActiveLeaf = null;
    const totalFiles = leaves.filter((l) => !!this.getFilePathFromLeafState(l)).length;
    let progressNotice = null;
    if (totalFiles > 1 && this.plugin.settings.showNotifications !== false) {
      progressNotice = new import_obsidian3.Notice(`\u{1F504} ${t("restoreModal.restoringLayout")}... (0/${totalFiles})`, 0);
    }
    let processedCount = 0;
    const activeLeafForWindow = this.getActiveLeafForCurrentWindow(currentWin);
    for (let i = 0; i < leaves.length; i++) {
      const leafState = leaves[i];
      const filePath = this.getFilePathFromLeafState(leafState);
      if (!filePath) {
        const targetLeaf2 = leavesById.get(leafState.id) || (i < windowLeaves.length ? windowLeaves[i] : null);
        if (targetLeaf2) {
          const extLeaf = targetLeaf2;
          const currentState = typeof extLeaf.getViewState === "function" ? extLeaf.getViewState() : null;
          const stateMatches = currentState?.type === leafState.type && JSON.stringify(currentState.state || {}) === JSON.stringify(leafState.state || {});
          if (skipUnchanged && !stateMatches) {
            try {
              await extLeaf.setViewState({
                type: leafState.type,
                active: false,
                state: leafState.state || {}
              });
            } catch (error) {
              console.warn("Failed to reconcile non-file view state:", error);
            }
          }
          if (targetLeaf2 !== activeLeafForWindow) {
            this.ensureViewRenderedWithRetries(currentWin, targetLeaf2);
          }
        }
        continue;
      }
      processedCount++;
      if (progressNotice) {
        progressNotice.setMessage(`\u{1F504} ${t("restoreModal.restoringLayout")}... (${processedCount}/${totalFiles})`);
      }
      let targetLeaf = leavesById.get(leafState.id) || null;
      if (i < windowLeaves.length && !targetLeaf) targetLeaf = windowLeaves[i];
      if (!targetLeaf && windowLeaves.length > 0) {
        try {
          const baseLeaf = windowLeaves[windowLeaves.length - 1];
          targetLeaf = this.app.workspace.createLeafBySplit(baseLeaf, "vertical");
          if (targetLeaf) windowLeaves.push(targetLeaf);
        } catch (e) {
          console.warn("Failed to create leaf by split for target window:", e);
        }
      }
      const file = this.app.vault.getAbstractFileByPath(filePath);
      if (file instanceof import_obsidian3.TFile) {
        if (targetLeaf) {
          const stateObj = leafState.state;
          const viewMode = stateObj?.mode || stateObj?.state?.mode;
          const currentState = targetLeaf.getViewState?.();
          const currentFilePath = currentState ? this.getFilePathFromLeafState(currentState) : null;
          const currentStateObj = currentState?.state;
          const currentViewMode = currentStateObj?.mode || currentStateObj?.state?.mode;
          if (skipUnchanged && currentFilePath === filePath && (!viewMode || currentViewMode === viewMode)) {
            if (activeFilePath && filePath === activeFilePath) {
              targetActiveLeaf = targetLeaf;
            }
            continue;
          }
          const openOptions = { active: false };
          if (viewMode) {
            openOptions.state = { mode: viewMode };
          }
          await targetLeaf.openFile(file, openOptions);
          if (activeFilePath && filePath === activeFilePath) {
            targetActiveLeaf = targetLeaf;
          }
        }
      } else {
        const fileName = filePath.split("/").pop() || filePath;
        missingFiles.push(fileName);
        if (targetLeaf && typeof targetLeaf.setViewState === "function") {
          try {
            await targetLeaf.setViewState({ type: "empty" });
          } catch (e) {
            console.warn("Failed to set empty view state:", e);
          }
        }
      }
    }
    if (progressNotice) {
      progressNotice.hide();
    }
    const leafToFocus = targetActiveLeaf || null;
    if (leafToFocus) {
      try {
        await this.app.workspace.revealLeaf(leafToFocus);
        this.app.workspace.setActiveLeaf(leafToFocus, { focus: true });
        if (leafToFocus?.containerEl && typeof leafToFocus.containerEl.focus === "function") {
          leafToFocus.containerEl.focus();
        }
      } catch (e) {
        console.warn("Failed to set active leaf:", e);
      }
    } else {
      const currentActive = typeof this.app.workspace.getMostRecentLeaf === "function" ? this.app.workspace.getMostRecentLeaf() : this.app.workspace.activeLeaf;
      if (!currentActive || !windowLeaves.includes(currentActive)) {
        const fallbackLeaf = windowLeaves[0] || null;
        if (fallbackLeaf) {
          try {
            await this.app.workspace.revealLeaf(fallbackLeaf);
            this.app.workspace.setActiveLeaf(fallbackLeaf, { focus: true });
          } catch (e) {
            console.warn("Failed to set active leaf:", e);
          }
        }
      }
    }
    return missingFiles;
  }
  /** 等待 changeLayout 完成 leaf 建立，避免只對第一個已建立的 leaf 開檔。 */
  async waitForWindowLeaves(targetWin, expectedCount) {
    let leaves = this.getLeavesForWindow(targetWin);
    for (let attempt = 0; attempt < 20 && leaves.length < expectedCount; attempt++) {
      await new Promise((resolve) => window.setTimeout(resolve, 50));
      leaves = this.getLeavesForWindow(targetWin);
    }
    return leaves;
  }
  /** 取得 leaf 所屬的 DOM Window。 */
  getWindowForLeaf(leaf) {
    return leaf?.containerEl?.ownerDocument?.defaultView || null;
  }
  /** 根據保存的 leaf 集合辨識還原後的目標視窗。 */
  findWindowForSavedLeaves(leaves, excludedWindow, preferredWindow, claimedWindows = /* @__PURE__ */ new Set(), requirePositiveScore = false) {
    if (leaves.length === 0) return null;
    const savedIds = new Set(leaves.map((leaf) => leaf.id));
    const savedFiles = new Set(
      leaves.map((leaf) => this.getFilePathFromLeafState(leaf)).filter((filePath) => !!filePath)
    );
    const windows = /* @__PURE__ */ new Map();
    const idMatches = /* @__PURE__ */ new Map();
    const matchedSavedFiles = /* @__PURE__ */ new Map();
    const observedPanelIds = /* @__PURE__ */ new Map();
    const matchedPanelIds = /* @__PURE__ */ new Map();
    const savedPanelIds = new Set(
      leaves.map((leaf) => leaf.state?.panelId).filter((panelId) => typeof panelId === "string" && panelId.length > 0)
    );
    let bestWindow = null;
    let bestScore = 0;
    this.app.workspace.iterateAllLeaves((leaf) => {
      const targetWindow = this.getWindowForLeaf(leaf);
      if (!targetWindow || targetWindow === excludedWindow || claimedWindows.has(targetWindow) || !this.isPopoutDocument(targetWindow.document)) return;
      const viewState = typeof leaf.getViewState === "function" ? leaf.getViewState() : null;
      const filePath = this.getFilePathFromLeafState({
        state: viewState?.state || {}
      });
      const panelId = viewState?.state?.panelId;
      if (typeof panelId === "string" && panelId) {
        const observed = observedPanelIds.get(targetWindow) || /* @__PURE__ */ new Set();
        observed.add(panelId);
        observedPanelIds.set(targetWindow, observed);
        if (savedPanelIds.has(panelId)) {
          matchedPanelIds.set(targetWindow, (matchedPanelIds.get(targetWindow) || 0) + 1);
        }
      }
      const idHit = savedIds.has(leaf.id) ? 1 : 0;
      const fileHit = filePath && savedFiles.has(filePath) ? 1 : 0;
      const score = (windows.get(targetWindow) || 0) + (idHit ? 100 : 0) + (fileHit ? 10 : 0) + (typeof panelId === "string" && savedPanelIds.has(panelId) ? 300 : 0);
      windows.set(targetWindow, score);
      idMatches.set(targetWindow, (idMatches.get(targetWindow) || 0) + idHit);
      if (fileHit) {
        const set = matchedSavedFiles.get(targetWindow) || /* @__PURE__ */ new Set();
        set.add(filePath);
        matchedSavedFiles.set(targetWindow, set);
      }
      if (score > bestScore) {
        bestScore = score;
        bestWindow = targetWindow;
      }
    });
    if (preferredWindow && windows.has(preferredWindow)) {
      return preferredWindow;
    }
    if (bestWindow && savedPanelIds.size > 0) {
      const observed = observedPanelIds.get(bestWindow);
      const panelMatchCount = matchedPanelIds.get(bestWindow) || 0;
      if (observed && observed.size > 0 && panelMatchCount === 0) {
        bestWindow = null;
        bestScore = 0;
        for (const [candidate, score] of windows) {
          const candidateObserved = observedPanelIds.get(candidate);
          const candidatePanelMatches = matchedPanelIds.get(candidate) || 0;
          if (candidateObserved && candidateObserved.size > 0 && candidatePanelMatches === 0) continue;
          if (score > bestScore) {
            bestWindow = candidate;
            bestScore = score;
          }
        }
      }
    }
    if (bestWindow) {
      if (requirePositiveScore && (idMatches.get(bestWindow) || 0) === 0) {
        const coverage = savedFiles.size > 0 ? (matchedSavedFiles.get(bestWindow)?.size || 0) / savedFiles.size : 0;
        if (coverage < 0.5) return null;
      }
      return bestWindow;
    }
    if (requirePositiveScore) return null;
    return windows.size === 1 ? Array.from(windows.keys())[0] : null;
  }
  /**
   * 將 restore 前保存的所有 layout 名稱與視窗幾何座標，一對一重新套用至 restore 後的
   * live popout。避免跨螢幕/異質 DPI 下 changeLayout 導至既有視窗尺寸微幅縮小。
   */
  restorePreservedWindowLabels(snapshots, restoredTargetWindow) {
    const claimedWindows = /* @__PURE__ */ new Set();
    if (restoredTargetWindow) claimedWindows.add(restoredTargetWindow);
    snapshots.forEach((snapshot) => {
      const currentWindow = this.findWindowForSavedLeaves(
        snapshot.leaves,
        restoredTargetWindow || void 0,
        snapshot.window,
        claimedWindows
      );
      if (!currentWindow) return;
      this.setLayoutLabelForWindow(currentWindow, snapshot.layoutName);
      if (snapshot.windowState) {
        this.restoreWindowGeometry(currentWindow, snapshot.windowState, true, true);
      }
      claimedWindows.add(currentWindow);
    });
  }
  /** 在 changeLayout 重建 popout 後恢復實際視窗尺寸與座標（僅在幾何有顯著差異或強制時呼叫，防止重複無謂位移與 OS 邊框重算）。 */
  restoreWindowGeometry(targetWin, windowState, includeGeometry = true, force = true) {
    if (!windowState || includeGeometry === false || !targetWin) return;
    const size = windowState.size;
    if (size && size.width > 0 && size.height > 0 && typeof targetWin.resizeTo === "function") {
      const currentWidth = typeof targetWin.outerWidth === "number" ? targetWin.outerWidth : 0;
      const currentHeight = typeof targetWin.outerHeight === "number" ? targetWin.outerHeight : 0;
      if (force || currentWidth === 0 || Math.abs(currentWidth - size.width) > WINDOW_GEOMETRY_TOLERANCE || Math.abs(currentHeight - size.height) > WINDOW_GEOMETRY_TOLERANCE) {
        targetWin.resizeTo(size.width, size.height);
      }
    }
    if (windowState.position && typeof targetWin.moveTo === "function") {
      const currentX = typeof targetWin.screenX === "number" ? targetWin.screenX : 0;
      const currentY = typeof targetWin.screenY === "number" ? targetWin.screenY : 0;
      if (force || currentX === 0 || Math.abs(currentX - windowState.position.x) > WINDOW_GEOMETRY_TOLERANCE || Math.abs(currentY - windowState.position.y) > WINDOW_GEOMETRY_TOLERANCE) {
        targetWin.moveTo(windowState.position.x, windowState.position.y);
      }
    }
  }
  /**
   * 當發動 changeLayout 時，將非 Restore 目標之活體 Popout 視窗最新實際 outerWidth/outerHeight/screenX/screenY 帶入 floating schema，
   * 避免多螢幕異質 DPI 縮放率下呼叫全域 changeLayout 時，其他存活 Popout 視窗尺寸被重複縮放衰減。
   */
  syncLiveWindowBoundsToFloatingChild(child, liveWin) {
    if (!child || typeof child !== "object" || !liveWin || liveWin.closed) return child;
    try {
      const cloned = { ...child };
      if (typeof liveWin.outerWidth === "number" && liveWin.outerWidth > 0) {
        cloned.width = liveWin.outerWidth;
      }
      if (typeof liveWin.outerHeight === "number" && liveWin.outerHeight > 0) {
        cloned.height = liveWin.outerHeight;
      }
      if (typeof liveWin.screenX === "number") {
        cloned.x = liveWin.screenX;
      }
      if (typeof liveWin.screenY === "number") {
        cloned.y = liveWin.screenY;
      }
      return cloned;
    } catch {
      return child;
    }
  }
  /** changeLayout 重建期間 DOM 尚未穩定，單次安全套用並於 200ms 做單次保險檢查。 */
  applyHiddenStateAfterRestore(targetWin, hidden) {
    const engine = this.plugin.popoutLayout;
    const apply = () => {
      try {
        engine.applyHiddenState(targetWin, hidden);
      } catch {
      }
    };
    apply();
    if (typeof targetWin.setTimeout === "function") {
      targetWin.setTimeout(apply, 200);
    }
  }
  /** 是否為檔案類 view（markdown / pdf / 圖片等），此類 view 不參與強制渲染。 */
  isFileView(leaf) {
    return !!leaf && !!leaf.view?.file;
  }
  /** 檢查 leaf 是否已渲染出實際內容。 */
  hasRenderedContent(leaf) {
    if (!leaf) return false;
    const leafEl = leaf.containerEl || leaf.view?.containerEl;
    if (!(leafEl instanceof HTMLElement)) return false;
    const content = leafEl.querySelector(".view-content");
    if (content) return content.children.length > 0;
    const navFiles = leafEl.querySelector(".nav-files-container");
    if (navFiles) return navFiles.children.length > 0;
    return false;
  }
  /** 強制重新渲染 leaf 的 view（重建視圖，重新執行 onOpen）。 */
  forceRenderView(leaf) {
    const extLeaf = leaf;
    if (typeof extLeaf.rebuildView === "function") {
      try {
        extLeaf.rebuildView();
        return;
      } catch (e) {
        console.warn("rebuildView failed, falling back to setViewState:", e);
      }
    }
    const current = typeof extLeaf.getViewState === "function" ? extLeaf.getViewState() : null;
    const type = current?.type;
    if (!type) return;
    void (async () => {
      try {
        await extLeaf.setViewState({ type: "empty", active: false, state: {} });
        await extLeaf.setViewState({ type, active: false, state: current.state || {} });
      } catch (e) {
        console.warn("Failed to force render view:", e);
      }
    })();
  }
  /**
   * 單次檢查：先載入 deferred view，再於 DOM 仍為空時最多重建一次。
   * 多個 lifecycle retry 不得因 DOM 尚未及時更新而連續重建同一個 leaf。
   */
  ensureViewRendered(leaf) {
    if (!leaf || this.isFileView(leaf)) return;
    if (this.hasRenderedContent(leaf)) {
      this.renderAttemptedLeaves.delete(leaf);
      return;
    }
    if (leaf.isDeferred) {
      if (this.deferredViewLoads.has(leaf)) return;
      const pendingLoad = leaf.loadIfDeferred().then(() => {
        this.deferredViewLoads.delete(leaf);
        if (!this.hasRenderedContent(leaf)) this.rebuildViewIfNeeded(leaf);
      }).catch(() => {
        this.deferredViewLoads.delete(leaf);
        this.rebuildViewIfNeeded(leaf);
      });
      this.deferredViewLoads.set(leaf, pendingLoad);
      return;
    }
    this.rebuildViewIfNeeded(leaf);
  }
  rebuildViewIfNeeded(leaf) {
    if (this.hasRenderedContent(leaf) || this.renderAttemptedLeaves.has(leaf)) return;
    this.renderAttemptedLeaves.add(leaf);
    this.forceRenderView(leaf);
  }
  /**
   * 在 Popout tab 切換完成後延遲檢查 DOM，避免 active-leaf-change 的
   * mousedown/mouseup 階段重建 view 而吞掉原生 click 事件。
   */
  scheduleViewRenderAfterActivation(leaf, targetWin) {
    if (!leaf || !targetWin || targetWin.closed) return;
    targetWin.setTimeout(() => {
      if (targetWin.closed) return;
      const container = leaf.containerEl;
      if (!container?.isConnected) return;
      this.ensureViewRendered(leaf);
    }, 150);
  }
  /** 以受管控方式確保非檔案 view 已渲染，所有重試皆通過 per-leaf guard 防止重複 DOM 重建。 */
  ensureViewRenderedWithRetries(targetWin, leaf) {
    if (!leaf || !targetWin || targetWin.closed) return;
    this.ensureViewRendered(leaf);
    if (typeof targetWin.setTimeout === "function") {
      targetWin.setTimeout(() => {
        if (targetWin.closed) return;
        this.ensureViewRendered(leaf);
      }, 200);
    }
  }
  /**
   * 視窗已顯示後，對仍無內容的 deferred view（file-explorer / search 等）
   * 強制 loadIfDeferred——此時 containerEl 可見，setViewState 會建立真實
   * view（而非 DeferredView），內容即可渲染。300ms 後再確認一次，涵蓋
   * 視窗建立初期 onOpen 的非同步渲染延遲。
   */
  ensureDeferredViewsLoaded(targetWin) {
    if (!targetWin || targetWin.closed) return;
    const attempt = async () => {
      if (targetWin.closed) return;
      const leaves = this.getLeavesForWindow(targetWin);
      for (const leaf of leaves) {
        if (this.isFileView(leaf) || this.hasRenderedContent(leaf)) continue;
        const extLeaf = leaf;
        if (extLeaf.isDeferred && typeof extLeaf.loadIfDeferred === "function") {
          try {
            await extLeaf.loadIfDeferred();
          } catch {
          }
          continue;
        }
        this.renderAttemptedLeaves.delete(leaf);
        if (typeof extLeaf.rebuildView === "function") {
          try {
            await extLeaf.rebuildView();
          } catch {
          }
        } else {
          this.rebuildViewIfNeeded(leaf);
        }
      }
    };
    void attempt();
    if (typeof targetWin.setTimeout === "function") {
      [300, 1200, 2500].forEach((ms) => targetWin.setTimeout(() => void attempt(), ms));
    }
    const doc = targetWin.document;
    if (doc && typeof doc.addEventListener === "function") {
      doc.addEventListener(
        "visibilitychange",
        () => {
          if (targetWin.closed) return;
          if (doc.visibilityState === "visible") void attempt();
        },
        { once: true }
      );
    }
  }
  /**
   * 根據 ID 查找 leaf
   */
  findLeafById(id) {
    if (!id) return null;
    let targetLeaf = null;
    this.app.workspace.iterateAllLeaves((leaf) => {
      if (!targetLeaf && leaf.id === id) {
        targetLeaf = leaf;
      }
    });
    return targetLeaf;
  }
  /**
   * 調整視窗位置
   */
  adjustWindowPosition(position) {
    try {
      if (window.moveTo) {
        window.moveTo(position.x, position.y);
      }
    } catch (error) {
      console.warn("Failed to adjust window position:", error);
    }
  }
  /**
   * 驗證佈局數據
   */
  validateLayout(layout) {
    return !!(layout && layout.id && layout.name && layout.workspace && layout.workspace.layout);
  }
  /**
   * 生成唯一 ID
   */
  generateId() {
    return Date.now().toString(36) + Math.random().toString(36).substring(2);
  }
};

// src/modals/saveModal.ts
var import_obsidian5 = require("obsidian");
var obsidian2 = __toESM(require("obsidian"), 1);

// src/settings.ts
var import_obsidian4 = require("obsidian");
var obsidian = __toESM(require("obsidian"), 1);

// src/settingsActivityBar.ts
var ACTIVITY_BAR_DRAG_DATA_TYPE = "application/x-window-spaces-view-type";
function isActivityBarItem(value) {
  return !!value && typeof value.viewType === "string" && value.viewType.trim().length > 0;
}
function reorderActivityBarItems(items, draggedViewType, targetViewType, placeAfter) {
  const fromIndex = items.findIndex((item) => isActivityBarItem(item) && item.viewType === draggedViewType);
  const targetIndex = items.findIndex((item) => isActivityBarItem(item) && item.viewType === targetViewType);
  if (fromIndex < 0 || targetIndex < 0 || fromIndex === targetIndex) return null;
  const reordered = [...items];
  const [moved] = reordered.splice(fromIndex, 1);
  if (!moved) return null;
  const adjustedTargetIndex = targetIndex > fromIndex ? targetIndex - 1 : targetIndex;
  const insertIndex = placeAfter ? adjustedTargetIndex + 1 : adjustedTargetIndex;
  reordered.splice(insertIndex, 0, moved);
  return reordered;
}
function canRemoveActivityBarItem(items, availableItems) {
  if (items.length !== 1) return true;
  const currentType = items[0]?.viewType;
  return !availableItems.some((item) => item.viewType !== currentType);
}

// src/settings.ts
function isActivityBarItem2(value) {
  return !!value && typeof value.viewType === "string" && value.viewType.trim().length > 0;
}
var SettingGroupCtor = obsidian.SettingGroup;
var IconPickerModal = class extends import_obsidian4.Modal {
  onSelect;
  constructor(app, onSelect) {
    super(app);
    this.onSelect = onSelect;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.addClass("window-spaces-icon-picker");
    contentEl.createEl("h3", { text: t("settings.pickIcon") });
    const grid = contentEl.createDiv({ cls: "window-spaces-icon-grid" });
    ICON_CHOICES.forEach((iconName) => {
      const btn = grid.createEl("button", {
        cls: "clickable-icon",
        attr: { type: "button", title: iconName }
      });
      (0, import_obsidian4.setIcon)(btn, iconName);
      btn.onclick = () => {
        this.onSelect(iconName);
        this.close();
      };
    });
  }
  onClose() {
    this.contentEl.empty();
  }
};
var WindowSpacesSettingTab = class extends import_obsidian4.PluginSettingTab {
  plugin;
  autoSaveTimeout = null;
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  getSettingDefinitions() {
    return [];
  }
  /** 建立 SettingGroup（若當前 Obsidian 版本支援）；不支援則回傳 null。 */
  createGroup(containerEl) {
    if (SettingGroupCtor) {
      try {
        return new SettingGroupCtor(containerEl);
      } catch {
        return null;
      }
    }
    return null;
  }
  /** 在 SettingGroup 或 HTMLElement 容器中建立並設定一個 Setting。 */
  createSettingIn(container, configure) {
    const group = container;
    if (group && typeof group.addSetting === "function") {
      let result = null;
      group.addSetting((setting2) => {
        result = setting2;
        configure(setting2);
      });
      return result;
    }
    const setting = new import_obsidian4.Setting(container);
    configure(setting);
    return setting;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    new import_obsidian4.Setting(containerEl).setName(t("settings.generalSection")).setHeading();
    const generalGroup = this.createGroup(containerEl) ?? containerEl;
    this.createSettingIn(generalGroup, (s) => {
      s.setName(t("settings.showNotifications")).setDesc(t("settings.showNotificationsDesc"));
      s.addToggle((toggle) => {
        toggle.setValue(this.plugin.settings.showNotifications !== false);
        toggle.onChange(async (value) => {
          this.plugin.settings.showNotifications = value;
          await this.plugin.saveSettings();
        });
      });
    });
    this.createSettingIn(generalGroup, (s) => {
      s.setName(t("settings.showWindowLayoutsRibbonIcon")).setDesc(t("settings.showWindowLayoutsRibbonIconDesc"));
      s.addToggle((toggle) => {
        toggle.setValue(this.plugin.settings.showWindowLayoutsRibbonIcon !== false);
        toggle.onChange(async (value) => {
          this.plugin.settings.showWindowLayoutsRibbonIcon = value;
          await this.plugin.saveSettings();
          this.plugin.refreshRibbonIcons();
        });
      });
    });
    this.createSettingIn(generalGroup, (s) => {
      s.setName(t("settings.showLayoutStatusBar")).setDesc(t("settings.showLayoutStatusBarDesc"));
      s.addToggle((toggle) => {
        toggle.setValue(this.plugin.settings.showLayoutStatusBar === true);
        toggle.onChange(async (value) => {
          this.plugin.settings.showLayoutStatusBar = value;
          await this.plugin.saveSettings();
          this.plugin.manager.refreshLayoutLabels();
        });
      });
    });
    this.createSettingIn(generalGroup, (s) => {
      s.setName(t("settings.popoutAccentsEnable")).setDesc(t("settings.popoutAccentsEnableDesc"));
      s.addToggle((toggle) => {
        toggle.setValue(this.plugin.settings.popoutAccents?.enabled !== false);
        toggle.onChange(async (value) => {
          if (!this.plugin.settings.popoutAccents) {
            this.plugin.settings.popoutAccents = { enabled: value, splitter: true, activityBar: true };
          } else {
            this.plugin.settings.popoutAccents.enabled = value;
          }
          await this.plugin.saveSettings();
          this.plugin.activityBars.refreshAll();
          this.display();
        });
      });
    });
    if (this.plugin.settings.popoutAccents?.enabled !== false) {
      this.createSettingIn(generalGroup, (s) => {
        s.setName(t("settings.popoutAccentsSplitter")).setDesc(t("settings.popoutAccentsSplitterDesc"));
        s.addToggle((toggle) => {
          toggle.setValue(this.plugin.settings.popoutAccents?.splitter !== false);
          toggle.onChange(async (value) => {
            if (!this.plugin.settings.popoutAccents) {
              this.plugin.settings.popoutAccents = { enabled: true, splitter: value, activityBar: true };
            } else {
              this.plugin.settings.popoutAccents.splitter = value;
            }
            await this.plugin.saveSettings();
            this.plugin.activityBars.refreshAll();
          });
        });
      });
      this.createSettingIn(generalGroup, (s) => {
        s.setName(t("settings.popoutAccentsActivityBar")).setDesc(t("settings.popoutAccentsActivityBarDesc"));
        s.addToggle((toggle) => {
          toggle.setValue(this.plugin.settings.popoutAccents?.activityBar !== false);
          toggle.onChange(async (value) => {
            if (!this.plugin.settings.popoutAccents) {
              this.plugin.settings.popoutAccents = { enabled: true, splitter: true, activityBar: value };
            } else {
              this.plugin.settings.popoutAccents.activityBar = value;
            }
            await this.plugin.saveSettings();
            this.plugin.activityBars.refreshAll();
          });
        });
      });
    }
    this.createSettingIn(generalGroup, (s) => {
      s.setName(t("settings.enableInterceptor")).setDesc(t("settings.enableInterceptorDesc"));
      s.addToggle((toggle) => {
        toggle.setValue(this.plugin.settings.workspaceInterceptorEnabled !== false);
        toggle.onChange(async (value) => {
          this.plugin.settings.workspaceInterceptorEnabled = value;
          this.plugin.workspaceInterceptor.enabled = value;
          await this.plugin.saveSettings();
        });
      });
    });
    new import_obsidian4.Setting(containerEl).setName(t("settings.popoutDefaultsSection")).setHeading();
    const defaultsGroup = this.createGroup(containerEl) ?? containerEl;
    this.createSettingIn(defaultsGroup, (s) => {
      s.setName(t("settings.autoSaveEnabled")).setDesc(t("settings.autoSaveDescription"));
      s.addToggle((toggle) => {
        toggle.setValue(this.plugin.settings.autoSave === true);
        toggle.onChange(async (value) => {
          this.plugin.settings.autoSave = value;
          await this.plugin.saveSettings();
          if (value) {
            this.setupAutoSave();
          } else {
            this.removeAutoSave();
          }
        });
      });
    });
    this.createSettingIn(defaultsGroup, (s) => {
      s.setName(t("settings.defaultIcon")).setDesc(t("settings.defaultIconDesc"));
      s.controlEl.addClass("window-space-icon-setting-control");
      let currentIcon = this.plugin.settings.defaultIcon || DEFAULT_SPACE_ICON;
      let iconInputEl;
      s.addText((text) => {
        iconInputEl = text.inputEl;
        text.setPlaceholder(t("saveModal.iconPlaceholder"));
        text.setValue(currentIcon);
        text.onChange(async (val) => {
          currentIcon = val.trim() || DEFAULT_SPACE_ICON;
          this.plugin.settings.defaultIcon = currentIcon;
          await this.plugin.saveSettings();
          updatePreview();
          this.plugin.activityBars.refreshAll();
        });
      });
      const pickIconBtn = s.controlEl.createEl("button", {
        cls: "clickable-icon",
        attr: { type: "button", title: t("settings.pickIcon") }
      });
      (0, import_obsidian4.setIcon)(pickIconBtn, "image");
      pickIconBtn.onclick = () => {
        new IconPickerModal(this.app, async (selected) => {
          currentIcon = selected;
          iconInputEl.value = selected;
          this.plugin.settings.defaultIcon = selected;
          await this.plugin.saveSettings();
          updatePreview();
          this.plugin.activityBars.refreshAll();
        }).open();
      };
      const previewEl = s.controlEl.createDiv({ cls: "window-space-icon-preview" });
      const updatePreview = () => {
        previewEl.empty();
        const val = currentIcon || DEFAULT_SPACE_ICON;
        const isEmoji = isSpaceEmoji(val);
        if (isEmoji) {
          previewEl.createSpan({ text: val });
        } else {
          const iconDiv = previewEl.createDiv();
          if (!setIconWithCheck(iconDiv, val)) {
            (0, import_obsidian4.setIcon)(iconDiv, "layout");
          }
        }
      };
      updatePreview();
    });
    this.createSettingIn(defaultsGroup, (s) => {
      s.setName(t("settings.defaultBorderInset")).setDesc(t("settings.defaultBorderInsetDesc"));
      s.addSlider((slider) => {
        slider.setLimits(0, 5, 1).setValue(this.getDefaultBorderInset()).setDynamicTooltip().onChange(async (value) => {
          this.plugin.settings.defaultBorderInset = value;
          await this.plugin.saveSettings();
          this.plugin.activityBars.refreshAll();
        });
      });
    });
    this.createSettingIn(defaultsGroup, (s) => {
      s.setName(t("settings.defaultFoldedCorner")).setDesc(t("settings.defaultFoldedCornerDesc"));
      s.addToggle((toggle) => {
        toggle.setValue(this.plugin.settings.defaultShowFoldedCorner !== false);
        toggle.onChange(async (value) => {
          this.plugin.settings.defaultShowFoldedCorner = value;
          await this.plugin.saveSettings();
          this.plugin.activityBars.refreshAll();
        });
      });
    });
    this.renderActivityBarSide(containerEl, "left", t("settings.leftBar"));
    this.renderActivityBarSide(containerEl, "right", t("settings.rightBar"));
    new import_obsidian4.Setting(containerEl).setName(t("settings.resetSettings")).setHeading();
    const dangerGroup = this.createGroup(containerEl) ?? containerEl;
    this.createSettingIn(dangerGroup, (s) => {
      s.setName(t("settings.resetSettings")).setDesc(t("settings.resetSettingsDescription"));
      s.addButton((button) => {
        button.setButtonText(t("settings.resetButton")).setWarning().onClick(async () => {
          const confirmed = await this.showConfirmDialog(
            t("settings.resetConfirmMessage"),
            t("settings.resetConfirmTitle")
          );
          if (confirmed) {
            this.plugin.settings.spaces = [];
            await this.plugin.saveSettings();
            this.display();
            new import_obsidian4.Notice(t("settings.resetSuccess"));
          }
        });
      });
    });
  }
  getDefaultBorderInset() {
    const value = this.plugin.settings.defaultBorderInset;
    return typeof value === "number" && Number.isFinite(value) ? Math.max(0, Math.min(5, value)) : 1;
  }
  /** 渲染單一側欄 view 項目列，回傳用於 surgical 更新的 handle。 */
  renderSideItemRow(container, side, item, index, onChanged) {
    let iconBtn = null;
    const row = this.createSettingIn(container, (s) => {
      const resolvedLabel = resolveViewLabel(this.app, item.viewType);
      s.setName(item.label || resolvedLabel);
      s.addButton((button) => {
        iconBtn = button;
        applyItemIcon(button.buttonEl, this.app, item);
        button.setTooltip(t("settings.pickIcon"));
        button.onClick(() => {
          const modal = new IconPickerModal(this.app, (iconName) => {
            item.icon = iconName;
            void this.plugin.saveSettings().then(() => {
              this.plugin.activityBars.refreshAll();
              iconBtn?.setIcon(iconName);
            });
          });
          modal.open();
        });
      });
      s.addButton((button) => {
        button.setIcon("rotate-ccw").setTooltip(t("settings.restoreDefaultIcon"));
        button.onClick(() => {
          item.icon = void 0;
          void this.plugin.saveSettings().then(() => {
            this.plugin.activityBars.refreshAll();
            iconBtn?.setIcon(resolveViewIcon(this.app, item.viewType));
            void ensureViewIcon(this.app, item.viewType).then((icon) => {
              if (!icon || item.icon) return;
              iconBtn?.setIcon(icon);
            });
          });
        });
      });
      s.addButton((button) => {
        button.setButtonText(t("settings.removeView")).setWarning().onClick(() => {
          const current = this.plugin.settings.activityBars?.[side] ?? [];
          if (!canRemoveActivityBarItem(current, enumerateAvailableViews(this.app)[side])) {
            new import_obsidian4.Notice(t("settings.keepOneActivityBarView"));
            return;
          }
          const idx = current.indexOf(item);
          if (idx >= 0) {
            current.splice(idx, 1);
            this.plugin.settings.activityBars = this.plugin.settings.activityBars ?? { left: [], right: [] };
            this.plugin.settings.activityBars[side] = current;
          }
          void this.plugin.saveSettings().then(() => {
            this.plugin.activityBars.refreshAll();
            row.settingEl.remove();
            onChanged?.();
          });
        });
      });
    });
    row.settingEl.setAttr("data-window-spaces-activity-item", side);
    row.settingEl.setAttr("data-drag-index", String(index));
    row.settingEl.setAttr("data-drag-view-type", item.viewType);
    row.settingEl.setAttr("draggable", "true");
    row.settingEl.addEventListener("dragstart", (e) => {
      row.settingEl.classList.add("drag-source");
      if (!e.dataTransfer) return;
      e.dataTransfer.effectAllowed = "move";
      e.dataTransfer.setData(ACTIVITY_BAR_DRAG_DATA_TYPE, item.viewType);
    });
    row.settingEl.addEventListener("dragend", () => {
      row.settingEl.classList.remove("drag-source");
    });
    row.settingEl.querySelectorAll("button, input, select, .checkbox-container, .slider").forEach((el) => {
      el.setAttribute("draggable", "false");
    });
    const gripEl = row.settingEl.createDiv({ cls: "window-spaces-activity-drag-handle" });
    gripEl.setAttr("aria-label", t("settings.dragToReorder"));
    (0, import_obsidian4.setIcon)(gripEl, "grip-vertical");
    row.settingEl.insertBefore(gripEl, row.settingEl.firstChild);
    return { row, updateIcon: (icon) => iconBtn?.setIcon(icon) };
  }
  /** 重建「新增 view」下拉選單的選項（排除已加入的 view type）。 */
  rebuildViewSelect(selectEl, side) {
    selectEl.empty();
    const available = enumerateAvailableViews(this.app);
    const allTypes = sortViewTypesByLabel(this.app, Array.from(
      new Set([...available.left, ...available.right].map((item) => item.viewType))
    ));
    const current = this.plugin.settings.activityBars?.[side] ?? [];
    allTypes.forEach((viewType) => {
      if (current.some((item) => item.viewType === viewType)) return;
      const label = resolveViewLabel(this.app, viewType);
      const option = selectEl.createEl("option", {
        value: viewType,
        text: label
      });
      option.setAttr("data-icon", resolveViewIcon(this.app, viewType));
    });
  }
  renderActivityBarSide(section, side, heading) {
    new import_obsidian4.Setting(section).setName(heading).setHeading();
    const group = this.createGroup(section) ?? section;
    this.createSettingIn(group, (s) => {
      s.setName(t("settings.defaultActivityBarVisibility"));
      s.setDesc(t("settings.defaultActivityBarVisibilityDesc"));
      s.addToggle((toggle) => {
        toggle.setValue(this.plugin.settings.activityBarDefaults?.[side] !== false);
        toggle.onChange(async (value) => {
          this.plugin.settings.activityBarDefaults = this.plugin.settings.activityBarDefaults ?? { left: true, right: true };
          this.plugin.settings.activityBarDefaults[side] = value;
          await this.plugin.saveSettings();
          this.plugin.activityBars.refreshAll();
        });
      });
    });
    const items = this.plugin.settings.activityBars?.[side] ?? [];
    if (items.length === 0) {
      this.createSettingIn(group, (s) => s.setDesc(t("settings.addView")));
    }
    let selectEl;
    let customInput;
    const addRow = this.createSettingIn(group, (s) => {
      selectEl = s.controlEl.createEl("select", {
        cls: "dropdown"
      });
      customInput = s.controlEl.createEl("input", {
        type: "text",
        placeholder: t("settings.viewTypePlaceholder")
      });
      customInput.addClass("window-spaces-view-type-input");
      s.addButton((button) => {
        button.setButtonText(t("settings.addView")).onClick(() => {
          const selected = selectEl.value.trim();
          const custom = customInput.value.trim();
          const viewType = custom || selected;
          if (!viewType) return;
          const current = this.plugin.settings.activityBars?.[side] ?? [];
          if (current.some((item) => item.viewType === viewType)) return;
          const newItem = {
            viewType,
            side,
            label: void 0,
            // 見 saveModal.Add：不把 resolveViewIcon 的 fallback（"layout"）寫死——
            // 社群 view（notebook-navigator 等）的實 icon 需動態偵測，寫死會蓋過
            // 正確 icon；一律以動態（applyItemIcon）+ ensureViewIcon 補正。
            icon: void 0
          };
          current.push(newItem);
          this.plugin.settings.activityBars = this.plugin.settings.activityBars ?? { left: [], right: [] };
          this.plugin.settings.activityBars[side] = current;
          void this.plugin.saveSettings().then(() => {
            this.plugin.activityBars.refreshAll();
            renderItemRows();
            customInput.value = "";
            void ensureViewIcon(this.app, viewType).then((icon) => {
              if (!icon) return;
              newItem.icon = icon;
              renderItemRows();
              this.plugin.activityBars.refreshAll();
              void this.plugin.saveSettings();
            });
          });
        });
      });
    });
    const refreshSelect = () => this.rebuildViewSelect(selectEl, side);
    const renderItemRows = () => {
      const current = this.plugin.settings.activityBars?.[side];
      const items2 = Array.isArray(current) ? current : [];
      const validItems = items2.filter(isActivityBarItem2);
      if (validItems.length !== items2.length) {
        this.plugin.settings.activityBars = this.plugin.settings.activityBars ?? { left: [], right: [] };
        this.plugin.settings.activityBars[side] = validItems;
        void this.plugin.saveSettings().catch((error) => {
          console.warn("Failed to repair activity bar settings:", error);
        });
      }
      const existingRows = /* @__PURE__ */ new Map();
      section.querySelectorAll(`[data-window-spaces-activity-item="${side}"]`).forEach((el) => {
        const viewType = el.getAttribute("data-drag-view-type");
        if (viewType) existingRows.set(viewType, el);
      });
      const activeTypes = new Set(validItems.map((item) => item.viewType));
      existingRows.forEach((row, viewType) => {
        if (!activeTypes.has(viewType)) row.remove();
      });
      validItems.forEach((item, index) => {
        const existingRow = existingRows.get(item.viewType);
        if (existingRow) {
          existingRow.setAttr("data-drag-index", String(index));
          addRow.settingEl.before(existingRow);
          const existingIconBtn = existingRow.querySelector(
            ".setting-item-control .clickable-icon"
          );
          if (existingIconBtn) applyItemIcon(existingIconBtn, this.app, item);
          return;
        }
        const handle = this.renderSideItemRow(group, side, item, index, () => {
          renderItemRows();
          refreshSelect();
          this.plugin.activityBars.refreshAll();
        });
        addRow.settingEl.before(handle.row.settingEl);
      });
      refreshSelect();
    };
    renderItemRows();
    addRow.settingEl.parentElement?.appendChild(addRow.settingEl);
    section.addEventListener("dragover", (e) => {
      e.preventDefault();
      section.querySelectorAll(
        `[data-window-spaces-activity-item="${side}"].drag-over-top, [data-window-spaces-activity-item="${side}"].drag-over-bottom`
      ).forEach((el) => {
        el.classList.remove("drag-over-top", "drag-over-bottom");
      });
      const target = e.target.closest(`[data-window-spaces-activity-item="${side}"]`);
      if (!target) return;
      const rect = target.getBoundingClientRect();
      const isBottom = e.clientY > rect.top + rect.height / 2;
      target.classList.add(isBottom ? "drag-over-bottom" : "drag-over-top");
    });
    section.addEventListener("drop", (e) => {
      e.preventDefault();
      const indicatorEl = section.querySelector(
        `[data-window-spaces-activity-item="${side}"].drag-over-top, [data-window-spaces-activity-item="${side}"].drag-over-bottom`
      );
      const isBottom = indicatorEl?.classList.contains("drag-over-bottom") ?? false;
      section.querySelectorAll(
        `[data-window-spaces-activity-item="${side}"].drag-over-top, [data-window-spaces-activity-item="${side}"].drag-over-bottom`
      ).forEach((el) => {
        el.classList.remove("drag-over-top", "drag-over-bottom");
      });
      if (!indicatorEl) return;
      const draggedViewType = e.dataTransfer?.getData(ACTIVITY_BAR_DRAG_DATA_TYPE) ?? "";
      const targetViewType = indicatorEl.getAttribute("data-drag-view-type") ?? "";
      if (!draggedViewType || !targetViewType) return;
      const current = this.plugin.settings.activityBars?.[side];
      if (!Array.isArray(current)) return;
      const reordered = reorderActivityBarItems(current, draggedViewType, targetViewType, isBottom);
      if (!reordered) return;
      this.plugin.settings.activityBars = this.plugin.settings.activityBars ?? { left: [], right: [] };
      this.plugin.settings.activityBars[side] = reordered;
      void this.plugin.saveSettings().then(() => {
        this.plugin.activityBars.refreshAll();
        renderItemRows();
      }).catch((error) => {
        console.warn("Failed to save activity bar order:", error);
      });
    });
  }
  setupAutoSave() {
    this.plugin.registerEvent(
      this.app.workspace.on("layout-change", () => {
        if (this.plugin.settings.autoSave) {
          if (this.autoSaveTimeout !== null) {
            window.clearTimeout(this.autoSaveTimeout);
          }
          this.autoSaveTimeout = window.setTimeout(async () => {
            try {
              const layout = await this.plugin.manager.captureCurrentLayout({
                name: t("settings.autoSaveEnabled")
              });
              await this.plugin.manager.saveLayout(layout);
            } catch (error) {
              console.warn("Auto save failed:", error);
            }
          }, 2e3);
        }
      })
    );
  }
  removeAutoSave() {
    if (this.autoSaveTimeout !== null) {
      window.clearTimeout(this.autoSaveTimeout);
      this.autoSaveTimeout = null;
    }
  }
  async showConfirmDialog(message, title = t("common.confirm")) {
    return new Promise((resolve) => {
      const modal = new import_obsidian4.Modal(this.app);
      modal.setTitle(title);
      modal.onOpen = () => {
        modal.contentEl.createEl("p", { text: message });
        const buttonContainer = modal.contentEl.createDiv("ws-dialog-actions");
        const cancelBtn = buttonContainer.createEl("button", {
          text: t("common.cancel"),
          cls: "mod-cta"
        });
        cancelBtn.onclick = () => {
          resolve(false);
          modal.close();
        };
        const confirmBtn = buttonContainer.createEl("button", {
          text: t("common.confirm"),
          cls: "mod-warning"
        });
        confirmBtn.onclick = () => {
          resolve(true);
          modal.close();
        };
      };
      modal.open();
    });
  }
};

// src/modals/saveModal.ts
var SettingGroupCtor2 = obsidian2.SettingGroup;
var SaveLayoutModal = class extends import_obsidian5.Modal {
  plugin;
  layout;
  onSubmit;
  constructor(app, plugin, layout, onSubmit) {
    super(app);
    this.plugin = plugin;
    this.layout = layout;
    this.onSubmit = onSubmit;
  }
  createGroup(containerEl) {
    if (!SettingGroupCtor2) return null;
    try {
      return new SettingGroupCtor2(containerEl);
    } catch {
      return null;
    }
  }
  createSettingIn(container, configure) {
    const group = container;
    if (typeof group.addSetting === "function") {
      let result = null;
      group.addSetting((setting2) => {
        result = setting2;
        configure(setting2);
      });
      return result;
    }
    const setting = new import_obsidian5.Setting(container);
    configure(setting);
    return setting;
  }
  /**
   * Obsidian's SettingGroup has changed its internal DOM property across
   * versions. Resolve the rendered panel from the group when possible and
   * fall back to the most recently-created panel in this modal.
   */
  getGroupElement(group, fallback) {
    const candidate = group.settingEl;
    if (candidate && typeof candidate.querySelectorAll === "function") return candidate;
    const groups = fallback.querySelectorAll(".setting-group");
    return groups[groups.length - 1] ?? fallback;
  }
  onOpen() {
    this.setTitle(t("saveModal.title"));
    const { contentEl } = this;
    contentEl.empty();
    this.modalEl.addClass("window-layouts-modal");
    let selectedSections = Array.from(this.layout.sections || []);
    const isExistingSpace = this.plugin.settings.spaces.some(
      (space) => space.id === this.layout.id || !!this.layout.name && space.name === this.layout.name
    );
    let borderInset = this.getEffectiveBorderInset();
    let borderInsetOverride = this.layout.borderInset;
    let showFoldedCorner = this.getEffectiveFoldedCorner();
    let showFoldedCornerOverride = this.layout.showFoldedCorner;
    const activityBarSettings = {
      left: this.createActivityBarDraft("left", isExistingSpace),
      right: this.createActivityBarDraft("right", isExistingSpace)
    };
    const i18n = getI18n();
    const totalTabs = this.layout.metadata?.tabCount || this.layout.workspace?.leaves?.length || 0;
    const statsContainer = contentEl.createDiv("window-spaces-modal-top-stats");
    statsContainer.createDiv({
      cls: "window-spaces-stat-item",
      text: `${t("manageModal.tabCount")}: ${totalTabs}`
    });
    statsContainer.createDiv({
      cls: "window-spaces-stat-item",
      text: `${t("manageModal.createdDate")}: ${i18n.formatDate(new Date(this.layout.timestamp))}`
    });
    statsContainer.createDiv({
      cls: "window-spaces-stat-item",
      text: `${t("manageModal.updatedDate")}: ${i18n.formatDate(new Date(this.layout.updatedAt || this.layout.timestamp || this.layout.createdAt || Date.now()))}`
    });
    statsContainer.createDiv({
      cls: "window-spaces-stat-item",
      text: `${t("saveModal.windowSize")}: ${this.layout.windowState.size.width} x ${this.layout.windowState.size.height}`
    });
    const identityGroup = this.createGroup(contentEl) ?? contentEl;
    let nameInput;
    const nameSetting = this.createSettingIn(identityGroup, (setting) => {
      setting.setName(t("saveModal.nameLabel"));
      setting.addText((text) => {
        nameInput = text.inputEl;
        text.inputEl.value = this.layout.name || this.generateDefaultName();
        text.inputEl.focus();
        text.inputEl.select();
        text.inputEl.addEventListener("keydown", (e) => {
          if (e.key === "Enter") {
            e.preventDefault();
            void this.submitForm(
              nameInput,
              includeGeometry,
              autoSave,
              selectedSections,
              archived,
              currentIcon,
              currentColor,
              borderInsetOverride,
              showFoldedCornerOverride,
              activityBarSettings
            );
          }
        });
      });
    });
    const noticeContainer = nameSetting.infoEl.createDiv("save-overwrite-notice");
    const allSpaces = this.plugin.manager.getSavedLayouts();
    const existingSectionsSet = /* @__PURE__ */ new Set();
    (this.plugin.settings?.sectionsOrder || []).forEach((s) => existingSectionsSet.add(s));
    allSpaces.forEach((s) => (s.sections || []).forEach((sec) => existingSectionsSet.add(sec)));
    const existingSections = Array.from(existingSectionsSet);
    const sectionsSetting = this.createSettingIn(identityGroup, (setting) => {
      setting.setName(t("manageModal.sectionsLabel") || "Sections");
      setting.addText((text) => {
        text.setPlaceholder(t("manageModal.sectionsPlaceholder") || "Add section...");
        text.inputEl.addEventListener("keydown", (e) => {
          if (e.key === "Enter" || e.key === ",") {
            e.preventDefault();
            const val = text.inputEl.value.trim().replace(/^,+|,+$/g, "");
            if (val && !selectedSections.includes(val)) {
              selectedSections.push(val);
              text.inputEl.value = "";
              renderPills();
            }
          }
        });
      });
    });
    const pillsContainer = sectionsSetting.infoEl.createDiv("space-sections-pills");
    const renderPills = () => {
      pillsContainer.empty();
      selectedSections.forEach((sec) => {
        const pill = pillsContainer.createDiv("space-section-pill");
        pill.createSpan({ text: sec });
        const closeSpan = pill.createSpan({ text: "\u2716", cls: "space-section-pill-close" });
        closeSpan.onclick = (e) => {
          e.stopPropagation();
          selectedSections = selectedSections.filter((s) => s !== sec);
          renderPills();
        };
      });
      existingSections.forEach((sec) => {
        if (selectedSections.includes(sec)) return;
        const unselectedPill = pillsContainer.createDiv("space-section-pill-unselected");
        unselectedPill.setText(`+ ${sec}`);
        unselectedPill.onclick = () => {
          selectedSections.push(sec);
          renderPills();
        };
      });
    };
    renderPills();
    let currentIcon = this.layout.icon || "";
    let currentColor = this.layout.color || "";
    let iconInputEl;
    const iconSetting = this.createSettingIn(identityGroup, (setting) => {
      setting.setName(t("saveModal.iconLabel"));
      setting.setDesc(t("saveModal.iconDesc"));
      setting.addText((text) => {
        iconInputEl = text.inputEl;
        text.setPlaceholder(t("saveModal.iconPlaceholder"));
        text.setValue(currentIcon);
        text.onChange((val) => {
          currentIcon = val.trim();
          updateIconPreview();
        });
      });
    });
    iconSetting.controlEl.addClass("window-space-icon-setting-control");
    const pickIconBtn = iconSetting.controlEl.createEl("button", {
      cls: "clickable-icon",
      attr: { type: "button", title: t("settings.pickIcon") }
    });
    (0, import_obsidian5.setIcon)(pickIconBtn, "image");
    pickIconBtn.onclick = () => {
      new IconPickerModal(this.app, (selected) => {
        currentIcon = selected;
        iconInputEl.value = selected;
        updateIconPreview();
      }).open();
    };
    const iconPreviewEl = iconSetting.controlEl.createDiv({ cls: "window-space-icon-preview" });
    const updateIconPreview = () => {
      iconPreviewEl.empty();
      const val = currentIcon || this.plugin.settings.defaultIcon || DEFAULT_SPACE_ICON;
      const isEmoji = isSpaceEmoji(val);
      if (isEmoji) {
        iconPreviewEl.createSpan({ text: val });
      } else {
        const iconDiv = iconPreviewEl.createDiv();
        if (!setIconWithCheck(iconDiv, val)) {
          (0, import_obsidian5.setIcon)(iconDiv, "layout");
        }
      }
    };
    updateIconPreview();
    const presets = this.plugin.settings.colorPresets || DEFAULT_COLOR_PRESETS;
    const colorSetting = this.createSettingIn(identityGroup, (setting) => {
      setting.setName(t("saveModal.colorLabel"));
    });
    const colorPickerContainer = colorSetting.controlEl.createDiv({ cls: "window-space-color-picker-container" });
    const colorInput = colorPickerContainer.createEl("input", {
      attr: { type: "color" },
      value: currentColor || "#3b82f6"
    });
    const swatchesContainer = colorPickerContainer.createDiv({ cls: "window-space-color-swatches" });
    const renderSwatches = () => {
      swatchesContainer.empty();
      presets.forEach((hex) => {
        const swatch = swatchesContainer.createDiv({
          cls: `window-space-color-swatch${currentColor === hex ? " is-selected" : ""}`
        });
        swatch.style.backgroundColor = hex;
        swatch.onclick = () => {
          currentColor = hex;
          colorInput.value = hex;
          renderSwatches();
        };
      });
      if (currentColor) {
        const clearBtn = swatchesContainer.createEl("button", {
          text: "\u2716",
          cls: "clickable-icon",
          attr: { title: t("saveModal.clearColor") }
        });
        clearBtn.onclick = () => {
          currentColor = "";
          renderSwatches();
        };
      }
    };
    colorInput.onchange = () => {
      currentColor = colorInput.value;
      renderSwatches();
    };
    renderSwatches();
    new import_obsidian5.Setting(contentEl).setName(t("saveModal.windowAppearanceSection")).setHeading();
    const windowGroup = this.createGroup(contentEl) ?? contentEl;
    let includeGeometry = this.layout.includeGeometry ?? (this.layout.windowState.position !== void 0 || this.layout.windowState.size && this.layout.windowState.size.width > 0);
    let geometryToggleComponent = null;
    let autoSave = this.layout.autoSave ?? false;
    let autoSaveToggleComponent = null;
    this.createSettingIn(windowGroup, (setting) => {
      setting.setName(t("saveModal.includeGeometry")).setDesc(t("saveModal.includeGeometryDesc"));
      setting.addToggle((toggle) => {
        geometryToggleComponent = toggle;
        toggle.setValue(includeGeometry);
        toggle.onChange((value) => {
          includeGeometry = value;
        });
      });
    });
    this.createSettingIn(windowGroup, (setting) => {
      setting.setName(t("saveModal.borderInset")).setDesc(t("saveModal.borderInsetDesc"));
      setting.addSlider((slider) => {
        slider.setLimits(0, 5, 1).setValue(borderInset).setDynamicTooltip().onChange((value) => {
          borderInset = value;
          borderInsetOverride = value;
        });
      });
    });
    this.createSettingIn(windowGroup, (setting) => {
      setting.setName(t("saveModal.foldedCorner")).setDesc(t("saveModal.foldedCornerDesc"));
      setting.addToggle((toggle) => {
        toggle.setValue(showFoldedCorner);
        toggle.onChange((value) => {
          showFoldedCorner = value;
          showFoldedCornerOverride = value;
        });
      });
    });
    new import_obsidian5.Setting(contentEl).setName(t("saveModal.behaviorSection")).setHeading();
    const behaviorGroup = this.createGroup(contentEl) ?? contentEl;
    let archived = this.layout.archived ?? false;
    this.createSettingIn(behaviorGroup, (setting) => {
      setting.setName(t("saveModal.autoSaveToggle"));
      setting.addToggle((toggle) => {
        autoSaveToggleComponent = toggle;
        toggle.setValue(autoSave);
        toggle.onChange((value) => {
          autoSave = value;
        });
      });
    });
    this.createSettingIn(behaviorGroup, (setting) => {
      setting.setName(t("manageModal.archiveSpace") || "Archive Space");
      setting.addToggle((toggle) => {
        toggle.setValue(archived);
        toggle.onChange((value) => {
          archived = value;
        });
      });
    });
    this.renderActivityBarSettings(contentEl, "left", activityBarSettings.left);
    this.renderActivityBarSettings(contentEl, "right", activityBarSettings.right);
    const checkDuplicateName = () => {
      const currentName = nameInput?.value.trim() || "";
      if (!currentName) {
        noticeContainer.setText("");
        return;
      }
      const existingLayouts = this.plugin.manager.getSavedLayouts();
      const match = existingLayouts.find((l) => l.name === currentName);
      if (match) {
        noticeContainer.setText(`\u2139\uFE0F ${t("saveModal.overwriteNotice")}\u300C${currentName}\u300D`);
        if (autoSaveToggleComponent && match.autoSave !== void 0) {
          autoSave = !!match.autoSave;
          autoSaveToggleComponent.setValue(autoSave);
        }
        if (geometryToggleComponent && match.includeGeometry !== void 0) {
          includeGeometry = !!match.includeGeometry;
          geometryToggleComponent.setValue(includeGeometry);
        }
        if (match.sections && Array.isArray(match.sections) && selectedSections.length === 0) {
          selectedSections = Array.from(match.sections);
          renderPills();
        }
      } else {
        noticeContainer.setText("");
      }
    };
    nameInput.addEventListener("input", checkDuplicateName);
    checkDuplicateName();
    const buttonContainer = contentEl.createDiv("ws-dialog-actions");
    const cancelButton = buttonContainer.createEl("button", {
      text: t("common.cancel")
    });
    cancelButton.onclick = () => this.close();
    const saveButton = buttonContainer.createEl("button", {
      text: t("common.save"),
      cls: "mod-cta"
    });
    saveButton.onclick = () => {
      void this.submitForm(
        nameInput,
        includeGeometry,
        autoSave,
        selectedSections,
        archived,
        currentIcon,
        currentColor,
        borderInsetOverride,
        showFoldedCornerOverride,
        activityBarSettings
      );
    };
    window.setTimeout(() => nameInput?.focus(), 50);
  }
  async submitForm(nameInput, includeGeometry, autoSave, selectedSections, archived, icon, color, borderInset, showFoldedCorner, activityBars) {
    const name = nameInput.value.trim();
    if (!name) {
      new import_obsidian5.Notice(t("saveModal.emptyNameError"));
      nameInput.focus();
      return;
    }
    this.layout.name = name;
    this.layout.autoSave = autoSave;
    this.layout.includeGeometry = includeGeometry;
    this.layout.sections = selectedSections;
    this.layout.archived = archived;
    this.layout.icon = icon ? icon.trim() : void 0;
    this.layout.color = color ? color.trim() : void 0;
    this.layout.borderInset = borderInset;
    this.layout.showFoldedCorner = showFoldedCorner;
    this.layout.activityBars = activityBars;
    if (!includeGeometry) {
      this.layout.windowState.position = void 0;
    }
    this.onSubmit(this.layout);
    this.close();
  }
  onClose() {
    const { contentEl } = this;
    contentEl.empty();
  }
  generateDefaultName() {
    if (this.plugin.manager?.generateSmartLayoutName) {
      return this.plugin.manager.generateSmartLayoutName(this.layout);
    }
    const now = /* @__PURE__ */ new Date();
    const i18n = getI18n();
    const dateStr = i18n.formatDate(now);
    return `${t("saveModal.title")} ${dateStr}`;
  }
  getDefaultBorderInset() {
    const value = this.plugin.settings.defaultBorderInset;
    return typeof value === "number" && Number.isFinite(value) ? Math.max(0, Math.min(5, value)) : 1;
  }
  getEffectiveBorderInset() {
    const value = this.layout.borderInset ?? this.getDefaultBorderInset();
    return Math.max(0, Math.min(5, Number.isFinite(value) ? value : 1));
  }
  getEffectiveFoldedCorner() {
    return this.layout.showFoldedCorner ?? this.plugin.settings.defaultShowFoldedCorner !== false;
  }
  createActivityBarDraft(side, isExistingSpace) {
    const globalItems = this.plugin.settings.activityBars?.[side] ?? [];
    const saved = this.layout.activityBars?.[side];
    const savedItems = saved?.items ? saved.items : saved?.viewTypes ? globalItems.filter((item) => saved.viewTypes?.includes(item.viewType)) : globalItems;
    return {
      show: saved?.show ?? (isExistingSpace ? false : this.plugin.settings.activityBarDefaults?.[side] !== false),
      items: savedItems.map((item) => ({ ...item }))
    };
  }
  renderActivityBarSettings(contentEl, side, draft) {
    new import_obsidian5.Setting(contentEl).setName(
      side === "left" ? t("settings.leftBar") : t("settings.rightBar")
    ).setHeading();
    const activityGroup = this.createGroup(contentEl) ?? contentEl;
    const groupEl = this.getGroupElement(activityGroup, contentEl);
    this.createSettingIn(activityGroup, (setting) => {
      setting.setName(
        side === "left" ? t("saveModal.showLeftActivityBar") || "\u986F\u793A\u5DE6\u5074 Activity Bar" : t("saveModal.showRightActivityBar") || "\u986F\u793A\u53F3\u5074 Activity Bar"
      );
      setting.setDesc(t("saveModal.showActivityBarDesc") || "\u958B\u555F\u6642\uFF0C\u6B64\u7A7A\u9593\u6703\u5728 Popout \u8996\u7A97\u986F\u793A\u8A72\u5074 Activity Bar");
      setting.addToggle((toggle) => {
        toggle.setValue(draft.show);
        toggle.onChange((value) => {
          draft.show = value;
        });
      });
    });
    let selectEl;
    let customInput;
    const addRow = this.createSettingIn(activityGroup, (setting) => {
      selectEl = setting.controlEl.createEl("select", { cls: "dropdown" });
      customInput = setting.controlEl.createEl("input", {
        type: "text",
        placeholder: t("settings.viewTypePlaceholder")
      });
      customInput.addClass("window-spaces-view-type-input");
    });
    addRow.addButton((button) => {
      button.setButtonText(t("settings.addView")).onClick(() => {
        const viewType = customInput.value.trim() || selectEl.value.trim();
        if (!viewType || draft.items?.some((item) => item.viewType === viewType)) return;
        draft.items = draft.items ?? [];
        const newItem = { viewType, side, icon: void 0 };
        draft.items.push(newItem);
        customInput.value = "";
        renderItems();
        void ensureViewIcon(this.app, viewType).then((icon) => {
          if (!icon) return;
          newItem.icon = icon;
          renderItems();
        });
      });
    });
    groupEl.addEventListener("dragover", (e) => {
      e.preventDefault();
      groupEl.querySelectorAll(
        `[data-window-spaces-activity-item="${side}"].drag-over-top, [data-window-spaces-activity-item="${side}"].drag-over-bottom`
      ).forEach((el) => {
        el.classList.remove("drag-over-top", "drag-over-bottom");
      });
      const target = e.target.closest(`[data-window-spaces-activity-item="${side}"]`);
      if (!target) return;
      const rect = target.getBoundingClientRect();
      const isBottom = e.clientY > rect.top + rect.height / 2;
      target.classList.add(isBottom ? "drag-over-bottom" : "drag-over-top");
    });
    groupEl.addEventListener("drop", (e) => {
      e.preventDefault();
      const indicatorEl = groupEl.querySelector(
        `[data-window-spaces-activity-item="${side}"].drag-over-top, [data-window-spaces-activity-item="${side}"].drag-over-bottom`
      );
      const isBottom = indicatorEl?.classList.contains("drag-over-bottom") ?? false;
      groupEl.querySelectorAll(
        `[data-window-spaces-activity-item="${side}"].drag-over-top, [data-window-spaces-activity-item="${side}"].drag-over-bottom`
      ).forEach((el) => {
        el.classList.remove("drag-over-top", "drag-over-bottom");
      });
      if (!indicatorEl) return;
      const draggedViewType = e.dataTransfer?.getData(ACTIVITY_BAR_DRAG_DATA_TYPE) ?? "";
      const targetViewType = indicatorEl.getAttribute("data-drag-view-type") ?? "";
      if (!draggedViewType || !targetViewType) return;
      const current = draft.items ?? [];
      const reordered = reorderActivityBarItems(current, draggedViewType, targetViewType, isBottom);
      if (!reordered) return;
      draft.items = reordered;
      renderItems();
    });
    const renderItems = () => {
      const existingRows = /* @__PURE__ */ new Map();
      groupEl.querySelectorAll(`[data-window-spaces-activity-item="${side}"]`).forEach((el) => {
        const viewType = el.getAttribute("data-drag-view-type");
        if (viewType) existingRows.set(viewType, el);
      });
      const items = draft.items ?? [];
      const activeTypes = new Set(items.map((item) => item.viewType));
      existingRows.forEach((row, viewType) => {
        if (!activeTypes.has(viewType)) row.remove();
      });
      items.forEach((item, index) => {
        const existing = existingRows.get(item.viewType);
        if (existing) {
          existing.setAttr("data-drag-index", String(index));
          addRow.settingEl.before(existing);
          const existingIconBtn = existing.querySelector(
            ".setting-item-control .clickable-icon"
          );
          if (existingIconBtn) applyItemIcon(existingIconBtn, this.app, item);
          return;
        }
        let iconButton = null;
        const row = this.createSettingIn(activityGroup, (setting) => {
          setting.setName(item.label || resolveViewLabel(this.app, item.viewType));
        });
        row.settingEl.setAttr("data-window-spaces-activity-item", side);
        row.settingEl.setAttr("data-drag-index", String(index));
        row.settingEl.setAttr("data-drag-view-type", item.viewType);
        row.settingEl.setAttr("draggable", "true");
        row.settingEl.addEventListener("dragstart", (e) => {
          row.settingEl.classList.add("drag-source");
          if (!e.dataTransfer) return;
          e.dataTransfer.effectAllowed = "move";
          e.dataTransfer.setData(ACTIVITY_BAR_DRAG_DATA_TYPE, item.viewType);
        });
        row.settingEl.addEventListener("dragend", () => {
          row.settingEl.classList.remove("drag-source");
        });
        row.settingEl.querySelectorAll("button, input, select, .checkbox-container, .slider").forEach((el) => {
          el.setAttribute("draggable", "false");
        });
        const gripEl = row.settingEl.createDiv({ cls: "window-spaces-activity-drag-handle" });
        gripEl.setAttr("aria-label", t("settings.dragToReorder"));
        (0, import_obsidian5.setIcon)(gripEl, "grip-vertical");
        row.settingEl.insertBefore(gripEl, row.settingEl.firstChild);
        row.addButton((button) => {
          iconButton = button;
          applyItemIcon(button.buttonEl, this.app, item);
          button.setTooltip(t("settings.pickIcon"));
          button.onClick(() => {
            new IconPickerModal(this.app, (icon) => {
              item.icon = icon;
              iconButton?.setIcon(icon);
            }).open();
          });
        });
        row.addButton((button) => {
          button.setIcon("rotate-ccw").setTooltip(t("settings.restoreDefaultIcon"));
          button.onClick(() => {
            item.icon = void 0;
            iconButton?.setIcon(resolveViewIcon(this.app, item.viewType));
          });
        });
        row.addButton((button) => {
          button.setButtonText(t("settings.removeView")).setWarning().onClick(() => {
            const items2 = draft.items ?? [];
            if (!canRemoveActivityBarItem(items2, enumerateAvailableViews(this.app)[side])) {
              new import_obsidian5.Notice(t("settings.keepOneActivityBarView"));
              return;
            }
            draft.items = items2.filter((entry) => entry !== item);
            renderItems();
          });
        });
        addRow.settingEl.before(row.settingEl);
      });
      selectEl.empty();
      const available = enumerateAvailableViews(this.app);
      const availableTypes = sortViewTypesByLabel(this.app, Array.from(
        new Set([...available.left, ...available.right].map((item) => item.viewType))
      ));
      availableTypes.forEach((viewType) => {
        if (activeTypes.has(viewType)) return;
        selectEl.createEl("option", { value: viewType, text: resolveViewLabel(this.app, viewType) });
      });
    };
    renderItems();
    addRow.settingEl.parentElement?.appendChild(addRow.settingEl);
  }
};

// src/views/windowLayoutsView.ts
var import_obsidian6 = require("obsidian");
var WINDOW_LAYOUTS_VIEW_TYPE = "window-spaces-layouts";
var WindowLayoutsView = class extends import_obsidian6.ItemView {
  plugin;
  contentController;
  navigation = false;
  constructor(leaf, plugin) {
    super(leaf);
    this.plugin = plugin;
  }
  getViewType() {
    return WINDOW_LAYOUTS_VIEW_TYPE;
  }
  getDisplayText() {
    return t("common.windowLayouts");
  }
  getIcon() {
    return "layout";
  }
  async onOpen() {
    this.containerEl.closest(".workspace-leaf")?.classList.add("mod-window-spaces-leaf");
    this.contentController = new WindowLayoutsModal(this.app, this.plugin);
    this.contentController.mountInContainer(this.contentEl, void 0, () => {
      const ws = this.app.workspace;
      return ws.getMostRecentLeaf?.() === this.leaf || ws.activeLeaf === this.leaf;
    });
  }
  async onClose() {
    this.contentController?.unmountFromContainer();
    this.contentController = void 0;
  }
};

// src/shared/popoutLayout.ts
var INITIAL_SPLIT_RATIO = 0.34;
function isPopoutWindow(win) {
  if (!win || typeof window !== "undefined" && win === window) return false;
  const body = win.document?.body;
  return !!body && (body.classList.contains("is-popout-window") || body.classList.contains("mod-popout"));
}
function getWindowOfLeaf(leaf) {
  if (!leaf) return null;
  const extLeaf = leaf;
  const container = extLeaf.containerEl || leaf.view?.containerEl;
  return container?.ownerDocument?.defaultView ?? null;
}
function getPaneRect(tabs) {
  const container = tabs?.containerEl;
  if (container instanceof HTMLElement) {
    const rect = container.getBoundingClientRect();
    if (rect.width > 0 && rect.height > 0) return rect;
  }
  const children = tabs?.children ?? [];
  for (const leaf of children) {
    const extLeaf = leaf;
    const leafContainer = extLeaf.containerEl || leaf.view?.containerEl;
    if (leafContainer instanceof HTMLElement) {
      const rect = leafContainer.getBoundingClientRect();
      if (rect.width > 0 && rect.height > 0) return rect;
    }
  }
  return null;
}
function getTopLevelNodeInWindow(leaf) {
  let curr = leaf;
  while (curr && curr.parent) {
    const parent = curr.parent;
    if (!parent || !parent.parent || parent.type === "root" || parent.isRoot || parent.kind === "root") {
      return curr;
    }
    curr = parent;
  }
  return curr;
}
function findRootSplitElement(element) {
  let node = element;
  while (node) {
    if (node.classList.contains("workspace-split") && node.classList.contains("mod-root")) {
      return node;
    }
    node = node.parentElement;
  }
  return null;
}
function getDirectChildOf(root, element) {
  let current = element;
  while (current && current.parentElement !== root) {
    current = current.parentElement;
  }
  return current && current.parentElement === root ? current : null;
}
function getPaneContainerElement(leaf) {
  const extLeaf = leaf;
  const container = extLeaf.containerEl || leaf.view?.containerEl;
  if (!(container instanceof HTMLElement)) return null;
  let current = container;
  while (current && current.parentElement) {
    if (current.parentElement.classList.contains("workspace-split")) {
      return current;
    }
    current = current.parentElement;
  }
  return null;
}
function isElementHidden(el) {
  return !!el && (el.style.display === "none" || el.classList.contains("window-spaces-column-hidden"));
}
function setElementDisplay(el, display) {
  const customEl = el;
  if (typeof customEl.setCssProps === "function") {
    customEl.setCssProps({ display });
  } else {
    el.style.display = display;
  }
}
function setElementCssStyles(el, styles) {
  const customEl = el;
  if (typeof customEl.setCssStyles === "function") {
    customEl.setCssStyles(styles);
  } else if (typeof customEl.setCssProps === "function") {
    customEl.setCssProps(styles);
  } else {
    for (const [key, value] of Object.entries(styles)) {
      el.style.setProperty(key, value);
    }
  }
}
function getViewContainer(leaf) {
  const extLeaf = leaf;
  const container = extLeaf.containerEl || leaf.view?.containerEl;
  return container instanceof HTMLElement ? container : null;
}
function getDirectSplitChild(split, element) {
  let current = element;
  while (current && current.parentElement !== split) {
    current = current.parentElement;
  }
  return current;
}
function scheduleInitialSplitSizing(panelLeaf, editorLeaf, win) {
  const raf = win.requestAnimationFrame?.bind(win) || window.requestAnimationFrame.bind(window);
  raf(() => {
    raf(() => {
      applyInitialSplitSizing(panelLeaf, editorLeaf);
    });
  });
}
function applyInitialSplitSizing(panelLeaf, editorLeaf) {
  const panelContainer = getViewContainer(panelLeaf);
  const editorContainer = getViewContainer(editorLeaf);
  if (!panelContainer || !editorContainer) return;
  const split = panelContainer.closest(".workspace-split.mod-vertical");
  if (!split || !split.contains(editorContainer)) return;
  const panelPane = getDirectSplitChild(split, panelContainer);
  const editorPane = getDirectSplitChild(split, editorContainer);
  if (!panelPane || !editorPane || panelPane === editorPane) return;
  setElementCssStyles(panelPane, { flex: `0 0 ${INITIAL_SPLIT_RATIO * 100}%` });
  setElementCssStyles(editorPane, { flex: "1 1 0%" });
}
function findLeafInTabs(tabs, viewType) {
  const children = tabs?.children ?? [];
  for (const leaf of children) {
    if (leaf.getViewState()?.type === viewType) {
      return leaf;
    }
  }
  return null;
}
function isSidebarColumnElement(columnEl) {
  return !!columnEl && columnEl.classList.contains("window-spaces-sidebar-column");
}
var PopoutLayoutEngine = class {
  app;
  sidebarSidesByWindow = /* @__PURE__ */ new WeakMap();
  constructor(app) {
    this.app = app;
  }
  get workspace() {
    return this.app.workspace;
  }
  /** 取得指定視窗中最新的 active leaf（限定該 window）。 */
  getActiveLeafInWindow(win) {
    const activeLeaf = typeof this.workspace.getMostRecentLeaf === "function" ? this.workspace.getMostRecentLeaf() : this.workspace.activeLeaf;
    return activeLeaf && getWindowOfLeaf(activeLeaf) === win ? activeLeaf : null;
  }
  /** 取得指定視窗中最後一個 leaf（限定該 window）。 */
  getLastLeafInWindow(win) {
    let lastLeaf = null;
    this.workspace.iterateAllLeaves((leaf) => {
      if (getWindowOfLeaf(leaf) === win) {
        lastLeaf = leaf;
      }
    });
    return lastLeaf;
  }
  /** 取得指定視窗中所有 leaf。 */
  getLeavesForWindow(win) {
    const leaves = [];
    this.workspace.iterateAllLeaves((leaf) => {
      if (getWindowOfLeaf(leaf) === win) {
        leaves.push(leaf);
      }
    });
    return leaves;
  }
  /** Record which endpoint columns are real sidebars for a newly-created Popout. */
  setSidebarSides(win, sides) {
    this.sidebarSidesByWindow.set(win, { ...sides });
  }
  getSidebarSides(win) {
    const sides = this.sidebarSidesByWindow.get(win);
    return sides ? { ...sides } : void 0;
  }
  /** 取得所有目前存活的 Popout DOM Window（去重）。 */
  getLivePopoutWindows() {
    const wins = [];
    this.workspace.iterateAllLeaves((leaf) => {
      const win = getWindowOfLeaf(leaf);
      if (win && isPopoutWindow(win) && !wins.includes(win)) {
        wins.push(win);
      }
    });
    return wins;
  }
  /** 收集 Popout 視窗中的所有 Pane（測量 tabs 容器）。 */
  collectPopoutPanes(win) {
    const tabsSet = /* @__PURE__ */ new Set();
    this.workspace.iterateAllLeaves((leaf) => {
      if (getWindowOfLeaf(leaf) !== win) return;
      const parent = leaf.parent;
      if (parent) {
        tabsSet.add(parent);
      }
    });
    const panes = [];
    for (const tabs of tabsSet) {
      const rect = getPaneRect(tabs);
      const left = rect ? rect.left : 0;
      const width = rect ? rect.width : 400;
      panes.push({ tabs, left, width, center: left + width / 2 });
    }
    panes.sort((a, b) => a.left - b.left);
    return panes;
  }
  /** 在指定頂層欄位內尋找特定 view type 的 leaf（限定該欄位，不跨 tab group / 不跨側欄）。 */
  findLeafOfTypeInColumn(win, columnEl, viewType) {
    let found = null;
    this.workspace.iterateAllLeaves((leaf) => {
      if (found) return;
      if (getWindowOfLeaf(leaf) !== win || leaf.getViewState()?.type !== viewType) return;
      const extLeaf = leaf;
      const container = extLeaf.containerEl || leaf.view?.containerEl;
      if (container instanceof HTMLElement && columnEl.contains(container)) {
        found = leaf;
      }
    });
    return found;
  }
  findLeafByIdInWindow(win, leafId) {
    let found = null;
    this.workspace.iterateAllLeaves((leaf) => {
      if (!found && getWindowOfLeaf(leaf) === win && leaf.id === leafId) {
        found = leaf;
      }
    });
    return found;
  }
  pickCenterPopoutPane(panes, win) {
    const firstPane = panes[0];
    if (!firstPane) return null;
    if (panes.length === 1) return firstPane;
    const winCenter = win.innerWidth / 2;
    let bestPane = firstPane;
    let bestDistance = Infinity;
    for (const pane of panes) {
      const distance = Math.abs(pane.center - winCenter);
      if (distance < bestDistance) {
        bestDistance = distance;
        bestPane = pane;
      }
    }
    return bestPane;
  }
  /**
   * 確保指定 Popout 的某側存在「側欄欄位」（結構化：第一/最後頂層欄位即側欄），
   * 並開啟/聚焦指定 view type。
   * - viewType 有值：建立/聚焦該 view 的 leaf。
   * - viewType 無值：僅確保欄位存在並回傳一個空 leaf（供攔截器由第三方設定 view）。
   *
   * 主動定位語意：open-in-left/right-sidebar 應把 view 開到「最左/最右邊的 split」。
   * 該側沒有側欄欄位（getColumnElement null，例如欄位數不足或 activity bar 未開）時
   * 直接建立新的頂層欄位，不再回退塞入現有的最外欄位（避免把 view 誤塞進內容欄位）。
   */
  async ensureSideColumn(win, side, viewType) {
    const workspace = this.workspace;
    const columnEl = this.getColumnElement(win, side);
    if (columnEl) {
      const tabs = this.getSidebarTabsInColumn(win, columnEl);
      if (tabs) {
        if (viewType) {
          const existingInSidebar = findLeafInTabs(tabs, viewType);
          if (existingInSidebar) {
            await workspace.revealLeaf(existingInSidebar);
            workspace.setActiveLeaf(existingInSidebar, { focus: true });
            return existingInSidebar;
          }
        }
        const newLeaf = viewType ? await this.openPanelInTabs(tabs, viewType) : this.createLeafInTabs(tabs);
        await workspace.revealLeaf(newLeaf);
        workspace.setActiveLeaf(newLeaf, { focus: true });
        return newLeaf;
      }
    }
    const centerPane = this.getCenterPanes(win)[0];
    let editorLeaf = centerPane?.tabs.children?.[0] || this.getActiveLeafInWindow(win) || this.getLastLeafInWindow(win);
    if (editorLeaf && viewType && editorLeaf.getViewState()?.type === viewType) {
      let otherLeaf = null;
      workspace.iterateAllLeaves((l) => {
        if (!otherLeaf && getWindowOfLeaf(l) === win && l.getViewState()?.type !== viewType) {
          otherLeaf = l;
        }
      });
      if (otherLeaf) editorLeaf = otherLeaf;
    }
    if (!editorLeaf) {
      return this.openPanelInEditor(win, viewType);
    }
    const targetNode = getTopLevelNodeInWindow(editorLeaf) || editorLeaf;
    const isParentNode = targetNode !== editorLeaf && Boolean(targetNode.children);
    const before = side === "left" ? !isParentNode : isParentNode;
    const panelLeaf = workspace.createLeafBySplit(targetNode, "vertical", before);
    if (viewType) {
      await panelLeaf.setViewState({
        type: viewType,
        active: false,
        state: {}
      });
    }
    scheduleInitialSplitSizing(panelLeaf, editorLeaf, win);
    await workspace.revealLeaf(panelLeaf);
    workspace.setActiveLeaf(panelLeaf, { focus: true });
    return panelLeaf;
  }
  /**
   * 在指定側欄欄位（頂層 column 元素）內解析目標 tabs 群組：
   * 優先取該欄位中 active leaf 所在的 pane，否則第一個 pane 的 tabs。
   */
  getSidebarTabsInColumn(win, columnEl) {
    const activeLeaf = this.getActiveLeafInWindow(win);
    if (activeLeaf) {
      const tabs = this.getTabsForLeafInColumn(win, columnEl, activeLeaf);
      if (tabs) return tabs;
    }
    let firstTabs = null;
    this.workspace.iterateAllLeaves((leaf) => {
      if (firstTabs) return;
      const tabs = this.getTabsForLeafInColumn(win, columnEl, leaf);
      if (tabs) firstTabs = tabs;
    });
    return firstTabs;
  }
  /** 若 leaf 位於指定 column 元素內，回傳其所屬 tabs 群組；否則 null。 */
  getTabsForLeafInColumn(win, columnEl, leaf) {
    if (getWindowOfLeaf(leaf) !== win) return null;
    const extLeaf = leaf;
    const container = extLeaf.containerEl || leaf.view?.containerEl;
    if (container instanceof HTMLElement && columnEl.contains(container)) {
      return extLeaf.parent ?? null;
    }
    return null;
  }
  /** 攔截器專用：在指定側欄回傳一個 leaf（欄位不存在則建立），供第三方設定 view state。 */
  async openSideLeaf(win, side) {
    return this.ensureSideColumn(win, side);
  }
  /**
   * 攔截器專用（同步版本）：`app.workspace.getLeftLeaf` 為同步 API，無法 await。
   * 同步建立/回傳側欄 leaf，不進行 reveal / setViewState（由第三方呼叫端後續設定）。
   */
  openSideLeafSync(win, side) {
    const workspace = this.workspace;
    const columnEl = this.getColumnElement(win, side);
    if (columnEl) {
      const tabs = this.getSidebarTabsInColumn(win, columnEl);
      if (tabs) {
        return this.createLeafInTabs(tabs);
      }
    }
    let editorLeaf = this.getActiveLeafInWindow(win) || this.getLastLeafInWindow(win);
    if (!editorLeaf) return null;
    const targetNode = getTopLevelNodeInWindow(editorLeaf) || editorLeaf;
    const isParentNode = targetNode !== editorLeaf && Boolean(targetNode.children);
    const before = side === "left" ? !isParentNode : isParentNode;
    const panelLeaf = workspace.createLeafBySplit(targetNode, "vertical", before);
    scheduleInitialSplitSizing(panelLeaf, editorLeaf, win);
    return panelLeaf;
  }
  createLeafInTabs(tabs) {
    const workspace = this.workspace;
    const children = tabs?.children ?? [];
    const leaf = workspace.createLeafInParent(
      tabs,
      children.length
    );
    return leaf;
  }
  async openPanelInTabs(tabs, viewType) {
    const leaf = this.createLeafInTabs(tabs);
    await leaf.setViewState({
      type: viewType,
      active: true,
      state: {}
    });
    return leaf;
  }
  /**
   * 統一入口：在指定 Popout 開啟/聚焦指定 view type。
   * - location "left"/"right"：側欄欄位。
   * - location "tab"：中央編輯區域。
   */
  async openPanel(win, location, viewType) {
    if (location === "left" || location === "right") {
      return this.ensureSideColumn(win, location, viewType);
    }
    return this.ensureCenterPanel(win, viewType);
  }
  /** 在指定 Popout 的中央編輯區域開啟/聚焦指定 view type。 */
  async ensureCenterPanel(win, viewType) {
    const workspace = this.workspace;
    const panes = this.collectPopoutPanes(win);
    const targetPane = this.pickCenterPopoutPane(panes, win);
    if (targetPane) {
      const existingInTabs = findLeafInTabs(targetPane.tabs, viewType);
      if (existingInTabs) {
        await workspace.revealLeaf(existingInTabs);
        workspace.setActiveLeaf(existingInTabs, { focus: true });
        return existingInTabs;
      }
      return this.openPanelInTabs(targetPane.tabs, viewType);
    }
    const baseLeaf = this.getActiveLeafInWindow(win) ?? this.getLastLeafInWindow(win);
    if (!baseLeaf) {
      const leaf2 = workspace.getLeaf("tab");
      await leaf2.setViewState({
        type: viewType,
        active: true,
        state: {}
      });
      await workspace.revealLeaf(leaf2);
      workspace.setActiveLeaf(leaf2, { focus: true });
      return leaf2;
    }
    const tabs = baseLeaf.parent;
    if (tabs) {
      const existingInTabs = findLeafInTabs(tabs, viewType);
      if (existingInTabs) {
        await workspace.revealLeaf(existingInTabs);
        workspace.setActiveLeaf(existingInTabs, { focus: true });
        return existingInTabs;
      }
    }
    const leaf = await this.openPanelInTabs(tabs, viewType);
    await workspace.revealLeaf(leaf);
    workspace.setActiveLeaf(leaf, { focus: true });
    return leaf;
  }
  async openPanelInEditor(win, viewType) {
    const workspace = this.workspace;
    const panes = this.collectPopoutPanes(win);
    const targetPane = this.pickCenterPopoutPane(panes, win);
    if (targetPane) {
      if (viewType) {
        const existingInTabs = findLeafInTabs(targetPane.tabs, viewType);
        if (existingInTabs) return existingInTabs;
      }
      return viewType ? this.openPanelInTabs(targetPane.tabs, viewType) : this.createLeafInTabs(targetPane.tabs);
    }
    const baseLeaf = this.getActiveLeafInWindow(win) ?? this.getLastLeafInWindow(win);
    if (!baseLeaf) {
      const leaf = workspace.getLeaf("tab");
      if (viewType) {
        await leaf.setViewState({ type: viewType, active: true, state: {} });
      }
      return leaf;
    }
    const tabs = baseLeaf.parent;
    if (tabs && viewType) {
      const existingInTabs = findLeafInTabs(tabs, viewType);
      if (existingInTabs) return existingInTabs;
    }
    return viewType ? this.openPanelInTabs(tabs, viewType) : this.createLeafInTabs(tabs);
  }
  /** 判斷 leaf 是否位於 Popout 視窗的「偽側欄」（被 UI 標記為 sidebar 的頂層欄位）中。 */
  isLeafInSideColumn(win, leaf) {
    if (!leaf || getWindowOfLeaf(leaf) !== win) return false;
    const extLeaf = leaf;
    const container = extLeaf.containerEl || leaf.view?.containerEl;
    if (!(container instanceof HTMLElement)) return false;
    return isSidebarColumnElement(this.getTopLevelColumnForContainer(container));
  }
  /** 回傳 container 所在的頂層欄位元素（root split 的 direct child）。 */
  getTopLevelColumnForContainer(container) {
    const rootEl = findRootSplitElement(container);
    if (!rootEl) return null;
    return getDirectChildOf(rootEl, container);
  }
  /**
   * 取得 Popout 視窗非側欄的「中央編輯區」Tabs 群組。
   * 排除被 UI 標記為 sidebar（window-spaces-sidebar-column）的頂層欄位內 Tabs。
   */
  getCenterPanes(win) {
    const panes = this.collectPopoutPanes(win);
    if (panes.length === 0) return [];
    const centerPanes = panes.filter((pane) => {
      const tabs = pane.tabs;
      const container = tabs.containerEl;
      if (container instanceof HTMLElement) {
        if (isSidebarColumnElement(this.getTopLevelColumnForContainer(container))) return false;
      }
      const children = tabs.children ?? [];
      for (const leaf of children) {
        const extLeaf = leaf;
        const leafContainer = extLeaf.containerEl || leaf.view?.containerEl;
        if (leafContainer instanceof HTMLElement) {
          if (isSidebarColumnElement(this.getTopLevelColumnForContainer(leafContainer))) return false;
        }
      }
      return true;
    });
    return centerPanes;
  }
  /** 判斷 leaf 的 containerEl 目前是否真的顯示在視窗中（非 display:none / 隱藏 tab）。 */
  isLeafVisibleInPane(leaf) {
    const extLeaf = leaf;
    const container = extLeaf.containerEl || leaf.view?.containerEl;
    if (!(container instanceof HTMLElement)) return false;
    return container.offsetParent !== null;
  }
  /**
   * 同步取得/建立位於 Popout 視窗「中央編輯區」的 WorkspaceLeaf。
   * 用於避免側欄觸發開啟檔案時覆蓋側欄 View。
   */
  getCenterLeafSync(win, newLeaf) {
    const workspace = this.workspace;
    const centerPanes = this.getCenterPanes(win);
    const targetPane = this.pickCenterPopoutPane(centerPanes, win);
    if (targetPane) {
      const isNewTabRequested = newLeaf === true || newLeaf === "tab" || newLeaf === "split";
      if (!isNewTabRequested) {
        const children = targetPane.tabs.children ?? [];
        const visibleLeaf = children.find((leaf) => this.isLeafVisibleInPane(leaf));
        if (visibleLeaf) {
          const isPinned = Boolean(
            visibleLeaf.pinned || visibleLeaf.getViewState()?.pinned
          );
          if (!isPinned) {
            return visibleLeaf;
          }
        }
      }
      return this.createLeafInTabs(targetPane.tabs);
    }
    const baseLeaf = this.getActiveLeafInWindow(win) || this.getLastLeafInWindow(win);
    if (!baseLeaf) {
      return workspace.getLeaf("tab");
    }
    const targetNode = getTopLevelNodeInWindow(baseLeaf) || baseLeaf;
    const topCountBefore = this.getTopLevelColumnElements(win).length;
    const flexSnapshot = this.getTopLevelColumnElements(win).map(
      (col) => Number(col.style.flexGrow || getComputedStyle(col).flexGrow) || 0
    );
    let centerLeaf;
    try {
      centerLeaf = workspace.createLeafBySplit(targetNode, "vertical", false);
    } catch {
      centerLeaf = null;
    }
    const topCountAfter = this.getTopLevelColumnElements(win).length;
    if (!centerLeaf || topCountAfter <= topCountBefore) {
      return this.pickBestEditorLeaf(win) || workspace.getLeaf("tab");
    }
    const windowRoot = this.getWindowRootOf(win);
    if (windowRoot && typeof windowRoot.removeChild === "function") {
      const newTabs = centerLeaf.parent;
      if (newTabs) {
        const children = windowRoot.children ?? [];
        const newIdx = children.indexOf(newTabs);
        if (newIdx >= 0 && newIdx !== 1 && typeof windowRoot.insertChild === "function") {
          windowRoot.removeChild(newTabs);
          windowRoot.insertChild(1, newTabs);
        }
      }
    }
    this.applyNewColumnSizing(win, centerLeaf, flexSnapshot);
    return centerLeaf;
  }
  /** 找出「含 editor view」的 leaf（開檔 fallback 用），否則第一個 leaf。 */
  pickBestEditorLeaf(win) {
    const editorViewTypes = /* @__PURE__ */ new Set(["markdown", "pdf", "canvas", "excalidraw", "image", "audio", "video"]);
    const leaves = this.getLeavesForWindow(win);
    for (const leaf of leaves) {
      const type = leaf.getViewState()?.type;
      if (type && editorViewTypes.has(type)) return leaf;
    }
    return leaves[0] ?? null;
  }
  /** 取得指定視窗的 window root 節點（type === "window" 的最外層容器）。 */
  getWindowRootOf(win) {
    const leaves = this.getLeavesForWindow(win);
    for (const leaf of leaves) {
      let p = leaf.parent;
      while (p) {
        if (p.type === "window" && Array.isArray(p.children)) return p;
        p = p.parent;
      }
    }
    return null;
  }
  /**
   * 新欄位建立後分配權重：最左/最右欄位還原建欄前權重，
   * 新欄位（中央 content）取中間權重。透過 Obsidian 的 setDimension
   * （等同 flex-grow 權重）設定，避免 inline style 被 Obsidian 重算覆蓋。
   */
  applyNewColumnSizing(win, centerLeaf, flexSnapshot) {
    const windowRoot = this.getWindowRootOf(win);
    const children = windowRoot?.children ?? [];
    if (children.length < 3) return;
    children.forEach((child, i) => {
      let dimension;
      if (i === 0) {
        dimension = flexSnapshot[0] || 20;
      } else if (i === children.length - 1) {
        dimension = flexSnapshot[flexSnapshot.length - 1] || 20;
      } else {
        dimension = 40;
      }
      const item = child;
      if (typeof item.setDimension === "function") {
        item.setDimension(dimension);
      }
    });
  }
  // ===== 隱藏/還原 =====
  /**
   * 取得 Popout 視窗 Root split 的頂層欄位元素（依 DOM 結構，不依賴幾何測量，
   * 因此即使欄位被 display:none 隱藏仍能正確定址）。
   */
  getTopLevelColumnElements(win) {
    const leaves = this.getLeavesForWindow(win);
    if (leaves.length === 0) return [];
    const leaf = leaves[0];
    if (!leaf) return [];
    const container = leaf.containerEl || leaf.view?.containerEl;
    if (!(container instanceof HTMLElement)) return [];
    const rootEl = findRootSplitElement(container);
    if (!rootEl) return [];
    let topEls = Array.from(rootEl.children).filter((el) => {
      if (!(el instanceof HTMLElement)) return false;
      return el.classList.contains("workspace-tabs") || el.classList.contains("workspace-split");
    });
    if (isPopoutWindow(win) && topEls.length === 1 && topEls[0].classList.contains("workspace-split")) {
      const containerChildren = Array.from(topEls[0].children).filter((el) => {
        if (!(el instanceof HTMLElement)) return false;
        return el.classList.contains("workspace-tabs") || el.classList.contains("workspace-split");
      });
      if (containerChildren.length > 0) {
        topEls = containerChildren;
      }
    }
    return topEls;
  }
  /** 取得指定側「最外側」的頂層欄位（不問是否為 sidebar），供主動 open-in-sidebar 定位。 */
  getEdgeColumnElement(win, side) {
    const topEls = this.getTopLevelColumnElements(win);
    if (topEls.length === 0) return null;
    return side === "left" ? topEls[0] ?? null : topEls[topEls.length - 1] ?? null;
  }
  /**
   * 取得指定側的「物理側欄」頂層欄位元素（DOM 結構優先，display-independent）。
   *
   * 語意：主動/實體定位（open-in-sidebar、hide/show column、隱藏狀態 capture/apply）。
   * - 有 hints（activityBar 寫入的物理側欄映射）：該側有側欄 → 最左/最右欄位；
   *   該側無側欄 → null（側欄可能被 close all 刪除，由 plugin 的完整性守護補欄）。
   * - 無 hints（非 Window Spaces 管理或尚未同步）：以 UI 標記
   *   （window-spaces-sidebar-column class）保守推斷；無標記 → null。
   *
   * 優先判定規則：邊緣欄位是否仍帶該側 sidebar 標記（mod-left-split /
   * mod-right-split）優先於欄位數算術。標記由 activityBar 的
   * syncSidebarColumnClasses 依「activity bar 可見 + 位於最左/最右 + 頂層欄位數
   * ≥ 2」持續同步，只要該側側欄欄位還存在，即使使用者關閉了「內容欄位」使
   * 頂層欄位數低於原始欄位數（2026-08 實測：關閉第 3 欄的兩個 split 後欄位數
   * 由 4 減為 3），此處仍認定側欄存在、不需補欄。僅當側欄真的被 close-all
   * 移除（邊緣欄變成內容欄位、不再帶該側標記）時才落到欄位數算術判缺失。
   *
   * 注意：被動判定（isLeafInSideColumn / getCenterPanes）不使用本方法，
   * 一律以 UI 標記 class 為準，確保開檔排除與視覺樣式永遠一致。
   */
  getColumnElement(win, side) {
    const edge = this.getEdgeColumnElement(win, side);
    if (!edge) return null;
    const isSidebarForSide = side === "left" ? edge.classList.contains("mod-left-split") : edge.classList.contains("mod-right-split");
    if (isSidebarForSide) return edge;
    const configuredSides = this.sidebarSidesByWindow.get(win);
    if (configuredSides) {
      if (!configuredSides[side]) return null;
      let requiredColumns;
      if (typeof configuredSides.originalCount === "number") {
        const initialLeft = configuredSides.initialLeft ?? configuredSides.left;
        const initialRight = configuredSides.initialRight ?? configuredSides.right;
        const leftDelta = (configuredSides.left ? 1 : 0) - (initialLeft ? 1 : 0);
        const rightDelta = (configuredSides.right ? 1 : 0) - (initialRight ? 1 : 0);
        requiredColumns = configuredSides.originalCount + leftDelta + rightDelta;
      } else {
        requiredColumns = Number(configuredSides.left) + Number(configuredSides.right) + 1;
      }
      if (this.getTopLevelColumnElements(win).length < Math.max(2, requiredColumns)) return null;
      return edge;
    }
    return isSidebarColumnElement(edge) ? edge : null;
  }
  /** 目前仍可見的頂層欄位數量（display:none 的欄位不計）。 */
  getVisibleColumnCount(win) {
    return this.getTopLevelColumnElements(win).filter((el) => !isElementHidden(el)).length;
  }
  isColumnHidden(win, side) {
    const column = this.getColumnElement(win, side);
    return isElementHidden(column) || !!column?.classList.contains("window-spaces-column-hidden");
  }
  hideColumn(win, side) {
    const column = this.getColumnElement(win, side);
    if (!column) return;
    column.classList.add("window-spaces-column-hidden");
    setElementDisplay(column, "none");
  }
  showColumn(win, side) {
    const column = this.getColumnElement(win, side);
    if (!column) return;
    column.classList.remove("window-spaces-column-hidden");
    setElementDisplay(column, "");
  }
  hidePaneGroup(leaf) {
    const container = getPaneContainerElement(leaf);
    if (container) setElementDisplay(container, "none");
  }
  // ===== 持久化 =====
  /** 讀取指定視窗目前隱藏的欄位與 pane group。 */
  captureHiddenState(win) {
    const state = {};
    const leftEl = this.getColumnElement(win, "left");
    if (leftEl) state.leftSidebar = isElementHidden(leftEl);
    const rightEl = this.getColumnElement(win, "right");
    if (rightEl) state.rightSidebar = isElementHidden(rightEl);
    const hiddenIds = [];
    this.getLeavesForWindow(win).forEach((leaf) => {
      const container = getPaneContainerElement(leaf);
      if (container && isElementHidden(container)) {
        const id = leaf.id;
        if (id) hiddenIds.push(id);
      }
    });
    if (hiddenIds.length > 0) {
      state.hiddenLeafIds = hiddenIds;
    }
    return state;
  }
  /** 於 restore 完成後重新套用隱藏狀態。 */
  applyHiddenState(win, state) {
    if (!state || !win || win.closed) return;
    if (state.leftSidebar) this.hideColumn(win, "left");
    if (state.rightSidebar) this.hideColumn(win, "right");
    if (Array.isArray(state.hiddenLeafIds)) {
      state.hiddenLeafIds.forEach((leafId) => {
        const leaf = this.findLeafByIdInWindow(win, leafId);
        if (leaf) this.hidePaneGroup(leaf);
      });
    }
  }
};

// src/shared/popoutLayoutRegistry.ts
var POPOUT_LAYOUT_REGISTRY_KEY = "__obsidian_popout_layout_registry_v1__";
function compareRevisions(left, right) {
  return left === right ? 0 : left > right ? 1 : -1;
}
function compareCandidatePriority(left, right) {
  if (left.apiVersion !== right.apiVersion) {
    return right.apiVersion - left.apiVersion;
  }
  if (left.compatibleFrom !== right.compatibleFrom) {
    return left.compatibleFrom - right.compatibleFrom;
  }
  const revisionOrder = compareRevisions(left.implementationRevision, right.implementationRevision);
  return revisionOrder === 0 ? 0 : -revisionOrder;
}
function compareCandidates(left, right) {
  const priorityOrder = compareCandidatePriority(left, right);
  if (priorityOrder !== 0) return priorityOrder;
  return left.id < right.id ? -1 : left.id > right.id ? 1 : 0;
}
function supportsApiVersion(candidate, apiVersion) {
  return candidate.compatibleFrom <= apiVersion && apiVersion <= candidate.apiVersion;
}
function isBackwardCompatibleWithRegisteredConsumers(candidate, candidates) {
  return candidates.filter((consumer) => consumer.apiVersion < candidate.apiVersion).every((consumer) => supportsApiVersion(candidate, consumer.apiVersion));
}
function selectActiveCandidate(state) {
  const candidates = Array.from(state.candidates.values());
  const compatibleCandidates = candidates.filter((candidate) => isBackwardCompatibleWithRegisteredConsumers(candidate, candidates)).sort(compareCandidates);
  const currentActive = state.activeId ? state.candidates.get(state.activeId) ?? null : null;
  const currentRemainsBest = currentActive !== null && compatibleCandidates.some((candidate) => candidate.id === currentActive.id) && compatibleCandidates.every((candidate) => compareCandidatePriority(currentActive, candidate) <= 0);
  const active = currentRemainsBest ? currentActive : compatibleCandidates[0] ?? null;
  state.activeId = active?.id ?? null;
  state.activeEngine = active?.engine ?? null;
}
function isPopoutDocument(targetDocument) {
  const body = targetDocument?.body;
  return !!body && (body.classList.contains("is-popout-window") || body.classList.contains("mod-popout"));
}
async function waitForPopoutWindow(leaf) {
  const extLeaf = leaf;
  for (let attempt = 0; attempt < 40; attempt++) {
    const candidate = extLeaf.containerEl?.ownerDocument?.defaultView;
    if (candidate && isPopoutDocument(candidate.document)) return candidate;
    await new Promise((resolve) => setTimeout(resolve, 50));
  }
  return null;
}
function createStableProxy(state) {
  const target = /* @__PURE__ */ Object.create(null);
  target.openNewPopoutWindow = async () => {
    const engine = state.activeEngine;
    if (!engine) return null;
    const workspace = engine.workspace;
    const leaf = workspace.openPopoutLeaf?.();
    if (!leaf) return null;
    const extLeaf = leaf;
    await Promise.resolve(extLeaf.setViewState({ type: "empty" }));
    const win = await waitForPopoutWindow(leaf);
    if (!win) {
      console.warn("Popout leaf was created, but its Window was not mounted in time.");
      return null;
    }
    try {
      win.focus?.();
    } catch (e) {
      console.warn("Failed to focus new popout window:", e);
    }
    for (const candidate of state.candidates.values()) {
      if (candidate.initializeNewPopoutWindow) {
        try {
          await candidate.initializeNewPopoutWindow(win);
        } catch (e) {
          console.warn(`Popout new-window initializer failed (${candidate.id}):`, e);
        }
      }
    }
    return { win, leaf };
  };
  return new Proxy(target, {
    get(proxyTarget, property, receiver) {
      if (property in proxyTarget) {
        return Reflect.get(proxyTarget, property, receiver);
      }
      const delegate = state.activeEngine;
      if (!delegate) return void 0;
      const value = Reflect.get(delegate, property, delegate);
      if (typeof value !== "function") return value;
      let wrapper = state.methodWrappers.get(property);
      if (!wrapper) {
        wrapper = (...args) => {
          const current = state.activeEngine;
          if (!current) throw new Error("No active Popout layout engine candidate");
          const currentMethod = Reflect.get(current, property, current);
          if (typeof currentMethod !== "function") {
            throw new Error(`Active Popout layout engine has no method '${String(property)}'`);
          }
          return currentMethod.apply(current, args);
        };
        state.methodWrappers.set(property, wrapper);
      }
      return wrapper;
    },
    has(proxyTarget, property) {
      return property in proxyTarget || Boolean(state.activeEngine && property in state.activeEngine);
    },
    getOwnPropertyDescriptor(proxyTarget, property) {
      const own = Reflect.getOwnPropertyDescriptor(proxyTarget, property);
      if (own) return own;
      const delegate = state.activeEngine;
      if (delegate && property in delegate) {
        return {
          configurable: true,
          enumerable: false,
          writable: true,
          value: Reflect.get(delegate, property, delegate)
        };
      }
      return void 0;
    },
    set(proxyTarget, property, value, receiver) {
      return Reflect.set(proxyTarget, property, value, receiver);
    }
  });
}
function getRegistryState() {
  const namespace = globalThis;
  const existing = namespace[POPOUT_LAYOUT_REGISTRY_KEY];
  if (existing) return existing;
  const state = {
    candidates: /* @__PURE__ */ new Map(),
    activeId: null,
    activeEngine: null,
    methodWrappers: /* @__PURE__ */ new Map(),
    proxy: void 0
  };
  state.proxy = createStableProxy(state);
  namespace[POPOUT_LAYOUT_REGISTRY_KEY] = state;
  return state;
}
function acquirePopoutLayoutEngine(candidate) {
  if (!candidate.id) throw new Error("Popout layout candidate id is required");
  if (!Number.isInteger(candidate.apiVersion) || candidate.apiVersion < 1) {
    throw new Error("Popout layout candidate apiVersion must be a positive integer");
  }
  if (!Number.isInteger(candidate.compatibleFrom) || candidate.compatibleFrom < 1) {
    throw new Error("Popout layout candidate compatibleFrom must be a positive integer");
  }
  if (candidate.compatibleFrom > candidate.apiVersion) {
    throw new Error("Popout layout candidate compatibleFrom cannot exceed apiVersion");
  }
  if (typeof candidate.create !== "function") {
    throw new Error("Popout layout candidate create must be a function");
  }
  const state = getRegistryState();
  const engine = candidate.create();
  if (!engine) throw new Error(`Popout layout candidate '${candidate.id}' returned no engine`);
  state.candidates.set(candidate.id, {
    id: candidate.id,
    apiVersion: candidate.apiVersion,
    compatibleFrom: candidate.compatibleFrom,
    implementationRevision: candidate.implementationRevision,
    engine,
    initializeNewPopoutWindow: candidate.initializeNewPopoutWindow
  });
  selectActiveCandidate(state);
  return state.proxy;
}
function releasePopoutLayoutEngine(id) {
  const state = getRegistryState();
  state.candidates.delete(id);
  selectActiveCandidate(state);
}

// src/shared/sharedVersion.ts
var SHARED_API_VERSION = 5;
var SHARED_COMPATIBLE_FROM_VERSION = 1;
var SHARED_IMPLEMENTATION_REVISION = "2026-08-19T14:30:29Z";

// src/popout/activityBar.ts
var import_obsidian7 = require("obsidian");
var PopoutActivityBarManager = class {
  app;
  plugin;
  engine;
  barsByWindow = /* @__PURE__ */ new WeakMap();
  injectedWindows = /* @__PURE__ */ new Set();
  /** 初始化中的視窗：初始化期間暫停 syncSidebarColumnClasses，避免過渡狀態誤判。 */
  initializingWindows = /* @__PURE__ */ new Set();
  sidebarHintsByWindow = /* @__PURE__ */ new WeakMap();
  columnEnsurePromises = /* @__PURE__ */ new WeakMap();
  constructor(plugin, engine) {
    this.app = plugin.app;
    this.plugin = plugin;
    this.engine = engine;
  }
  get settings() {
    return this.plugin.settings;
  }
  isEnabled() {
    return true;
  }
  getItemsForSide(side) {
    return this.settings.activityBars?.[side] ?? [];
  }
  /** Snapshot the global Activity Bar defaults for a newly-created Space. */
  getDefaultSettingsForNewSpace() {
    const copy = (side) => ({
      show: this.settings.activityBarDefaults?.[side] !== false,
      items: this.getItemsForSide(side).map((item) => ({ ...item }))
    });
    return { left: copy("left"), right: copy("right") };
  }
  /**
   * Build the initial columns for a brand-new empty Popout. The first
   * configured button on each enabled side becomes that sidebar's first view.
   * When no view is available, ensureSideColumn intentionally leaves a New Tab
   * panel in that sidebar instead.
   */
  async initializeNewWindow(win) {
    if (!win || win.closed) return;
    this.initializingWindows.add(win);
    try {
      await this.initializeNewWindowInternal(win);
    } finally {
      this.initializingWindows.delete(win);
      this.syncSidebarColumnClasses(win);
    }
  }
  async initializeNewWindowInternal(win) {
    if (!win || win.closed) return;
    this.injectForWindow(win);
    const leftVisible = this.settings.activityBarDefaults?.left !== false;
    const rightVisible = this.settings.activityBarDefaults?.right !== false;
    const engineWithSidebarHints = this.engine;
    engineWithSidebarHints.setSidebarSides?.(win, { left: leftVisible, right: rightVisible });
    if (leftVisible) {
      await this.engine.ensureSideColumn(win, "left", this.getItemsForSide("left")[0]?.viewType);
    }
    if (leftVisible && rightVisible) {
      const centerLeaf = this.engine.getCenterLeafSync(win);
      this.app.workspace.setActiveLeaf(centerLeaf, { focus: false });
    }
    if (rightVisible) {
      await this.engine.ensureSideColumn(win, "right", this.getItemsForSide("right")[0]?.viewType);
    }
    await this.waitForLayoutFrame(win);
    this.applyDefaultColumnSizing(win, leftVisible, rightVisible);
    this.renderWindow(win);
  }
  /**
   * Keep a real content column while Activity Bar visibility changes on an
   * existing Space. A newly enabled side is added next to the current center;
   * enabling the second side then produces [left sidebar, content, right
   * sidebar].
   */
  ensureLayoutColumns(win) {
    const existing = this.columnEnsurePromises.get(win);
    if (existing) return existing;
    const promise = (async () => {
      const layout = this.getLayoutForWindow(win);
      if (!layout) return;
      const leftVisible = this.isSideVisibleForWindow(win, "left");
      const rightVisible = this.isSideVisibleForWindow(win, "right");
      const initialColumns = this.engine.getTopLevelColumnElements(win).length;
      this.ensureSidebarHints(win);
      this.ensureSideColumnPresent(win, "left", leftVisible);
      this.ensureSideColumnPresent(win, "right", rightVisible);
      this.ensureContentColumnPresent(win);
      const finalColumns = this.engine.getTopLevelColumnElements(win).length;
      if (finalColumns !== initialColumns) {
        await this.waitForLayoutFrame(win);
        if (!this.hasSavedLayoutDimensions(layout.workspace?.layout)) {
          this.applyDefaultColumnSizing(win, leftVisible, rightVisible);
        }
      }
    })().finally(() => {
      this.columnEnsurePromises.delete(win);
    });
    this.columnEnsurePromises.set(win, promise);
    return promise;
  }
  getEngineSidebarHints(win) {
    const engineWithSidebarHints = this.engine;
    return engineWithSidebarHints.getSidebarSides?.(win);
  }
  setEngineSidebarHints(win, hints) {
    this.sidebarHintsByWindow.set(win, { ...hints });
    const engineWithSidebarHints = this.engine;
    engineWithSidebarHints.setSidebarSides?.(win, hints);
  }
  /**
   * 建立/更新該視窗的實體側欄 hints。
   *
   * 規則：sidebar = activity bar 旁的最外層欄位（不看欄位內容）。因此 hints
   * 的 left/right 直接同步為「該側 activity bar 是否顯示」。首次呼叫時記錄
   * 原始頂層欄位數（originalCount）與初始顯示狀態，供補欄判斷使用。
   */
  ensureSidebarHints(win) {
    const leftVisible = this.isSideVisibleForWindow(win, "left");
    const rightVisible = this.isSideVisibleForWindow(win, "right");
    const existing = this.sidebarHintsByWindow.get(win) || this.getEngineSidebarHints(win);
    if (existing && typeof existing.originalCount === "number") {
      const updated = {
        ...existing,
        left: leftVisible,
        right: rightVisible
      };
      this.setEngineSidebarHints(win, updated);
      return updated;
    }
    const hints = {
      left: leftVisible,
      right: rightVisible,
      originalCount: this.engine.getTopLevelColumnElements(win).length,
      initialLeft: leftVisible,
      initialRight: rightVisible
    };
    this.setEngineSidebarHints(win, hints);
    return hints;
  }
  /**
   * 清除該視窗的側欄 hints，讓下一次 ensureSidebarHints 以目前的頂層欄位數
   * 重新記錄 originalCount。用於 leaf 層級重建（rebuildTargetWindowStructure）
   * 完成後：舊 hints 的 originalCount 記錄的是重建前的欄位數，若重建後欄位
   * 數減少，ensureLayoutIntegrity 會誤判側欄缺失而重複補欄（多出一個欄位）。
   * 這裡不修改 shared engine 的刪除 API，僅以「目前顯示狀態」覆寫 sides，
   * 使 getColumnElement 走舊式欄位數判斷（需求 = left + right + 1）。
   */
  resetSidebarHints(win) {
    if (!win || win.closed) return;
    this.sidebarHintsByWindow.delete(win);
    this.engine.setSidebarSides(win, {
      left: this.isSideVisibleForWindow(win, "left"),
      right: this.isSideVisibleForWindow(win, "right")
    });
  }
  /**
   * 清除視窗內所有頂層欄位（含巢狀 split / tab group）的 sidebar 標記，
   * 將它們的語意重置為 content area。用於舊 space 升級的第一刻：即使既有
   * 欄位殘留 mod-left-split / mod-right-split / window-spaces-sidebar-column
   * 等標記，也能確保 shared getColumnElement 不因「側欄標記優先」而誤把
   * 舊 content 欄當成 sidebar；後續 ensure 只補出新的側欄，原有欄位全數
   * 保留為 content。
   */
  clearSidebarMarks(win) {
    const markClasses = [
      "window-spaces-sidebar-column",
      "mod-sidedock",
      "mod-left-split",
      "mod-right-split",
      "window-spaces-has-hidden-next"
    ];
    this.engine.getTopLevelColumnElements(win).forEach((col) => {
      col.classList.remove(...markClasses);
      col.querySelectorAll(".workspace-tabs, .workspace-split").forEach((el) => {
        el.classList.remove(...markClasses);
      });
    });
  }
  /**
   * 若任一「顯示且 sidebar 欄位缺失」的側存在，以全新 layout 重建該視窗，
   * 在欄位容器層級補出標準 workspace-tabs sidebar 欄位（B2：每次新增側都走
   * 重構）。保留原 content 的 leaf view state；既有側欄欄位原封保留。
   *
   * @returns 是否執行了重建。
   */
  async rebuildMissingSidebars(win, layout) {
    if (!win || win.closed || !layout) return false;
    const leftShow = this.isSideVisibleForWindow(win, "left");
    const rightShow = this.isSideVisibleForWindow(win, "right");
    const leftView = leftShow ? this.getItemsForWindowSide(win, "left")[0]?.viewType : void 0;
    const rightView = rightShow ? this.getItemsForWindowSide(win, "right")[0]?.viewType : void 0;
    const topCols = this.engine.getTopLevelColumnElements(win);
    const last = topCols.length - 1;
    const leftMissing = leftShow && !(topCols[0] && this.columnContainsViewType(topCols[0], leftView));
    const rightMissing = rightShow && !(topCols[last] && this.columnContainsViewType(topCols[last], rightView));
    if (!leftMissing && !rightMissing) return false;
    const manager = this.plugin.manager;
    if (!manager?.rebuildPopoutLayoutWithSidebars) return false;
    await manager.rebuildPopoutLayoutWithSidebars(
      win,
      layout,
      leftMissing ? leftView : void 0,
      rightMissing ? rightView : void 0
    );
    this.resetSidebarHints(win);
    return true;
  }
  /** 欄位元素內是否包含指定 viewType 的 leaf。 */
  columnContainsViewType(colEl, viewType) {
    if (!colEl || !viewType) return false;
    return Array.from(colEl.querySelectorAll(".workspace-leaf [data-type]")).some((el) => el.getAttribute("data-type") === viewType);
  }
  async waitForLayoutFrame(win) {
    const raf = win.requestAnimationFrame?.bind(win);
    if (raf) {
      await new Promise((resolve) => raf(() => resolve()));
      await new Promise((resolve) => raf(() => resolve()));
      return;
    }
    await new Promise((resolve) => win.setTimeout(resolve, 0));
  }
  applyDefaultColumnSizing(win, leftVisible, rightVisible) {
    const columns = this.engine.getTopLevelColumnElements(win);
    const weights = leftVisible && rightVisible ? [20, 60, 20] : leftVisible ? [24, 76] : rightVisible ? [76, 24] : [100];
    columns.forEach((column, index) => {
      const weight = weights[index];
      if (weight === void 0) return;
      const customEl = column;
      const flexGrow = String(weight);
      if (typeof customEl.setCssProps === "function") {
        customEl.setCssProps({ "flex-grow": flexGrow });
      } else {
        column.style.setProperty("flex-grow", flexGrow);
      }
    });
  }
  hasSavedLayoutDimensions(node) {
    if (!node || !Array.isArray(node.children)) return false;
    return node.children.some(
      (child) => typeof child?.dimension === "number" && Number.isFinite(child.dimension) || this.hasSavedLayoutDimensions(child)
    );
  }
  /**
   * Reapply Obsidian's serialized split dimensions after a leaf-level restore.
   * The leaf APIs recreate the tree but initially distribute every split
   * equally; saved `dimension` values are the source of truth for final sizes.
   *
   * 以 flex-grow 權重語意（0~100，與 Obsidian 原生 setDimension 一致）遞迴
   * 套用：容器縮放或側欄收合時剩餘欄位自動重新分配，不會像 flex-basis 百分比
   * 那樣需要手動 rebalance。
   */
  applySavedLayoutDimensions(win) {
    const layout = this.getLayoutForWindow(win);
    let rootNode = layout?.workspace?.layout;
    if (!rootNode) return;
    if (rootNode.type === "floating" && rootNode.children?.length === 1) {
      rootNode = rootNode.children[0];
    }
    const rootEl = win.document.querySelector(
      ".workspace-split.mod-root"
    );
    if (!rootEl) return;
    const getSplitChildren = (splitEl) => Array.from(splitEl.children).filter(
      (child) => child instanceof HTMLElement && (child.classList.contains("workspace-tabs") || child.classList.contains("workspace-split"))
    );
    const setFlex = (el, dimension) => {
      const customEl = el;
      const flexGrow = String(dimension);
      if (typeof customEl.setCssProps === "function") {
        customEl.setCssProps({ "flex-grow": flexGrow });
      } else {
        el.style.setProperty("flex-grow", flexGrow);
      }
    };
    const applyNode = (node, splitEl) => {
      if (!node || !Array.isArray(node.children)) return;
      const domChildren = getSplitChildren(splitEl);
      if (domChildren.length !== node.children.length) return;
      node.children.forEach((child, index) => {
        const domChild = domChildren[index];
        if (!domChild) return;
        const dimension = Number(child?.dimension);
        if (Number.isFinite(dimension) && dimension > 0) {
          setFlex(domChild, dimension);
        }
        if (child?.type === "split") {
          applyNode(child, domChild);
        }
      });
    };
    applyNode(rootNode, rootEl);
  }
  getItemsForWindowSide(win, side) {
    const layout = this.getLayoutForWindow(win);
    const savedSettings = layout?.activityBars?.[side];
    if (savedSettings?.items) return savedSettings.items;
    const items = this.getItemsForSide(side);
    const selectedViewTypes = savedSettings?.viewTypes;
    if (!selectedViewTypes) return items;
    const selected = new Set(selectedViewTypes);
    return items.filter((item) => selected.has(item.viewType));
  }
  isSideVisibleForWindow(win, side) {
    const layout = this.getLayoutForWindow(win);
    if (layout?.activityBars?.[side]) {
      return layout.activityBars[side]?.show === true;
    }
    if (layout) return false;
    return this.settings.activityBarDefaults?.[side] !== false;
  }
  /**
   * 套用 sidebar toggle icon（對齊 Obsidian 主視窗原生按鈕 SVG 結構與 CSS）：
   * 包含外框 <rect> 與內部側欄指示條 <rect class="sidebar-toggle-icon-inner">，
   * 透過 mod-left / mod-right 與 is-open / is-collapsed 類別控制側欄開合狀態與方向。
   */
  applySidebarToggleIcon(btn, side, hidden) {
    btn.classList.add("window-spaces-sidebar-toggle-btn");
    btn.classList.toggle("mod-left", side === "left");
    btn.classList.toggle("mod-right", side === "right");
    btn.classList.toggle("is-collapsed", hidden);
    btn.classList.toggle("is-open", !hidden);
    let svg = btn.querySelector("svg.sidebar-toggle-button-icon");
    if (!svg) {
      btn.innerHTML = `
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="svg-icon sidebar-toggle-button-icon">
          <rect x="1" y="2" width="22" height="20" rx="4"></rect>
          <rect x="4" y="5" width="2" height="14" rx="2" fill="currentColor" class="sidebar-toggle-icon-inner"></rect>
        </svg>
      `.trim();
      svg = btn.querySelector("svg.sidebar-toggle-button-icon");
    }
    const innerRect = svg?.querySelector(".sidebar-toggle-icon-inner");
    if (innerRect) {
      if (hidden) {
        innerRect.setAttribute("width", "2");
        innerRect.style.setProperty("width", "var(--sidebar-left-toggle-inner-width, 8.33%)");
      } else {
        innerRect.setAttribute("width", "5.76");
        innerRect.style.setProperty("width", "var(--sidebar-left-toggle-inner-width-open, 24%)");
      }
    }
  }
  /** 針對單一 Popout 注入（若已注入且仍連接著 DOM 則重新渲染按鈕）。 */
  injectForWindow(win) {
    if (!win || win.closed || !isPopoutWindow(win)) return;
    if (!this.isEnabled()) {
      this.cleanupWindow(win);
      return;
    }
    const body = win.document?.body;
    if (!body) {
      this.cleanupWindow(win);
      return;
    }
    if (body.querySelector(".modal-container")) {
      this.cleanupWindow(win);
      return;
    }
    const existing = this.barsByWindow.get(win);
    if (existing && existing.left.isConnected) {
      this.renderWindow(win);
      return;
    }
    const spaceIdentity = body.createDiv({ cls: "window-spaces-space-identity" });
    spaceIdentity.oncontextmenu = (evt) => {
      evt.preventDefault();
      evt.stopPropagation();
      this.showVisibilityMenu(win, evt);
    };
    const left = body.createDiv({ cls: "window-spaces-activity-bar window-spaces-activity-left" });
    const drag = left.createDiv({ cls: "window-spaces-activity-drag" });
    drag.oncontextmenu = (evt) => {
      evt.preventDefault();
      evt.stopPropagation();
      this.showVisibilityMenu(win, evt);
    };
    const right = body.createDiv({ cls: "window-spaces-activity-bar window-spaces-activity-right" });
    this.barsByWindow.set(win, {
      left,
      right,
      spaceIdentity,
      viewButtons: /* @__PURE__ */ new Map(),
      columnButtons: {
        left: left.createEl("button", { cls: "window-spaces-activity-btn clickable-icon", attr: { type: "button", "aria-label": t("activityBar.toggleColumn") } }),
        right: right.createEl("button", { cls: "window-spaces-activity-btn clickable-icon", attr: { type: "button", "aria-label": t("activityBar.toggleColumn") } })
      }
    });
    body.classList.add("window-spaces-has-left-activity");
    body.classList.add("window-spaces-has-right-activity");
    this.injectedWindows.add(win);
    this.renderWindow(win);
  }
  /** 移除單一 Popout 的 activity bar 與 body class。 */
  cleanupWindow(win) {
    const bars = this.barsByWindow.get(win);
    if (bars) {
      bars.left.remove();
      bars.right.remove();
      bars.spaceIdentity.remove();
    }
    this.barsByWindow.delete(win);
    this.injectedWindows.delete(win);
    const body = win.document?.body;
    body?.classList.remove("window-spaces-has-left-activity");
    body?.classList.remove("window-spaces-has-right-activity");
    body?.classList.remove("window-spaces-has-left-activity-hidden");
    body?.classList.remove("window-spaces-has-right-activity-hidden");
    body?.classList.remove("window-spaces-left-activity-hidden");
    body?.classList.remove("window-spaces-right-activity-hidden");
    this.clearTabHeaderAvoidance(win);
    body?.style.removeProperty("--window-space-color");
    body?.style.removeProperty("--window-space-border-inset");
    body?.classList.remove("has-window-space-color");
    body?.classList.remove("has-window-space-border");
    body?.classList.remove("has-window-space-folded-corner");
    body?.classList.remove("has-space-accents-tab");
    body?.classList.remove("has-space-accents-splitter");
    body?.classList.remove("has-space-accents-panel");
    body?.classList.remove("has-space-accents-activity-bar");
  }
  /** 清理所有已注入的 Popout。 */
  cleanupAll() {
    Array.from(this.injectedWindows).forEach((win) => this.cleanupWindow(win));
  }
  /** 重新注入並渲染所有存活 Popout，並清理已關閉視窗的殘留（layout-change 時呼叫）。 */
  refreshAll() {
    const live = this.engine.getLivePopoutWindows();
    live.forEach((win) => {
      this.injectForWindow(win);
      this.scheduleLayoutIntegrityCheck(win);
    });
    Array.from(this.injectedWindows).forEach((win) => {
      if (!live.includes(win)) this.cleanupWindow(win);
    });
  }
  getLayoutForWindow(win) {
    if (!win || win.closed) return null;
    const explicitId = win._windowSpacesLayoutId;
    if (explicitId) {
      const found = this.settings.spaces.find((s) => s.id === explicitId);
      if (found) return found;
    }
    const manager = this.plugin.manager;
    const name = manager?.getLayoutNameForWindow?.(win);
    if (name) {
      const found = this.settings.spaces.find((s) => s.name === name);
      if (found) return found;
    }
    for (const space of this.settings.spaces ?? []) {
      if (manager?.layoutWindows?.get(space) === win) {
        return space;
      }
    }
    return null;
  }
  updateDragHandleIcon(bars, win) {
    const drag = bars.left.querySelector(".window-spaces-activity-drag");
    if (!drag) return;
    drag.empty();
    bars.spaceIdentity.empty();
    const layout = this.getLayoutForWindow(win);
    const icon = resolveSpaceIcon(layout?.icon, this.settings.defaultIcon);
    const color = layout?.color?.trim();
    const hasCustomColor = Boolean(color);
    const rawBorderInset = typeof layout?.borderInset === "number" && Number.isFinite(layout.borderInset) ? layout.borderInset : this.settings.defaultBorderInset ?? 1;
    const borderInset = Math.max(0, Math.min(5, rawBorderInset));
    const showFoldedCorner = hasCustomColor && (layout?.showFoldedCorner ?? this.settings.defaultShowFoldedCorner !== false);
    const body = win.document?.body;
    if (body) {
      if (hasCustomColor) {
        body.style.setProperty("--window-space-color", color);
        body.classList.add("has-window-space-color");
      } else {
        body.style.removeProperty("--window-space-color");
        body.classList.remove("has-window-space-color");
      }
      if (hasCustomColor && borderInset > 0) {
        body.style.setProperty("--window-space-border-inset", `${borderInset}px`);
      } else {
        body.style.removeProperty("--window-space-border-inset");
      }
      body.classList.toggle("has-window-space-border", hasCustomColor && borderInset > 0);
      body.classList.toggle("has-window-space-folded-corner", showFoldedCorner);
      const accents = this.settings.popoutAccents ?? {
        enabled: true,
        splitter: true,
        activityBar: true
      };
      const accentsActive = hasCustomColor && accents.enabled !== false;
      body.classList.toggle("has-space-accents-tab", accentsActive);
      body.classList.toggle("has-space-accents-splitter", accentsActive && accents.splitter !== false);
      body.classList.toggle("has-space-accents-panel", accentsActive);
      body.classList.toggle("has-space-accents-activity-bar", accentsActive && accents.activityBar !== false);
    }
    const isEmoji = isSpaceEmoji(icon);
    if (isEmoji) {
      bars.spaceIdentity.createSpan({ cls: "window-spaces-space-icon window-spaces-drag-emoji", text: icon });
    } else {
      const iconEl = bars.spaceIdentity.createDiv({ cls: "window-spaces-space-icon window-spaces-drag-icon" });
      if (!setIconWithCheck(iconEl, icon)) {
        (0, import_obsidian7.setIcon)(iconEl, "square");
      }
    }
    bars.spaceIdentity.setAttribute("aria-label", layout?.name || "Window Space");
  }
  /** 重建指定視窗的按鈕內容。 */
  renderWindow(win) {
    void this.ensureLayoutColumns(win).then(() => this.renderWindowNow(win));
    this.renderWindowNow(win);
    this.scheduleLayoutIntegrityCheck(win);
  }
  renderWindowNow(win) {
    const bars = this.barsByWindow.get(win);
    if (!bars) return;
    this.updateDragHandleIcon(bars, win);
    const body = win.document?.body;
    if (body) {
      const leftVisible = this.isSideVisibleForWindow(win, "left");
      const rightVisible = this.isSideVisibleForWindow(win, "right");
      bars.spaceIdentity.classList.toggle(
        "window-spaces-space-identity-drag-region",
        !leftVisible
      );
      body.classList.toggle("window-spaces-has-left-activity", leftVisible);
      body.classList.toggle("window-spaces-has-right-activity", rightVisible);
      body.classList.toggle("window-spaces-left-activity-hidden", !leftVisible);
      body.classList.toggle("window-spaces-right-activity-hidden", !rightVisible);
      this.updateTabHeaderAvoidance(win, leftVisible);
    }
    this.renderBar(bars, win, "left");
    this.renderBar(bars, win, "right");
    this.applySavedLayoutDimensions(win);
    this.updateActiveStates(win);
  }
  /**
   * When the left Activity Bar is hidden, reserve its exact width only in
   * the first tab header of the first visible root column. A root column may
   * contain multiple stacked split headers, so the class belongs on the
   * header itself rather than on the whole column.
   */
  updateTabHeaderAvoidance(win, leftActivityVisible) {
    this.clearTabHeaderAvoidance(win);
    if (leftActivityVisible) return;
    const firstVisibleColumn = this.engine.getTopLevelColumnElements(win).find((column) => !isElementHidden(column));
    const firstTabHeader = firstVisibleColumn?.querySelector(
      ".workspace-tab-header-container"
    );
    firstTabHeader?.classList.add("window-spaces-space-identity-tab-header");
  }
  clearTabHeaderAvoidance(win) {
    win.document.querySelectorAll(".window-spaces-space-identity-tab-header").forEach((header) => header.classList.remove("window-spaces-space-identity-tab-header"));
  }
  renderBar(bars, win, side) {
    const bar = side === "left" ? bars.left : bars.right;
    const isVisible = this.isSideVisibleForWindow(win, side);
    bar.classList.toggle("window-spaces-activity-hidden", !isVisible);
    bar.querySelectorAll(".window-spaces-activity-view, .window-spaces-activity-divider").forEach((el) => {
      bars.viewButtons.forEach((button, key) => {
        if (button === el) bars.viewButtons.delete(key);
      });
      el.remove();
    });
    const colBtn = side === "left" ? bars.columnButtons.left : bars.columnButtons.right;
    const isHidden = this.engine.isColumnHidden(win, side);
    this.applySidebarToggleIcon(colBtn, side, isHidden);
    colBtn.onclick = (evt) => {
      evt.preventDefault();
      evt.stopPropagation();
      void this.toggleColumn(win, side);
    };
    colBtn.oncontextmenu = (evt) => {
      evt.preventDefault();
      evt.stopPropagation();
      this.showVisibilityMenu(win, evt, side);
    };
    const drag = bar.querySelector(".window-spaces-activity-drag");
    if (drag) {
      drag.after(colBtn);
    } else {
      bar.prepend(colBtn);
    }
    bar.createDiv({ cls: "window-spaces-activity-divider" });
    const items = this.getItemsForWindowSide(win, side);
    const configuredTypes = /* @__PURE__ */ new Set();
    for (const item of items) {
      const label = item.label || resolveViewLabel(this.app, item.viewType);
      const btn = bar.createEl("button", {
        cls: "window-spaces-activity-btn window-spaces-activity-view clickable-icon",
        attr: { type: "button", "aria-label": label }
      });
      applyItemIcon(btn, this.app, item);
      btn.onclick = (evt) => {
        evt.preventDefault();
        evt.stopPropagation();
        void this.toggleView(win, item);
      };
      bars.viewButtons.set(`${side}:${item.viewType}`, btn);
      configuredTypes.add(item.viewType);
    }
  }
  /** 更新所有按鈕的 active 狀態。 */
  updateActiveStates(win) {
    const bars = this.barsByWindow.get(win);
    if (!bars) return;
    this.syncSidebarColumnClasses(win);
    this.scheduleDeferredSync(win);
    bars.viewButtons.forEach((btn, key) => {
      const side = bars.left.contains(btn) ? "left" : "right";
      const viewType = key.slice(key.indexOf(":") + 1);
      const columnEl = this.engine.getColumnElement(win, side);
      let active = false;
      if (columnEl) {
        const leaf = this.engine.findLeafOfTypeInColumn(win, columnEl, viewType);
        if (leaf) {
          active = this.isLeafVisibleInSideColumn(win, side, leaf);
        }
      }
      btn.classList.toggle("is-active", active);
    });
    this.setColumnActive(bars, win, "left");
    this.setColumnActive(bars, win, "right");
  }
  /** 判斷 Leaf 在所屬 Popout 側欄中是否處於 active (可見/選中) 狀態。 */
  isLeafVisibleInSideColumn(win, side, leaf) {
    if (this.engine.isColumnHidden(win, side)) return false;
    const extLeaf = leaf;
    const container = extLeaf.containerEl;
    if (!container) return false;
    if (extLeaf.tabEl?.classList.contains("is-active")) return true;
    const parent = container.parentElement;
    if (!parent) return false;
    const isHidden = container.classList.contains("mod-hidden") || container.style.display === "none" || parent.style.display === "none" || parent.classList.contains("mod-hidden");
    return !isHidden;
  }
  /**
   * 將視窗最左/最右的頂層欄位標記為 sidebar column，並套用 Obsidian 的
   * sidebar 相關 class（mod-sidedock / mod-left-split / mod-right-split），
   * 使其 tab 使用與主視窗 sidebar 一致的樣式，且讓側欄的 resize handle 可拖曳。
   *
   * 注意：`.workspace-split` 容器也會套用這些 class——Obsidian 的
   * `.workspace-split.mod-left-split, .workspace-split.mod-right-split { flex: 0 0 auto }`
   * 確實會把巢狀 split 容器壓扁成 width 0，因此 styles.css 以相同 specificity、
   * 較晚載入的非 important `flex: 1 1 0` 覆蓋它，同時保留 Obsidian 的 inline
   * `flex-grow`（popout 的 resize 機制），sidebar 才不會消失、也才能拖寬。
   */
  syncSidebarColumnClasses(win) {
    if (this.initializingWindows.has(win)) return;
    const columns = this.engine.getTopLevelColumnElements(win);
    const last = columns.length - 1;
    const leftActivityVisible = this.isSideVisibleForWindow(win, "left");
    const rightActivityVisible = this.isSideVisibleForWindow(win, "right");
    columns.forEach((el, index) => {
      const isLeftSidebar = columns.length >= 2 && leftActivityVisible && index === 0;
      const isRightSidebar = columns.length >= 2 && rightActivityVisible && index === last;
      const isSidebar = isLeftSidebar || isRightSidebar;
      el.classList.toggle("window-spaces-sidebar-column", isSidebar);
      el.classList.toggle("mod-sidedock", isSidebar);
      el.classList.toggle("mod-left-split", isLeftSidebar);
      el.classList.toggle("mod-right-split", isRightSidebar);
      const next = columns[index + 1];
      el.classList.toggle(
        "window-spaces-has-hidden-next",
        !!next && next.classList.contains("window-spaces-column-hidden")
      );
      const tabGroups = this.getSidebarTabGroups(el);
      tabGroups.forEach((tabsEl) => {
        if (tabsEl !== el) {
          tabsEl.classList.remove("window-spaces-sidebar-column");
        }
        tabsEl.classList.toggle("mod-sidedock", isSidebar);
        tabsEl.classList.toggle("mod-left-split", isLeftSidebar);
        tabsEl.classList.toggle("mod-right-split", isRightSidebar);
      });
      el.querySelectorAll(".workspace-split").forEach((nestedSplit) => {
        if (nestedSplit !== el) {
          nestedSplit.classList.remove("window-spaces-sidebar-column");
          nestedSplit.classList.toggle("mod-sidedock", isSidebar);
          nestedSplit.classList.toggle("mod-left-split", isLeftSidebar);
          nestedSplit.classList.toggle("mod-right-split", isRightSidebar);
        }
      });
      if (isSidebar) {
        this.ensureSidebarFileTabIcons(win, el);
      }
    });
  }
  /**
   * 判斷欄位內是否包含 editor 型 view（markdown / pdf / canvas 等內容 view）。
   *
   * 已停用：sidebar 判定改為「activity bar 旁就是 sidebar」，不再依欄位內容。
   * 保留定義以防外部引用；不再被調用。
   */
  columnContainsEditor(win, columnEl) {
    const editorViewTypes = /* @__PURE__ */ new Set(["markdown", "pdf", "canvas", "excalidraw", "image", "audio", "video"]);
    const leaves = this.engine.getLeavesForWindow(win);
    for (const leaf of leaves) {
      const extLeaf = leaf;
      const container = extLeaf.containerEl || leaf.view?.containerEl;
      if (container instanceof HTMLElement && columnEl.contains(container)) {
        const type = leaf.getViewState()?.type;
        if (type && editorViewTypes.has(type)) return true;
      }
    }
    return columnEl.querySelector(
      ".markdown-source-view, .markdown-reading-view, .canvas-wrapper, .pdf-container, .excalidraw-wrapper"
    ) !== null;
  }
  /**
   * activity bar 隱藏時，該側欄位是 content area（不可隱藏）。
   * 若欄位正被 toggle 隱藏（狀態 4），強制解除隱藏，否則使用者永遠無法
   * 把這個被隱藏的 column 叫出來（activity bar 已不存在，沒有 toggle 鈕）。
   */
  ensureContentColumnNotHidden(win, side) {
    const edge = this.engine.getEdgeColumnElement(win, side);
    if (!edge) return;
    if (edge.classList.contains("window-spaces-column-hidden") || edge.style.display === "none") {
      edge.classList.remove("window-spaces-column-hidden");
      setElementDisplay(edge, "");
    }
  }
  /** 取得欄位內需要套用 sidebar 樣式的 tab group 元素（不含 split 容器本身）。 */
  getSidebarTabGroups(columnEl) {
    if (columnEl.classList.contains("workspace-tabs")) {
      return [columnEl];
    }
    return Array.from(columnEl.querySelectorAll(".workspace-tabs"));
  }
  /**
   * 於下一幀重新同步 sidebar class。Obsidian 拖曳 tab 時會非同步重建頂層欄位
   * 結構（例如把單一 workspace-tabs 拆成巢狀 workspace-split），重建可能在
   * layout-change 事件之後才完成；此延遲確保新節點也能套用到 sidebar 樣式，
   * 避免 sidebar 視覺樣式在拖曳後失效。
   */
  scheduleDeferredSync(win) {
    const raf = (win && typeof win.requestAnimationFrame === "function" ? win.requestAnimationFrame : window.requestAnimationFrame).bind(
      win && typeof win.requestAnimationFrame === "function" ? win : window
    );
    raf(() => {
      if (win.closed) return;
      raf(() => {
        if (win.closed) return;
        if (this.barsByWindow.has(win)) {
          this.syncSidebarColumnClasses(win);
        }
      });
    });
  }
  /** DOM fallback：側欄中頁籤若無 icon（部分情境 Obsidian 不渲染），補上 icon。 */
  ensureSidebarFileTabIcons(win, columnEl) {
    columnEl.querySelectorAll(".workspace-tab-header").forEach((tabEl) => {
      let iconEl = tabEl.querySelector(".workspace-tab-header-inner-icon");
      if (!iconEl) {
        iconEl = tabEl.createDiv({ cls: "workspace-tab-header-inner-icon" });
        tabEl.prepend(iconEl);
      }
      if (iconEl.querySelector("svg")) return;
      const viewType = tabEl.getAttribute("data-type") || "markdown";
      const leaf = this.engine.findLeafOfTypeInColumn(win, columnEl, viewType);
      const leafIcon = leaf ? leaf.view.getIcon?.() : "";
      if (leafIcon && setIconWithCheck(iconEl, leafIcon)) return;
      applyViewIcon(iconEl, this.app, viewType);
    });
  }
  setColumnActive(bars, win, side) {
    const columnEl = this.engine.getColumnElement(win, side);
    const hidden = !!columnEl && this.engine.isColumnHidden(win, side);
    const active = !!columnEl && !hidden;
    bars.columnButtons[side].classList.toggle("is-active", active);
    bars.columnButtons[side].classList.remove("is-disabled");
    bars.columnButtons[side].disabled = false;
    this.applySidebarToggleIcon(bars.columnButtons[side], side, hidden);
  }
  /** 該視窗是否已注入 Activity Bar（供攔截器判斷是否為本外掛管理的視窗）。 */
  isInjected(win) {
    return this.barsByWindow.has(win);
  }
  /** 更新所有已注入視窗的 active 狀態（layout-change 時呼叫）。 */
  updateActiveStatesAll() {
    Array.from(this.injectedWindows).forEach((win) => this.updateActiveStates(win));
  }
  // ===== 佈局完整性守護 =====
  integrityCheckTimers = /* @__PURE__ */ new Map();
  autoHideBlockedUntil = /* @__PURE__ */ new WeakMap();
  /** 補欄嘗試時間戳（防護：createLeafBySplit 建欄失敗時避免無限重試循環） */
  columnFillAttempts = /* @__PURE__ */ new WeakMap();
  /**
   * 排程佈局完整性檢查（debounced）。layout-change 事件觸發時 Obsidian 可能
   * 仍在異步重建頂層欄位（例如拖曳 tab 後把 workspace-tabs 拆成巢狀 split），
   * 因此延遲一段時間等結構穩定後再檢查，避免在重建途中誤判。
   */
  scheduleLayoutIntegrityCheck(win) {
    if (!win || win.closed || !this.barsByWindow.has(win)) return;
    const existing = this.integrityCheckTimers.get(win);
    if (existing !== void 0) {
      win.clearTimeout(existing);
    }
    const timer = win.setTimeout(() => {
      this.integrityCheckTimers.delete(win);
      if (win.closed || !this.barsByWindow.has(win)) return;
      void this.ensureLayoutIntegrity(win);
    }, 350);
    this.integrityCheckTimers.set(win, timer);
  }
  /**
   * 檢查並修正 Popout 的頂層佈局結構，維持「activity bar 旁就是 sidebar」
   * 的三欄語意：
   *
   * 1. 補足缺失的側欄欄位：兩側 activity bar 可見時，頂層必須有
   *    [left sidebar, content, right sidebar] 三欄。側欄在 close all 後被
   *    Obsidian 清空/移除時，補一個空的側欄欄位（New Tab），避免使用者
   *    拖曳 tab 時 Obsidian 建立「大欄包小欄」的巢狀結構。
   * 2. 藏起空的側欄：側欄欄位內只剩 New Tab（empty leaf）時藏起整個欄位
   *    （模仿 Obsidian 主視窗：tabs 全被關掉 → 先藏起左邊欄）。使用者點
   *    activity bar 的 toggle 按鈕時，再顯示空 panel 提醒開一個新的 view。
   */
  async ensureLayoutIntegrity(win) {
    if (!win || win.closed) return;
    try {
      const plugin = this.plugin;
      if (plugin.manager?.isRestoringLayout) return;
      this.ensureSidebarHints(win);
      const leftVisible = this.isSideVisibleForWindow(win, "left");
      const rightVisible = this.isSideVisibleForWindow(win, "right");
      this.ensureSideColumnPresent(win, "left", leftVisible);
      this.ensureSideColumnPresent(win, "right", rightVisible);
      this.ensureContentColumnPresent(win);
      const blocked = this.autoHideBlockedUntil.get(win);
      const now = Date.now();
      for (const side of ["left", "right"]) {
        const visible = side === "left" ? leftVisible : rightVisible;
        const columnEl = this.engine.getColumnElement(win, side);
        if (visible) {
          if (blocked && blocked[side] > now) continue;
          if (columnEl && !this.columnHasNonEmptyLeaf(win, columnEl)) {
            this.engine.hideColumn(win, side);
          }
        } else {
          this.ensureContentColumnNotHidden(win, side);
        }
      }
      this.syncSidebarColumnClasses(win);
      this.updateActiveStates(win);
    } catch (error) {
      console.warn("[Window Spaces] Layout integrity check failed:", error);
    }
  }
  /**
   * 若指定側的物理側欄欄位缺失，補一個帶預設 view 的側欄欄位。
   *
   * 判斷依據：getColumnElement 依「需求欄位數」（原始欄位數 + activity bar
   * 新開啟的側數）決定該側是否有側欄。close all 清空/移除側欄後欄位數不足，
   * 會回 null → 需要補欄。補欄以該側最外欄位的第一個 leaf（或任一 leaf）為
   * 基準建立新的頂層欄位，並顯示該側 activity bar 設定的第一個 view。
   *
   * 防護（避免 layout 循環）：
   * - edge 是巢狀 split（workspace-split）時跳過——createLeafBySplit 會把
   *   新欄位建在該 split 內部（錯誤層級），頂層欄位數不會增加。
   * - 補欄後若欄位數未增加 → 短時間內不重試，防止每次 layout-change 都補欄。
   *
   * @returns 建立的 leaf（若建立成功），否則 null。
   */
  ensureSideColumnPresent(win, side, visible) {
    if (!visible) return null;
    if (this.engine.getColumnElement(win, side)) return null;
    const now = Date.now();
    const lastAttempt = this.columnFillAttempts.get(win);
    if (lastAttempt && lastAttempt[side] > now - 3e3) return null;
    const workspace = this.engine.workspace;
    if (typeof workspace.createLeafBySplit !== "function") return null;
    const edge = this.engine.getEdgeColumnElement(win, side);
    const topColumns = this.engine.getTopLevelColumnElements(win);
    if (edge && !topColumns.includes(edge)) return null;
    let targetLeaf = edge ? this.getFirstLeafInColumn(edge) : null;
    if (edge && !edge.classList.contains("workspace-tabs")) {
      const tabsCol = topColumns.find((el) => el.classList.contains("workspace-tabs")) || edge;
      targetLeaf = this.getFirstLeafInColumn(tabsCol);
    }
    if (!targetLeaf) {
      targetLeaf = this.engine.getActiveLeafInWindow(win) || this.engine.getLastLeafInWindow(win);
    }
    if (!targetLeaf) return null;
    const before = side === "left";
    const topCountBefore = this.engine.getTopLevelColumnElements(win).length;
    const panelLeaf = workspace.createLeafBySplit(targetLeaf, "vertical", before);
    const viewType = this.getItemsForWindowSide(win, side)[0]?.viewType;
    if (panelLeaf && viewType && typeof panelLeaf.setViewState === "function") {
      void panelLeaf.setViewState({ type: viewType, active: false, state: {} });
    }
    const attempts = { ...lastAttempt || { left: 0, right: 0 }, [side]: now };
    this.columnFillAttempts.set(win, attempts);
    if (this.engine.getTopLevelColumnElements(win).length > topCountBefore) {
      attempts[side] = now + 1e4;
      this.columnFillAttempts.set(win, attempts);
    }
    return panelLeaf || null;
  }
  /**
   * 確保頂層欄位數 ≥（顯示的 activity bar 數 + 1），即空間至少保留一欄
   * content area——完全以「目前顯示的 activity bar 個數」為準，不參考
   * hints 的 originalCount。
   *
   * 背景：getColumnElement 改為「側欄標記優先」判定後，guard 只守「該側
   * sidebar 存在」；使用者把 content 欄全部關閉後會得到 [sidebar, sidebar]
   * 而沒有 content。此方法於必要時在側欄之間補一欄空 content（New Tab）。
   *
   * 語意（activity bar 數 + 1 原則）：
   * - 兩側 bar 都顯示 → 至少 3 欄：[左側欄, content, 右側欄]
   * - 單側 bar 顯示 → 至少 2 欄：[側欄, content]
   * - bar 全隱藏（舊 space 未 turn on）→ 至少 1 欄（任一欄位即為 content，不重排）
   * - 舊 space 使用者 turn on bar 的當下：兩側欄立即成為 sidebar，若此時
   *   沒有 content 欄 → 自動補一欄（Professional 型 2 欄 [L, R] 開雙側 bar
   *   後會被補成 [L, C, R]——預期行為）。
   *
   * 補欄位置：有左側欄 → 建在其右側（before=false）；只有右側欄 → 建在其
   * 左側（before=true）；皆無時以任一 leaf 為基準。與 ensureSideColumnPresent
   * 相同的防護：createLeafBySplit 建欄後若頂層欄位數未增加（巢狀結構建錯層
   * 級），下次等 3 秒後再重試。
   *
   * @returns 建立的 leaf（若建立成功），否則 null。
   */
  ensureContentColumnPresent(win) {
    const leftVisible = this.isSideVisibleForWindow(win, "left");
    const rightVisible = this.isSideVisibleForWindow(win, "right");
    const requiredColumns = Number(leftVisible) + Number(rightVisible) + 1;
    if (this.engine.getTopLevelColumnElements(win).length >= Math.max(1, requiredColumns)) return null;
    const now = Date.now();
    const lastAttempt = this.columnFillAttempts.get(win);
    if (lastAttempt && lastAttempt.content && lastAttempt.content > now - 3e3) return null;
    const workspace = this.engine.workspace;
    if (!workspace || typeof workspace.createLeafBySplit !== "function") return null;
    const leftCol = this.engine.getColumnElement(win, "left");
    const rightCol = this.engine.getColumnElement(win, "right");
    const baseLeaf = (leftCol ? this.getFirstLeafInColumn(leftCol) : null) || (rightCol ? this.getFirstLeafInColumn(rightCol) : null) || this.engine.getActiveLeafInWindow(win) || this.engine.getLastLeafInWindow(win);
    if (!baseLeaf) return null;
    const before = !leftCol && !!rightCol;
    const topCountBefore = this.engine.getTopLevelColumnElements(win).length;
    const panelLeaf = workspace.createLeafBySplit(baseLeaf, "vertical", before);
    const attempts = { ...lastAttempt || { left: 0, right: 0 }, content: now };
    this.columnFillAttempts.set(win, attempts);
    if (panelLeaf && this.engine.getTopLevelColumnElements(win).length > topCountBefore) {
      attempts.content = now + 1e4;
      this.columnFillAttempts.set(win, attempts);
    }
    return panelLeaf || null;
  }
  /** 欄位內是否存在非 New Tab 的 leaf（empty leaf 視為空）。 */
  columnHasNonEmptyLeaf(win, columnEl) {
    let has = false;
    this.app.workspace.iterateAllLeaves((leaf) => {
      if (has) return;
      const extLeaf = leaf;
      const container = extLeaf.containerEl || leaf.view?.containerEl;
      if (container instanceof HTMLElement && columnEl.contains(container)) {
        const type = leaf.getViewState()?.type;
        if (type && type !== "empty") has = true;
      }
    });
    return has;
  }
  /** 取得欄位元素內的第一個 leaf。 */
  getFirstLeafInColumn(columnEl) {
    let found = null;
    this.app.workspace.iterateAllLeaves((leaf) => {
      if (found) return;
      const extLeaf = leaf;
      const container = extLeaf.containerEl || leaf.view?.containerEl;
      if (container instanceof HTMLElement && columnEl.contains(container)) {
        found = leaf;
      }
    });
    return found;
  }
  /** 使用者手動展開側欄後，在一段時間內不自動藏起。 */
  markColumnAutoHideBlocked(win, side, ms) {
    const existing = this.autoHideBlockedUntil.get(win) || { left: 0, right: 0 };
    existing[side] = Date.now() + ms;
    this.autoHideBlockedUntil.set(win, existing);
  }
  // ===== 互動邏輯 =====
  async toggleView(win, item) {
    const side = item.side;
    const columnEl = this.engine.getColumnElement(win, side);
    const leaf = columnEl ? this.engine.findLeafOfTypeInColumn(win, columnEl, item.viewType) : null;
    if (leaf) {
      if (this.engine.isColumnHidden(win, side)) {
        this.engine.showColumn(win, side);
        await this.revealAndActivate(leaf);
      } else {
        if (this.isLeafVisibleInSideColumn(win, side, leaf)) {
          this.engine.hideColumn(win, side);
        } else {
          await this.revealAndActivate(leaf);
        }
      }
    } else {
      const created = this.ensureSideColumnPresent(win, side, true);
      if (created && typeof created.setViewState === "function") {
        await created.setViewState({ type: item.viewType, active: true, state: {} });
      } else {
        await this.engine.ensureSideColumn(win, side, item.viewType);
      }
      if (this.engine.isColumnHidden(win, side)) {
        this.engine.showColumn(win, side);
        this.markColumnAutoHideBlocked(win, side, 3e3);
      }
    }
    this.updateActiveStates(win);
  }
  async revealAndActivate(leaf) {
    try {
      await this.app.workspace.revealLeaf(leaf);
      this.app.workspace.setActiveLeaf(leaf, { focus: true });
    } catch {
    }
  }
  /**
   * 顯示 Space Logo / Toggle Button 的上下文選單：
   * - Space Logo 右鍵：顯示 Left activity bar, Left sidebar, Right activity bar, Right sidebar
   * - Left toggle button 右鍵：顯示 Left activity bar, Left sidebar
   * - Right toggle button 右鍵：顯示 Right activity bar, Right sidebar
   *
   * 規則：
   * - activity bar 不打勾 = activity bar 不顯示
   * - activity bar 未打勾時，sidebar disable，sidebar 一律顯示
   * - activity bar 打勾、sidebar 不打勾 = 顯示 activity bar，但隱藏 sidebar
   * - 變更打勾項目時，儲存對應的 space setting
   */
  showVisibilityMenu(win, evt, targetSide) {
    const menu = new import_obsidian7.Menu();
    const sides = targetSide ? [targetSide] : ["left", "right"];
    sides.forEach((side, idx) => {
      if (idx > 0) {
        menu.addSeparator();
      }
      const isLeft = side === "left";
      const barTitle = isLeft ? t("activityBar.leftActivityBar") : t("activityBar.rightActivityBar");
      const sidebarTitle = isLeft ? t("activityBar.leftSidebar") : t("activityBar.rightSidebar");
      const isBarVisible = this.isSideVisibleForWindow(win, side);
      const isSidebarHidden = this.engine.isColumnHidden(win, side);
      const isSidebarVisible = !isSidebarHidden;
      menu.addItem((item) => {
        item.setTitle(barTitle).setChecked(isBarVisible).onClick(() => {
          void this.toggleSideActivityBar(win, side);
        });
      });
      menu.addItem((item) => {
        item.setTitle(sidebarTitle);
        if (!isBarVisible) {
          item.setDisabled(true);
          item.setChecked(true);
        } else {
          item.setChecked(isSidebarVisible);
          item.onClick(() => {
            void this.toggleSideSidebar(win, side);
          });
        }
      });
    });
    menu.showAtMouseEvent(evt);
  }
  /** 切換指定側 Activity Bar 的顯示狀態，並持久化至 Space 設定。 */
  async toggleSideActivityBar(win, side) {
    const layout = this.getLayoutForWindow(win);
    const currentVisible = this.isSideVisibleForWindow(win, side);
    const nextVisible = !currentVisible;
    if (layout) {
      if (!layout.activityBars) {
        this.ensureSidebarHints(win);
        this.clearSidebarMarks(win);
        layout.activityBars = {};
      }
      if (!layout.activityBars[side]) {
        layout.activityBars[side] = {
          show: nextVisible,
          items: this.getItemsForSide(side).map((item) => ({ ...item }))
        };
      } else {
        layout.activityBars[side].show = nextVisible;
      }
      if (!nextVisible) {
        if (this.engine.isColumnHidden(win, side)) {
          this.engine.showColumn(win, side);
          this.markColumnAutoHideBlocked(win, side, 3e3);
        }
        if (layout.hidden) {
          layout.hidden[side === "left" ? "leftSidebar" : "rightSidebar"] = false;
        }
      }
      await this.plugin.saveSettings?.();
    }
    if (nextVisible && layout) {
      await this.rebuildMissingSidebars(win, layout);
    }
    this.renderWindow(win);
  }
  /** 切換指定側 Sidebar 的顯示狀態，並持久化至 Space 設定。 */
  async toggleSideSidebar(win, side) {
    const isHidden = this.engine.isColumnHidden(win, side);
    const layout = this.getLayoutForWindow(win);
    if (isHidden) {
      const columnEl = this.engine.getColumnElement(win, side);
      if (!columnEl) {
        await this.openFallbackViewForSide(win, side);
      } else {
        this.engine.showColumn(win, side);
        this.markColumnAutoHideBlocked(win, side, 3e3);
        await this.ensureColumnViewsRendered(win, columnEl);
      }
      if (layout) {
        if (!layout.hidden) layout.hidden = {};
        layout.hidden[side === "left" ? "leftSidebar" : "rightSidebar"] = false;
        await this.plugin.saveSettings?.();
      }
    } else {
      if (this.engine.getVisibleColumnCount(win) < 2) {
        new import_obsidian7.Notice(t("activityBar.cannotHideLastPane"));
        return;
      }
      this.engine.hideColumn(win, side);
      if (layout) {
        if (!layout.hidden) layout.hidden = {};
        layout.hidden[side === "left" ? "leftSidebar" : "rightSidebar"] = true;
        await this.plugin.saveSettings?.();
      }
    }
    this.updateTabHeaderAvoidance(win, this.isSideVisibleForWindow(win, "left"));
    this.updateActiveStates(win);
  }
  async toggleColumn(win, side) {
    await this.toggleSideSidebar(win, side);
  }
  /** 側欄無物理欄位時：開啟活動列第一個 view 按鈕對應的 view；若無 view 按鈕則開啟 New Tab。 */
  async openFallbackViewForSide(win, side) {
    const layout = this.getLayoutForWindow(win);
    const items = layout?.activityBars?.[side]?.items ?? [];
    if (items.length > 0) {
      await this.toggleView(win, items[0]);
      return;
    }
    try {
      const leaf = this.app.workspace.getLeaf(true);
      await leaf.setViewState({ type: "empty", active: true, state: {} });
      this.app.workspace.setActiveLeaf(leaf, { focus: true });
    } catch (e) {
      console.warn("Failed to open default New Tab:", e);
    }
  }
  /**
   * Views restored into a hidden sidebar can remain DeferredViews with an
   * empty container. Once the sidebar becomes visible, load and render every
   * leaf in that column so split panels such as Tags and Search are populated.
   */
  async ensureColumnViewsRendered(win, columnEl) {
    const leaves = this.engine.getLeavesForWindow(win).filter((leaf) => {
      const extLeaf = leaf;
      const container = extLeaf.containerEl || leaf.view?.containerEl;
      return container instanceof HTMLElement && columnEl.contains(container);
    });
    const manager = this.plugin.manager;
    for (const leaf of leaves) {
      const extLeaf = leaf;
      if (extLeaf.isDeferred && typeof extLeaf.loadIfDeferred === "function") {
        try {
          await extLeaf.loadIfDeferred();
        } catch {
        }
      }
      manager?.ensureViewRendered?.(leaf);
      manager?.scheduleViewRenderAfterActivation?.(leaf, win);
    }
  }
};

// src/shared/workspaceInterceptor.ts
function getState(app) {
  const namespace = globalThis;
  const existing = namespace.__obsidian_workspace_interceptor_state_v1__;
  if (existing) {
    if (existing.participants.size === 0 && !existing.installed) {
      existing.workspace = app.workspace;
    }
    return existing;
  }
  const state = {
    workspace: app.workspace,
    participants: /* @__PURE__ */ new Map(),
    installed: false,
    originalMethods: void 0
  };
  namespace.__obsidian_workspace_interceptor_state_v1__ = state;
  return state;
}
function hasWindowFocus(win) {
  const document2 = win?.document;
  if (!document2) return false;
  if (typeof document2.hasFocus !== "function") return true;
  return document2.hasFocus();
}
function getActivePopoutWindow(state) {
  const mainWindow = typeof window !== "undefined" ? window : null;
  if (hasWindowFocus(mainWindow)) return null;
  const focusedWindow = typeof activeWindow !== "undefined" ? activeWindow : null;
  if (focusedWindow === mainWindow) return null;
  if (focusedWindow && focusedWindow !== mainWindow && isPopoutWindow(focusedWindow) && hasWindowFocus(focusedWindow)) {
    return focusedWindow;
  }
  let focusedPopout = null;
  const workspace = state.workspace;
  workspace.iterateAllLeaves?.call(workspace, (leaf) => {
    const win = getWindowOfLeaf(leaf);
    if (!focusedPopout && win && isPopoutWindow(win) && hasWindowFocus(win)) {
      focusedPopout = win;
    }
  });
  return focusedPopout;
}
function isParticipantEnabled(participant) {
  try {
    return participant.isEnabled ? participant.isEnabled() : true;
  } catch {
    return false;
  }
}
function getParticipantForWindow(state, win) {
  for (const participant of state.participants.values()) {
    if (!isParticipantEnabled(participant)) continue;
    try {
      if (!participant.isManagedWindow || participant.isManagedWindow(win)) return participant;
    } catch {
    }
  }
  return null;
}
function routeSideLeaf(state, side) {
  const activeWindow2 = getActivePopoutWindow(state);
  const participant = activeWindow2 ? getParticipantForWindow(state, activeWindow2) : null;
  const engine = participant?.engine ?? null;
  if (!activeWindow2 || !engine) return null;
  try {
    return engine.openSideLeafSync(activeWindow2, side);
  } catch {
    return null;
  }
}
function routeGetLeaf(state, newLeaf) {
  const activeWindow2 = getActivePopoutWindow(state);
  const participant = activeWindow2 ? getParticipantForWindow(state, activeWindow2) : null;
  const engine = participant?.engine ?? null;
  if (!activeWindow2 || !engine) return null;
  if (newLeaf === "left" || newLeaf === "right") {
    return engine.openSideLeafSync(activeWindow2, newLeaf);
  }
  if (newLeaf === "window") {
    return null;
  }
  const activeLeaf = engine.getActiveLeafInWindow(activeWindow2);
  if (activeLeaf && engine.isLeafInSideColumn(activeWindow2, activeLeaf)) {
    if (newLeaf === "split") {
      const centerLeaf = engine.getCenterLeafSync(activeWindow2);
      return splitCenterLeaf(state.workspace, centerLeaf);
    }
    return engine.getCenterLeafSync(activeWindow2, newLeaf);
  }
  if (!activeLeaf || getWindowOfLeaf(activeLeaf) !== activeWindow2) {
    return engine.getCenterLeafSync(activeWindow2, newLeaf);
  }
  return null;
}
function splitCenterLeaf(workspace, centerLeaf) {
  if (!centerLeaf || typeof workspace.createLeafBySplit !== "function") return null;
  try {
    return workspace.createLeafBySplit(centerLeaf);
  } catch {
    return null;
  }
}
async function routeEnsureSideLeaf(state, viewType, side, options) {
  const activeWindow2 = getActivePopoutWindow(state);
  const participant = activeWindow2 ? getParticipantForWindow(state, activeWindow2) : null;
  const engine = participant?.engine ?? null;
  if (!activeWindow2 || !engine) return null;
  const column = engine.getColumnElement(activeWindow2, side);
  const existing = column ? engine.findLeafOfTypeInColumn(activeWindow2, column, viewType) : null;
  const leaf = existing ?? engine.openSideLeafSync(activeWindow2, side);
  if (!leaf) return null;
  if (!existing) {
    await leaf.setViewState({
      type: viewType,
      active: options.active,
      state: options.state
    });
  } else if (options.state) {
    await leaf.setViewState({ type: viewType, state: options.state });
  }
  await leaf.loadIfDeferred();
  if (options.reveal !== false) {
    await state.workspace.revealLeaf?.(leaf);
  }
  if (options.active) {
    state.workspace.setActiveLeaf?.(leaf, { focus: true });
  }
  if (!existing) {
    await state.workspace.requestSaveLayout?.();
  }
  return leaf;
}
function hasOwnMethod(workspace, key) {
  return Object.prototype.hasOwnProperty.call(workspace, key);
}
function restoreMethod(workspace, key, original) {
  if (original.hadOwn) {
    workspace[key] = original.value;
  } else {
    delete workspace[key];
  }
}
function install(state) {
  if (state.installed) return;
  const workspace = state.workspace;
  state.originalMethods = {
    getLeftLeaf: { hadOwn: hasOwnMethod(workspace, "getLeftLeaf"), value: workspace.getLeftLeaf },
    getRightLeaf: { hadOwn: hasOwnMethod(workspace, "getRightLeaf"), value: workspace.getRightLeaf },
    getLeaf: { hadOwn: hasOwnMethod(workspace, "getLeaf"), value: workspace.getLeaf },
    getLeavesOfType: { hadOwn: hasOwnMethod(workspace, "getLeavesOfType"), value: workspace.getLeavesOfType },
    ensureSideLeaf: { hadOwn: hasOwnMethod(workspace, "ensureSideLeaf"), value: workspace.ensureSideLeaf }
  };
  workspace.__workspaceInterceptorOriginalGetLeftLeaf = workspace.getLeftLeaf;
  workspace.__workspaceInterceptorOriginalGetRightLeaf = workspace.getRightLeaf;
  workspace.__workspaceInterceptorOriginalGetLeaf = workspace.getLeaf;
  workspace.__workspaceInterceptorOriginalGetLeavesOfType = workspace.getLeavesOfType;
  workspace.__workspaceInterceptorOriginalEnsureSideLeaf = workspace.ensureSideLeaf;
  workspace.getLeftLeaf = function(split) {
    const original = state.originalMethods?.getLeftLeaf.value;
    return routeSideLeaf(state, "left") ?? original?.call(workspace, split) ?? null;
  };
  workspace.getRightLeaf = function(split) {
    const original = state.originalMethods?.getRightLeaf.value;
    return routeSideLeaf(state, "right") ?? original?.call(workspace, split) ?? null;
  };
  workspace.getLeaf = function(newLeaf) {
    const original = state.originalMethods?.getLeaf.value;
    return routeGetLeaf(state, newLeaf) ?? original?.call(workspace, newLeaf) ?? null;
  };
  workspace.getLeavesOfType = function(type) {
    const original = state.originalMethods?.getLeavesOfType.value;
    const leaves = original?.call(workspace, type) ?? [];
    const activeWindow2 = getActivePopoutWindow(state);
    const participant = activeWindow2 ? getParticipantForWindow(state, activeWindow2) : null;
    return participant ? leaves.filter((leaf) => getWindowOfLeaf(leaf) === activeWindow2) : leaves;
  };
  workspace.ensureSideLeaf = function(viewType, side, options = {}) {
    const original = state.originalMethods?.ensureSideLeaf.value;
    return routeEnsureSideLeaf(state, viewType, side, options).then((leaf) => {
      if (leaf) return leaf;
      if (original) return original.call(workspace, viewType, side, options);
      return Promise.reject(new Error("Workspace.ensureSideLeaf is unavailable"));
    });
  };
  workspace.__workspaceInterceptorInstalled = true;
  state.installed = true;
}
function uninstall(state) {
  if (!state.installed) return;
  const workspace = state.workspace;
  const original = state.originalMethods;
  if (original) {
    restoreMethod(workspace, "getLeftLeaf", original.getLeftLeaf);
    restoreMethod(workspace, "getRightLeaf", original.getRightLeaf);
    restoreMethod(workspace, "getLeaf", original.getLeaf);
    restoreMethod(workspace, "getLeavesOfType", original.getLeavesOfType);
    restoreMethod(workspace, "ensureSideLeaf", original.ensureSideLeaf);
  }
  delete workspace.__workspaceInterceptorOriginalGetLeftLeaf;
  delete workspace.__workspaceInterceptorOriginalGetRightLeaf;
  delete workspace.__workspaceInterceptorOriginalGetLeaf;
  delete workspace.__workspaceInterceptorOriginalGetLeavesOfType;
  delete workspace.__workspaceInterceptorOriginalEnsureSideLeaf;
  delete workspace.__workspaceInterceptorInstalled;
  state.originalMethods = void 0;
  state.installed = false;
}
function acquireWorkspaceInterceptor(app, participant) {
  if (!participant.id) throw new Error("Workspace interceptor participant id is required");
  const state = getState(app);
  state.participants.set(participant.id, participant);
  install(state);
}
function releaseWorkspaceInterceptor(id) {
  const namespace = globalThis;
  const state = namespace.__obsidian_workspace_interceptor_state_v1__;
  if (!state) return;
  state.participants.delete(id);
  if (state.participants.size === 0) uninstall(state);
}

// src/popout/workspaceInterceptor.ts
var WorkspaceInterceptor = class {
  app;
  engine;
  installed = false;
  enabled = true;
  isManagedWindow = null;
  constructor(app, engine) {
    this.app = app;
    this.engine = engine ?? acquirePopoutLayoutEngine({
      id: "window-spaces",
      apiVersion: SHARED_API_VERSION,
      compatibleFrom: SHARED_COMPATIBLE_FROM_VERSION,
      implementationRevision: SHARED_IMPLEMENTATION_REVISION,
      create: () => new PopoutLayoutEngine(app)
    });
  }
  install() {
    if (this.installed) return;
    acquireWorkspaceInterceptor(this.app, {
      id: "window-spaces",
      engine: this.engine,
      isEnabled: () => this.enabled,
      isManagedWindow: (win) => this.isManagedWindow?.(win) ?? true
    });
    this.installed = true;
  }
  uninstall() {
    if (!this.installed) return;
    releaseWorkspaceInterceptor("window-spaces");
    this.installed = false;
  }
};

// src/main.ts
var DEFAULT_SETTINGS = {
  spaces: [],
  autoSave: false,
  showNotifications: true,
  version: "1.0.0",
  showLayoutStatusBar: true,
  layoutStatusBarDefaultApplied: false,
  showWindowLayoutsRibbonIcon: true,
  sortBy: "updated-desc",
  sectionsOrder: [],
  groupBySection: true,
  showArchived: false,
  defaultIcon: DEFAULT_SPACE_ICON,
  colorPresets: DEFAULT_COLOR_PRESETS,
  defaultBorderInset: 1,
  visualDefaultsVersion: 1,
  defaultShowFoldedCorner: true,
  activityBars: {
    left: BUILTIN_SIDEBAR_VIEWS.filter((item) => item.side === "left"),
    right: BUILTIN_SIDEBAR_VIEWS.filter((item) => item.side === "right")
  },
  activityBarDefaults: {
    left: true,
    right: true
  },
  popoutAccents: {
    enabled: true,
    splitter: true,
    activityBar: true
  },
  workspaceInterceptorEnabled: true
};
var WindowSpacesPlugin = class extends import_obsidian8.Plugin {
  manager;
  windowLayoutsRibbonEl = null;
  popoutLayout;
  activityBars;
  workspaceInterceptor;
  async onload() {
    initI18n(this.app);
    await this.loadSettings();
    this.manager = new WindowLayoutManager(this);
    this.manager.registerExistingPopoutWindows();
    this.popoutLayout = acquirePopoutLayoutEngine({
      id: "window-spaces",
      apiVersion: SHARED_API_VERSION,
      compatibleFrom: SHARED_COMPATIBLE_FROM_VERSION,
      implementationRevision: SHARED_IMPLEMENTATION_REVISION,
      create: () => new PopoutLayoutEngine(this.app),
      // 新 popout window 的初始化 policy：注入 activity bars、依 activityBarDefaults
      // 建立側欄結構與 sizing。供 Folder Spaces 等外掛經 shared openNewPopoutWindow()
      // 開啟視窗時，仍由 Window Spaces 統一管理「新視窗行為」。
      initializeNewPopoutWindow: (win) => this.activityBars ? this.activityBars.initializeNewWindow(win) : Promise.resolve()
    });
    this.activityBars = new PopoutActivityBarManager(this, this.popoutLayout);
    this.workspaceInterceptor = new WorkspaceInterceptor(this.app, this.popoutLayout);
    this.workspaceInterceptor.enabled = this.settings.workspaceInterceptorEnabled !== false;
    this.workspaceInterceptor.isManagedWindow = (win) => {
      if (!this.activityBars.isInjected(win)) return false;
      if (win.document?.body?.querySelector(".modal-container")) return false;
      return true;
    };
    this.workspaceInterceptor.install();
    this.registerView(
      WINDOW_LAYOUTS_VIEW_TYPE,
      (leaf) => new WindowLayoutsView(leaf, this)
    );
    this.registerCommands();
    this.refreshRibbonIcons();
    this.addSettingTab(new WindowSpacesSettingTab(this.app, this));
    this.setupEventListeners();
    this.activityBars.refreshAll();
    this.app.workspace.onLayoutReady(() => {
      void this.manager.reconcileOpenSpacesOnStartup();
    });
    if (this.settings.showStatusBarIndicator === true) {
      this.addStatusBarIndicator();
    }
  }
  onunload() {
    this.windowLayoutsRibbonEl?.remove();
    this.windowLayoutsRibbonEl = null;
    this.manager?.clearLayoutLabels();
    this.workspaceInterceptor?.uninstall();
    this.activityBars?.cleanupAll();
    if (this.autoSaveCleanup) {
      this.autoSaveCleanup();
    }
    releasePopoutLayoutEngine("window-spaces");
  }
  refreshRibbonIcons() {
    if (this.settings.showWindowLayoutsRibbonIcon) {
      if (!this.windowLayoutsRibbonEl) {
        this.windowLayoutsRibbonEl = this.addRibbonIcon(
          "layout",
          t("commands.openLayoutsRibbon"),
          () => this.openWindowLayoutsModal()
        );
      }
    } else if (this.windowLayoutsRibbonEl) {
      this.windowLayoutsRibbonEl.remove();
      this.windowLayoutsRibbonEl = null;
    }
  }
  async loadSettings() {
    const savedSettings = await this.loadData();
    if (savedSettings && savedSettings.layouts && !savedSettings.spaces) {
      savedSettings.spaces = savedSettings.layouts;
      delete savedSettings.layouts;
    }
    this.settings = Object.assign({}, DEFAULT_SETTINGS, savedSettings);
    if (savedSettings?.visualDefaultsVersion !== 1) {
      if (savedSettings?.defaultBorderInset === 3) {
        this.settings.defaultBorderInset = 1;
      }
      this.settings.visualDefaultsVersion = 1;
      await this.saveSettings();
    }
    if (savedSettings?.defaultIcon === "layout") {
      this.settings.defaultIcon = DEFAULT_SPACE_ICON;
      await this.saveSettings();
    }
    if (this.settings.layoutStatusBarDefaultApplied !== true) {
      this.settings.showLayoutStatusBar = true;
      this.settings.layoutStatusBarDefaultApplied = true;
      await this.saveSettings();
    }
    if (!this.settings.version) {
      this.settings.version = DEFAULT_SETTINGS.version;
      await this.saveSettings();
    }
  }
  async saveSettings() {
    await this.saveData(this.settings);
  }
  registerCommands() {
    this.addCommand({
      id: "save-current-window-layout",
      name: t("commands.saveLayout"),
      icon: "save",
      callback: async () => {
        try {
          await this.openSaveCurrentLayoutModal();
        } catch (error) {
          const message = error instanceof Error ? error.message : String(error);
          new import_obsidian8.Notice(`${t("errors.failedToSave")}: ${message}`);
        }
      }
    });
    this.addCommand({
      id: "open-window-layouts",
      name: t("commands.openLayouts"),
      icon: "layout",
      callback: () => this.openWindowLayoutsModal()
    });
    this.addCommand({
      id: "open-window-layouts-panel",
      name: t("commands.openLayoutsPanel"),
      icon: "layout",
      callback: () => void this.openWindowLayoutsPanel("tab")
    });
    this.addCommand({
      id: "open-window-layouts-panel-left",
      name: t("commands.openLayoutsPanelLeft"),
      icon: "panel-left",
      callback: () => void this.openWindowLayoutsPanel("left")
    });
    this.addCommand({
      id: "open-window-layouts-panel-right",
      name: t("commands.openLayoutsPanelRight"),
      icon: "panel-right",
      callback: () => void this.openWindowLayoutsPanel("right")
    });
    this.addCommand({
      id: "toggle-left-activity-bar",
      name: t("commands.toggleLeftActivityBar"),
      icon: "panel-left",
      callback: () => this.toggleActivityBarVisibility("left")
    });
    this.addCommand({
      id: "toggle-right-activity-bar",
      name: t("commands.toggleRightActivityBar"),
      icon: "panel-right",
      callback: () => this.toggleActivityBarVisibility("right")
    });
  }
  toggleActivityBarVisibility(side) {
    const win = (this.manager ? this.manager.getActiveWindow() : void 0) || (typeof activeWindow !== "undefined" ? activeWindow : window);
    if (!win || win === window) {
      new import_obsidian8.Notice(t("activityBar.onlyInPopout"));
      return;
    }
    const columnEl = this.popoutLayout.getColumnElement(win, side);
    if (!columnEl) {
      new import_obsidian8.Notice(t("activityBar.cannotHideLastPane"));
      return;
    }
    if (this.popoutLayout.isColumnHidden(win, side)) {
      this.popoutLayout.showColumn(win, side);
    } else {
      if (this.popoutLayout.getVisibleColumnCount(win) < 2) {
        new import_obsidian8.Notice(t("activityBar.cannotHideLastPane"));
        return;
      }
      this.popoutLayout.hideColumn(win, side);
    }
    this.activityBars.updateActiveStates(win);
  }
  openSaveLayoutModal(layout, targetWindow) {
    const modal = new SaveLayoutModal(
      this.app,
      this,
      layout,
      (savedLayout) => {
        void (async () => {
          try {
            await this.manager.saveLayout(savedLayout);
          } catch (error) {
            const message = error instanceof Error ? error.message : String(error);
            new import_obsidian8.Notice(`${t("errors.failedToSave")}: ${message}`);
          }
        })();
      }
    );
    if (targetWindow && targetWindow !== window) {
      modal.open(targetWindow.document.body);
      return;
    }
    modal.open();
  }
  async openSaveCurrentLayoutModal(targetWindow) {
    try {
      const layout = await this.manager.captureCurrentLayout({}, targetWindow);
      this.openSaveLayoutModal(layout, targetWindow);
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      new import_obsidian8.Notice(`${t("errors.failedToSave")}: ${message}`);
    }
  }
  openWindowLayoutsModal(targetWindow) {
    try {
      const win = targetWindow || (this.manager ? this.manager.getActiveWindow() : void 0);
      const hostModal = new import_obsidian8.Modal(this.app);
      const controller = new WindowLayoutsModal(this.app, this, win);
      hostModal.setTitle(t("common.windowLayouts"));
      hostModal.onOpen = () => {
        try {
          hostModal.modalEl.addClass("window-layouts-modal");
          hostModal.modalEl.addClass("window-layouts-popout-modal");
          controller.mountInModalContainer(
            hostModal.contentEl,
            () => hostModal.close()
          );
          const titleHeader = hostModal.containerEl.querySelector(".modal-title");
          if (titleHeader) {
            controller.mountHeaderActions(titleHeader);
          }
        } catch (error) {
          console.error("[WindowSpaces] Error mounting Window Spaces picker:", error);
          const message = error instanceof Error ? error.message : String(error);
          hostModal.contentEl.empty();
          hostModal.contentEl.createEl("p", {
            text: `Error loading Window Spaces: ${message}`
          });
        }
      };
      hostModal.onClose = () => {
        controller.unmountFromContainer();
      };
      hostModal.open();
    } catch (error) {
      console.error("[WindowSpaces] Error opening WindowLayoutsModal:", error);
      const message = error instanceof Error ? error.message : String(error);
      new import_obsidian8.Notice(`Window Spaces Error: ${message}`);
    }
  }
  async openWindowLayoutsPanel(location = "tab", targetWindow) {
    const win = targetWindow || (this.manager ? this.manager.getActiveWindow() : void 0) || (typeof activeWindow !== "undefined" ? activeWindow : window);
    const isPopout = win !== window && Boolean(
      win.document?.body?.classList.contains("is-popout-window") || win.document?.body?.classList.contains("mod-popout")
    );
    const workspace = this.app.workspace;
    if (isPopout) {
      return this.popoutLayout.openPanel(win, location, WINDOW_LAYOUTS_VIEW_TYPE);
    }
    let root;
    if (location === "left") {
      root = workspace.leftSplit;
    } else if (location === "right") {
      root = workspace.rightSplit;
    } else {
      root = workspace.rootSplit;
    }
    const existingLeaf = findLeafInRoot(workspace, root, WINDOW_LAYOUTS_VIEW_TYPE);
    if (existingLeaf) {
      await workspace.revealLeaf(existingLeaf);
      workspace.setActiveLeaf(existingLeaf, { focus: true });
      return existingLeaf;
    }
    let leaf;
    if (location === "left") {
      leaf = workspace.getLeftLeaf(false);
    } else if (location === "right") {
      leaf = workspace.getRightLeaf(false);
    } else {
      let mainLeaf = null;
      workspace.iterateAllLeaves((candidate) => {
        const extCandidate = candidate;
        const body = extCandidate.containerEl?.ownerDocument?.body;
        const isCandidatePopout = body?.classList?.contains("is-popout-window") || body?.classList?.contains("mod-popout");
        if (!mainLeaf && !isCandidatePopout) mainLeaf = candidate;
      });
      if (mainLeaf) workspace.setActiveLeaf(mainLeaf, { focus: false });
      leaf = workspace.getLeaf("tab");
    }
    if (!leaf) {
      throw new Error("Unable to create a Window Layouts panel");
    }
    await leaf.setViewState({ type: WINDOW_LAYOUTS_VIEW_TYPE, state: {} });
    await workspace.revealLeaf(leaf);
    workspace.setActiveLeaf(leaf, { focus: true });
    return leaf;
  }
  setupEventListeners() {
    this.registerEvent(
      this.app.workspace.on("window-open", (_workspaceWindow, popoutWindow) => {
        this.manager.registerPopoutWindow(popoutWindow);
        this.activityBars.injectForWindow(popoutWindow);
        this.activityBars.scheduleLayoutIntegrityCheck(popoutWindow);
        WindowLayoutsModal.renderAllInstances();
      })
    );
    this.registerEvent(
      this.app.workspace.on("window-close", (_workspaceWindow, popoutWindow) => {
        this.manager.unregisterPopoutWindow(popoutWindow);
        this.activityBars.cleanupWindow(popoutWindow);
        WindowLayoutsModal.renderAllInstances();
      })
    );
    this.registerEvent(
      this.app.workspace.on("layout-change", () => {
        if (this.manager?.isRestoringLayout) return;
        this.manager.matchUnlabeledPopoutWindows();
        this.manager.checkAndDebouncedAutoSaveAll();
        this.activityBars.refreshAll();
      })
    );
    this.registerEvent(
      this.app.workspace.on("active-leaf-change", (leaf) => {
        if (this.manager?.isRestoringLayout) return;
        this.manager.checkAndDebouncedAutoSaveAll();
        this.activityBars.updateActiveStatesAll();
        const leafWindow = leaf ? getWindowOfLeaf(leaf) : null;
        if (leaf && leafWindow && isPopoutWindow(leafWindow)) {
          this.manager.scheduleViewRenderAfterActivation(leaf, leafWindow);
        }
      })
    );
  }
  addStatusBarIndicator() {
    const statusBarItem = this.addStatusBarItem();
    statusBarItem.setText("Window Spaces");
    statusBarItem.onClickEvent((evt) => {
      if (evt.shiftKey) {
        void this.manager.captureCurrentLayout().then((layout) => {
          this.openSaveLayoutModal(layout);
        }).catch((error) => {
          const message = error instanceof Error ? error.message : String(error);
          new import_obsidian8.Notice(`${t("errors.failedToSave")}: ${message}`);
        });
      } else {
        this.openWindowLayoutsModal();
      }
    });
    statusBarItem.setAttribute(
      "aria-label",
      "Window Spaces - Click to restore space, Shift+Click to save space"
    );
  }
  autoSaveCleanup = null;
};
function findLeafInRoot(workspace, root, viewType) {
  if (!root) return null;
  let found = null;
  workspace.iterateAllLeaves((leaf) => {
    if (found) return;
    const extLeaf = leaf;
    if (extLeaf.getRoot?.() === root && leaf.getViewState()?.type === viewType) {
      found = leaf;
    }
  });
  return found;
}
//# sourceMappingURL=main.js.map
