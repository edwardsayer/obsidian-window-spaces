/**
 * Backward-compatible import path for Window Spaces.
 *
 * The implementation lives in the self-contained shared module so Folder
 * Spaces can copy the same file without importing Window Spaces types.
 */
export * from "../shared/popoutLayout";
